# Work Log 2026-06-11

## Release overlay restart hardening

Time: 2026-06-11T02:10:00+08:00

Scope:

- Continued with the updated constraint: no multi-subagent strategy.
- Hardened `tools/restart_remote_image_agent_api.sh` so accepted release overlays can be the serving path without depending on a venv or `.env` copied into the overlay.
- Added explicit restart knobs:
  - `IMAGE_AGENT_RELEASE_ROOT` for the accepted release directory.
  - `IMAGE_AGENT_API_DIR` for direct override to a release `apps/api`.
  - `IMAGE_AGENT_ENV_FILE` for the shared operator-managed `.env`.
  - `IMAGE_AGENT_SHARED_VENV_BIN` for the shared remote API venv.
- Kept the existing drain, port-owner, stop-timeout, start-timeout, and `/health app=image_agent` checks intact.
- Documented the release-overlay restart pattern in `docs/deployment/remote-agent-production.md` so the dirty remote main worktree is not the accidental production source.

Verification:

- RED local test run confirmed the new restart overlay contract was missing:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_remote_restart_script.py -q` -> `2 failed, 2 passed`.
- GREEN local verification passed:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_remote_restart_script.py -q` -> `4 passed`.
- Remote isolated verification passed without touching the live API process or dirty remote main worktree:
  - Copied the patch archive to `/tmp/image_agent_restart_overlay_20260611020729.tgz`.
  - Applied it over a temporary copy of `/home/yyf/project/image_agent_releases/codex-7e7ff94-20260610`.
  - `bash -n tools/restart_remote_image_agent_api.sh`
  - `/home/yyf/project/image_agent/apps/api/.venv/bin/python -m pytest apps/api/tests/test_remote_restart_script.py -q` -> `4 passed`.

Release gate:

- The prior live-port strict remote acceptance evidence still stands as the latest full product gate evidence, but this change has not restarted the live API process. Frontend design remains blocked until the operational restart path is committed/deployed and a fresh post-restart strict acceptance package is saved or explicitly accepted as unchanged by the operator.

## OpenAI-compatible metadata opt-out

Time: 2026-06-11T02:45:00+08:00

Scope:

- Investigated the live remote model-smoke failure after serving from the `codex-1883251-20260611T0215` release overlay.
- Root cause: the OpenAI-compatible reverse-tunnel gateway rejected the Responses `metadata` request field, but the app only received a generic upstream gateway error. The existing automatic retry catches direct `Unsupported parameter: metadata` errors, but cannot recover when the provider hides the original parameter name.
- Added `OPENAI_DISABLE_METADATA=true` / `OPENAI_RESPONSES_DISABLE_METADATA=true` support so operators can explicitly omit Responses `metadata` for compatible gateways that mask upstream validation details.
- `/agent/model/status` now exposes `metadata_enabled` as a safe boolean and still never returns the API key.
- Updated remote deployment and OpenAI SDK boundary docs to make this an explicit operational setting, not an ad hoc remote-only workaround.

Verification:

- RED regression run:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_model_gateway.py::test_provider_status_reports_metadata_disabled_without_secrets apps/api/tests/test_model_gateway.py::test_request_can_disable_metadata_for_opaque_openai_compatible_gateways -q` -> `2 failed`.
- GREEN focused run:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_model_gateway.py::test_provider_status_reports_metadata_disabled_without_secrets apps/api/tests/test_model_gateway.py::test_request_can_disable_metadata_for_opaque_openai_compatible_gateways -q` -> `2 passed`.

Release gate:

- Frontend design remains blocked. This change still needs full local model-gateway regression, remote release-overlay deployment, live restart with `OPENAI_DISABLE_METADATA=true`, and fresh strict remote smoke acceptance evidence.

## Live release overlay strict acceptance refresh

Time: 2026-06-11T02:40:00+08:00

Scope:

- Created release overlay `/home/yyf/project/image_agent_releases/codex-f57a2ea-20260611T023456` from committed local HEAD `f57a2ea`.
- Linked the release `data` directory to the shared remote production data root.
- Verified the release in isolation:
  - `bash -n tools/restart_remote_image_agent_api.sh`
  - `PYTHONPATH=. /home/yyf/project/image_agent/apps/api/.venv/bin/python -m pytest tests/test_model_gateway.py -q` -> `20 passed`.
  - A safe status probe with `OPENAI_DISABLE_METADATA=true` returned `configured=True`, `metadata_enabled=False`, and no `api_key` field.
- Updated the shared remote `.env` with `OPENAI_DISABLE_METADATA=true` without printing secret values.
- Restarted the live API from the release overlay with:
  - `IMAGE_AGENT_RELEASE_ROOT=/home/yyf/project/image_agent_releases/codex-f57a2ea-20260611T023456`
  - `IMAGE_AGENT_ENV_FILE=/home/yyf/project/image_agent/.env`
  - `IMAGE_AGENT_SHARED_VENV_BIN=/home/yyf/project/image_agent/apps/api/.venv/bin`
- Confirmed the live process cwd is `/home/yyf/project/image_agent_releases/codex-f57a2ea-20260611T023456/apps/api`.
- Confirmed `/agent/model/status` reports configured OpenAI-compatible Responses gateway over `ssh_reverse_tunnel` with `metadata_enabled=false`.

Strict remote acceptance:

- Ran strict live-port smoke against `http://127.0.0.1:8000` with real evidence ids `project_id=13`, `upload_session_id=5`, and `task_id=118`.
- Saved evidence to `/tmp/image_agent_task118_live8000_post_restart_f57a2ea_20260611.json`.
- The smoke output reported:
  - `model_smoke_status=passed`
  - `remote_evidence_ids_status=passed`
  - `rag_vendor_pointer_integrity_status=passed`
  - `rag_launchability_query_status=passed`
  - `project_contract_status=passed`
  - `upload_inventory_contract_status=passed`
  - `task_artifact_manifest_status=passed`
  - `container_native_qc_status=passed`
  - `scientific_report_artifacts_status=passed`
- Offline verifier passed:
  - `PYTHONPATH=. /home/yyf/project/image_agent/apps/api/.venv/bin/python scripts/verify_remote_smoke_acceptance.py /tmp/image_agent_task118_live8000_post_restart_f57a2ea_20260611.json` -> `status=passed`.

Residual maturity risk:

- The restart required `IMAGE_AGENT_ALLOW_RESTART_WITH_ACTIVE_TASKS=1` because legacy tasks `83` and `84` still have `running` state. They appear stale, but normal product operations should not require overriding the active-task drain gate.
- Frontend design remains blocked until there is an auditable stale-task reconciliation path or the stale task records are resolved through an approved operator procedure.

## Stale task reconciliation tooling

Time: 2026-06-11T03:05:00+08:00

Scope:

- Added `app.workflows.stale_tasks` to audit stale `queued` / `running` task rows by age without mutating the database in dry-run mode.
- Added `apps/api/scripts/reconcile_stale_tasks.py` as the operator CLI:
  - default mode is read-only JSON dry-run;
  - `--apply` requires a Docker label check for running Image Agent containers;
  - Docker label check failure raises instead of being treated as an empty running-container set;
  - tasks with matching running labelled containers are blocked from reconciliation;
  - updated stale rows are marked `failed` with a concise audit reason and a task-log audit line.
- Documented the dry-run/apply flow in `docs/deployment/remote-agent-production.md`.

Verification:

- RED test run:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_stale_task_reconciliation.py -q` -> `3 failed` because `app.workflows.stale_tasks` did not exist.
- GREEN test run:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_stale_task_reconciliation.py -q` -> `3 passed`.

Operator note:

- The tool has not been applied to production task rows. Tasks `83` and `84` should be reviewed with dry-run output and reconciled only after explicit operator approval.
- Remote non-live release overlay `/home/yyf/project/image_agent_releases/codex-118c407-20260611T024415` verified the tool against the production DB in dry-run mode:
  - `PYTHONPATH=. /home/yyf/project/image_agent/apps/api/.venv/bin/python -m pytest tests/test_stale_task_reconciliation.py -q` -> `3 passed`.
  - `scripts/reconcile_stale_tasks.py --max-age-hours 24` reported `active_task_count=2`, `stale_candidates=[83,84]`, both `dwi_qsirecon`, both `running`, both older than 531 hours, and `updated_task_ids=[]`.
  - A shared-env Docker label check reported no running labelled Image Agent task ids: `[]`.

Follow-up refinement:

- Added `--check-containers` to `scripts/reconcile_stale_tasks.py` so dry-run approval evidence can include the read-only Docker label check directly in the JSON report.
- The JSON report now includes `container_check_status` and `running_container_task_ids`; dry-run remains read-only even when container checking is enabled.
- Verification:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_stale_task_reconciliation.py -q` -> `5 passed`.
- Remote non-live release overlay `/home/yyf/project/image_agent_releases/codex-7358c41-20260611T025518` verified the refined CLI:
  - `PYTHONPATH=. /home/yyf/project/image_agent/apps/api/.venv/bin/python -m pytest tests/test_stale_task_reconciliation.py -q` -> `5 passed`.
  - `scripts/reconcile_stale_tasks.py --max-age-hours 24` reported `container_check_status=not_requested`, `running_container_task_ids=null`, `stale_candidates=[83,84]`, and `updated_task_ids=[]`.
  - with the shared remote `.env` loaded, `scripts/reconcile_stale_tasks.py --max-age-hours 24 --check-containers` reported `container_check_status=passed`, `running_container_task_ids=[]`, `stale_candidates=[83,84]`, and `updated_task_ids=[]`.
- Added scoped `--task-id` support so approval/apply can be limited to exact task rows. The report includes `target_task_ids` and `out_of_scope_stale_task_ids`, and apply mode only updates stale candidates inside the provided target set.
- Verification:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_stale_task_reconciliation.py -q` -> `7 passed`.
- Remote non-live release overlay `/home/yyf/project/image_agent_releases/codex-1f62829-20260611T025956` verified the scoped CLI:
  - `PYTHONPATH=. /home/yyf/project/image_agent/apps/api/.venv/bin/python -m pytest tests/test_stale_task_reconciliation.py -q` -> `7 passed`.
  - with shared remote `.env` loaded, `scripts/reconcile_stale_tasks.py --max-age-hours 24 --check-containers --task-id 83 --task-id 84` reported `container_check_status=passed`, `running_container_task_ids=[]`, `target_task_ids=[83,84]`, `out_of_scope_stale_task_ids=[]`, `stale_candidates=[83,84]`, and `updated_task_ids=[]`.
