# Work Log - 2026-06-16

## Completed task strict smoke gate

- Added `--require-completed-task` to `apps/api/scripts/smoke_remote_agent.py`.
- When enabled, strict smoke fetches `/tasks/{task_id}` and fails unless the task is `completed`, matches the supplied project/task ids, has a positive series id, and reports a privacy-safe workflow type.
- The saved strict smoke JSON now includes `task_status_status` and a safe `task_status` summary. It intentionally omits backend paths such as `log_path`.
- Updated `apps/api/scripts/verify_remote_smoke_acceptance.py` so offline strict acceptance rejects JSON unless `smoke_gate.require_completed_task=true`, `task_status_status=passed`, `task_status.status=completed`, and `task_status.task_id` matches `smoke_gate.task_id`.
- Updated the remote release gate command plan, production guide, and acceptance evidence template to require `--require-completed-task` for production handoff.
- TDD red evidence: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_require_completed_task_requires_task_id apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_require_completed_task_rejects_running_task apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_require_completed_task_records_safe_task_status -q` initially failed because `--require-completed-task` was not recognized.
- TDD red evidence: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_rejects_weak_evidence apps/api/tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_accepts_strict_payload -q` initially failed because the offline verifier did not inspect completed task status.
- TDD red evidence: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_release_gate_command_plan.py::test_remote_release_gate_command_plan_orders_safe_remote_acceptance_steps -q` initially failed because the remote release command plan did not include `--require-completed-task`.
- Focused smoke/verifier verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_smoke_remote_agent.py apps/api/tests/test_verify_remote_smoke_acceptance.py -q` -> `194 passed`.
- Release docs verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_release_gate_command_plan.py apps/api/tests/test_skill_and_rag_docs.py -q` -> `60 passed`.
- Full API verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests -q` -> `684 passed, 5 warnings`.
- Compile verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m compileall -q apps/api/app apps/api/scripts` passed.
- Boundary: this strengthens the remote acceptance evidence for "workflow processing completed". It still does not perform a real remote upload or container run by itself; the operator must deploy, upload real imaging data, launch a workflow, wait for task completion, and then run strict remote smoke on the server.

## Completed upload strict smoke gate

- Added `--require-completed-upload` to `apps/api/scripts/smoke_remote_agent.py`.
- When enabled, strict smoke requires `--project-id` and `--upload-session-id`, validates `/projects/{project_id}/datasets/{upload_session_id}/inventory`, and fails unless upload ingestion reports `completed`.
- The saved strict smoke JSON now includes `upload_inventory_completion_status=passed` and `upload_inventory_status=completed` for accepted remote evidence.
- Updated `apps/api/scripts/verify_remote_smoke_acceptance.py` so offline strict acceptance rejects JSON unless `smoke_gate.require_completed_upload=true`, `upload_inventory_completion_status=passed`, and `upload_inventory_status=completed`.
- Updated the remote release gate command plan, production guide, and acceptance evidence template to require `--require-completed-upload` for production handoff.
- TDD red evidence: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_require_completed_upload_requires_upload_session_id apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_require_completed_upload_rejects_running_inventory apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_require_completed_upload_records_completion -q` initially failed because `--require-completed-upload` was not recognized.
- TDD red evidence: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_rejects_weak_evidence -q` initially failed because the offline verifier did not inspect upload completion status.
- TDD red evidence: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_release_gate_command_plan.py::test_remote_release_gate_command_plan_orders_safe_remote_acceptance_steps -q` initially failed because the remote release command plan did not include `--require-completed-upload`.
- Focused smoke/verifier verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_smoke_remote_agent.py apps/api/tests/test_verify_remote_smoke_acceptance.py -q` -> `200 passed`.
- Release docs verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_release_gate_command_plan.py apps/api/tests/test_skill_and_rag_docs.py -q` -> `60 passed`.
- Full API verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests -q` -> `690 passed, 5 warnings`.
- Compile verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m compileall -q apps/api/app apps/api/scripts` passed.
- Boundary: this strengthens the remote acceptance evidence for "complete upload data". It still does not perform a real remote upload by itself; the remote operator must upload real imaging data and produce fresh strict smoke evidence from the server.

## Project-scoped Agent strict smoke gate

- Added `--require-project-agent-context` to `apps/api/scripts/smoke_remote_agent.py`.
- When enabled, strict smoke requires `--project-id`, posts `/agent/runs` with that project id, and fails unless the Agent response reports the same `project_id`.
- The saved strict smoke JSON now includes `agent_project_context_status` and `agent_run_project_id`, so production handoff proves Agent chat is scoped to the real uploaded project instead of only proving generic model access.
- Updated `apps/api/scripts/verify_remote_smoke_acceptance.py` so offline strict acceptance rejects JSON unless `smoke_gate.require_project_agent_context=true`, `agent_project_context_status=passed`, and `agent_run_project_id` matches `smoke_gate.project_id`.
- Updated the remote release gate command plan, production guide, and acceptance evidence template to require project-scoped Agent smoke evidence for production handoff.
- TDD red evidence: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_require_project_agent_context_requires_project_id apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_require_project_agent_context_sends_project_id_and_records_scope apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_require_project_agent_context_rejects_unscoped_agent_run -q` initially failed because `--require-project-agent-context` was not recognized.
- TDD red evidence: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_rejects_weak_evidence apps/api/tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_accepts_strict_payload -q` initially failed because the offline verifier did not inspect project-scoped Agent evidence.
- TDD red evidence: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_release_gate_command_plan.py::test_remote_release_gate_command_plan_orders_safe_remote_acceptance_steps -q` initially failed because the remote release command plan did not include `--require-project-agent-context`.
- Focused smoke/verifier verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_strict_output_passes_offline_acceptance_verifier apps/api/tests/test_smoke_remote_agent.py apps/api/tests/test_verify_remote_smoke_acceptance.py -q` -> `206 passed`.
- Release docs verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_release_gate_command_plan.py apps/api/tests/test_skill_and_rag_docs.py -q` -> `60 passed`.
- Full API verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests -q` -> `696 passed, 5 warnings`.
- Compile verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m compileall -q apps/api/app apps/api/scripts` passed.
- Boundary: this strengthens the remote acceptance evidence for "Agent interaction tied to the uploaded project". It does not give the Agent authority to start long-running workflow tasks; workflow launch remains a deterministic backend task API with explicit validation and remote runtime evidence.

## Task workflow selection strict smoke evidence

- Added `task_workflow_selection_status` and `task_workflow_selection` to `apps/api/scripts/smoke_remote_agent.py`.
- When `--require-completed-task` and `--project-id` are supplied, strict smoke now checks that the completed task's `series_id` exists in `/projects/{project_id}/series` and that the completed `workflow_type` appears in that series `workflow_eligibility.runnable_workflows`.
- The saved strict smoke JSON records only a safe summary: `series_id`, `workflow_type`, and `matched_runnable_workflow=true`.
- Updated `apps/api/scripts/verify_remote_smoke_acceptance.py` so offline strict acceptance rejects saved JSON unless `task_workflow_selection_status=passed`, `matched_runnable_workflow=true`, and the selection series/workflow match `task_status`.
- Updated the remote release gate command plan, production guide, and acceptance evidence template so production handoff requires workflow selection evidence in addition to completed task evidence.
- TDD red evidence: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_require_completed_task_records_workflow_selection apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_require_completed_task_rejects_task_not_in_runnable_workflows -q` initially failed because strict smoke did not record workflow selection and did not reject non-runnable completed-task workflows.
- TDD red evidence: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_rejects_weak_evidence apps/api/tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_accepts_strict_payload -q` initially failed because the offline verifier did not inspect workflow selection evidence.
- Focused smoke/verifier verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_smoke_remote_agent.py apps/api/tests/test_verify_remote_smoke_acceptance.py -q` -> `212 passed`.
- Release docs verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_release_gate_command_plan.py apps/api/tests/test_skill_and_rag_docs.py -q` -> `60 passed`.
- Full API verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests -q` -> `702 passed, 5 warnings`.
- Compile verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m compileall -q apps/api/app apps/api/scripts` passed.
- Boundary: this strengthens the remote acceptance evidence for "choose workflow and start processing". It still does not run a real remote upload or container task by itself; the remote operator must create fresh evidence from the deployed server after uploading real imaging data and running a workflow through the backend task API.

## Console Agent page uses Agent run endpoint

- Added `api.runAgent(projectId, message)` in `apps/console/src/lib/api.ts`, posting to `/agent/runs` with `project_id` and `message`.
- Updated `apps/console/src/routes/AgentPage.tsx` so project Agent chat uses the formal Agent run endpoint rather than `/agent/rag/query`.
- Added `AgentRunResponse` typing while preserving the existing evidence panel fields (`answer`, `intent`, `recommended_next_step`, `tool_invocations`, and citations).
- TDD red evidence: `npm.cmd test -- AgentPage.test.tsx` initially failed because `api.runAgent` was never called and the page still used the RAG query endpoint for chat.
- Focused frontend verification: `npm.cmd test -- api.test.ts AgentPage.test.tsx` -> `7 passed`.
- Full console verification: `npm.cmd test` -> `38 passed`.
- Production build verification: `npm.cmd run build` passed.
- Boundary: this connects the Gemini-generated frontend Agent chat to the backend Agent run API. It does not give the Agent permission to launch workflow tasks; workflow execution remains behind the deterministic `/series/{series_id}/run` backend API and its validation gates.

## Dashboard Agent copilot uses Agent run endpoint

- Updated the Gemini-generated project dashboard sidebar Agent Copilot so quick prompts and typed messages call `api.runAgent(projectId, message)` instead of the legacy `/chat` compatibility endpoint.
- Kept workflow execution separate: the dashboard Agent chat can answer, retrieve backend/RAG context, and explain status, but pipeline launch still requires the explicit dashboard run control and `/series/{series_id}/run`.
- TDD red evidence: `npm.cmd test -- DashboardPage.test.tsx` initially failed because `api.runAgent` was never called and `DashboardPage.tsx` still consumed the old `reply` field from `api.chat`.
- Focused frontend verification: `npm.cmd test -- DashboardPage.test.tsx AgentPage.test.tsx api.test.ts` -> `13 passed`.
- Full console verification: `npm.cmd test` -> `38 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.

## Production readiness blocks localhost-only CORS

- Added a production readiness blocker when `IMAGE_AGENT_ENV=production` has only localhost/127.0.0.1 CORS origins. Production now requires at least one HTTPS, non-localhost console origin before `/deployment.production_readiness.ready` can be true.
- Existing startup behavior is unchanged: production still refuses missing `IMAGE_AGENT_CORS_ORIGINS` or wildcard `*`, while development keeps localhost defaults.
- TDD red evidence: `python -m pytest apps/api/tests/test_app_factory.py -q` failed because localhost-only production CORS still reported `ready=true`.
- Focused verification after the fix: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_app_factory.py -q` -> `4 passed`.
- Boundary: this is a deployment gate only. It does not replace real remote smoke acceptance; the remote server still must run strict smoke with `--require-production-readiness` using real public console origins and real upload/task evidence.

## Console shell reports backend disconnection

- Updated the Gemini-generated console shell so the top API badge uses the `/deployment` query state instead of always showing `API connected`.
- When deployment status cannot be loaded, the shell now shows `API disconnected` and the sidebar backend field shows `Unavailable`, making broken backend/API base configuration visible before users try upload, Agent chat, or workflow launch.
- TDD red evidence: `npm.cmd test -- AppShell.test.tsx` initially failed because `API disconnected` was absent and the shell still rendered the optimistic connected state.
- Focused frontend verification: `npm.cmd test -- AppShell.test.tsx DashboardPage.test.tsx AgentPage.test.tsx SettingsPage.test.tsx` -> `11 passed`.
- Full console verification: `npm.cmd test` -> `39 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this improves local/remote deployment diagnosability in the frontend. It does not prove the remote server can run a real upload or container workflow; strict remote smoke acceptance is still required.

## Console Settings can point to remote API base

- Added `setApiBase()` and `resetApiBase()` helpers that normalize the saved API base by trimming trailing slashes and store it in `localStorage.apiBase`.
- Replaced the read-only Settings API base display with an editable endpoint field. `Save Changes` now persists the remote backend base; `Reset Defaults` clears the override and returns to the default current-host `:8000` backend.
- TDD red evidence: `npm.cmd test -- api.test.ts` initially failed because `setApiBase` did not exist; `npm.cmd test -- SettingsPage.test.tsx` then failed because the API Base Endpoint label had no form control.
- Focused frontend verification: `npm.cmd test -- SettingsPage.test.tsx api.test.ts AppShell.test.tsx DashboardPage.test.tsx AgentPage.test.tsx` -> `18 passed`.
- Full console verification: `npm.cmd test` -> `41 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this lets the Gemini-generated console target a remote backend without editing code. It does not configure remote CORS or prove the remote upload/workflow path; remote strict smoke acceptance is still required after the operator sets the deployed API base.

## Console Settings refresh backend status after API base changes

- Updated Settings so `Save Changes` and `Reset Defaults` invalidate the deployment and runtime queries after changing `localStorage.apiBase`.
- The console now immediately re-probes the selected backend for runtime mode, production readiness, Agent provider, and container/license signals instead of continuing to display stale status from the previous API base.
- Tightened `SettingsPage` test isolation with `localStorage.clear()` and `vi.clearAllMocks()` before each test.
- TDD red evidence: `npm.cmd test -- SettingsPage.test.tsx` initially failed because `api.deployment` remained at 1 call after saving a new API base.
- Focused frontend verification: `npm.cmd test -- SettingsPage.test.tsx` -> `3 passed`.
- Main-chain frontend verification: `npm.cmd test -- SettingsPage.test.tsx api.test.ts AppShell.test.tsx DashboardPage.test.tsx AgentPage.test.tsx` -> `18 passed`.
- Full console verification: `npm.cmd test` -> `41 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this improves remote backend switching and diagnostics in the Gemini-generated console. It still does not prove a public remote server can complete real upload, Agent, workflow, and container processing; strict remote smoke evidence remains required before declaring the product ready for use.

## Console Settings reports backend disconnection

- Updated the Settings API Connection card so failed deployment/runtime queries show `API disconnected` instead of the previous optimistic `Connected` badge.
- Settings now shows `Backend status unavailable` and `Container status unavailable` when those backend probes fail, making broken API base, CORS, or remote server availability visible before users attempt upload, Agent chat, or workflow launch.
- TDD red evidence: `npm.cmd test -- SettingsPage.test.tsx` initially failed because `API disconnected` was absent and the card still rendered `Connected`.
- Focused frontend verification: `npm.cmd test -- SettingsPage.test.tsx` -> `4 passed`.
- Main-chain frontend verification: `npm.cmd test -- SettingsPage.test.tsx AppShell.test.tsx DashboardPage.test.tsx AgentPage.test.tsx api.test.ts` -> `19 passed`.
- Full console verification: `npm.cmd test` -> `42 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this improves remote API diagnostics in the Gemini-generated console. It does not replace strict remote smoke evidence with real upload, project-scoped Agent interaction, workflow selection, completed processing, container-native QC, and artifact/report provenance.

## Dashboard selects newly uploaded series

- Updated the Gemini-generated dashboard upload success path to select the backend-returned `series.id` and clear any previous manual workflow override.
- This keeps the recommended pipeline aligned with the newly uploaded imaging series before the user clicks `Start recommended pipeline`, reducing the risk of launching a workflow against an older project series.
- TDD red evidence: `npm.cmd test -- DashboardPage.test.tsx` initially failed because uploading a DWI sidecar set into a project that already had a T1 series still left the recommendation on `t1_deepprep`.
- Focused frontend verification: `npm.cmd test -- DashboardPage.test.tsx` -> `7 passed`.
- Main-chain frontend verification: `npm.cmd test -- DashboardPage.test.tsx AgentPage.test.tsx api.test.ts SettingsPage.test.tsx AppShell.test.tsx` -> `20 passed`.
- Full console verification: `npm.cmd test` -> `43 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this improves the frontend upload-to-workflow selection handoff. Workflow execution still uses the deterministic backend `/series/{series_id}/run` API and remote strict smoke evidence is still required before product release.

## Dashboard recomputes workflow after series change

- Updated the Dashboard input-series selector so choosing a different series clears the prior manual workflow override.
- This lets the recommended pipeline recalculate from the newly selected series and backend workflow catalog instead of keeping an incompatible workflow from the previous series.
- TDD red evidence: `npm.cmd test -- DashboardPage.test.tsx` initially failed because changing from T1 to DWI after manually selecting `t1_deepprep_anat_report` did not show `Pipeline: dwi_fast_gpu_dti`.
- Focused frontend verification: `npm.cmd test -- DashboardPage.test.tsx` -> `8 passed`.
- Main-chain frontend verification: `npm.cmd test -- DashboardPage.test.tsx AgentPage.test.tsx api.test.ts SettingsPage.test.tsx AppShell.test.tsx` -> `21 passed`.
- Full console verification: `npm.cmd test` -> `44 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this improves the user-driven series-to-workflow handoff. Actual workflow execution remains behind the deterministic backend `/series/{series_id}/run` API, and product release still requires strict remote smoke evidence.

## Dashboard surfaces workflow block reason in recommendation

- Updated the Gemini-generated dashboard recommended plan card so incompatible workflow selections show the deterministic eligibility block reason beside the disabled `Run now` action.
- This makes frontend workflow boundaries visible in the primary recommendation area, instead of hiding them inside advanced pipeline parameters or only surfacing them after a blocked run attempt.
- TDD red evidence: `npm.cmd test -- DashboardPage.test.tsx` initially failed because the recommended plan card did not include `Requires a DWI series.` after selecting `dwi_fast_gpu_dti` for a T1 series.
- Focused frontend verification: `npm.cmd test -- DashboardPage.test.tsx` -> `9 passed`.
- Main-chain frontend verification: `npm.cmd test -- DashboardPage.test.tsx AgentPage.test.tsx api.test.ts SettingsPage.test.tsx AppShell.test.tsx` -> `22 passed`.
- Full console verification: `npm.cmd test` -> `45 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this is a user-facing safety/clarity improvement only. Workflow execution is still gated by the deterministic backend `/series/{series_id}/run` path and release still requires remote smoke evidence with real upload, Agent interaction, workflow launch, container processing, QC/artifact provenance, and production CORS configuration.

## Dashboard zip upload creates upload-session evidence

- Updated the Gemini-generated dashboard zip upload path to create a dataset upload session and call `/projects/{project_id}/datasets/{upload_session_id}/ingest` instead of the older `/upload-dicom` shortcut.
- The dashboard now surfaces `Upload session #...` after a zip ingest starts, giving the operator the `upload_session_id` required by strict remote smoke acceptance.
- NIfTI and complete DWI sidecar uploads still use their existing direct contracts; only single zip archives are routed through dataset ingest because that path produces inventory evidence.
- TDD red evidence: `npm.cmd test -- DashboardPage.test.tsx` initially failed because `api.createUploadSession` was never called for a zip upload.
- Focused frontend verification: `npm.cmd test -- DashboardPage.test.tsx` -> `10 passed`.
- Main-chain frontend verification: `npm.cmd test -- DashboardPage.test.tsx IngestPage.test.tsx AgentPage.test.tsx api.test.ts SettingsPage.test.tsx AppShell.test.tsx` -> `25 passed`.
- Full console verification: `npm.cmd test` -> `46 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this improves the main dashboard's ability to produce upload-session evidence for remote release gates. It still does not prove a real remote server has completed upload inventory, workflow execution, container-native QC, or artifact/report provenance; strict remote smoke with real project/upload/task ids remains required.

## Dashboard polls upload inventory status

- Added a shared console query key for upload-session inventory and updated the Gemini-generated dashboard to poll `/projects/{project_id}/datasets/{upload_session_id}/inventory` after zip ingest starts.
- The dashboard now shows `Ingest completed` and the inventoried file count beside the upload session id, then refreshes detected series when inventory reaches `completed`.
- TDD red evidence: `npm.cmd test -- DashboardPage.test.tsx` initially failed because `api.getInventory` was never called after a zip upload.
- Focused frontend verification: `npm.cmd test -- DashboardPage.test.tsx` -> `10 passed`.
- Main-chain frontend verification: `npm.cmd test -- DashboardPage.test.tsx IngestPage.test.tsx AgentPage.test.tsx api.test.ts SettingsPage.test.tsx AppShell.test.tsx` -> `25 passed`.
- Full console verification: `npm.cmd test` -> `46 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this improves dashboard visibility into completed upload inventory, which is required for strict remote acceptance. It still does not replace remote smoke evidence from the deployed server with real upload, Agent interaction, workflow launch, completed container processing, QC artifacts, reports, and production CORS readiness.

## Dashboard surfaces launched task evidence

- Updated the Gemini-generated dashboard recommended plan card to show the backend-returned task id, status, and workflow immediately after `Start recommended pipeline` succeeds.
- This gives the operator the `task_id` needed for strict remote smoke acceptance without letting the Agent bypass deterministic workflow launch validation.
- TDD red evidence: `npm.cmd test -- DashboardPage.test.tsx` initially failed because `Task #131 queued` was not rendered after the backend returned a queued task.
- Focused frontend verification: `npm.cmd test -- DashboardPage.test.tsx` -> `10 passed`.
- Main-chain frontend verification: `npm.cmd test -- DashboardPage.test.tsx IngestPage.test.tsx AgentPage.test.tsx api.test.ts SettingsPage.test.tsx AppShell.test.tsx` -> `25 passed`.
- Full console verification: `npm.cmd test` -> `46 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this only exposes deterministic backend task-launch evidence in the UI. It does not prove the task completed remotely or produced container-native QC/artifact/report provenance; strict remote smoke with real project/upload/task ids is still required before release.

## Dashboard polls launched task progress

- Updated the Gemini-generated dashboard to poll `/tasks/{task_id}` after a workflow launch and display the freshest task status/progress in the recommended plan card.
- The launch evidence now advances from the initial queued response to backend task state such as `Task #131 running` and `Progress: 42%`.
- TDD red evidence: `npm.cmd test -- DashboardPage.test.tsx` initially failed because `Task #131 running` was not rendered and the dashboard did not call `api.getTask(131)` after launch.
- Focused frontend verification: `npm.cmd test -- DashboardPage.test.tsx` -> `10 passed`.
- Main-chain frontend verification: `npm.cmd test -- DashboardPage.test.tsx IngestPage.test.tsx AgentPage.test.tsx api.test.ts SettingsPage.test.tsx AppShell.test.tsx` -> `25 passed`.
- Full console verification: `npm.cmd test` -> `46 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this improves task progress visibility after deterministic backend launch. It still does not prove remote completion, container-native QC, report artifacts, or production readiness; those remain strict remote smoke requirements with real project/upload/task ids.

## Dashboard links completed launched task to results

- Updated the Gemini-generated dashboard recommended plan card to show a `View task results` link when the freshly polled launched task reaches `completed` or `completed_with_partial_failures`.
- The link targets `/projects/{project_id}/results/{task_id}`, keeping the user on the deterministic backend result-summary/artifact path after workflow processing completes.
- TDD red evidence: `npm.cmd test -- DashboardPage.test.tsx` initially failed because `Task #131 completed` and `Progress: 100%` rendered, but no accessible `View task results` link existed for `/projects/13/results/131`.
- Focused frontend verification: `npm.cmd test -- DashboardPage.test.tsx` -> `11 passed`.
- Main-chain frontend verification: `npm.cmd test -- DashboardPage.test.tsx IngestPage.test.tsx AgentPage.test.tsx api.test.ts SettingsPage.test.tsx AppShell.test.tsx` -> `26 passed`.
- Full console verification: `npm.cmd test` -> `47 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this only completes the local UI handoff from workflow completion to results review. Product release still requires strict remote smoke evidence for real upload, project-scoped Agent interaction, workflow launch, completed container processing, QC artifacts, reports, production CORS, and deployment configuration.

## Strict smoke verifies result-summary contract

- Updated `smoke_remote_agent.py` so any strict remote run with `--task-id` now requests `/tasks/{task_id}/result-summary` in addition to `/artifact-manifest`.
- The saved acceptance JSON now records `task_result_summary_status=passed` plus a privacy-safe result-summary contract digest: `contract_version`, `task_id`, `workflow_type`, `modality`, `feature_groups`, output group/item counts, and provenance keys.
- Updated `verify_remote_smoke_acceptance.py` to reject saved remote evidence unless the result-summary task id matches the smoke gate task id, workflow matches the completed task workflow, output counts are positive, and provenance keys are present.
- Updated the remote acceptance template so operators must capture and verify real result-summary evidence, which is the endpoint used by the frontend result detail page.
- TDD red evidence: focused pytest initially failed because both smoke output and offline verifier lacked `task_result_summary_status`.
- Focused verification: `python -m pytest apps/api/tests/test_smoke_remote_agent.py apps/api/tests/test_verify_remote_smoke_acceptance.py -q` -> `217 passed`.
- Boundary: this is a local acceptance-gate hardening. It does not itself prove the remote server has produced a fresh completed task; operators still must run strict smoke remotely and verify the saved JSON with `--max-age-hours 24`.

## Strict smoke cross-checks result-summary downloads

- Tightened the strict remote smoke gate so result-summary outputs with `relative_path`/`download_url` must use safe task artifact routes and must also exist in the same task artifact manifest.
- The saved acceptance JSON now records `task_result_summary.downloadable_output_count`, `downloadable_output_paths`, and `downloadable_output_urls`, giving the offline verifier concrete evidence that frontend result links are backed by manifest-served artifacts.
- Updated `verify_remote_smoke_acceptance.py` to reject saved evidence when downloadable output counts are missing, paths are unsafe, or URLs are not recomputed from the accepted `task_id`.
- The saved acceptance JSON also carries `artifact_manifest_relative_paths` and `artifact_manifest_download_urls`; the offline verifier now checks every downloadable result-summary output against those manifest summaries.
- Updated the remote acceptance template to make result-summary downloadability and artifact-manifest alignment an explicit production acceptance requirement.
- TDD red evidence: `python -m pytest apps/api/tests/test_smoke_remote_agent.py -q` initially failed because smoke output lacked downloadable output fields and did not reject a result-summary output absent from the manifest.
- Focused verification: `python -m pytest apps/api/tests/test_smoke_remote_agent.py apps/api/tests/test_verify_remote_smoke_acceptance.py -q` -> `225 passed`.
- Contract verification: `python -m pytest apps/api/tests/test_smoke_remote_agent.py apps/api/tests/test_verify_remote_smoke_acceptance.py apps/api/tests/test_artifact_manifest_contract.py apps/api/tests/test_result_contract.py apps/api/tests/test_fixed_workflow_api_contract.py -q` -> `259 passed, 3 warnings`.
- Compile verification: `python -m py_compile apps/api/scripts/smoke_remote_agent.py apps/api/scripts/verify_remote_smoke_acceptance.py apps/api/tests/test_smoke_remote_agent.py apps/api/tests/test_verify_remote_smoke_acceptance.py` passed.
- Boundary: this strengthens local and saved remote evidence only. It still does not replace a fresh strict smoke run on the remote server with real `project_id`, `upload_session_id`, and completed `task_id`.

## Console artifact client matches backend artifact routes

- Updated the Gemini-generated console API client so `api.getArtifactUrl(taskId, relativePath)` uses slash-preserving backend artifact routes such as `/tasks/4/artifacts/maps/fa.nii.gz` instead of `maps%2Ffa.nii.gz`.
- Added a Blob-specific request helper so artifact downloads are returned as binary `Blob` values instead of being parsed through the JSON request path.
- TDD red evidence: `npm.cmd test -- api.test.ts` initially failed with `Unexpected token 'o', "[object Blob]" is not valid JSON`, proving the client was using the wrong response parser for artifact bytes.
- Focused verification: `npm.cmd test -- api.test.ts` -> `6 passed`.
- Main-chain frontend verification: `npm.cmd test -- api.test.ts ResultDetailPage.test.tsx ResultStudioLayout.test.tsx DashboardPage.test.tsx` -> `21 passed`.
- Full console verification: `npm.cmd test` -> `47 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: result tables already prefer backend-provided `download_url` fields, but this keeps the shared API helper aligned with the strict smoke artifact route contract before it is reused by download or preview controls.

## Console result artifact fallback URLs are route-safe

- Updated the result studio artifact URL helper so legacy result-summary items without `download_url` still generate backend-safe artifact routes.
- Fallback URLs now preserve route slashes while encoding path segments, so paths such as `reports/qc figure 1.html` become `/tasks/{task_id}/artifacts/reports/qc%20figure%201.html`.
- TDD red evidence: `npm.cmd test -- resultArtifacts.test.ts` initially failed because the fallback URL contained raw spaces.
- Focused verification: `npm.cmd test -- resultArtifacts.test.ts` -> `6 passed`.
- Result-page verification: `npm.cmd test -- resultArtifacts.test.ts ResultDetailPage.test.tsx ResultStudioLayout.test.tsx api.test.ts` -> `16 passed`.
- Full console verification: `npm.cmd test` -> `48 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: strict remote smoke requires `download_url` for accepted real outputs, but this keeps older or partial result summaries from producing broken frontend artifact links.

## Console global shell shows production blockers

- Updated the Gemini-generated AppShell so `/deployment.production_readiness` can block the global status badge even when the backend API itself is reachable.
- The top badge now shows `Production blocked`, and the sidebar System Status shows the first backend-provided blocking reason such as a missing Agent model gateway.
- TDD red evidence: `npm.cmd test -- AppShell.test.tsx` initially failed because `Production blocked` was not rendered.
- Focused verification: `npm.cmd test -- AppShell.test.tsx` -> `2 passed`.
- Main-chain frontend verification: `npm.cmd test -- DashboardPage.test.tsx IngestPage.test.tsx AgentPage.test.tsx api.test.ts SettingsPage.test.tsx AppShell.test.tsx` -> `27 passed`.
- Full console verification: `npm.cmd test` -> `49 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this only surfaces backend production-readiness gates in the operator UI. It does not relax Agent boundaries, launch workflows through the LLM, or replace strict remote smoke evidence for real upload, workflow execution, QC artifacts, CORS, and deployment configuration.

## Agent page shows model gateway state

- Updated the Gemini-generated Agent page so the sidebar now reads backend deployment data and shows `Model Gateway` plus provider/model detail.
- The page now makes the current boundary explicit: configured gateways show `Configured`, while unconfigured gateways show `Not configured` even though the chat UI still works as a grounded backend/agent surface.
- TDD red evidence: `npm.cmd test -- AgentPage.test.tsx` initially failed because `Model Gateway` was absent from the page.
- Focused verification: `npm.cmd test -- AgentPage.test.tsx` -> `3 passed`.
- Boundary: this improves operator visibility without changing task launch authority. Agent chat still must not bypass the deterministic backend task API or remote smoke evidence gates.

## Missing project ids fail before upload and workflow actions

- Tightened project-scoped backend list endpoints so `/projects/{project_id}/series` and `/projects/{project_id}/tasks` now return `404 Project not found` when the project id is absent, instead of presenting an empty project state.
- Updated the Gemini-generated dashboard to show a clear `Project data unavailable` state with a `Switch project` link when project-scoped backend data cannot load.
- Updated the console API client to convert backend JSON error payloads such as `{"detail":"Project not found"}` into clean user-facing messages.
- This prevents `/projects/13/dashboard` from looking usable against a database that only contains a different project id; users must select or create a real backend project before uploading, chatting in project context, or launching workflows.
- TDD red evidence: `python -m pytest tests/test_api_flow.py::test_missing_project_scoped_lists_return_404 -q` initially failed because `/projects/999/series` returned `200`; `npm.cmd test -- DashboardPage.test.tsx -t "blocks the dashboard main flow"` initially failed because the upload panel still rendered on project data errors; `npm.cmd test -- api.test.ts -t "uses backend JSON detail"` initially failed because the client error message was the raw JSON body.
- Focused backend verification: `python -m pytest tests/test_api_flow.py::test_missing_project_scoped_lists_return_404 tests/test_main_architecture.py::test_project_existence_checks_live_in_project_service -q` -> `2 passed`.
- Focused frontend verification: `npm.cmd test -- DashboardPage.test.tsx` -> `12 passed`; `npm.cmd test -- api.test.ts` -> `7 passed`.
- Browser verification: after restarting the local API, `/projects/13/dashboard` shows `Project data unavailable` and `Project not found` without the upload panel; `/projects/1/dashboard` still shows Upload Data, Workflow Status, Agent Copilot, and Start recommended pipeline.
- Boundary: this is an availability and safety gate for the local console. It does not prove a remote upload, container workflow execution, QC artifact generation, report rendering, model gateway configuration, or production CORS readiness.

## Agent project context requires a real backend project

- Tightened project-scoped Agent endpoints so `/agent/runs`, `/agent/rag/query`, and `/projects/{project_id}/agent-runs` now validate `project_id` through the project service before returning or creating project-context Agent state.
- Updated the Gemini-generated Agent page to probe project-scoped backend data and show `Project data unavailable` plus a `Switch project` link instead of rendering the chat input when the project id is not available in the backend.
- This keeps Agent chat aligned with the product boundary: Agent can explain, query, and suggest, but project-scoped interaction must not fabricate context for a missing project or bypass deterministic backend validation.
- TDD red evidence: `python -m pytest tests/test_agent_api.py::test_agent_project_scoped_endpoints_reject_missing_project -q` initially failed because `/agent/runs` reached the model path and returned `502` for a missing project; `npm.cmd test -- AgentPage.test.tsx -t "blocks project-scoped"` initially failed because the Agent page still displayed the normal chat UI on project data errors.
- Focused backend verification: `python -m pytest tests/test_agent_api.py tests/test_api_flow.py::test_missing_project_scoped_lists_return_404 -q` -> `35 passed, 3 warnings`.
- Focused frontend verification: `npm.cmd test -- AgentPage.test.tsx` -> `4 passed`; `npm.cmd test -- DashboardPage.test.tsx IngestPage.test.tsx AgentPage.test.tsx api.test.ts SettingsPage.test.tsx AppShell.test.tsx` -> `31 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: unscoped/general Agent and RAG requests can still run without a `project_id` when explicitly used that way. This change only blocks fake project-scoped context; it does not prove remote model gateway configuration, real upload, container workflow execution, QC artifact generation, production CORS, or remote deployment readiness.

## Workflows page blocks missing project launches

- Updated the Gemini-generated Workflows page so project-scoped `series` and `tasks` queries are treated as launch prerequisites, matching the Dashboard and Agent project-context boundary.
- When backend project data is unavailable, the page now shows `Project data unavailable`, the backend error detail such as `Project not found`, and a `Switch project` link instead of rendering workflow launch controls.
- This prevents the standalone `/projects/{project_id}/workflows` route from presenting a usable workflow shell for a project id that the backend database does not recognize.
- TDD red evidence: `npm.cmd test -- WorkflowsPage.test.tsx -t "blocks workflow launch"` initially failed because the page still rendered workflow cards and the empty-series state after `listSeries` rejected.
- Focused verification: `npm.cmd test -- WorkflowsPage.test.tsx` -> `3 passed`.
- Main-chain frontend verification: `npm.cmd test -- DashboardPage.test.tsx IngestPage.test.tsx AgentPage.test.tsx WorkflowsPage.test.tsx api.test.ts SettingsPage.test.tsx AppShell.test.tsx` -> `34 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this is a frontend launch-safety gate. It does not prove remote upload, real container workflow execution, task completion, QC artifact rendering, Agent model gateway readiness, production CORS, or strict remote smoke acceptance.

## Dashboard links launched completed tasks to results preview

- Updated the Gemini-generated Dashboard so a just-launched task returned by task polling is used as the authoritative result preview target once it reaches a completed status, even before `/projects/{project_id}/tasks` has refreshed.
- The Results Preview card now requests `/tasks/{task_id}/result-summary` for the completed launched task and links `View Full Results` directly to `/projects/{project_id}/results/{task_id}`.
- This closes a product-flow gap where the top task notice could say `Task #131 completed` while the result preview still had no task-specific result link because the task list remained temporarily stale.
- TDD red evidence: `npm.cmd test -- DashboardPage.test.tsx -t "uses the completed launched task"` initially failed because `api.getResultSummary` was never called with the launched task id.
- Focused verification: `npm.cmd test -- DashboardPage.test.tsx` -> `13 passed`.
- Main-chain frontend verification: `npm.cmd test -- DashboardPage.test.tsx IngestPage.test.tsx AgentPage.test.tsx WorkflowsPage.test.tsx api.test.ts SettingsPage.test.tsx AppShell.test.tsx` -> `35 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this improves the local console handoff from task completion to result review. It does not prove remote upload, real container workflow execution, QC artifact generation, report rendering, production CORS, or strict remote smoke acceptance.

## Strict smoke binds completed task to uploaded series

- Tightened the remote strict smoke evidence chain so upload inventory validation now records `upload_inventory_series_ids`.
- Updated the offline acceptance verifier to reject saved strict smoke evidence unless `task_status.series_id` is present in `upload_inventory_series_ids`.
- This prevents a release gate from accidentally combining a newly completed upload with an older completed task from the same project/workflow; the completed task must belong to a series discovered in the accepted upload inventory.
- TDD red evidence: `python -m pytest tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_rejects_weak_evidence -q` initially failed because bad `upload_inventory_series_ids` were accepted; `python -m pytest tests/test_smoke_remote_agent.py::test_smoke_remote_agent_validates_dataset_inventory_workflow_eligibility -q` initially failed because the smoke output lacked `upload_inventory_series_ids`.
- Focused verification: `python -m pytest tests/test_smoke_remote_agent.py::test_smoke_remote_agent_validates_dataset_inventory_workflow_eligibility -q` -> `1 passed`; `python -m pytest tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_rejects_weak_evidence -q` -> `77 passed`.
- Strict smoke contract verification: `python -m pytest tests/test_smoke_remote_agent.py tests/test_verify_remote_smoke_acceptance.py -q` -> `227 passed`.
- Compile verification: `python -m py_compile scripts/smoke_remote_agent.py scripts/verify_remote_smoke_acceptance.py tests/test_smoke_remote_agent.py tests/test_verify_remote_smoke_acceptance.py` passed.
- Boundary: this hardens local smoke tooling and saved remote evidence validation. It still requires an operator to run fresh strict smoke on the remote server with real `project_id`, `upload_session_id`, completed `task_id`, model gateway, production CORS, QC/report artifacts, and deployment identity.

## Strict smoke proves deterministic workflow launch

- Added `--require-launched-task`, `--launch-series-id`, `--launch-workflow-type`, `--wait-task-completion-timeout-seconds`, and `--wait-task-completion-poll-seconds` to `apps/api/scripts/smoke_remote_agent.py`.
- Strict smoke now launches a workflow through deterministic backend `POST /series/{series_id}/run`, records privacy-safe `launched_task`, can use the backend-returned task id when `--task-id` is omitted, and can poll `/tasks/{task_id}` until `completed` before QC/report checks.
- Tightened `apps/api/scripts/verify_remote_smoke_acceptance.py` so saved strict smoke JSON must include `smoke_gate.require_launched_task=true`, `launched_task_status=passed`, and launched task id/project/series/workflow consistency with `smoke_gate` and `task_status`.
- Updated `docs/deployment/remote-release-gate-command-plan.json`, `docs/deployment/remote-agent-production.md`, and `docs/deployment/remote-agent-acceptance-template.md` so the production release gate launches from uploaded series/workflow inputs, waits up to 21600 seconds, and no longer requires the operator to know the task id before launch.
- TDD red evidence: `python -m pytest tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_rejects_weak_evidence -q` initially failed because bad or skipped `launched_task` evidence was accepted; `python -m pytest tests/test_smoke_remote_agent.py -k "launched_task" -q` initially failed before the launch arguments existed.
- Focused verification: `python -m pytest tests/test_smoke_remote_agent.py -k "launched_task or waits_for_launched_task_completion or strict_output_passes_offline_acceptance_verifier" -q` -> `5 passed`; `python -m pytest tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_rejects_weak_evidence tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_accepts_strict_payload -q` -> `84 passed`.
- Contract verification: `python -m pytest tests/test_smoke_remote_agent.py tests/test_verify_remote_smoke_acceptance.py -q` -> `237 passed`.
- Release-gate/doc verification: `python -m pytest apps/api/tests/test_release_gate_command_plan.py -q` -> `5 passed`; `python -m pytest apps/api/tests/test_skill_and_rag_docs.py -k "remote_smoke or acceptance or production or release_gate" -q` -> `6 passed, 49 deselected`.
- Compile verification: `python -m py_compile scripts/smoke_remote_agent.py` passed.
- Final related-suite verification: `python -m pytest tests/test_smoke_remote_agent.py tests/test_verify_remote_smoke_acceptance.py tests/test_release_gate_command_plan.py tests/test_skill_and_rag_docs.py -q` -> `297 passed`.
- Syntax verification without writing `.pyc`: `python -c "from pathlib import Path; ... compile(...)"` -> `syntax ok`. A later `python -m py_compile ...` run hit Windows `[WinError 5]` while replacing an existing `__pycache__` file, so the non-writing syntax check was used for final compile coverage.
- Diff whitespace verification: `git diff --check -- ...` passed, with only the existing CRLF warning for `docs/skills/image-agent-developer/references/testing-matrix.md`.
- Boundary: the Agent still only answers, explains, and queries project context. Workflow launch remains a deterministic backend API call performed by the operator-run remote smoke gate, not by LLM tool autonomy. This local change validates tooling only; real product readiness still requires running the updated strict smoke on the remote server with real uploaded imaging data, configured model gateway, production CORS/config, Docker/container runtime, and license/environment prerequisites.

## Task log API redacts secrets and backend paths

- Tightened `apps/api/app/services/result_service.py` so `/tasks/{task_id}/logs` redacts API keys, tokens, passwords, patient-like identifiers, Windows host paths, and Unix backend paths from both the main task log and collected remote container log tails.
- Removed `log_paths` and per-remote-log `path` from the public task-log response so the console and Agent-facing surfaces do not expose backend filesystem layout.
- Updated `docs/product-readiness.md` to call `/tasks/{task_id}/logs` a redacted task-log contract and to block raw task log paths from production-facing surfaces.
- TDD red evidence: `python -m pytest tests/test_api_flow.py::test_task_logs_redact_secrets_and_backend_paths -q` initially failed because `sk-test-secret`, `IMAGE_AGENT_SUDO_PASSWORD`, `C:/Users/A/private`, patient-like path text, and backend `log_paths` were returned by `/tasks/77/logs`.
- Focused verification: `python -m pytest tests/test_api_flow.py::test_task_logs_redact_secrets_and_backend_paths tests/test_api_flow.py::test_full_t1_mock_flow -q` -> `2 passed, 3 warnings`.
- Agent log-tool regression verification: `python -m pytest tests/test_agent_tools.py::test_read_task_and_events_include_remote_wrapper_logs tests/test_agent_tool_dispatcher.py -q` -> `13 passed`.
- Public result/API contract verification: `python -m pytest tests/test_api_flow.py tests/test_fixed_workflow_api_contract.py tests/test_result_contract.py tests/test_artifact_manifest_contract.py -q` -> `97 passed, 3 warnings`.
- Readiness documentation verification: `python -m pytest tests/test_skill_and_rag_docs.py -q` -> `55 passed`.
- Syntax verification without writing `.pyc`: `python -c "from pathlib import Path; ... compile(...)"` -> `syntax ok`.
- Boundary: this hardens the API response used by the frontend. It does not rewrite existing log files on disk, and it does not replace remote strict smoke evidence for real processing, QC, or production deployment.

## Dashboard Agent chat uses backend fallback path

- Updated the Gemini-generated Dashboard Agent Copilot to call `api.chat(projectId, message)` instead of `api.runAgent(projectId, message)`.
- This keeps the fastest product main flow usable when `/agent/model/status` is not fully configured: the dashboard can still receive backend/rules/status-style `reply` responses from `/chat` while the full Agent run path remains available on the dedicated Agent page.
- TDD red evidence: `npm.cmd test -- DashboardPage.test.tsx -t "renders the processing console"` initially failed because the quick chat action did not call `api.chat`.
- Focused verification: `npm.cmd test -- DashboardPage.test.tsx -t "renders the processing console"` -> `1 passed, 12 skipped`.
- Main-chain frontend verification: `npm.cmd test -- DashboardPage.test.tsx IngestPage.test.tsx AgentPage.test.tsx WorkflowsPage.test.tsx api.test.ts SettingsPage.test.tsx AppShell.test.tsx` -> `35 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this does not let the Agent launch workflows. Chat remains explanatory/status-oriented; workflow creation still goes through deterministic `/series/{series_id}/run` and remote strict smoke remains required for production acceptance.

## Results Studio blocks missing project data

- Updated the Gemini-generated Results Studio index so a failed project-scoped task query is treated as a blocking project-data state instead of an empty result set.
- When `/projects/{project_id}/tasks` fails, the page now shows `Project data unavailable`, the backend error detail such as `Project not found`, and a `Switch project` link instead of `No results available`.
- This keeps the result-review surface aligned with Dashboard, Agent, and Workflows: missing backend project state must not be presented as a valid but empty project.
- TDD red evidence: `npm.cmd test -- ResultsIndexPage.test.tsx` initially failed because `Project data unavailable` was missing and the page rendered `No results available`.
- Focused verification: `npm.cmd test -- ResultsIndexPage.test.tsx` -> `1 passed`.
- Main-chain frontend verification: `npm.cmd test -- DashboardPage.test.tsx IngestPage.test.tsx AgentPage.test.tsx WorkflowsPage.test.tsx ResultsIndexPage.test.tsx api.test.ts SettingsPage.test.tsx AppShell.test.tsx` -> `36 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this is a local frontend safety gate only. It does not prove remote upload, real container workflow execution, QC/report artifact generation, Agent model gateway readiness, production CORS, or strict remote smoke acceptance.

## Result summary project scope is explicit

- Updated the public `/tasks/{task_id}/result-summary` response so it includes `project_id` and removes the top-level `summary_path` backend filesystem pointer.
- Kept artifact manifest behavior intact by loading the internal result-summary payload for manifest construction; `/tasks/{task_id}/artifact-manifest` can still expose a safe relative `result_summary.summary_path` such as `summary/dwi_result_summary.json`.
- Updated the Gemini-generated result detail page to block rendering when `result-summary.project_id` does not match the route `/projects/{project_id}/results/{task_id}`.
- Tightened the console `ResultSummary` type and mock summaries so frontend result rendering now depends on explicit project scope from the backend contract.
- TDD red evidence: `python -m pytest tests/test_api_flow.py::test_task_result_summary_endpoint_returns_frontend_contract -q` initially failed with missing `project_id`; `npm.cmd test -- ResultDetailPage.test.tsx` initially failed because the page rendered `Scientific Results Studio` for a summary from project `99` under project `13`.
- Focused backend verification: `python -m pytest tests/test_api_flow.py::test_task_result_summary_endpoint_returns_frontend_contract tests/test_api_flow.py::test_t1_deepprep_pipeline_registers_real_freesurfer_stats_summary tests/test_artifact_manifest_api.py tests/test_result_contract.py tests/test_fixed_workflow_api_contract.py -q` -> `33 passed, 3 warnings`.
- Main-chain frontend verification: `npm.cmd test -- DashboardPage.test.tsx IngestPage.test.tsx AgentPage.test.tsx WorkflowsPage.test.tsx ResultsIndexPage.test.tsx ResultDetailPage.test.tsx api.test.ts SettingsPage.test.tsx AppShell.test.tsx` -> `38 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Syntax verification: `python -c "from pathlib import Path; compile(Path('app/services/result_service.py').read_text(encoding='utf-8'), 'app/services/result_service.py', 'exec'); print('syntax ok')"` -> `syntax ok`.
- Boundary: this prevents local console cross-project result rendering and removes one public backend path leak from `result-summary`. It does not prove remote upload, real processing, container-native QC/report serving, production CORS, model gateway configuration, or fresh strict remote smoke acceptance.

## Task Monitor blocks missing project data

- Updated the Gemini-generated Task Monitor so project-scoped task query failure is a blocking project-data state instead of a red inline error plus an empty task table.
- When `/projects/{project_id}/tasks` fails, the page now shows `Project data unavailable`, the backend error detail such as `Project not found`, and a `Switch project` link.
- This keeps task monitoring aligned with Dashboard, Agent, Workflows, Results Index, and Result Detail boundaries: missing backend project state must not be presented as a valid empty project.
- TDD red evidence: `npm.cmd test -- TasksPage.test.tsx` initially failed because the page did not show `Project data unavailable` and still rendered `Recent Execution History` plus `No backend tasks detected`.
- Focused verification: `npm.cmd test -- TasksPage.test.tsx` -> `2 passed`.
- Main-chain frontend verification: `npm.cmd test -- DashboardPage.test.tsx IngestPage.test.tsx AgentPage.test.tsx WorkflowsPage.test.tsx TasksPage.test.tsx ResultsIndexPage.test.tsx ResultDetailPage.test.tsx api.test.ts SettingsPage.test.tsx AppShell.test.tsx` -> `40 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this is a local console safety gate only. It does not prove remote task execution, worker/container runtime, QC/report artifact serving, model gateway configuration, production CORS, or fresh strict remote smoke acceptance.

## Scientific Reports blocks missing project data

- Updated the Gemini-generated Scientific Reports page so project-scoped task query failure is a blocking project-data state instead of `0 Reports Available` plus `No ready reports yet`.
- When `/projects/{project_id}/tasks` fails, the page now shows `Project data unavailable`, the backend error detail such as `Project not found`, and a `Switch project` link.
- This keeps report entry points aligned with Dashboard, Agent, Workflows, Task Monitor, Results Index, and Result Detail boundaries: missing backend project state must not be presented as a valid project with no reports.
- TDD red evidence: `npm.cmd test -- ReportsPage.test.tsx` initially failed because the page rendered `0 Reports Available` and `No ready reports yet` instead of `Project data unavailable`.
- Focused verification: `npm.cmd test -- ReportsPage.test.tsx` -> `2 passed`.
- Main-chain frontend verification: `npm.cmd test -- DashboardPage.test.tsx IngestPage.test.tsx AgentPage.test.tsx WorkflowsPage.test.tsx TasksPage.test.tsx ResultsIndexPage.test.tsx ResultDetailPage.test.tsx ReportsPage.test.tsx api.test.ts SettingsPage.test.tsx AppShell.test.tsx` -> `42 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this is a local console report-entry safety gate only. It does not prove remote task execution, native QC/report artifact generation, model gateway configuration, production CORS, or fresh strict remote smoke acceptance.

## Ingest upload blocks missing project data

- Updated the Gemini-generated Data & QC upload page so project-scoped series query failure is a blocking project-data state instead of leaving upload controls active.
- When `/projects/{project_id}/series` fails, the page now shows `Project data unavailable`, the backend error detail such as `Project not found`, and a `Switch project` link.
- This protects the fastest-launch upload entry point: users should not be able to pick upload files or start DWI upload flows for a project id that the backend database does not recognize.
- TDD red evidence: `npm.cmd test -- IngestPage.test.tsx` initially failed because the page rendered `Standard Uploads` and `Upload DWI set` after `listSeries` rejected.
- Focused verification: `npm.cmd test -- IngestPage.test.tsx` -> `3 passed`.
- Main-chain frontend verification: `npm.cmd test -- DashboardPage.test.tsx IngestPage.test.tsx AgentPage.test.tsx WorkflowsPage.test.tsx TasksPage.test.tsx ResultsIndexPage.test.tsx ResultDetailPage.test.tsx ReportsPage.test.tsx api.test.ts SettingsPage.test.tsx AppShell.test.tsx` -> `43 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this is a local console upload-entry safety gate only. It does not prove remote upload storage, real DICOM/NIfTI ingest, container workflow execution, model gateway configuration, production CORS, or fresh strict remote smoke acceptance.

## Task outputs API omits backend paths

- Updated the public `/tasks/{task_id}/outputs` response so frontend and Agent-facing consumers receive safe artifact fields instead of backend filesystem pointers.
- Public output items now expose `relative_path`, `download_url`, `content_type`, and `size_bytes` when the registered file resolves inside the task output directory.
- Removed public `path` and `preview_path` from output rows, and stripped host-path metadata keys such as `path`, `preview_path`, `storage_path`, `log_path`, `summary_path`, `absolute_path`, and `host_path`.
- Kept internal raw output rows for `/tasks/{task_id}/result-summary` and `/tasks/{task_id}/artifact-manifest`, so result summary discovery and manifest generation still have the original DB paths they need.
- TDD red evidence: `python -m pytest tests/test_api_flow.py::test_task_outputs_endpoint_returns_frontend_safe_artifact_contract -q` initially failed because `/tasks/88/outputs` had no `relative_path`/`download_url` public contract and still used the legacy raw DB shape.
- Focused verification: `python -m pytest tests/test_api_flow.py::test_task_outputs_endpoint_returns_frontend_safe_artifact_contract tests/test_api_flow.py::test_task_result_summary_endpoint_returns_frontend_contract -q` -> `2 passed, 3 warnings`.
- Contract regression verification: `python -m pytest tests/test_api_flow.py::test_task_result_summary_endpoint_returns_frontend_contract tests/test_api_flow.py::test_task_outputs_endpoint_returns_frontend_safe_artifact_contract tests/test_api_flow.py::test_t1_deepprep_pipeline_registers_real_freesurfer_stats_summary tests/test_api_flow.py::test_task_logs_redact_secrets_and_backend_paths tests/test_artifact_manifest_api.py tests/test_fixed_workflow_api_contract.py::test_task_artifact_manifest_lists_previewable_result_summary_artifacts -q` -> `6 passed, 3 warnings`.
- Expanded workflow/result verification: `python -m pytest tests/test_fixed_workflow_api_contract.py tests/test_result_contract.py tests/test_artifact_manifest_api.py -q` -> `31 passed, 3 warnings`.
- Syntax verification: `python -c "from pathlib import Path; compile(Path('app/services/result_service.py').read_text(encoding='utf-8'), 'app/services/result_service.py', 'exec'); print('syntax ok')"` -> `syntax ok`.
- Boundary: this hardens one legacy public API surface. It does not prove remote server upload, real container processing, native QC/report artifact rendering, Agent model gateway configuration, production CORS, or fresh strict remote smoke acceptance.

## Task status APIs omit backend log paths

- Updated public task status responses so `POST /series/{series_id}/run`, `GET /tasks/{task_id}`, and `GET /projects/{project_id}/tasks` return frontend-safe task objects without `log_path`.
- Kept raw task rows available inside `task_service.get_task()` and task creation internals so logs, result summaries, artifact manifests, and worker execution can still use the stored backend log path.
- Updated `docs/api.md` to document safe task status fields, redacted task logs, and safe legacy output artifact fields.
- TDD red evidence: `python -m pytest tests/test_api_flow.py::test_task_status_responses_omit_backend_log_path -q` initially failed because the task creation response exposed `log_path`.
- Focused backend verification: `python -m pytest tests/test_api_flow.py::test_task_status_responses_omit_backend_log_path -q` -> `1 passed, 3 warnings`.
- Main-chain/backend boundary verification: `python -m pytest tests/test_api_flow.py::test_full_t1_mock_flow tests/test_api_flow.py::test_task_status_responses_omit_backend_log_path tests/test_api_flow.py::test_task_logs_redact_secrets_and_backend_paths tests/test_api_flow.py::test_task_outputs_endpoint_returns_frontend_safe_artifact_contract tests/test_api_flow.py::test_task_result_summary_endpoint_returns_frontend_contract -q` -> `5 passed, 3 warnings`.
- Artifact/log regression verification: `python -m pytest tests/test_artifact_manifest_api.py tests/test_fixed_workflow_api_contract.py::test_task_logs_endpoint_includes_remote_wrapper_logs tests/test_fixed_workflow_api_contract.py::test_task_artifact_manifest_lists_previewable_result_summary_artifacts -q` -> `3 passed, 3 warnings`.
- Console task-flow verification: `npm.cmd test -- TasksPage.test.tsx DashboardPage.test.tsx WorkflowsPage.test.tsx api.test.ts` -> `4 files, 25 passed`.
- Syntax verification: `python -c "from pathlib import Path; compile(Path('app/services/task_service.py').read_text(encoding='utf-8'), 'app/services/task_service.py', 'exec'); compile(Path('app/routes/tasks.py').read_text(encoding='utf-8'), 'app/routes/tasks.py', 'exec'); print('syntax ok')"` -> `syntax ok`.
- Boundary: this removes another public backend path leak from local API contracts. It does not prove remote upload storage, remote worker/container execution, configured model gateway, production CORS, native QC/report serving, or fresh strict remote smoke acceptance.

## Console API client strips leaked backend paths

- Added a frontend defense-in-depth layer in `apps/console/src/lib/api.ts` so task APIs and the legacy outputs API sanitize backend path fields before route components see the response.
- `api.runSeries`, `api.getTask`, and `api.listProjectTasks` now strip `log_path` and redact host-path substrings in task strings such as `error_message`.
- `api.getOutputs` now strips backend path keys such as `path`, `preview_path`, `log_path`, `storage_path`, `summary_path`, `absolute_path`, `host_path`, and `output_dir` recursively from the legacy outputs response.
- Tightened the frontend `Task` type by removing `log_path`, matching the public backend contract.
- TDD red evidence: `npm.cmd test -- api.test.ts` initially failed because `api.runSeries`, `api.listProjectTasks`, `api.getTask`, and `api.getOutputs` returned mocked backend path fields unchanged.
- Focused verification: `npm.cmd test -- api.test.ts` -> `1 file, 9 passed`.
- Main-chain frontend verification: `npm.cmd test -- DashboardPage.test.tsx WorkflowsPage.test.tsx TasksPage.test.tsx ResultsIndexPage.test.tsx ResultDetailPage.test.tsx ReportsPage.test.tsx api.test.ts` -> `7 files, 32 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this is a UI-side safety net for accidental backend regressions. The backend contracts still remain authoritative, and product acceptance still requires fresh remote upload, Agent, workflow launch, container processing, QC/report artifact, model gateway, production CORS, and strict smoke evidence.

## Upload responses omit backend storage paths

- Updated upload responses for `/projects/{project_id}/upload`, `/projects/{project_id}/upload-dwi`, and `/projects/{project_id}/upload-dicom` so public `file`/`files` objects omit backend `storage_path`.
- Kept stored paths in the `files` table and internal workflow code. Tests that need physical sidecar placement now read the internal `storage_path` from the database by `file.id`, instead of relying on the public upload response.
- Tightened public series metadata by removing backend path fields such as `dicom_dir`, `storage_path`, `file_storage_path`, `sidecars`, `output_dir`, `work_dir`, `bids_dir`, and `log_path` after workflow eligibility is computed.
- Added a frontend API-client defense so `api.uploadNifti`, `api.uploadDwi`, and `api.uploadDicom` strip `storage_path` if an old or regressed backend returns it.
- Updated `docs/api.md` to document the safe upload file contract and the public series metadata path boundary.
- TDD red evidence: `python -m pytest tests/test_api_flow.py::test_upload_responses_omit_backend_storage_paths -q` initially failed because `/upload` returned `file.storage_path`; `npm.cmd test -- api.test.ts` initially failed because the console API client returned mocked `storage_path` unchanged.
- Focused backend verification: `python -m pytest tests/test_api_flow.py::test_upload_responses_omit_backend_storage_paths -q` -> `1 passed, 3 warnings`.
- Backend upload/workflow regression verification: `python -m pytest tests/test_api_flow.py::test_upload_responses_omit_backend_storage_paths tests/test_api_flow.py::test_full_t1_mock_flow tests/test_api_flow.py::test_pipeline_bids_preserves_uncompressed_nifti_and_sidecars tests/test_api_flow.py::test_dwi_bids_includes_project_t1_when_available tests/test_api_flow.py::test_dwi_stage_copies_uploaded_json_sidecar_by_file_id tests/test_fixed_workflow_api_contract.py::test_dwi_fast_gpu_dti_requires_json_sidecar_metadata -q` -> `6 passed, 3 warnings`.
- Agent/series context regression verification: `python -m pytest tests/test_agent_tools.py tests/test_agent_api.py::test_agent_run_response_redacts_nested_backend_paths -q` -> `16 passed, 3 warnings`.
- Frontend upload/main-chain verification: `npm.cmd test -- api.test.ts IngestPage.test.tsx DashboardPage.test.tsx WorkflowsPage.test.tsx` -> `4 files, 29 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Syntax verification: `python -c "from pathlib import Path; compile(Path('app/services/upload_service.py').read_text(encoding='utf-8'), 'app/services/upload_service.py', 'exec'); compile(Path('app/imaging/series_records.py').read_text(encoding='utf-8'), 'app/imaging/series_records.py', 'exec'); print('syntax ok')"` -> `syntax ok`.
- Boundary: this hardens local upload and series contracts for the fastest launch path. It does not prove remote upload storage, remote workflow completion, native QC/report serving, configured model gateway, production CORS, or fresh strict remote smoke acceptance.

## Upload-session inventory omits backend paths

- Updated public upload-session inventory responses from `/projects/{project_id}/datasets/{upload_session_id}/ingest` and `/inventory` so they do not expose backend project roots, extracted DICOM source paths, sidecar absolute paths, storage paths, output directories, work directories, or log paths.
- Public inventory now reports `bids_dataset_root` as the safe relative value `bids/rawdata`, while per-series `bids_path` remains available for BIDS-like placement context.
- Kept workflow eligibility derivation intact: internal ingest code still uses real file paths before the public inventory sanitizer runs.
- Added the same defense to the console API client for `api.ingestDataset` and `api.getInventory`, so a stale backend cannot leak inventory paths into Dashboard or Ingest UI state.
- Tightened the console `Inventory` type to include `dicom.failures` and `series`, matching the actual inventory contract used by the upload main flow and strict smoke.
- TDD red evidence: `python -m pytest tests/test_api_flow.py::test_mixed_dataset_ingest_inventory_and_bids tests/test_api_flow.py::test_mixed_dataset_ingest_keeps_nifti_inventory_when_dcm2niix_missing -q` initially failed because `bids_dataset_root` was an absolute project path and DICOM failures included `source`; `npm.cmd test -- api.test.ts` initially failed because frontend inventory calls returned those paths unchanged.
- Focused backend verification: `python -m pytest tests/test_api_flow.py::test_mixed_dataset_ingest_inventory_and_bids tests/test_api_flow.py::test_mixed_dataset_ingest_keeps_nifti_inventory_when_dcm2niix_missing -q` -> `2 passed, 3 warnings`.
- Backend ingest/storage regression verification: `python -m pytest tests/test_api_flow.py::test_mixed_dataset_ingest_inventory_and_bids tests/test_api_flow.py::test_dataset_inventory_endpoint_enriches_legacy_series_workflow_eligibility tests/test_api_flow.py::test_mixed_dataset_ingest_keeps_nifti_inventory_when_dcm2niix_missing tests/test_upload_file_storage.py -q` -> `4 passed, 3 warnings`.
- Remote-smoke contract verification: `python -m pytest tests/test_smoke_remote_agent.py::test_smoke_remote_agent_validates_dataset_inventory_workflow_eligibility tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_rejects_weak_evidence -q` -> `84 passed`.
- Frontend inventory verification: `npm.cmd test -- api.test.ts IngestPage.test.tsx DashboardPage.test.tsx` -> `3 files, 27 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Syntax verification: `python -c "from pathlib import Path; compile(Path('app/services/upload_service.py').read_text(encoding='utf-8'), 'app/services/upload_service.py', 'exec'); print('syntax ok')"` -> `syntax ok`.
- Boundary: this hardens local and strict-smoke-facing inventory contracts. It does not prove a fresh remote upload session has completed, nor does it prove remote workflow completion, container-native QC/report artifacts, configured model gateway, production CORS, or final strict remote acceptance.

## Upload inventory top-level errors redact paths

- Tightened `/projects/{project_id}/datasets/{upload_session_id}/inventory` so the top-level `error_message` from the `upload_sessions` row is redacted before it reaches frontend or Agent-facing consumers.
- This closes a remaining upload-session path leak where the nested inventory payload was safe, but a stored session error such as `failed under /home/.../uploads/1` could still be returned outside `inventory`.
- TDD red evidence: `python -m pytest tests/test_api_flow.py::test_dataset_inventory_endpoint_enriches_legacy_series_workflow_eligibility -q` initially failed because the response JSON still contained `/home/yyf/project/image_agent`.
- Focused verification: `python -m pytest tests/test_api_flow.py::test_dataset_inventory_endpoint_enriches_legacy_series_workflow_eligibility -q` -> `1 passed, 3 warnings`.
- Upload-session regression verification: `python -m pytest tests/test_api_flow.py::test_mixed_dataset_ingest_inventory_and_bids tests/test_api_flow.py::test_dataset_inventory_endpoint_enriches_legacy_series_workflow_eligibility tests/test_api_flow.py::test_mixed_dataset_ingest_keeps_nifti_inventory_when_dcm2niix_missing -q` -> `3 passed, 3 warnings`.
- Frontend inventory verification: `npm.cmd test -- api.test.ts IngestPage.test.tsx DashboardPage.test.tsx` -> `3 files, 27 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Syntax verification: `python -c "from pathlib import Path; compile(Path('app/services/upload_service.py').read_text(encoding='utf-8'), 'app/services/upload_service.py', 'exec'); print('syntax ok')"` -> `syntax ok`.
- Boundary: this hardens failed upload-session status reporting only. It still does not prove remote upload completion, workflow execution, container-native QC/report artifact serving, model gateway configuration, production CORS, or fresh strict remote smoke acceptance.

## Agent task tools omit backend log paths

- Tightened Agent-facing task tools so `read_task` strips `log_path` before returning task state to chat/tool callers.
- Tightened `read_task_events` so `main_log` no longer exposes a filesystem `path`, remote wrapper log items no longer expose `path`, and host-style paths in log tails or task `error_message` are replaced with `[redacted-host-path]`.
- Kept the raw internal task lookup available inside `read_task_events`, so deterministic backend code can still read the stored log path before emitting the safe Agent-facing response.
- TDD red evidence: `python -m pytest tests/test_agent_tools.py::test_read_task_and_events_include_remote_wrapper_logs -q` first failed because `events["main_log"]` exposed `path`; after that fix, the extended test failed because `read_task()` still exposed `log_path`.
- Focused verification: `python -m pytest tests/test_agent_tools.py::test_read_task_and_events_include_remote_wrapper_logs -q` -> `1 passed`.
- Agent regression verification: `python -m pytest tests/test_agent_tools.py tests/test_agent_tool_dispatcher.py tests/test_agent_api.py::test_agent_run_response_redacts_nested_backend_paths -q` -> `28 passed, 3 warnings`.
- Syntax verification: `python -c "from pathlib import Path; compile(Path('app/agent/tools.py').read_text(encoding='utf-8'), 'app/agent/tools.py', 'exec'); print('syntax ok')"` -> `syntax ok`.
- Boundary: this only hardens Agent tool output. The authoritative deterministic log endpoint remains `/tasks/{task_id}/logs`, and remote acceptance still requires fresh remote upload, workflow execution, container-native QC/report artifacts, configured model gateway, production CORS, and strict smoke evidence.

## Agent runs fall back when model gateway is unconfigured

- Added a read-only fallback for `POST /agent/runs` when the default model gateway is not configured, so the full Agent page can still answer from backend task state and local RAG/tool inspection instead of returning `502`.
- The fallback is explicitly marked with `selected_skill=backend-status-fallback` and `safe_metadata.fallback_reason=model_gateway_unconfigured`.
- The fallback does not create workflow tasks, does not bypass confirmation, and does not use raw output paths in its backend context; workflow execution still requires deterministic backend APIs and explicit user action.
- Kept existing test/script monkeypatch behavior intact: when tests replace `AgentRunner`, the replaced runner still executes even without an API key; only the default real runner uses this unconfigured-gateway fallback.
- TDD red evidence: `python -m pytest tests/test_agent_api.py::test_agent_run_falls_back_to_read_only_backend_answer_when_model_unconfigured -q` initially failed with `502 == 200` after the test fixture was corrected to use the real file/series/task schema.
- Focused verification: `python -m pytest tests/test_agent_api.py::test_agent_run_falls_back_to_read_only_backend_answer_when_model_unconfigured -q` -> `1 passed, 3 warnings`.
- Agent API regression verification: `python -m pytest tests/test_agent_api.py::test_agent_run_returns_answer_and_persists_privacy_safe_ledger tests/test_agent_api.py::test_agent_run_falls_back_to_read_only_backend_answer_when_model_unconfigured tests/test_agent_api.py::test_agent_run_failure_ledger_redacts_sensitive_error_text tests/test_agent_api.py::test_agent_run_response_redacts_nested_backend_paths -q` -> `4 passed, 3 warnings`.
- Wider Agent verification: `python -m pytest tests/test_agent_api.py tests/test_agent_run_ledger.py tests/test_agent_tools.py tests/test_agent_tool_dispatcher.py -q` -> `63 passed, 3 warnings`.
- Console Agent verification: `npm.cmd test -- AgentPage.test.tsx api.test.ts` -> `2 files, 15 passed`.
- Syntax verification: `python -c "from pathlib import Path; compile(Path('app/services/agent_service.py').read_text(encoding='utf-8'), 'app/services/agent_service.py', 'exec'); compile(Path('app/agent/run_ledger.py').read_text(encoding='utf-8'), 'app/agent/run_ledger.py', 'exec'); print('syntax ok')"` -> `syntax ok`.
- Boundary: this improves local/current usability for `configured:false`; it is not a substitute for configuring the OpenAI-compatible gateway before production, and it still does not prove remote upload completion, workflow execution, container-native QC/report artifact serving, production CORS, or fresh strict remote acceptance.

## Agent page can approve workflow confirmations

- Added a console `api.resumeAgent()` client for `POST /agent/runs/{thread_id}/resume` so the full Agent page can continue a server-side pending workflow confirmation.
- Updated the Agent page to render an `Approval required` panel when `/agent/runs` returns `status=confirmation_required`, including the backend-returned workflow and series id.
- The `Approve workflow` button sends the backend-returned confirmation payload back to `/agent/runs/{thread_id}/resume`; it does not construct workflow parameters in the frontend.
- After approval, the page appends the resulting task-created message and invalidates project task queries so the deterministic task record can appear in task-oriented pages.
- Added a cancel button for the same pending confirmation, using the same resume endpoint with `approved=false`.
- TDD red evidence: `npm.cmd test -- AgentPage.test.tsx api.test.ts` initially failed because `api.resumeAgent` did not exist and no `Approval required` UI was rendered for `confirmation_required`.
- Focused frontend verification: `npm.cmd test -- AgentPage.test.tsx api.test.ts` -> `2 files, 17 passed`.
- Main-chain frontend verification: `npm.cmd test -- DashboardPage.test.tsx WorkflowsPage.test.tsx TasksPage.test.tsx ResultsIndexPage.test.tsx ResultDetailPage.test.tsx AgentPage.test.tsx api.test.ts` -> `7 files, 38 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Backend resume boundary verification: `python -m pytest tests/test_agent_api.py::test_agent_resume_approved_confirmation_creates_real_task tests/test_agent_api.py::test_agent_resume_rejects_confirmation_mismatch tests/test_agent_api.py::test_agent_run_falls_back_to_read_only_backend_answer_when_model_unconfigured -q` -> `3 passed, 3 warnings`.
- Boundary: this makes Agent-assisted workflow launch usable only after explicit user approval and backend confirmation matching. It still does not grant the model direct task-launch authority, and it does not replace remote acceptance for real upload, container processing, native QC/report artifacts, production CORS, configured model gateway, or fresh strict smoke evidence.

## Project creation enters dashboard

- Updated the console project entry flow so a successful `api.createProject()` response navigates directly to `/projects/{project.id}/dashboard`.
- This removes the empty-database launch friction where a user could create a workspace but still had to manually find and open the new project before upload, workflow selection, task monitoring, or Agent chat could begin.
- Kept the backend as the source of truth: the frontend only navigates to the project id returned by the deterministic create-project API.
- TDD red evidence: `npm.cmd test -- ProjectsPage.test.tsx` initially failed because `api.createProject()` was called but no route transition to `/projects/14/dashboard` occurred.
- Focused frontend verification: `npm.cmd test -- ProjectsPage.test.tsx` -> `1 file, 1 passed`.
- Main-chain frontend verification: `npm.cmd test -- ProjectsPage.test.tsx DashboardPage.test.tsx AgentPage.test.tsx WorkflowsPage.test.tsx TasksPage.test.tsx api.test.ts` -> `6 files, 36 passed`.
- Boundary: this improves the local console entry path into the upload/workflow/task/Agent surface. It does not prove remote upload completion, real container workflow execution, native QC/report artifact serving, configured model gateway, production CORS, or fresh strict remote smoke acceptance.

## Workflow launch shows task handoff

- Updated the standalone console Workflows page so a successful backend `api.runSeries()` response is preserved as the latest started task.
- The page now shows `Task #{id} started`, the backend-returned workflow type, and a `View task progress` link to `/projects/{project_id}/tasks`.
- This makes the workflow-selection path less ambiguous: users can see that the deterministic backend created a task and where to monitor queued/running/completed state.
- The frontend still does not create or infer tasks by itself; the handoff is rendered only from the task object returned by `/series/{series_id}/run`.
- TDD red evidence: `npm.cmd test -- WorkflowsPage.test.tsx` initially failed because `api.runSeries()` was called but `Task #91 started` was not rendered.
- Focused frontend verification: `npm.cmd test -- WorkflowsPage.test.tsx` -> `1 file, 3 passed`.
- Main-chain frontend verification: `npm.cmd test -- ProjectsPage.test.tsx DashboardPage.test.tsx WorkflowsPage.test.tsx TasksPage.test.tsx AgentPage.test.tsx api.test.ts` -> `6 files, 36 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this improves local console workflow-start feedback only. It does not prove remote upload completion, real container workflow execution, native QC/report artifact serving, configured model gateway, production CORS, or fresh strict remote smoke acceptance.

## Agent run failures render in chat

- Updated the full console Agent page so a failed `api.runAgent()` call appends an Agent chat message with the backend/client error text instead of leaving only the user's message visible.
- This keeps the Agent interaction path usable when the OpenAI-compatible gateway is unavailable, the backend returns a validation error, or the request fails during deployment bring-up.
- The change does not retry automatically, fabricate an answer, or create tasks; it only surfaces the deterministic failure state to the user.
- TDD red evidence: `npm.cmd test -- AgentPage.test.tsx` initially failed because `Agent model gateway unavailable.` was not rendered after a rejected `api.runAgent()` call.
- Focused frontend verification: `npm.cmd test -- AgentPage.test.tsx` -> `1 file, 6 passed`.
- Main-chain frontend verification: `npm.cmd test -- ProjectsPage.test.tsx DashboardPage.test.tsx WorkflowsPage.test.tsx TasksPage.test.tsx AgentPage.test.tsx api.test.ts` -> `6 files, 37 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this improves local console Agent failure visibility only. It does not replace configuring the model gateway, does not grant the Agent workflow-launch authority, and does not prove remote upload completion, real container workflow execution, native QC/report artifacts, production CORS, or fresh strict remote smoke acceptance.

## Workflow launch failures render backend reasons

- Updated the standalone console Workflows page so failed `api.runSeries()` calls show a `Workflow launch failed` message with the backend/client error text.
- Failed launches now clear any previous task-start handoff, so the page does not imply a task was created when the deterministic backend rejected the request.
- This is especially important for fast launch on the remote server, where runtime containers, licenses, environment variables, or workflow preflight can reject a launch and the operator needs the actual reason immediately.
- TDD red evidence: `npm.cmd test -- WorkflowsPage.test.tsx` initially failed because `Remote DeepPrep runtime is not configured.` was not rendered after a rejected `api.runSeries()` call.
- Focused frontend verification: `npm.cmd test -- WorkflowsPage.test.tsx` -> `1 file, 4 passed`.
- Main-chain frontend verification: `npm.cmd test -- ProjectsPage.test.tsx DashboardPage.test.tsx WorkflowsPage.test.tsx TasksPage.test.tsx AgentPage.test.tsx api.test.ts` -> `6 files, 38 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this improves local console workflow-launch failure visibility only. Workflow execution still depends on deterministic backend validation and fresh remote evidence for upload completion, real container execution, native QC/report artifacts, configured model gateway, production CORS, and strict remote smoke acceptance.

## Dashboard result preview uses backend artifacts

- Removed the static `/neuro-assets/*` MRI/QC placeholder gallery from the Dashboard results preview.
- The Dashboard now renders preview images only from backend `result-summary` outputs that include both an `image/*` content type and a `download_url`.
- Kept the Key Artifacts list backend-backed as before, so the preview and downloadable outputs are both derived from deterministic task artifacts instead of bundled local demo images.
- TDD red evidence: `npm.cmd test -- DashboardPage.test.tsx` initially failed because `reports/t1_brain_measures_overview.png` was not rendered as an image artifact preview and the old local `T1w axial MRI preview` placeholder still existed.
- Focused frontend verification: `npm.cmd test -- DashboardPage.test.tsx` -> `1 file, 14 passed`.
- Main-chain frontend verification: `npm.cmd test -- ProjectsPage.test.tsx DashboardPage.test.tsx WorkflowsPage.test.tsx TasksPage.test.tsx AgentPage.test.tsx api.test.ts` -> `6 files, 39 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this improves the local console result-preview contract only. It does not prove remote upload completion, real container workflow execution, container-native QC/report artifact availability, configured model gateway, production CORS, or fresh strict remote smoke acceptance.

## Stale-task apply handoff requires full main-flow smoke

- Strengthened `build_stale_task_apply_request.py` so the generated post-authorization strict remote smoke command now requires project-scoped Agent context, production readiness, expected health version, completed upload inventory, completed task status, deterministic launched task evidence, launch series/workflow ids, task-completion polling, container-native QC, and derived scientific report artifacts.
- Strengthened `verify_stale_task_apply_request.py` so a saved apply request is rejected if its strict smoke follow-up omits those full main-flow flags.
- The generated strict smoke handoff no longer uses a placeholder `<completed_task_id>`; it must launch through deterministic `/series/{series_id}/run` and let the smoke resolve the backend task id before checking completion, result-summary, artifact manifest, native QC, and report artifacts.
- Narrowed the API-key-shaped command scanner to avoid false positives on legitimate flags such as `--wait-task-completion-timeout-seconds` while still rejecting standalone `sk-...` shaped secrets.
- TDD red evidence: `python -m pytest test_build_stale_task_apply_request.py::test_build_stale_task_apply_request_includes_post_apply_gates -q` first failed because `--require-project-agent-context` was missing from the generated strict smoke command; `python -m pytest test_verify_stale_task_apply_request.py::test_verify_stale_task_apply_request_requires_full_main_flow_strict_smoke -q` first failed because the verifier did not reject a request missing that flag.
- Focused verification: both red tests now pass.
- Release-gate verification: `python -m pytest test_build_stale_task_apply_request.py test_verify_stale_task_apply_request.py test_release_gate_command_plan.py -q` -> `17 passed`; `python -m pytest test_verify_remote_smoke_acceptance.py test_smoke_remote_agent.py -q` -> `237 passed`.
- Readiness/docs verification: `python -m pytest test_release_gate_command_plan.py test_build_stale_task_apply_request.py test_verify_stale_task_apply_request.py test_verify_remote_smoke_acceptance.py test_smoke_remote_agent.py test_skill_and_rag_docs.py::test_developer_testing_matrix_requires_stale_task_gate_before_strict_acceptance test_skill_and_rag_docs.py::test_remote_stale_task_apply_request_docs_expose_expiry_field test_skill_and_rag_docs.py::test_product_readiness_gate_blocks_frontend_until_agent_contracts_are_verified test_skill_and_rag_docs.py::test_product_readiness_documents_fast_launch_main_flow_and_boundaries -q` -> `258 passed`.
- Script verification: `python -c "from pathlib import Path; files=['apps/api/scripts/build_stale_task_apply_request.py','apps/api/scripts/verify_stale_task_apply_request.py']; [compile(Path(f).read_text(encoding='utf-8'), f, 'exec') for f in files]; print('syntax ok')"` -> `syntax ok`; `python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` -> `status=passed`.
- Boundary: this only makes the remote operator handoff stricter and harder to misuse. It does not run stale-task apply, does not restart the remote service, does not configure the model gateway, and does not create fresh 24-hour strict remote smoke evidence.

## Apply request verifier enforces complete readiness flags

- Tightened `verify_stale_task_apply_request.py` so the apply request verifier now rejects strict-smoke follow-up commands that omit deployment identity, deployment id, RAG document/chunk thresholds, raw-source policy, vendor pointer integrity, launchability matrix evidence, native QC image minimums, scientific report image minimums, project id, or upload-session id.
- This closes the remaining gap where `build_stale_task_apply_request.py` generated the stronger strict-smoke command but a hand-edited saved request could still pass verifier checks with weaker readiness evidence.
- TDD red evidence: `python -m pytest test_verify_stale_task_apply_request.py::test_verify_stale_task_apply_request_requires_complete_readiness_smoke_flags -q` first failed 11 cases because the verifier did not reject missing readiness flags.
- Focused verification: `python -m pytest test_verify_stale_task_apply_request.py::test_verify_stale_task_apply_request_requires_complete_readiness_smoke_flags -q` -> `11 passed`; `python -m pytest test_verify_stale_task_apply_request.py -q` -> `20 passed`.
- Release-gate verification: `python -m pytest test_build_stale_task_apply_request.py test_verify_stale_task_apply_request.py test_release_gate_command_plan.py -q` -> `28 passed`; `python -m pytest test_verify_remote_smoke_acceptance.py test_smoke_remote_agent.py -q` -> `237 passed`.
- Script/docs verification: `python -c "from pathlib import Path; files=['apps/api/scripts/verify_stale_task_apply_request.py']; [compile(Path(f).read_text(encoding='utf-8'), f, 'exec') for f in files]; print('syntax ok')"` -> `syntax ok`; `python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` -> `status=passed`; product readiness doc slice -> `4 passed`.
- Boundary: this is still a verifier/handoff hardening step only. It does not mutate stale task rows, restart the remote API, configure the model gateway, upload new imaging data, launch a new workflow, or produce fresh strict remote acceptance JSON.

## Strict smoke follow-up is marked mutating

- Corrected `build_stale_task_apply_request.py` so the `run_strict_remote_smoke_acceptance` follow-up step is marked with `mutates_remote_state=true`.
- Added verifier coverage so `verify_stale_task_apply_request.py` rejects saved apply requests that mark strict remote smoke as read-only, because `--require-launched-task` creates a real backend task through the deterministic `/series/{series_id}/run` API.
- This aligns the operator handoff metadata with the release gate command plan, where `run_strict_remote_smoke_acceptance` is already treated as a mutating step.
- TDD red evidence: `python -m pytest test_build_stale_task_apply_request.py::test_build_stale_task_apply_request_includes_post_apply_gates -q` first failed because the generated strict-smoke step had `mutates_remote_state=false`; `python -m pytest test_verify_stale_task_apply_request.py::test_verify_stale_task_apply_request_requires_strict_smoke_marked_mutating -q` first failed because the verifier did not reject that unsafe metadata.
- Focused verification: both red tests now pass.
- Release-gate verification: `python -m pytest test_build_stale_task_apply_request.py test_verify_stale_task_apply_request.py test_release_gate_command_plan.py -q` -> `29 passed`; `python -m pytest test_verify_remote_smoke_acceptance.py test_smoke_remote_agent.py -q` -> `237 passed`.
- Script verification: `python -c "from pathlib import Path; files=['apps/api/scripts/build_stale_task_apply_request.py','apps/api/scripts/verify_stale_task_apply_request.py']; [compile(Path(f).read_text(encoding='utf-8'), f, 'exec') for f in files]; print('syntax ok')"` -> `syntax ok`; `python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` -> `status=passed`.
- Boundary: this only fixes handoff metadata and verifier enforcement. It does not execute stale-task apply, restart the remote API, launch a real workflow, or produce fresh strict remote acceptance evidence.

## Strict smoke evidence path is single-source

- Tightened `verify_stale_task_apply_request.py` so the strict smoke `--output-json` path, the `run_strict_remote_smoke_acceptance.expected_output_json` metadata, and the follow-up `verify_remote_smoke_acceptance.py` command must all reference the same JSON file.
- Added shell-token parsing for the static handoff command text so the verifier checks the actual `--output-json` argument instead of relying only on broad substring checks.
- This prevents an operator handoff from claiming one strict-smoke evidence file while the follow-up verifier reads a different file.
- TDD red evidence: `python -m pytest test_verify_stale_task_apply_request.py::test_verify_stale_task_apply_request_requires_single_strict_smoke_json_path -q` first failed both mismatch cases because the verifier did not compare the three strict-smoke JSON references.
- Focused verification: `python -m pytest test_verify_stale_task_apply_request.py::test_verify_stale_task_apply_request_requires_single_strict_smoke_json_path -q` -> `2 passed`; `python -m pytest test_verify_stale_task_apply_request.py -q` -> `23 passed`.
- Release-gate verification: `python -m pytest test_build_stale_task_apply_request.py test_verify_stale_task_apply_request.py test_release_gate_command_plan.py -q` -> `31 passed`; `python -m pytest test_verify_remote_smoke_acceptance.py test_smoke_remote_agent.py -q` -> `237 passed`.
- Script verification: `python -c "from pathlib import Path; files=['apps/api/scripts/verify_stale_task_apply_request.py']; [compile(Path(f).read_text(encoding='utf-8'), f, 'exec') for f in files]; print('syntax ok')"` -> `syntax ok`; `python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` -> `status=passed`.
- Boundary: this only hardens static apply-request validation. It does not run remote strict smoke, prove that the JSON exists, or replace fresh remote acceptance after normal restart.

## Command plan verifies strict smoke evidence path

- Tightened `verify_release_gate_command_plan.py` so the machine-checkable release command plan now compares the strict smoke `--output-json` path with the JSON path passed to `verify_remote_smoke_acceptance.py`.
- This aligns the higher-level release plan verifier with the stricter apply-request verifier, so both gates reject a plan that writes one strict-smoke JSON but verifies another.
- Added shell-token parsing for the command-plan verifier instead of relying only on broad substring checks for the strict-smoke output path.
- TDD red evidence: `python -m pytest test_release_gate_command_plan.py::test_remote_release_gate_command_plan_requires_single_strict_smoke_json_path -q` first failed because `verify_plan()` allowed a mismatched strict-smoke verifier JSON path.
- Focused verification: `python -m pytest test_release_gate_command_plan.py::test_remote_release_gate_command_plan_requires_single_strict_smoke_json_path -q` -> `1 passed`; `python -m pytest test_release_gate_command_plan.py -q` -> `6 passed`.
- Release-gate verification: `python -m pytest test_release_gate_command_plan.py test_build_stale_task_apply_request.py test_verify_stale_task_apply_request.py -q` -> `32 passed`; `python -m pytest test_verify_remote_smoke_acceptance.py test_smoke_remote_agent.py -q` -> `237 passed`.
- Script verification: `python -c "from pathlib import Path; files=['apps/api/scripts/verify_release_gate_command_plan.py','apps/api/scripts/verify_stale_task_apply_request.py']; [compile(Path(f).read_text(encoding='utf-8'), f, 'exec') for f in files]; print('syntax ok')"` -> `syntax ok`; `python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` -> `status=passed`.
- Boundary: this only hardens static command-plan validation. It does not execute the remote plan, create the strict-smoke JSON, or replace fresh remote acceptance evidence.

## Command plan verifier requires full strict-smoke flags

- Tightened `verify_release_gate_command_plan.py` so the release command plan verifier now requires the same main-flow strict-smoke flags that the apply-request verifier already enforces: project-scoped Agent context, production readiness, RAG document/chunk thresholds, completed upload evidence, and completed task evidence.
- This prevents the higher-level release plan verifier from accepting a strict-smoke command that is weaker than the operator apply-request verifier.
- TDD red evidence: `python -m pytest test_release_gate_command_plan.py::test_remote_release_gate_command_plan_requires_full_strict_smoke_flags -q` first failed all 6 cases because `verify_plan()` did not reject missing project/production/upload/task flags.
- Focused verification: `python -m pytest test_release_gate_command_plan.py::test_remote_release_gate_command_plan_requires_full_strict_smoke_flags -q` -> `6 passed`; `python -m pytest test_release_gate_command_plan.py -q` -> `12 passed`.
- Release-gate verification: `python -m pytest test_release_gate_command_plan.py test_build_stale_task_apply_request.py test_verify_stale_task_apply_request.py -q` -> `38 passed`; `python -m pytest test_verify_remote_smoke_acceptance.py test_smoke_remote_agent.py -q` -> `237 passed`.
- Script verification: `python -c "from pathlib import Path; files=['apps/api/scripts/verify_release_gate_command_plan.py','apps/api/scripts/verify_stale_task_apply_request.py']; [compile(Path(f).read_text(encoding='utf-8'), f, 'exec') for f in files]; print('syntax ok')"` -> `syntax ok`; `python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` -> `status=passed`.
- Boundary: this only aligns static release-plan validation with static apply-request validation. It does not run remote upload, task launch, model smoke, stale-task apply, restart, or fresh strict acceptance.

## Command plan verifier requires deployment id

- Tightened `verify_release_gate_command_plan.py` so the strict-smoke command must include `--deployment-id <accepted_release_or_commit>`, matching the apply-request verifier and the strict remote acceptance verifier.
- This matters because deployment identity prevents fresh smoke evidence from being accepted against the wrong release or an old serving API version.
- TDD red evidence: `python -m pytest test_release_gate_command_plan.py::test_remote_release_gate_command_plan_requires_full_strict_smoke_flags -q` first failed the new deployment-id case while the other six cases passed.
- Focused verification: `python -m pytest test_release_gate_command_plan.py::test_remote_release_gate_command_plan_requires_full_strict_smoke_flags -q` -> `7 passed`; `python -m pytest test_release_gate_command_plan.py -q` -> `13 passed`.
- Release-gate verification: `python -m pytest test_release_gate_command_plan.py test_build_stale_task_apply_request.py test_verify_stale_task_apply_request.py -q` -> `39 passed`; `python -m pytest test_verify_remote_smoke_acceptance.py test_smoke_remote_agent.py -q` -> `237 passed`.
- Script verification: `python -c "from pathlib import Path; files=['apps/api/scripts/verify_release_gate_command_plan.py']; [compile(Path(f).read_text(encoding='utf-8'), f, 'exec') for f in files]; print('syntax ok')"` -> `syntax ok`; `python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` -> `status=passed`.
- Boundary: this is still static command-plan validation only. It does not prove the remote deployment id, run model smoke, or create fresh remote acceptance evidence.

## Dashboard chat uses Agent runs first

- Updated the Dashboard embedded chat so quick actions and typed messages call `api.runAgent()` first, giving the main upload/workflow/task dashboard the same Agent run ledger and project-scoped `/agent/runs` path as the full Agent page.
- Kept legacy `api.chat()` as a fallback only when `/agent/runs` fails, so local bring-up still has a basic backend answer path without making legacy chat the primary Agent interaction.
- Added a Dashboard fallback test proving `api.chat()` is called only after `api.runAgent()` rejects.
- TDD red evidence: `npm.cmd test -- DashboardPage.test.tsx` first failed because `api.runAgent` was never called for `Explain this step`; after adding the fallback test, both the primary and fallback cases failed for the same reason.
- Focused frontend verification: `npm.cmd test -- DashboardPage.test.tsx` -> `1 file, 15 passed`.
- Main-chain frontend verification: `npm.cmd test -- ProjectsPage.test.tsx DashboardPage.test.tsx WorkflowsPage.test.tsx TasksPage.test.tsx AgentPage.test.tsx api.test.ts` -> `6 files, 40 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this improves the local/current console Agent interaction path only. It does not configure the remote model gateway, does not let the Agent bypass deterministic workflow APIs, and does not prove remote upload, workflow execution, container-native QC artifacts, production CORS, or fresh strict remote smoke acceptance.

## Dashboard refreshes tasks after Agent-created task

- Updated the Dashboard embedded chat success path so a project-scoped Agent run that returns `status=task_created` invalidates `queryKeys.tasks(projectId)` and immediately refetches the backend task list.
- The UI message still comes from backend-returned Agent run metadata; the frontend does not fabricate task rows or launch workflows outside the deterministic backend API path.
- Legacy `api.chat()` fallback responses do not trigger task refresh, because that path does not return authoritative task creation metadata.
- TDD red evidence: `npm.cmd test -- DashboardPage.test.tsx` first failed because the new `refreshes project tasks when a dashboard Agent run creates a backend task` test saw zero `invalidateQueries()` calls.
- Focused frontend verification: `npm.cmd test -- DashboardPage.test.tsx` -> `1 file, 16 passed`.
- Main-chain frontend verification: `npm.cmd test -- ProjectsPage.test.tsx DashboardPage.test.tsx WorkflowsPage.test.tsx TasksPage.test.tsx AgentPage.test.tsx api.test.ts` -> `6 files, 41 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this is a local/current console refresh behavior only. It does not configure the remote model gateway, prove remote upload completion, run a real MRI pipeline, produce container-native QC images, harden production CORS, or replace fresh strict remote smoke acceptance.

## Agent page refreshes tasks after backend-created task

- Updated the full Agent Review page so any `/agent/runs` response with `status=task_created` invalidates `queryKeys.tasks(projectId)` after rendering the backend task-created message.
- This matches the Dashboard behavior and keeps the task list cache from remaining stale when a backend Agent run reports an authoritative created task.
- The frontend still does not create workflow tasks itself; task creation remains behind the backend Agent contract, explicit confirmation/resume path, and deterministic workflow task validation.
- TDD red evidence: `npm.cmd test -- AgentPage.test.tsx` first failed because the new `refreshes project tasks when an Agent run returns a created task` test saw zero `invalidateQueries()` calls.
- Focused frontend verification: `npm.cmd test -- AgentPage.test.tsx` -> `1 file, 7 passed`.
- Main-chain frontend verification: `npm.cmd test -- ProjectsPage.test.tsx DashboardPage.test.tsx WorkflowsPage.test.tsx TasksPage.test.tsx AgentPage.test.tsx api.test.ts` -> `6 files, 42 passed`.
- Boundary: this is a local/current console cache refresh behavior only. It does not configure the remote model gateway, prove remote upload completion, run a real MRI pipeline, produce container-native QC images, harden production CORS, or replace fresh strict remote smoke acceptance.

## Frontend workflow list filters non-runnable incubation entries

- Aligned the console workflow catalog normalization with the backend registry runtime boundary: workflow objects with `lane` outside `fixed_workflow` are hidden from direct frontend run controls unless the backend explicitly marks them `api_runnable=true`.
- This prevents incubation entries such as `dwi_qsiprep` from appearing as direct `Run Workflow` options when the backend runtime allowlist would reject them; debug/API-runnable entries such as `t1_deepprep_mock` remain visible when explicitly allowed.
- Added `lane` to the TypeScript workflow catalog item contract so the frontend can honor backend registry metadata instead of guessing from `requires_confirmation` alone.
- TDD red evidence: `npm.cmd test -- workflows.test.ts` first failed because `normalizeWorkflowList()` incorrectly returned `dwi_qsiprep` for a `toolchain_incubation` workflow without `api_runnable=true`.
- Focused frontend verification: `npm.cmd test -- workflows.test.ts` -> `1 file, 6 passed`; `npm.cmd test -- WorkflowsPage.test.tsx` -> `1 file, 4 passed`.
- Boundary: this only removes unsafe direct-run UI entries. It does not change backend workflow validation, run Docker/container jobs locally, configure remote scripts/licenses, or replace strict remote smoke evidence for real workflow execution.

## Agent evidence JSON is redacted in the console

- Added a shared console redaction helper for Agent evidence JSON blocks so Windows host paths, Linux backend paths, `sk-...` API key patterns, and common model/sudo secret assignments are replaced before rendering.
- Applied the helper to the full Agent Review page and the reusable `AgentEvidencePanel`, covering both current and legacy evidence display surfaces.
- This is a frontend display-layer guardrail on top of the backend redaction contracts; it does not mutate the Agent response object or make raw backend paths acceptable API output.
- TDD red evidence: `npm.cmd test -- AgentPage.test.tsx` first failed because the Agent Review Evidence panel rendered `C:/Users/A/private/task.log`, `/home/yyf/project/image_agent/...`, and `sk-test-secret` verbatim; `npm.cmd test -- AgentEvidencePanel.test.tsx` then failed for the same direct JSON rendering issue.
- Focused frontend verification: `npm.cmd test -- AgentPage.test.tsx` -> `1 file, 8 passed`; `npm.cmd test -- AgentPage.test.tsx AgentEvidencePanel.test.tsx` -> `2 files, 10 passed`.
- Main-chain frontend verification: `npm.cmd test -- workflows.test.ts ProjectsPage.test.tsx DashboardPage.test.tsx WorkflowsPage.test.tsx TasksPage.test.tsx AgentPage.test.tsx AgentEvidencePanel.test.tsx api.test.ts` -> `8 files, 51 passed`.
- Boundary: this reduces frontend leakage risk when inspecting Agent evidence. It does not replace backend no-secret/no-path tests, configure the remote model gateway, prove real upload/workflow execution, or replace fresh strict remote smoke acceptance.

## Agent API client responses are sanitized before UI state

- Extended the console API client sanitizer to `/agent/runs`, `/agent/runs/{thread_id}/resume`, `/agent/rag/query`, and legacy `/chat` responses so Agent evidence is redacted before page state receives it.
- Reused the shared console redaction rules for host paths and `sk-...`/model secret strings, while preserving safe curated-document citation paths such as `docs/rag/vendor/*.md`.
- The Agent sanitizer removes backend-only path fields such as `log_path`, `storage_path`, `preview_path`, and `summary_path` but does not remove generic citation `path` fields, because those are the visible RAG source contract.
- TDD red evidence: `npm.cmd test -- api.test.ts` first failed because `api.runAgent()`, `api.resumeAgent()`, and `api.chat()` returned `/home/yyf/project/image_agent/...`, `C:/Users/A/private/task.log`, `log_path`, and `sk-test-secret` verbatim.
- Focused frontend verification: `npm.cmd test -- api.test.ts` -> `1 file, 14 passed`; `npm.cmd test -- AgentPage.test.tsx AgentEvidencePanel.test.tsx api.test.ts` -> `3 files, 24 passed`.
- Main-chain frontend verification: `npm.cmd test -- workflows.test.ts ProjectsPage.test.tsx DashboardPage.test.tsx WorkflowsPage.test.tsx TasksPage.test.tsx AgentPage.test.tsx AgentEvidencePanel.test.tsx api.test.ts` -> `8 files, 53 passed`.
- Boundary: this is a frontend API-client safety belt, not permission to emit unsafe backend data. Backend redaction tests and strict remote smoke evidence remain required before product release.

## Result summaries are sanitized before UI state

- Extended the console API client sanitizer to `/tasks/{task_id}/result-summary` so result-summary data is cleaned before Dashboard, Results, or report preview state receives it.
- Preserved frontend artifact contract fields such as `relative_path`, `download_url`, and `content_type`, while removing backend path fields such as `path`, `summary_path`, `log_path`, and `output_dir`.
- Host-style paths embedded in free-text provenance are replaced with `[redacted-host-path]`, matching the existing upload/task/output response sanitizer behavior.
- TDD red evidence: `npm.cmd test -- api.test.ts` first failed because `getResultSummary()` returned an output `path` and top-level `summary_path` verbatim from a backend-shaped response.
- Focused frontend verification: `npm.cmd test -- api.test.ts` -> `1 file, 15 passed`.
- Main-chain frontend verification: `npm.cmd test -- workflows.test.ts ProjectsPage.test.tsx DashboardPage.test.tsx WorkflowsPage.test.tsx TasksPage.test.tsx AgentPage.test.tsx AgentEvidencePanel.test.tsx api.test.ts` -> `8 files, 54 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this is a frontend state-layer safety belt for result-summary consumers. It does not replace backend artifact-manifest redaction, container-native QC provenance checks, or fresh strict remote smoke acceptance with real task artifacts.

## Artifact manifest client contract is available

- Added the frontend `ArtifactManifest` contract and `api.getArtifactManifest(taskId)` client method for `/tasks/{task_id}/artifact-manifest`.
- The client preserves the stable artifact-manifest fields needed for container-native QC and report previews: `relative_path`, `download_url`, `content_type`, `size_bytes`, `preview_kind`, `native_artifact`, and provenance.
- The response goes through the same frontend result-artifact sanitizer, removing backend path fields such as `path` and `log_path` and redacting host-style paths in free-text provenance or omitted-artifact reasons.
- TDD red evidence: `npm.cmd test -- api.test.ts` first failed because `api.getArtifactManifest` did not exist.
- Focused frontend verification: `npm.cmd test -- api.test.ts` -> `1 file, 16 passed`.
- Main-chain frontend verification: `npm.cmd test -- workflows.test.ts ProjectsPage.test.tsx DashboardPage.test.tsx WorkflowsPage.test.tsx TasksPage.test.tsx AgentPage.test.tsx AgentEvidencePanel.test.tsx api.test.ts` -> `8 files, 55 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this adds the API client foundation for artifact-manifest-based result/QC rendering. It does not yet migrate Dashboard or Results views to prefer artifact manifest over result-summary, and it does not replace remote evidence that real container-native QC artifacts exist and are served.

## Dashboard result preview prefers artifact manifest

- Updated the Dashboard results preview to query `/tasks/{task_id}/artifact-manifest` for the selected completed result task and prefer manifest artifacts over generated result-summary outputs.
- Preview images now come from backend artifact `download_url` entries with `preview_kind=image` or image content type; if the manifest is empty, the Dashboard falls back to existing result-summary outputs.
- The key artifact list also follows the same priority, so container-native QC/report artifacts from the manifest become the first-class result surface instead of frontend static placeholders or guessed summary previews.
- TDD red evidence: `npm.cmd test -- DashboardPage.test.tsx` first failed because the new `prefers artifact manifest previews over generated result summary previews` test could not find `qc/container-native-qc.png`.
- Focused frontend verification: `npm.cmd test -- DashboardPage.test.tsx` -> `1 file, 17 passed`.
- Main-chain frontend verification: `npm.cmd test -- workflows.test.ts ProjectsPage.test.tsx DashboardPage.test.tsx WorkflowsPage.test.tsx TasksPage.test.tsx AgentPage.test.tsx AgentEvidencePanel.test.tsx api.test.ts` -> `8 files, 56 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this is a frontend consumption change only. It does not create container QC artifacts, run Docker locally, prove remote MRI pipeline execution, configure model gateway, harden production CORS, or replace strict remote smoke evidence.

## Result detail studio prefers artifact manifest

- Updated the result detail route to request `/tasks/{task_id}/artifact-manifest` alongside the result summary and pass it into `ResultStudioLayout`.
- Updated the result studio layout so a non-empty artifact manifest becomes the source for report/QC preview figures, feature-group panels, and artifact tables; result-summary outputs remain the fallback when the manifest is empty.
- The manifest split treats report, QC, HTML, and image artifacts as preview/report artifacts, while map/table artifacts remain in the primary artifact manifest table and modality feature groups.
- TDD red evidence: `npm.cmd test -- ResultDetailPage.test.tsx` first failed because the new `prefers artifact manifest files for native QC previews and artifact tables` test could not find `qc/native-dwi-qc.png` from the manifest.
- Focused frontend verification: `npm.cmd test -- ResultDetailPage.test.tsx` -> `1 file, 3 passed`.
- Main-chain frontend verification: `npm.cmd test -- workflows.test.ts resultArtifacts.test.ts ResultStudioLayout.test.tsx ResultDetailPage.test.tsx ResultsIndexPage.test.tsx ProjectsPage.test.tsx DashboardPage.test.tsx WorkflowsPage.test.tsx TasksPage.test.tsx AgentPage.test.tsx AgentEvidencePanel.test.tsx api.test.ts` -> `12 files, 69 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this aligns the frontend result detail surface with backend artifact manifests. It does not prove the remote server has produced real container-native QC artifacts, execute MRI pipelines locally, configure model gateway credentials, or replace strict remote smoke testing.

## Production readiness requires public API base

- Added `IMAGE_AGENT_PUBLIC_BASE_URL` to the production readiness gate so `/deployment.production_readiness.ready` cannot be true in `IMAGE_AGENT_ENV=production` unless the API exposes a public HTTPS base URL for the console and remote smoke evidence.
- The new blocker is `IMAGE_AGENT_PUBLIC_BASE_URL must be set for production deployment.` and applies only in production mode; local development remains unchanged.
- Updated deployment docs and the product readiness gate to list `IMAGE_AGENT_PUBLIC_BASE_URL=https://<api-hostname>` beside production CORS configuration.
- TDD red evidence: `python -m pytest apps/api/tests/test_agent_api.py -k "deployment_status"` first failed because the new production-readiness test still saw `ready=true` with `IMAGE_AGENT_PUBLIC_BASE_URL` unset.
- Focused backend verification: `python -m pytest apps/api/tests/test_agent_api.py -k "deployment_status"` -> `3 passed`; `python -m pytest apps/api/tests/test_app_factory.py apps/api/tests/test_agent_api.py` -> `40 passed`.
- Smoke/readiness verifier check: `python -m pytest apps/api/tests/test_app_factory.py apps/api/tests/test_agent_api.py -k "deployment_status or cors or production_readiness" apps/api/tests/test_smoke_remote_agent.py -k "production_readiness" apps/api/tests/test_verify_remote_smoke_acceptance.py -k "production_readiness"` -> `8 passed`.
- Boundary: this hardens production configuration evidence only. It does not deploy the remote API, configure DNS/TLS, run the model gateway, upload data, execute workflows, produce container-native QC, or replace fresh strict remote smoke acceptance.

## Settings shows backend public API base

- Updated the console Settings API Connection panel to show the backend-reported `deployment.api_base_hint` as `Public API Base`, with `Not reported` when the backend has not exposed a production public API base.
- The production readiness blocker `IMAGE_AGENT_PUBLIC_BASE_URL must be set for production deployment.` is now visible in Settings alongside the missing public API base signal, so the operator can see both the blocker and the backend-reported value.
- TDD red evidence: `npm.cmd test -- SettingsPage.test.tsx` first failed because `Public API Base` and the backend `api_base_hint` were missing from the Settings UI; after the initial implementation, the same test exposed a duplicate `Backend status unavailable` text conflict, which was resolved by keeping public API base missing state as `Not reported`.
- Focused frontend verification: `npm.cmd test -- SettingsPage.test.tsx` -> `1 file, 5 passed`.
- Main-chain frontend verification: `npm.cmd test -- SettingsPage.test.tsx AppShell.test.tsx AgentPage.test.tsx DashboardPage.test.tsx ResultDetailPage.test.tsx api.test.ts` -> `6 files, 51 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this is a frontend observability change for production readiness. It does not configure DNS/TLS, change CORS, run the model gateway, upload MRI data, execute workflows, create QC artifacts, or replace strict remote smoke acceptance.

## Release gate requires refreshed stale-task approval

- Updated `docs/deployment/remote-release-gate-command-plan.json` so the remote release gate is now explicitly `approval_refresh_required` instead of `operator_authorization_required` while the last reviewed stale-task approval JSON is outside its 24 hour freshness window.
- Replaced the expired `/tmp/image_agent_stale_tasks_83_84_dry_run_20260614T080202Z.json` apply input with `<fresh_reviewed_approval_json>` in the verify/apply commands and preserved the expired path only as `approval_json_state.previous_approval_json`.
- Tightened `apps/api/scripts/verify_release_gate_command_plan.py` so the command plan fails if an expired approval JSON is treated as the current apply input; the verifier report now exposes `checked.approval_json_status=refresh_required`.
- TDD red evidence: `python -m pytest apps/api/tests/test_release_gate_command_plan.py -q` first failed because the verifier did not report `approval_json_status`, the plan still had `status=operator_authorization_required`, and the expired approval JSON still passed.
- Focused verification: `python -m pytest apps/api/tests/test_release_gate_command_plan.py -q` -> `14 passed`.
- Release-gate verifier CLI: `python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` -> `status=passed`, `checked.approval_json_status=refresh_required`, and `checked.frontend_gate_status=blocked`.
- Adjacent stale-task request verification: `PYTHONPATH=tests python -m pytest tests/test_build_stale_task_apply_request.py tests/test_verify_stale_task_apply_request.py tests/test_release_gate_command_plan.py -q` from `apps/api` -> `40 passed`.
- Syntax verification: `python -c "from pathlib import Path; files=['apps/api/scripts/verify_release_gate_command_plan.py']; [compile(Path(f).read_text(encoding='utf-8'), f, 'exec') for f in files]; print('syntax ok')"` -> `syntax ok`.
- Boundary: this prevents using stale approval evidence for a remote mutation. It does not refresh the remote approval JSON, apply stale-task resolution, restart the API, configure the model gateway, upload MRI data, launch workflows, generate QC artifacts, or replace fresh strict remote smoke acceptance.

## Product readiness records approval-refresh blocker

- Updated `docs/product-readiness.md` Current Status to match the release-gate command plan: the 2026-06-14 approval JSON and generated apply request are now outside the 24 hour freshness window.
- The gate now says the command plan is `approval_refresh_required`, the verifier reports `checked.approval_json_status=refresh_required`, the expired path is only `previous_approval_json`, and `<fresh_reviewed_approval_json>` must be replaced only after a refreshed operator-reviewed dry-run.
- TDD red evidence: `python -m pytest apps/api/tests/test_skill_and_rag_docs.py -k "product_readiness_gate_blocks_frontend" -q` first failed because `checked.approval_json_status=refresh_required` and the new expired-approval boundary text were missing from product readiness.
- Focused verification: `python -m pytest apps/api/tests/test_skill_and_rag_docs.py -k "product_readiness_gate_blocks_frontend or product_readiness_documents_fast_launch" -q` -> `2 passed`.
- Release-gate consistency checks: `python -m pytest apps/api/tests/test_release_gate_command_plan.py -q` -> `14 passed`; `python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` -> `status=passed`, `checked.approval_json_status=refresh_required`.
- Boundary: this keeps the operator-facing readiness record current. It does not run the remote approval refresh, mutate stale tasks, restart the API, configure the model gateway, upload MRI data, execute workflows, or replace strict remote smoke acceptance.

## Release gate verifier supports fresh-reviewed approval state

- Extended `apps/api/scripts/verify_release_gate_command_plan.py` so it now supports both valid release-gate phases: `approval_refresh_required` with `<fresh_reviewed_approval_json>`, and post-refresh `operator_authorization_required` with a concrete reviewed approval JSON path.
- The post-refresh phase requires `approval_json_state.status=fresh_reviewed`, preserves the expired 2026-06-14 path as `previous_approval_json`, points `next_required_step` at `apply_approved_stale_task_resolution`, and rejects the plan when `approval_json_state.approval_expires_at_utc` is older than `now_utc`.
- Added the CLI `--now-utc` testing hook for deterministic approval-expiry verification.
- Updated `docs/product-readiness.md` to document the state transition: after `stale_task_approval_refresh`, the plan can move to `operator_authorization_required` only while the reviewed approval expiry is still in the future.
- TDD red evidence: `python -m pytest apps/api/tests/test_release_gate_command_plan.py -q` first failed because `verify_plan()` did not accept `now_utc` and could not validate a fresh-reviewed `operator_authorization_required` plan.
- Focused verification: `python -m pytest apps/api/tests/test_release_gate_command_plan.py apps/api/tests/test_skill_and_rag_docs.py -k "release_gate_command_plan or product_readiness_gate_blocks_frontend or product_readiness_documents_fast_launch" -q` -> `18 passed`.
- Release-gate verifier CLI: `python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` -> `status=passed`, current `checked.approval_json_status=refresh_required`.
- Adjacent stale-task/release-gate verification from `apps/api`: `PYTHONPATH=tests python -m pytest tests/test_build_stale_task_apply_request.py tests/test_verify_stale_task_apply_request.py tests/test_release_gate_command_plan.py -q` -> `42 passed`.
- Syntax verification: `python -c "from pathlib import Path; files=['apps/api/scripts/verify_release_gate_command_plan.py']; [compile(Path(f).read_text(encoding='utf-8'), f, 'exec') for f in files]; print('syntax ok')"` -> `syntax ok`.
- Boundary: this prepares the next operator-reviewed plan state. It does not refresh the remote approval JSON, apply stale-task resolution, restart the API, configure the model gateway, upload MRI data, execute workflows, generate QC artifacts, or replace fresh strict remote smoke acceptance.

## Release gate plan builder materializes reviewed approval

- Added `apps/api/scripts/build_release_gate_command_plan.py` to convert the current refresh-required release gate plan plus a freshly reviewed stale-task approval dry-run JSON into a concrete `operator_authorization_required` plan.
- The builder verifies the approval dry-run with `verify_stale_task_approval.py`, computes `approval_json_state.approval_expires_at_utc`, replaces every `<fresh_reviewed_approval_json>` placeholder with the reviewed path, sets `approval_json_state.status=fresh_reviewed`, and self-checks the result through `verify_release_gate_command_plan.py`.
- Updated `docs/deployment/remote-agent-production.md` so operators run the builder after `stale_task_approval_refresh` instead of hand-editing the release gate command plan.
- Updated `docs/product-readiness.md` to record the builder as the required bridge from refreshed approval evidence to the apply-ready operator authorization plan.
- TDD red evidence: `python -m pytest apps/api/tests/test_build_release_gate_command_plan.py -q` first failed because `apps/api/scripts/build_release_gate_command_plan.py` did not exist.
- Focused verification: `python -m pytest apps/api/tests/test_build_release_gate_command_plan.py apps/api/tests/test_release_gate_command_plan.py apps/api/tests/test_skill_and_rag_docs.py -k "build_release_gate_command_plan or release_gate_command_plan or product_readiness_gate_blocks_frontend or product_readiness_documents_fast_launch" -q` -> `21 passed`.
- Adjacent stale-task/release-gate verification from `apps/api`: `PYTHONPATH=tests python -m pytest tests/test_build_release_gate_command_plan.py tests/test_build_stale_task_apply_request.py tests/test_verify_stale_task_apply_request.py tests/test_release_gate_command_plan.py -q` -> `45 passed`.
- Release-gate verifier CLI: `python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` -> `status=passed`, current `checked.approval_json_status=refresh_required`.
- Syntax verification: `python -c "from pathlib import Path; files=['apps/api/scripts/build_release_gate_command_plan.py','apps/api/scripts/verify_release_gate_command_plan.py']; [compile(Path(f).read_text(encoding='utf-8'), f, 'exec') for f in files]; print('syntax ok')"` -> `syntax ok`.
- Boundary: this only materializes and verifies the next command plan after a fresh approval dry-run exists. It does not run the remote refresh, apply stale-task resolution, restart the API, configure the model gateway, upload MRI data, execute workflows, generate QC artifacts, or replace fresh strict remote smoke acceptance.

## Release gate plan embeds materialize command

- Updated `docs/deployment/remote-release-gate-command-plan.json` so `stale_task_approval_refresh` now carries the exact `materialize_plan_command` for `build_release_gate_command_plan.py` after a refreshed approval dry-run is operator-reviewed.
- Tightened `apps/api/scripts/verify_release_gate_command_plan.py` so the refresh block must include that materialize command, the expected refreshed approval input pattern, task ids, `--max-age-hours 24`, and `/tmp/image_agent_remote_release_gate_plan_<timestamp>.json` output path.
- Updated the release-gate tests so `next_steps_after_refresh` now requires materializing and verifying an `operator_authorization_required` plan, not manually editing the approval JSON placeholder.
- TDD red evidence: `python -m pytest apps/api/tests/test_release_gate_command_plan.py -q` first failed because `stale_task_approval_refresh.materialize_plan_command` was missing from the command plan.
- Focused verification: `python -m pytest apps/api/tests/test_release_gate_command_plan.py -q` -> `16 passed`; `python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` -> `status=passed`, current `checked.approval_json_status=refresh_required`.
- Adjacent stale-task/release-gate verification from `apps/api`: `PYTHONPATH=tests python -m pytest tests/test_build_release_gate_command_plan.py tests/test_build_stale_task_apply_request.py tests/test_verify_stale_task_apply_request.py tests/test_release_gate_command_plan.py -q` -> `45 passed`.
- Syntax verification: `python -c "from pathlib import Path; files=['apps/api/scripts/build_release_gate_command_plan.py','apps/api/scripts/verify_release_gate_command_plan.py']; [compile(Path(f).read_text(encoding='utf-8'), f, 'exec') for f in files]; print('syntax ok')"` -> `syntax ok`.
- Boundary: this only makes the post-refresh plan generation command machine-checkable. It does not run the remote approval refresh, apply stale-task resolution, restart the API, configure the model gateway, upload MRI data, execute workflows, generate QC artifacts, or replace fresh strict remote smoke acceptance.

## Release gate builder separates local and remote approval paths

- Updated `apps/api/scripts/build_release_gate_command_plan.py` so it can read a local copied approval JSON while embedding a separate server-side `--approval-json-command-path` into the materialized remote command plan.
- The embedded command path must be a safe `/tmp/image_agent_*.json` remote path; local Windows paths such as `C:/Users/...` are rejected before any plan is produced.
- Updated `docs/deployment/remote-agent-production.md` and `docs/product-readiness.md` so operators know the readable approval file path and the remote command path are separate when materializing a release gate plan from copied evidence.
- TDD red evidence: `python -m pytest apps/api/tests/test_build_release_gate_command_plan.py -q` first failed with `unexpected keyword argument 'approval_json_command_path'` and an unrecognized CLI argument before the builder supported the new path split.
- Focused verification: `python -m pytest apps/api/tests/test_build_release_gate_command_plan.py -q` -> `4 passed`.
- Release-gate verification: `python -m pytest apps/api/tests/test_build_release_gate_command_plan.py apps/api/tests/test_release_gate_command_plan.py -q` -> `20 passed`; `python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` -> `status=passed`, current `checked.approval_json_status=refresh_required`.
- Adjacent stale-task/release-gate verification from `apps/api`: `PYTHONPATH=tests python -m pytest tests/test_build_release_gate_command_plan.py tests/test_build_stale_task_apply_request.py tests/test_verify_stale_task_apply_request.py tests/test_release_gate_command_plan.py -q` -> `46 passed`.
- Syntax verification: `python -c "from pathlib import Path; files=['apps/api/scripts/build_release_gate_command_plan.py','apps/api/scripts/verify_release_gate_command_plan.py']; [compile(Path(f).read_text(encoding='utf-8'), f, 'exec') for f in files]; print('syntax ok')"` -> `syntax ok`.
- Boundary: this only prevents local approval evidence paths from leaking into remote mutation commands. It does not refresh the remote approval JSON, apply stale-task resolution, restart the API, configure the model gateway, upload MRI data, execute workflows, generate QC artifacts, or replace fresh strict remote smoke acceptance.

## Strict smoke requires actual uploaded-series evidence

- Added `--require-uploaded-series` and `--upload-nifti-file` to `apps/api/scripts/smoke_remote_agent.py`. The strict smoke runner now uploads a remote NIfTI file through `/projects/{project_id}/upload`, validates the returned series and `workflow_eligibility`, records `uploaded_series_status=passed`, and uses the returned series id for deterministic `/series/{series_id}/run` when no explicit launch series is supplied.
- Updated `apps/api/scripts/verify_remote_smoke_acceptance.py` so strict saved evidence must include `smoke_gate.require_uploaded_series=true`, a positive `smoke_gate.uploaded_series_id`, `uploaded_series_status=passed`, and an uploaded series whose project id and series id match the smoke project and completed task.
- Updated the release-gate command plan, stale-task apply request builder/verifier, production deployment docs, acceptance template, and product-readiness notes so the final remote acceptance path proves actual upload-to-series behavior, not only launch from a pre-existing series id.
- TDD red evidence: `python -m pytest apps/api/tests/test_smoke_remote_agent.py -k "uploaded_series or require_launched_task_requires_launch_inputs" -q` first failed because `_upload_nifti`, `--require-uploaded-series`, and `--upload-nifti-file` did not exist; `python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py -k "strict_payload or weak_evidence" -q` first failed because uploaded-series evidence was not verified.
- Focused verification: `python -m pytest apps/api/tests/test_smoke_remote_agent.py -q` -> `98 passed`; `python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py -q` -> `144 passed`.
- Release-gate verification: `python -m pytest apps/api/tests/test_release_gate_command_plan.py -q` -> `16 passed`; from `apps/api`, `PYTHONPATH=tests python -m pytest tests/test_build_stale_task_apply_request.py tests/test_verify_stale_task_apply_request.py -q` -> `28 passed`.
- Boundary: this extends the remote acceptance contract and script. It does not upload data to the real remote server in this local run, does not configure the model gateway, does not resolve stale task rows, does not restart the API, does not execute real MRI containers here, and does not replace fresh remote strict smoke evidence.

## Dashboard artifact previews use API base

- Updated `apps/console/src/routes/DashboardPage.tsx` so Dashboard result preview images resolve artifact download URLs through the configured API base, matching ResultStudio behavior. This prevents Vite on `localhost:5173` from trying to fetch backend artifact paths such as `/tasks/.../artifacts/...` from the frontend server.
- Added Dashboard regression expectations so manifest and result-summary image previews must use `http://localhost:8000/tasks/...` when the API base is configured that way.
- TDD red evidence: `npm.cmd test -- DashboardPage.test.tsx -t "renders result preview images only from backend artifact download urls|prefers artifact manifest previews over generated result summary previews" --reporter=dot` first failed with image `src` still equal to `/tasks/...`.
- Focused verification: `npm.cmd test -- DashboardPage.test.tsx -t "renders result preview images only from backend artifact download urls|prefers artifact manifest previews over generated result summary previews" --reporter=dot` -> `2 passed`; `npm.cmd test -- DashboardPage.test.tsx ResultDetailPage.test.tsx ResultStudioLayout.test.tsx resultArtifacts.test.ts --reporter=dot` -> `29 passed`.
- Frontend verification: `npm.cmd run lint` -> `tsc -b --noEmit` passed; `npm.cmd run build` -> production Vite build passed.
- Boundary: this is frontend URL resolution only. It does not generate artifacts, run MRI containers, configure the remote API, configure the model gateway, or replace strict remote smoke acceptance with actual uploaded data.

## Production CORS gate rejects unsafe public origins

- Tightened `apps/api/app/app_factory.py` so `IMAGE_AGENT_ENV=production` rejects CORS origins with paths, query strings, fragments, or trailing slashes. This keeps `IMAGE_AGENT_CORS_ORIGINS` as exact browser origins rather than URL paths that Starlette CORS will not match consistently.
- Added `production_cors_has_insecure_public_origin()` and wired `/deployment.production_readiness` to report `Production CORS origins must use HTTPS for public console origins.` when a public console origin is configured with `http://`.
- TDD red evidence: `python -m pytest apps/api/tests/test_app_factory.py -q` first failed because `https://console.example.com/` did not raise and `http://console.example.com` only reported the generic missing-public-origin blocker.
- Focused verification: `python -m pytest apps/api/tests/test_app_factory.py -q` -> `6 passed`.
- Deployment readiness verification: `python -m pytest apps/api/tests/test_app_factory.py apps/api/tests/test_agent_api.py -k "deployment_status or production_cors or cors" -q` -> `9 passed`.
- Boundary: this hardens production configuration only. It does not configure the actual deployed CORS values, model gateway, DNS/TLS, remote containers, workflow execution, or strict remote smoke evidence.

## GPT-5.5 local model gateway uses Chat Completions compatibility

- Added `OPENAI_WIRE_API=chat_completions` support to `apps/api/app/agent/model_gateway.py` while keeping the existing Responses path intact. This lets local development use the configured OpenAI-compatible GPT-5.5 gateway through the official OpenAI SDK when Responses returns no usable output.
- Chat Completions text and structured JSON planning now parse `choices[*].message.content`; structured planning appends the strict planner schema as an instruction and requests `response_format={"type":"json_object"}` with a retry path for gateways that reject that parameter.
- Chat Completions does not run the model tool loop. `complete_structured_with_tools()` returns a privacy-safe skipped trace, and `AgentRunner` records `openai_structured_without_tool_loop` so acceptance evidence does not pretend model tools were dispatched. Workflow launch still requires deterministic backend confirmation/resume APIs.
- Updated local ignored `.env` for development only with the GPT-5.5 base URL, key, and `OPENAI_WIRE_API=chat_completions`. The key is not written to docs or tracked files.
- Updated remote deployment/product-readiness docs so operators choose `chat_completions` for gateways that expose GPT-5.5 through Chat Completions, and `responses` only when Responses API output is usable.
- TDD red evidence: `python -m pytest apps/api/tests/test_model_gateway.py -k "chat_completions" -q` first failed with `Unsupported wire_api: chat_completions`; `python -m pytest apps/api/tests/test_agent_graph.py -k "skips_tool_loop" -q` first failed because the planner trace still reported `openai_function_tools_dispatched`.
- Focused verification: `python -m pytest apps/api/tests/test_model_gateway.py -q` -> `30 passed`; `python -m pytest apps/api/tests/test_agent_graph.py -q` -> `17 passed`; `python -m pytest apps/api/tests/test_skill_and_rag_docs.py -k "product_readiness_gate_blocks_frontend or remote_smoke_acceptance_gate_is_documented" -q` -> `2 passed`.
- Live local SDK smoke: `/agent/model/status` equivalent reported `configured=True`, provider `OpenAI`, model `gpt-5.5`, wire API `chat_completions`, and no `api_key` field; `ModelGateway().complete_text()` returned `pong`; `ModelGateway().complete_structured()` returned `{"intent":"answer_question","summary":"ok"}`.
- Secret safety verification: `git check-ignore -v .env` -> `.gitignore:1:.env`; `git status --short --ignored .env` -> `!! .env`; `git grep -l --fixed-strings <local OPENAI_API_KEY>` found no tracked files.
- Boundary: this proves local model gateway connectivity and structured planning only. It does not configure the remote server environment, restart the remote API, upload MRI data, execute real workflows, generate container-native QC, or replace strict remote smoke acceptance with real project/upload/task evidence.

## Strict smoke requires Agent workflow confirmation boundary

- Added `--require-agent-workflow-confirmation` to `apps/api/scripts/smoke_remote_agent.py`. The strict smoke runner now asks `/agent/runs` to prepare a workflow confirmation for the uploaded or launched series and the selected `--launch-workflow-type`, then requires `status=confirmation_required`, `intent=run_workflow`, `selected_skill=image-agent-workflow-runner`, matching project/series/workflow fields, and `production_task_created=false`.
- Updated `apps/api/scripts/verify_remote_smoke_acceptance.py` so saved strict smoke JSON must include `smoke_gate.require_agent_workflow_confirmation=true`, `agent_workflow_confirmation_status=passed`, and a privacy-safe `agent_workflow_confirmation` object matching the deterministic backend task evidence.
- Updated the release-gate command plan, stale-task apply request builder/verifier, production deployment docs, acceptance template, product-readiness notes, and Image Agent testing matrix so final remote acceptance proves model-backed workflow confirmation before deterministic backend launch.
- TDD red evidence: `python -m pytest apps/api/tests/test_smoke_remote_agent.py -k "agent_workflow_confirmation" -q` first failed because the CLI flag did not exist; `python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py -k "strict_payload or agent_workflow_confirmation" -q` first failed because the verifier did not check the new confirmation evidence; `python -m pytest apps/api/tests/test_release_gate_command_plan.py -q` first failed because the command plan lacked the new flag; from `apps/api`, `PYTHONPATH=tests python -m pytest tests/test_build_stale_task_apply_request.py -k "post_apply_gates" -q` first failed because the generated strict smoke command lacked the new flag.
- Focused verification: `python -m pytest apps/api/tests/test_smoke_remote_agent.py -q` -> `100 passed`; `python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py -q` -> `152 passed`; `python -m pytest apps/api/tests/test_release_gate_command_plan.py -q` -> `17 passed`; from `apps/api`, `PYTHONPATH=tests python -m pytest tests/test_build_stale_task_apply_request.py tests/test_verify_stale_task_apply_request.py -q` -> `29 passed`.
- Boundary: this proves the local strict-smoke contract and saved-evidence verifier. It does not run the remote server smoke, upload real remote MRI data, execute MRI containers, generate container-native QC, or prove production readiness until the same gate passes against real remote project/upload/task evidence.

## Agent approval UI exposes no-task-created boundary

- Updated `apps/console/src/routes/AgentPage.tsx` so workflow confirmation approvals show a compact boundary status: `Task not created yet` and `Backend API creates the task after approval.`
- This mirrors the backend contract where `/agent/runs` prepares `confirmation_required` and `/agent/runs/{thread_id}/resume` calls deterministic task creation only after explicit approval.
- TDD red evidence: `npm.cmd test -- AgentPage.test.tsx -t "requires explicit approval" --reporter=dot` first failed because `Task not created yet` was not rendered in the approval panel.
- Focused verification: `npm.cmd test -- AgentPage.test.tsx -t "requires explicit approval" --reporter=dot` -> `1 passed`; `npm.cmd test -- AgentPage.test.tsx --reporter=dot` -> `8 passed`.
- Frontend verification: `npm.cmd run lint` -> `tsc -b --noEmit` passed; `npm.cmd run build` -> production Vite build passed.
- Boundary: this is product UI clarity only. It does not run the remote server, upload real MRI data, execute workflows, configure remote GPT-5.5, or replace strict remote smoke acceptance.

## Workflow launch refreshes task detail cache

- Updated `apps/console/src/routes/WorkflowsPage.tsx` so a successful workflow launch invalidates both `queryKeys.tasks(projectId)` and `queryKeys.task(task.id)`, matching the Dashboard behavior after deterministic backend task creation.
- This keeps the launched task handoff and any task detail consumers from serving stale cached task data after the user starts a workflow from the Workflows page.
- TDD red evidence: `npm.cmd test -- WorkflowsPage.test.tsx -t "QSIRecon" --reporter=dot` first failed because only `tasks(13)` was invalidated and `task(91)` was not.
- Focused verification: `npm.cmd test -- WorkflowsPage.test.tsx -t "QSIRecon" --reporter=dot` -> `1 passed`; `npm.cmd test -- WorkflowsPage.test.tsx --reporter=dot` -> `4 passed`; `npm.cmd test -- DashboardPage.test.tsx AgentPage.test.tsx --reporter=dot` -> `25 passed`.
- Frontend verification: `npm.cmd run lint` -> `tsc -b --noEmit` passed; `npm.cmd run build` -> production Vite build passed.
- Boundary: this is frontend cache consistency for the main workflow launch path. It does not run the remote server, upload real MRI data, execute workflows, configure remote GPT-5.5, generate QC artifacts, or replace strict remote smoke acceptance.

## Agent task creation refreshes task detail cache

- Updated `apps/console/src/routes/AgentPage.tsx` so both Agent-created task paths invalidate `queryKeys.task(task.id)` as well as `queryKeys.tasks(projectId)`: direct `task_created` responses from `/agent/runs` and approved `/agent/runs/{thread_id}/resume` responses.
- This keeps the Agent workflow confirmation path aligned with Dashboard and Workflows launch behavior after deterministic backend task creation.
- TDD red evidence: `npm.cmd test -- AgentPage.test.tsx -t "explicit approval|created task" --reporter=dot` first failed because `task(144)` was not invalidated, and then exposed a test-scope issue before the spy was placed in the approval test.
- Focused verification: `npm.cmd test -- AgentPage.test.tsx -t "explicit approval|created task" --reporter=dot` -> `2 passed`; `npm.cmd test -- AgentPage.test.tsx WorkflowsPage.test.tsx DashboardPage.test.tsx --reporter=dot` -> `29 passed`.
- Frontend verification: `npm.cmd run lint` -> `tsc -b --noEmit` passed; `npm.cmd run build` -> production Vite build passed.
- Boundary: this is frontend cache consistency for Agent task creation. It does not run the remote server, upload real MRI data, execute workflows, configure remote GPT-5.5, generate QC artifacts, or replace strict remote smoke acceptance.

## Dashboard Agent chat refreshes task detail cache

- Updated `apps/console/src/routes/DashboardPage.tsx` so Dashboard Agent chat responses with `status=task_created` invalidate `queryKeys.task(task.id)` in addition to `queryKeys.tasks(projectId)`.
- This aligns all current frontend task-creation entry points: Dashboard pipeline launch, Dashboard Agent chat, Workflows launch, Agent direct task creation, and Agent approval resume.
- TDD red evidence: `npm.cmd test -- DashboardPage.test.tsx -t "dashboard Agent run creates" --reporter=dot` first failed because `task(144)` was not invalidated.
- Focused verification: `npm.cmd test -- DashboardPage.test.tsx -t "dashboard Agent run creates" --reporter=dot` -> `1 passed`; `npm.cmd test -- DashboardPage.test.tsx AgentPage.test.tsx WorkflowsPage.test.tsx --reporter=dot` -> `29 passed`.
- Frontend verification: `npm.cmd run lint` -> `tsc -b --noEmit` passed; `npm.cmd run build` -> production Vite build passed.
- Boundary: this is frontend cache consistency for Dashboard Agent-created tasks. It does not run the remote server, upload real MRI data, execute workflows, configure remote GPT-5.5, generate QC artifacts, or replace strict remote smoke acceptance.

## Legacy chat fallback test follows OpenAI-first gateway

- Adjusted `apps/api/tests/test_api_flow.py::test_deepseek_chat_provider_can_be_used` so it explicitly simulates an unavailable OpenAI model gateway before asserting the DeepSeek fallback path.
- This matches the current launch architecture: `/chat` uses the OpenAI-compatible GPT-5.5 gateway first when `OPENAI_API_KEY` is configured, and falls back to DeepSeek only after `ModelGatewayError`.
- Root cause: the local ignored `.env` now configures the OpenAI-compatible gateway for development, so the old test expected `provider=deepseek` even though the product path correctly returned `provider=OpenAI`.
- Verification: `PYTHONPATH=apps/api pytest apps/api/tests/test_api_flow.py -q` -> `66 passed`; `PYTHONPATH=apps/api pytest apps/api/tests/test_model_gateway.py apps/api/tests/test_agent_api.py -q` -> `66 passed`.
- Frontend main-flow verification: `npm.cmd test -- DashboardPage.test.tsx WorkflowsPage.test.tsx AgentPage.test.tsx` -> `29 passed`; `npm.cmd run lint` -> `tsc -b --noEmit` passed; `npm.cmd run build` -> production Vite build passed.
- Secret safety verification: `git check-ignore -v .env` -> `.gitignore:1:.env`; `git status --short --ignored .env` -> `!! .env`; tracked grep for the provided key/base URL found no real secret in tracked files, only fake test strings and documented placeholders.
- Boundary: this is a test-alignment and verification step. It does not run the real remote server, upload real MRI data, execute MRI containers, generate container-native QC, or prove production readiness until strict remote smoke passes against actual remote evidence.

## Strict smoke binds launch to uploaded series

- Tightened `apps/api/scripts/smoke_remote_agent.py` so when `--require-uploaded-series` uploads a fresh NIfTI, any supplied `--launch-series-id` must match the uploaded series id before `/series/{series_id}/run` is called.
- Added regression coverage in `apps/api/tests/test_smoke_remote_agent.py` to fail fast when the smoke command uploads one series but tries to launch another. This protects the fastest-launch evidence chain from mixing upload evidence and workflow launch evidence from different series.
- TDD red evidence: `PYTHONPATH=apps/api pytest apps/api/tests/test_smoke_remote_agent.py -k "rejects_launch_series_id_that_differs" -q` first failed because the script continued to `POST /series/99/run`.
- Verification: `PYTHONPATH=apps/api pytest apps/api/tests/test_smoke_remote_agent.py -k "uploaded_series or rejects_launch_series_id_that_differs or require_launched_task_requires_launch_inputs" -q` -> `4 passed`; `PYTHONPATH=apps/api pytest apps/api/tests/test_model_gateway.py -q` -> `30 passed`.
- Expanded verification: `PYTHONPATH=apps/api pytest apps/api/tests/test_smoke_remote_agent.py -q` -> `101 passed`; `PYTHONPATH=apps/api pytest apps/api/tests/test_verify_remote_smoke_acceptance.py -q` -> `152 passed`; `PYTHONPATH=apps/api pytest apps/api/tests/test_release_gate_command_plan.py apps/api/tests/test_build_release_gate_command_plan.py -q` -> `21 passed`.
- Main-flow verification: `PYTHONPATH=apps/api pytest apps/api/tests/test_api_flow.py apps/api/tests/test_agent_api.py apps/api/tests/test_model_gateway.py -q` -> `132 passed`; `npm.cmd test -- DashboardPage.test.tsx WorkflowsPage.test.tsx AgentPage.test.tsx` -> `29 passed`; `npm.cmd run build` -> production Vite build passed.
- Secret safety verification: `.env` remains ignored, and tracked grep for the provided Krill key prefix / base URL returned no matches outside the work log.
- Boundary: this improves remote smoke input integrity only. It does not run the remote upload/workflow, execute containers, configure the remote model, or replace fresh strict remote smoke evidence.

## Model gateway status reports SDK route diagnostics

- Added privacy-safe `gateway_diagnostics` to `apps/api/app/agent/model_gateway.py` and `/agent/model/status`. It now states whether the current config routes through `responses.create` or `chat.completions.create`, which request shape is used, how structured output is requested, whether the model tool loop is enabled, and that workflow task creation remains confirmation-only through deterministic backend APIs.
- Extended `apps/api/scripts/smoke_remote_agent.py` so strict remote smoke JSON preserves only safe `gateway_diagnostics` entries from `/agent/model/status`; unsafe nested keys or values such as authorization headers or credential-bearing URLs are dropped.
- Extended `apps/api/scripts/verify_remote_smoke_acceptance.py` so saved acceptance evidence rejects non-allowlisted model gateway diagnostic fields.
- TDD red evidence: `PYTHONPATH=apps/api pytest apps/api/tests/test_model_gateway.py -k "gateway_diagnostics" -q` first failed with missing `gateway_diagnostics`; `PYTHONPATH=apps/api pytest apps/api/tests/test_smoke_remote_agent.py -k "gateway_diagnostics" -q` first failed because smoke output omitted the diagnostics; `PYTHONPATH=apps/api pytest apps/api/tests/test_verify_remote_smoke_acceptance.py -k "authorization" -q` first failed because unsafe nested diagnostics were accepted.
- Verification: `PYTHONPATH=apps/api pytest apps/api/tests/test_model_gateway.py apps/api/tests/test_agent_api.py -q` -> `68 passed`; `PYTHONPATH=apps/api pytest apps/api/tests/test_smoke_remote_agent.py apps/api/tests/test_verify_remote_smoke_acceptance.py -q` -> `255 passed`.
- Boundary: this improves observability and acceptance evidence only. It does not prove that the remote provider supports Responses, does not run the remote model, upload real MRI data, execute containers, or replace fresh strict remote smoke evidence.

## Mixed ingest completion refreshes workflow-ready series

- Updated `apps/console/src/routes/IngestPage.tsx` so when an async mixed dataset ingest session reaches `inventory_status=completed`, the console refreshes the project `series` and `tasks` caches once for that session.
- This closes a main-flow gap where a user could upload a mixed dataset, see inventory completion, but still not see the newly detected series until a manual refresh before moving to Workflows.
- TDD red evidence: `npm.cmd test -- IngestPage.test.tsx -t "refreshes detected series" --reporter=dot` first failed because `api.listSeries` was called only twice and the completed inventory did not trigger a follow-up series refresh.
- Verification: `npm.cmd test -- IngestPage.test.tsx -t "refreshes detected series" --reporter=dot` -> `1 passed`; `npm.cmd test -- IngestPage.test.tsx WorkflowsPage.test.tsx AgentPage.test.tsx DashboardPage.test.tsx --reporter=dot` -> `33 passed`; `npm.cmd run lint` -> `tsc -b --noEmit` passed.
- Boundary: this is frontend cache freshness for the upload-to-workflow handoff. It does not run remote ingest, execute MRI containers, configure the model gateway, or replace strict remote smoke acceptance.

## Agent page surfaces model gateway route diagnostics

- Updated `apps/console/src/lib/types.ts` and `apps/console/src/routes/AgentPage.tsx` so the Agent intelligence panel displays the backend-reported `gateway_diagnostics.sdk_method` and `gateway_diagnostics.model_tool_loop`.
- This lets an operator quickly distinguish full Responses routing (`responses.create` with tool loop enabled) from Chat Completions compatibility (`chat.completions.create` with model tool loop skipped) without exposing keys, base URL credentials, or backend commands.
- TDD red evidence: `npm.cmd test -- AgentPage.test.tsx -t "configured model gateway" --reporter=dot` first failed because the Agent page did not render `SDK Route`.
- Verification: `npm.cmd test -- AgentPage.test.tsx -t "configured model gateway" --reporter=dot` -> `1 passed`; `npm.cmd test -- AgentPage.test.tsx SettingsPage.test.tsx AppShell.test.tsx --reporter=dot` -> `15 passed`; `npm.cmd run lint` -> `tsc -b --noEmit` passed; `npm.cmd run build` -> production Vite build passed.
- Boundary: this is frontend observability only. It does not change model routing, prove remote Responses compatibility, execute remote workflows, or replace strict remote smoke acceptance.

## Local product main-flow smoke

- Added `apps/api/scripts/smoke_local_main_flow.py` as a fast local smoke for the fastest-launch product path: `/health`, project creation, NIfTI upload, project series refresh, deterministic `/series/{series_id}/run`, project task list, model status, and RAG grounding-policy status.
- The smoke generates a minimal T1 NIfTI when `--upload-nifti-file` is omitted and launches only `t1_deepprep_mock` by default, so it is quick enough for local development without Docker/container execution.
- Added `--require-agent-confirmation` for model-configured environments. The fast default checks `/agent/model/status` but does not call `/agent/runs`; when the flag is set, the smoke verifies `/agent/runs` returns `confirmation_required` with matching project/series/workflow and `production_task_created=false`. When the gateway is unconfigured, the default status is `skipped_missing_model_config`.
- Added `--min-rag-documents` so launch rehearsal can fail when the local RAG index is empty instead of silently recording `document_count=0`.
- Added `--rebuild-rag` and `--agent-workflow-type` so stricter local rehearsal can rebuild the RAG index and separately verify Agent confirmation for a formal fixed workflow while local processing launch remains on the quick mock workflow.
- Added `--wait-task-completion-timeout-seconds`, `--wait-task-completion-poll-seconds`, and `--require-task-outputs` so local rehearsal can prove the mock task reaches `completed` and registers frontend-safe outputs.
- Added safe failed-run JSON output for Agent confirmation failures; the smoke records only status/intent/skill/workflow ids and omits model answers, prompts, local paths, and secrets.
- Documented the smoke command in `docs/product-readiness.md` under the fast-launch main-flow goal.
- TDD red evidence: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_smoke_local_main_flow.py -q` first failed with `local product main-flow smoke script is missing`.
- Verification: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_smoke_local_main_flow.py -q` -> `8 passed`.
- Live local API smoke: started `uvicorn app.main:app` on `127.0.0.1:8012`, ran `python scripts/smoke_local_main_flow.py --api-base http://127.0.0.1:8012`, and got `status=passed` with project `5`, uploaded T1 series `5`, launched task `6`, `workflow_type=t1_deepprep_mock`, model `gpt-5.5`, and `wire_api=chat_completions`. The same live API with `--min-rag-documents 1` failed as intended because local `/agent/rag/status` reported `document_count=0`.
- Strict local rehearsal: started `uvicorn app.main:app` on `127.0.0.1:8015` and ran `python scripts/smoke_local_main_flow.py --api-base http://127.0.0.1:8015 --workflow-type t1_deepprep_mock --agent-workflow-type t1_deepprep_anat_report --rebuild-rag --min-rag-documents 60 --require-agent-confirmation`. It returned `status=passed`, uploaded T1 series `9`, launched task `10` with `workflow_type=t1_deepprep_mock`, rebuilt RAG to `document_count=73` and `chunk_count=300`, and verified Agent `confirmation_required` for `t1_deepprep_anat_report` with `production_task_created=false`.
- Completion rehearsal: started `uvicorn app.main:app` on `127.0.0.1:8016` and ran the same strict smoke plus `--wait-task-completion-timeout-seconds 20 --wait-task-completion-poll-seconds 0.5 --require-task-outputs`. It returned `status=passed`, uploaded T1 series `10`, launched task `11`, waited for `completed`, rebuilt RAG to `document_count=73` and `chunk_count=300`, verified Agent confirmation for `t1_deepprep_anat_report`, and confirmed `/tasks/11/outputs` returned four registered outputs.
- Boundary: this is local product-contract evidence only. It does not run remote real MRI processing, execute containers, generate container-native QC artifacts, configure production CORS, or replace strict remote smoke acceptance on the remote server.

## Local main-flow Agent resume gate

- Extended `apps/api/scripts/smoke_local_main_flow.py` with `--require-agent-resume`, which requires `--require-agent-confirmation`.
- The local smoke now proves the control-plane path `POST /agent/runs` -> `confirmation_required` -> `POST /agent/runs/{thread_id}/resume` -> `task_created` with `production_task_created=true` and `confirmation_gate=fingerprint_verified`.
- When `--require-agent-resume` is used, the resumed task becomes the unified task for task-list, completion, output, result-summary, and artifact-manifest checks. The script does not also call direct `/series/{series_id}/run` for that smoke.
- Updated `docs/product-readiness.md` and `docs/skills/image-agent-developer/references/testing-matrix.md` so local strict launch rehearsal includes `--require-agent-resume` and clearly states that local smoke is local-only control-plane evidence, not deployed-server local Docker/toolchain execution proof.
- TDD red evidence: `PYTHONPATH=... python -m pytest -q apps/api/tests/test_smoke_local_main_flow.py::test_local_main_flow_smoke_can_create_task_through_agent_resume` first failed with `unrecognized arguments: --require-agent-resume`.
- Verification: `PYTHONPATH=... python -m pytest -q apps/api/tests/test_smoke_local_main_flow.py` -> `14 passed`; Agent/API/Graph/remote smoke/verifier/local smoke focused group -> `355 passed, 3 warnings`; frontend API/AgentPage/DashboardPage tests -> `46 passed`; backend full suite -> `860 passed, 9 warnings`; console `npm.cmd run lint` passed.
- Boundary: this improves local mock/control-plane confidence. Real acceptance still requires the deployed API server to run real registered workflows with its local Docker/toolchain, real data, container-native QC, result summaries, and strict saved JSON verification.

## Local result artifact contract and Krill gateway probe

- Extended `apps/api/scripts/smoke_local_main_flow.py` with `--require-result-summary` and `--require-artifact-manifest` so fast-launch local rehearsal can fail when a completed workflow has no frontend-servable result contract.
- TDD red evidence: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_api_flow.py::test_full_t1_mock_flow -q` failed with `assert []` because the mock DeepPrep task completed and registered outputs, but `/tasks/{task_id}/artifact-manifest` had no artifacts.
- Root cause: the mock DeepPrep runner wrote outputs under a module-level `PROJECTS_ROOT`, while result endpoints computed the output directory through the task/project service root. When those roots diverged in tests or patched runtime contexts, artifact validation treated registered outputs as outside the task output directory.
- Fix: mock DeepPrep now writes under `derivatives/{task_id}/output` derived from the task log path, and result endpoints derive the task output directory from the same persisted task path before falling back to configured `PROJECTS_ROOT`.
- Verification: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_api_flow.py::test_full_t1_mock_flow -q` -> `1 passed`; `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_smoke_local_main_flow.py -q` -> `10 passed`; `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_model_gateway.py -q` -> `32 passed`.
- Live local main-flow smoke: started `uvicorn app.main:app` on `127.0.0.1:8020` and ran `python apps/api/scripts/smoke_local_main_flow.py --api-base http://127.0.0.1:8020 --workflow-type t1_deepprep_mock --rebuild-rag --min-rag-documents 60 --wait-task-completion-timeout-seconds 20 --wait-task-completion-poll-seconds 0.5 --require-task-outputs --require-result-summary --require-artifact-manifest`. It returned `status=passed`, uploaded T1 series `12`, launched task `13`, waited for `completed`, rebuilt RAG to `document_count=73` and `chunk_count=300`, confirmed four safe task outputs, `result_summary_status=passed`, and `artifact_manifest_status=passed` with `artifact_count=8`.
- Krill Responses live probe: with `OPENAI_WIRE_API=responses`, `OPENAI_DISABLE_METADATA=true`, and storage disabled, `client.responses.create(...)` returned a Responses-shaped object but `output=[]` and no `output_text`; `ModelGateway.complete_text()` failed safely with `Responses body did not include output text`.
- Krill Chat Completions live probe: with the same ignored local key and base URL but `OPENAI_WIRE_API=chat_completions`, `ModelGateway.complete_text()` returned `pong` and `ModelGateway.complete_structured()` returned `{"intent":"answer_question","summary":"ok"}`.
- Local ignored `.env` remains the development-only place for the Krill key. `git check-ignore -v .env .env.local` reports `.gitignore:1:.env` and `.gitignore:2:.env.*`; `.env` was updated only to label `MODEL_PROVIDER=krill` and disable metadata. The key was not printed or added to tracked docs.
- Boundary: this proves the local upload-to-result-contract path and the current usable Krill model route for development. It does not prove remote production readiness, real container execution, container-native QC artifacts, production CORS, or Responses tool-loop compatibility until the provider returns usable Responses output and strict remote smoke passes against real remote evidence.

## Local model-backed Agent confirmation main flow

- Ran the stricter local product smoke against a live FastAPI server on `127.0.0.1:8021` with Krill configured through `OPENAI_WIRE_API=chat_completions`.
- Command covered `/health`, generated T1 upload, `/projects/{project_id}/series`, deterministic `/series/{series_id}/run`, `/projects/{project_id}/tasks`, `/agent/model/status`, RAG rebuild, `/agent/runs` workflow confirmation, task completion, `/tasks/{task_id}/outputs`, `/tasks/{task_id}/result-summary`, and `/tasks/{task_id}/artifact-manifest`.
- Live evidence: smoke returned `status=passed`, uploaded T1 series `13`, launched deterministic mock task `14`, waited for `completed`, rebuilt RAG to `document_count=73` and `chunk_count=300`, verified Agent confirmation for `t1_deepprep_anat_report`, and confirmed `production_task_created=false`.
- The same smoke confirmed result delivery contracts: four safe task outputs, `result_summary_status=passed`, and `artifact_manifest_status=passed` with `artifact_count=8`.
- Model evidence: `/agent/model/status` reported provider `krill`, model `gpt-5.5`, `wire_api=chat_completions`, SDK route `chat.completions.create`, structured output `chat_response_format_json_object`, and `model_tool_loop=skipped`, which matches the current Krill compatibility boundary.
- Local ignored `.env` was adjusted to include `MODEL_REASONING_EFFORT=high` for the requested development profile. It remains ignored and must not be committed.
- Boundary: this proves the local model-backed Agent interaction and backend confirmation boundary for fast-launch rehearsal. It still does not prove the remote server can run real containers, serve container-native QC, satisfy production CORS, or pass strict remote acceptance with real project/upload/task evidence.

## Remote acceptance now pins current Krill wire API

- Answered the Responses gateway question from the code path: `apps/api/app/agent/model_gateway.py` already supports `OPENAI_WIRE_API=responses` through `client.responses.create(...)` and `OPENAI_WIRE_API=chat_completions` through `client.chat.completions.create(...)`. The blocker is not a missing Responses call path; the live Krill Responses probe returned a completed Responses-shaped body with no parseable output text, so `ModelGateway.complete_text()` correctly failed with `Responses body did not include output text`.
- Added `--expected-model-wire-api` to `apps/api/scripts/smoke_remote_agent.py`. Strict remote smoke can now fail immediately when `/agent/model/status.wire_api` is not the expected route. The current command plan pins this to `chat_completions` for the Krill development gateway.
- Updated `apps/api/scripts/verify_remote_smoke_acceptance.py` so saved acceptance JSON must include privacy-safe `smoke_gate.expected_model_wire_api` and `model_status.wire_api` must match it. This prevents a saved run with `responses` from opening the frontend gate while Krill Responses still returns empty output.
- Updated release-gate and stale-task handoff generators/verifiers plus docs so the strict remote command includes `--expected-model-wire-api chat_completions`, and the offline verifier reports `checked.model_wire_api=chat_completions`.
- Verification: `PYTHONPATH=apps/api;apps/api/tests python -m pytest apps/api/tests/test_smoke_remote_agent.py apps/api/tests/test_verify_remote_smoke_acceptance.py apps/api/tests/test_release_gate_command_plan.py apps/api/tests/test_build_stale_task_apply_request.py apps/api/tests/test_verify_stale_task_apply_request.py -q` -> `306 passed`.
- Gateway/local smoke verification: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_model_gateway.py apps/api/tests/test_smoke_local_main_flow.py -q` -> `42 passed`.
- Secret safety verification: tracked grep for the provided Krill key prefix and Krill base URL returned no matches; `git check-ignore -v .env .env.local` still reports `.env` and `.env.*` ignored, and `git status --short --ignored .env` reports `!! .env`.
- Boundary: this pins the current usable development route and makes remote acceptance stricter. It does not make Krill's Responses endpoint return output text, does not enable Responses tool-loop behavior for Chat Completions mode, and does not replace a fresh strict remote run on the real server.

## Rawchat GPT-5.5 Responses probe

- Used only the operator-provided base URL `https://rawchat.cn/codex` and API key value for an in-process probe; did not run the external `iwr ... | iex` setup script and did not write the key to source, docs, or ignored `.env`.
- Direct OpenAI SDK probe with `client.responses.create(...)`, `model=gpt-5.5`, `store=false`, metadata disabled, and high reasoning returned `status=completed`, one output item, and parseable text `pong`.
- Project gateway probe through `ModelGateway.complete_text()` with `OPENAI_WIRE_API=responses` also returned `pong`, confirming the project Responses code path works against this gateway.
- Started a separate local API on `127.0.0.1:8022` with process-only rawchat environment. `/agent/model/status` reported provider `rawchat`, configured `true`, model `gpt-5.5`, `wire_api=responses`, SDK route `responses.create`, and `model_tool_loop=enabled`.
- First strict local main-flow smoke failed at `/agent/runs` with `agent_model_call_failed`. Root cause was rawchat returning structured JSON followed by extra prose in a Responses output, causing strict JSON parsing to fail with `Extra data`.
- Added a small parser regression and fix so `parse_responses_json()` extracts the first complete JSON object when an OpenAI-compatible Responses gateway appends trailing explanatory text, while still rejecting non-JSON and non-object outputs.
- Verification: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_model_gateway.py -q` -> `33 passed`.
- Live rawchat Responses main-flow smoke on `127.0.0.1:8022` passed after the fix: upload, deterministic mock workflow launch/completion, task outputs, result summary, artifact manifest, RAG boundary, and Agent confirmation for `t1_deepprep_anat_report` all returned `status=passed`; Agent confirmation preserved `production_task_created=false`.
- Boundary: this is local gateway and product-contract evidence only. It does not execute the external setup script, does not commit provider secrets, does not prove remote container execution, and does not replace strict remote acceptance on the real server.

## Browser dashboard main-flow check

- Opened the Gemini console at `http://127.0.0.1:5173/projects/17/dashboard` and verified the page rendered against a real backend with project `17`, T1 series `17`, completed task `18`, result artifacts, and `API base: http://127.0.0.1:8000`.
- Used the Settings page to switch the console API base to the rawchat Responses backend on `http://127.0.0.1:8022`. The Settings page then reported `API base: http://127.0.0.1:8022` and Agent Provider `rawchat`.
- Returned to Dashboard and confirmed it still loaded project `17`, series `17`, completed task `18`, result preview, and key artifacts from the rawchat Responses backend.
- Selected `t1_deepprep_mock` in the workflow dropdown and clicked `Run now`. The page created task `19`, showed `Task #19 running`, `Progress: 10%`, and `Workflow: t1_deepprep_mock`, proving the frontend workflow selection reached deterministic `/series/{series_id}/run`.
- Waited on the page until task `19` reached `completed`, `Progress: 100%`, and the dashboard result preview switched to `RUN-19` with `/projects/17/results/19`.
- Sent a read-only Agent Copilot message asking whether task `19` completed and explicitly not to launch tasks. The page displayed the user message and then the model-backed response: `Task 19 已完成（completed，进度 100%），我没有启动任何新任务。`
- Backend cross-check: `/projects/17/tasks` on `127.0.0.1:8022` returned only task `18` and task `19`, both completed mock tasks; `/projects/17/agent-runs` showed the browser Agent message as `status=answered`, `intent=answer_question`, with no `task_id` or `workflow_type` created from that chat.
- Boundary: this is local browser-to-backend evidence for upload-derived project data, workflow selection/start, processing completion, result preview, and model Agent interaction. It still does not prove remote real MRI container execution, container-native QC images, production HTTPS/CORS, or strict remote acceptance on the server.

## Unified model provider profile compatibility

- Added a provider profile layer to `apps/api/app/agent/model_gateway.py` so the same `ModelGateway` can be configured for `openai`, `rawchat`, `krill`, `deepseek`, `glm`, or `custom` without adding separate model-call paths.
- Added preferred generic configuration keys: `IMAGE_AGENT_MODEL_PROVIDER`, `IMAGE_AGENT_MODEL_API_KEY`, `IMAGE_AGENT_MODEL_BASE_URL`, `IMAGE_AGENT_MODEL_NAME`, `IMAGE_AGENT_MODEL_REVIEW_NAME`, and `IMAGE_AGENT_MODEL_WIRE_API`.
- Kept backward compatibility for existing OpenAI-compatible variables such as `MODEL_PROVIDER`, `OPENAI_API_KEY`, `OPENAI_BASE_URL`, `OPENAI_MODEL`, `OPENAI_REVIEW_MODEL`, and `OPENAI_WIRE_API`.
- Added provider-specific aliases for operator convenience, including `RAWCHAT_*`, `KRILL_*`, `DEEPSEEK_*`, `GLM_*`, and `ZHIPU_*` key/base/model/wire API variables.
- Default wire API remains capability-based rather than provider-brand-based: `openai`, `rawchat`, and `krill` default to `responses`; `deepseek` and `glm` default to `chat_completions`. Operators can override with `IMAGE_AGENT_MODEL_WIRE_API`.
- `/agent/model/status` now exposes safe `provider_profile` and `capabilities` fields alongside the existing gateway diagnostics. The public status still omits API keys, reverse-tunnel command text, and secret-bearing values.
- Updated `docs/deployment/remote-agent-production.md` to document the new `IMAGE_AGENT_MODEL_*` variables, provider-specific aliases, and the Responses vs Chat Completions boundary.
- Updated `docs/product-readiness.md` so remote acceptance requires safe provider profile/capability visibility, not only a configured key.
- TDD red evidence: `PYTHONPATH=apps/api;apps/api/tests python -m pytest apps/api/tests/test_model_gateway.py -q -k "image_agent_model_environment or deepseek_provider_specific or glm_profile"` first failed because `IMAGE_AGENT_MODEL_*` and provider-specific variables were ignored. `PYTHONPATH=apps/api;apps/api/tests python -m pytest apps/api/tests/test_agent_api.py -q -k "model_status_uses_model_gateway"` then failed because public `/agent/model/status` did not expose `provider_profile`.
- Verification: `PYTHONPATH=apps/api;apps/api/tests python -m pytest apps/api/tests/test_model_gateway.py apps/api/tests/test_agent_api.py::test_agent_model_status_uses_model_gateway apps/api/tests/test_release_gate_command_plan.py apps/api/tests/test_build_stale_task_apply_request.py apps/api/tests/test_verify_stale_task_apply_request.py -q` -> `85 passed, 3 warnings`.
- Secret safety verification: tracked grep for the operator-provided rawchat/Krill key prefixes returned no matches; `git check-ignore -v .env .env.local` still reports `.env` and `.env.*` ignored, and `git status --short --ignored .env .env.local` reports `!! .env`.
- Boundary: this makes provider configuration compatible and observable. It does not run new live provider probes, does not make Chat Completions support the Responses tool loop, does not allow the Agent to create production tasks, and does not replace strict remote acceptance with real upload/task/container evidence.

## Rawchat GPT-5.5 remote gate pinning

- Upgraded `apps/api/scripts/smoke_remote_agent.py` with `--expected-model-provider-profile` and `--require-model-tool-loop`.
- `smoke_remote_agent.py` now keeps safe `model_status.provider_profile` and boolean `model_status.capabilities` in acceptance evidence while still dropping keys, URL credentials, and reverse-tunnel command text.
- Upgraded `apps/api/scripts/verify_remote_smoke_acceptance.py` so strict saved JSON requires `model_status.provider_profile` to match `smoke_gate.expected_model_provider_profile` and requires `model_status.capabilities.model_tool_loop=true` when `smoke_gate.require_model_tool_loop=true`.
- Updated release gate and stale-task handoff command generation/verification so the strict remote smoke command pins the current development model target with `--expected-model-wire-api responses --expected-model-provider-profile rawchat --require-model-tool-loop`.
- Updated `docs/deployment/remote-release-gate-command-plan.json`, `docs/deployment/remote-agent-production.md`, `docs/deployment/remote-agent-acceptance-template.md`, and `docs/product-readiness.md` to document the rawchat GPT-5.5 Responses gate.
- Switched ignored local `.env` from Krill Chat Completions to rawchat GPT-5.5 Responses for continued development testing. Sanitized local status shows `provider_profile=rawchat`, `model=gpt-5.5`, `wire_api=responses`, `sdk_method=responses.create`, and `model_tool_loop=true`.
- Live local provider probe from ignored `.env`: `ModelGateway().complete_text(... "Reply with exactly: pong")` returned `pong`.
- Verification: `PYTHONPATH=apps/api;apps/api/tests python -m pytest apps/api/tests/test_smoke_remote_agent.py apps/api/tests/test_verify_remote_smoke_acceptance.py apps/api/tests/test_release_gate_command_plan.py apps/api/tests/test_build_stale_task_apply_request.py apps/api/tests/test_verify_stale_task_apply_request.py -q` -> `314 passed`.
- Verification: `PYTHONPATH=apps/api;apps/api/tests python -m pytest apps/api/tests/test_model_gateway.py apps/api/tests/test_agent_api.py::test_agent_model_status_uses_model_gateway -q` -> `37 passed, 3 warnings`.
- Secret safety verification: `git check-ignore -v .env .env.local` still reports `.env` and `.env.*` ignored; tracked grep for the operator-provided rawchat/Krill key prefixes excluding `.env` returned no matches.
- Boundary: this pins the intended development and remote acceptance model target to rawchat GPT-5.5 Responses. It still does not prove remote real container execution, container-native QC serving, production HTTPS/CORS, or fresh remote strict acceptance until the remote server runs the updated gate against real upload/task evidence.

## Local smoke also pins rawchat GPT-5.5

- Extended `apps/api/scripts/smoke_local_main_flow.py` with `--expected-model-provider-profile`, `--expected-model-wire-api`, `--expected-model-name`, and `--require-model-tool-loop`.
- The local smoke now fails early when `/agent/model/status` is configured for the wrong provider profile, wire API, model name, or lacks Responses model tool-loop capability. This makes the current development target explicit instead of silently accepting a fallback provider.
- Updated `docs/product-readiness.md` so the strict local launch rehearsal command pins `rawchat`, `responses`, `gpt-5.5`, and `--require-model-tool-loop`.
- Added regression tests proving the local smoke accepts the rawchat GPT-5.5 Responses profile and rejects a mismatched model profile.
- Isolated model-gateway tests from ambient `.env` values so the ignored local rawchat development config does not make provider-specific unit tests flaky.
- Verification: `PYTHONPATH=apps/api;apps/api/tests python -m pytest apps/api/tests/test_smoke_local_main_flow.py apps/api/tests/test_model_gateway.py apps/api/tests/test_agent_api.py::test_agent_model_status_uses_model_gateway -q` -> `49 passed, 3 warnings`.
- Secret safety verification: tracked grep for the operator-provided rawchat/Krill key prefixes excluding `.env` returned no matches; `git status --short --ignored .env` still reports `!! .env`.
- Boundary: local development now defaults to rawchat GPT-5.5 Responses and catches accidental fallback in smoke tests. This still does not replace fresh strict remote acceptance with real upload, real workflow/container execution, native QC artifact serving, and production CORS/HTTPS evidence.

## Fast-launch readiness exposed on deployment status

- Added `/deployment.fast_launch_readiness` as an operator/frontend-safe summary of the fastest-launch gate.
- The new summary reports four checks: `model_gateway_target`, `agent_task_boundary`, `upload_workflow_result_contract`, and `strict_remote_acceptance`.
- `model_gateway_target` passes only for the current development target: rawchat GPT-5.5 through the Responses wire API with `model_tool_loop=true`.
- `agent_task_boundary` explicitly records that chat authority is read/explain/recommend and deterministic workflow launch still belongs to `/series/{series_id}/run` or server-side resume confirmation.
- `strict_remote_acceptance` remains `missing` until `IMAGE_AGENT_STRICT_REMOTE_ACCEPTANCE_STATUS=passed` and a privacy-safe `IMAGE_AGENT_STRICT_REMOTE_ACCEPTANCE_ID` are supplied from a fresh verified remote smoke JSON.
- Added test isolation in `apps/api/tests/test_agent_api.py` so ignored local rawchat `.env` development settings cannot make model-unconfigured tests pass or fail for the wrong reason.
- TDD red evidence: `PYTHONPATH=apps/api;apps/api/tests python -m pytest apps/api/tests/test_agent_api.py -q -k "fast_launch_readiness"` first failed with `KeyError: 'fast_launch_readiness'`.
- Verification after implementation and test isolation: `PYTHONPATH=apps/api;apps/api/tests python -m pytest apps/api/tests/test_agent_api.py apps/api/tests/test_app_factory.py apps/api/tests/test_model_gateway.py apps/api/tests/test_smoke_local_main_flow.py -q` -> `92 passed, 9 warnings`.
- Boundary: this exposes the main-chain launch gate to the console and operators, but it does not assert real remote readiness by itself. Formal release still requires the fresh strict remote smoke JSON, container-native QC evidence, real workflow execution, and production CORS/HTTPS configuration.

## Console surfaces fast-launch readiness

- Wired the Gemini console to `/deployment.fast_launch_readiness`.
- Added TypeScript support for the backend readiness payload, including model target, Agent task boundary, upload/workflow/result contract, and strict remote acceptance fields.
- Updated the global AppShell badge so a blocked fast-launch gate shows `Launch blocked` before the user enters a project page. The sidebar now shows the first main-chain blocker when the remote strict smoke evidence is missing.
- Updated the Settings page with a compact `Fast Launch Readiness` panel showing the rawchat GPT-5.5 Responses target, protected Agent boundary, result contract status, and strict remote acceptance state.
- TDD red evidence: `npm.cmd test -- SettingsPage.test.tsx AppShell.test.tsx` first failed because `Fast Launch Readiness` and `Launch blocked` were not rendered.
- Focused verification: `npm.cmd test -- SettingsPage.test.tsx AppShell.test.tsx` -> `9 passed`.
- Main-chain frontend verification: `npm.cmd test -- DashboardPage.test.tsx IngestPage.test.tsx AgentPage.test.tsx SettingsPage.test.tsx AppShell.test.tsx api.test.ts` -> `54 passed`.
- Full console verification: `npm.cmd test` -> `84 passed`.
- Type/build verification: `npm.cmd run lint` passed; `npm.cmd run build` passed.
- Boundary: this is frontend visibility only. It makes the current blocker visible in the UI, but it does not replace a fresh remote strict smoke run, real container execution, native QC evidence, or production HTTPS/CORS configuration.

## Strict remote smoke can emit fast-launch env

- Added `--emit-fast-launch-env` to `apps/api/scripts/verify_remote_smoke_acceptance.py`.
- The flag runs the normal strict saved-JSON verifier first, then prints only `IMAGE_AGENT_STRICT_REMOTE_ACCEPTANCE_STATUS=passed` and `IMAGE_AGENT_STRICT_REMOTE_ACCEPTANCE_ID=<deployment_id>`.
- The emitted ID must come from `smoke_gate.deployment_id` and pass the same privacy-safe symbol checks, so backend paths, host paths, URLs, and secrets cannot be promoted into `/deployment.fast_launch_readiness`.
- Updated deployment/readiness docs so operators derive these environment values from a fresh verified remote smoke JSON instead of guessing or hand-writing the acceptance ID.
- TDD red evidence: `PYTHONPATH=apps/api;apps/api/tests python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py -q -k "fast_launch_env"` first failed because the verifier CLI did not accept `--emit-fast-launch-env`.
- Verification: `PYTHONPATH=apps/api;apps/api/tests python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py -q` -> `160 passed`.
- Boundary: this only publishes a privacy-safe summary after strict acceptance has already passed. It does not run the remote smoke, execute containers, configure rawchat credentials on the server, or replace the saved remote acceptance JSON.

## Release gates require fast-launch env export

- Added `emit_fast_launch_acceptance_env_after_strict_verify` as the final read-only step in `docs/deployment/remote-release-gate-command-plan.json`.
- Updated `verify_release_gate_command_plan.py` so a machine-checkable release plan must include the export command with `verify_remote_smoke_acceptance.py`, `--max-age-hours 24`, `--emit-fast-launch-env`, and the exact same strict smoke JSON path produced by `--output-json`.
- Updated `build_stale_task_apply_request.py` and `verify_stale_task_apply_request.py` so generated post-authorization handoffs also include and verify the same final export step.
- Updated deployment/readiness docs to make clear that the fast-launch env values come from the verified strict remote smoke package, not from a manually invented acceptance label.
- TDD red evidence: `PYTHONPATH=apps/api;apps/api/tests python -m pytest apps/api/tests/test_release_gate_command_plan.py -q` first failed because the static plan had only 8 steps and the verifier did not require the export step; `PYTHONPATH=apps/api;apps/api/tests python -m pytest apps/api/tests/test_build_stale_task_apply_request.py apps/api/tests/test_verify_stale_task_apply_request.py -q` then failed because generated apply requests had no export step.
- Verification: `PYTHONPATH=apps/api;apps/api/tests python -m pytest apps/api/tests/test_release_gate_command_plan.py -q` -> `21 passed`; `PYTHONPATH=apps/api;apps/api/tests python -m pytest apps/api/tests/test_build_stale_task_apply_request.py apps/api/tests/test_verify_stale_task_apply_request.py -q` -> `33 passed`.
- Boundary: this only extends the operator gate and handoff after remote strict acceptance. It does not run the remote smoke, mutate remote tasks, install providers locally, expose secrets, or replace real upload/workflow/container/QC evidence.
