# Work Log - 2026-06-17

## Runtime image version lock for launch readiness

- Kept the active Codex goal on fastest viable image_agent launch with Full LangGraph orchestration, deterministic backend gates, mock/control-plane tests, and real execution-plane acceptance as separate layers.
- Added fixed runtime image contracts to the workflow registry so Agent/LangGraph-visible workflow metadata records execution images instead of relying on loose documentation:
  - `pbfslab/deepprep:25.1.0`
  - `nipreps/fmriprep:25.2.5`
  - `pennlinc/xcp_d:26.0.2`
  - `pennlinc/qsiprep:1.0.2`
  - `pennlinc/qsirecon:26.0.0`
- Hardened the deployment-server-local BOLD script wrapper:
  - default fixed images are exported as `IMAGE_AGENT_TASK_FMRIPREP_IMAGE` and `IMAGE_AGENT_TASK_XCPD_IMAGE`;
  - preflight rejects untagged images and `:latest` image tags;
  - existing path checks, secret-stripped child environment, log redaction, and native QC discovery remain unchanged.
- Updated image-agent developer skill references and product readiness so production guidance no longer recommends floating `latest` image tags for current execution contracts.
- TDD red evidence:
  - `python -m pytest -q apps/api/tests/test_remote_scripts.py::test_remote_script_preflight_rejects_unlocked_container_images apps/api/tests/test_remote_scripts.py::test_remote_script_task_environment_exports_locked_container_images` first failed because preflight did not reject `latest` and task env did not export image locks.
  - `python -m pytest -q apps/api/tests/test_skill_and_rag_docs.py::test_developer_skill_documents_locked_container_versions_for_execution_acceptance` first failed because developer skill references still documented floating image tags.
  - `python -m pytest -q apps/api/tests/test_workflow_registry.py::test_workflow_registry_exposes_openai_style_contract_fields apps/api/tests/test_workflow_registry.py::test_fixed_workflow_runtime_images_are_version_locked` first failed because registry metadata had no BOLD runtime image contract.
- Verification:
  - `python -m pytest -q apps/api/tests/test_workflow_registry.py apps/api/tests/test_remote_scripts.py` -> `22 passed`.
  - `python -m pytest -q apps/api/tests/test_agent_api.py apps/api/tests/test_agent_graph.py apps/api/tests/test_workflow_registry.py apps/api/tests/test_remote_scripts.py apps/api/tests/test_skill_and_rag_docs.py` -> `146 passed, 3 warnings`.
  - `python -m pytest -q apps/api/tests` -> `864 passed, 9 warnings`.
  - `npm.cmd test -- --run src/lib/api.test.ts src/routes/AgentPage.test.tsx src/routes/DashboardPage.test.tsx` -> `46 passed`.
  - `npm.cmd run lint` -> passed.
- Boundary:
  - This proves the control-plane version contract and frontend/backend compatibility.
  - It does not prove real container execution, GPU visibility, license mounts, native QC artifact quality, production HTTPS/CORS, or deployment acceptance. Those still require a fresh strict acceptance run on the deployment server's local Docker/toolchain environment.

## Workflow capability metadata and QC display boundary

- Continued the updated fast-launch goal after the objective added a formal workflow naming/capability metadata target.
- Preserved stable machine ids:
  - `workflow_type` remains the stable id for confirmation payloads, confirmation fingerprint, task rows, task outputs, logs, and acceptance evidence.
  - `runtime_workflow_type` remains the runner dispatch id when a product-facing workflow aliases to a concrete pipeline runner.
- Hardened the fixed workflow resume path:
  - Added RED coverage showing LangGraph resume created `bold_fmriprep_xcpd_report` instead of the preflight runtime workflow type.
  - Updated `create_workflow_task()` to honor `confirmation.preflight.runtime_workflow_type` after registry/fixed-lane validation and before calling `task_service.create_series_task()`.
- Normalized fixed workflow capability metadata:
  - Added registry coverage requiring every fixed workflow to expose `display_name`, `capability_summary`, `workflow_family`, `workflow_role`, `pipeline_stages`, `primary_outputs`, `qc_outputs`, `report_outputs`, `limitations`, and `is_report_only`.
  - Added conservative default metadata for fixed legacy/validate workflows in the registry clone layer, without renaming any workflow ids.
  - Kept richer hand-authored metadata for the primary T1, BOLD, and DWI workflows.
- Clarified frontend result QC boundaries:
  - Result Detail now shows a warning when an artifact manifest contains derived scientific report assets but no registered container-native QC artifact.
  - The warning states derived reports are available but do not replace native container QC evidence.
  - Console artifact types now include manifest classification fields such as `artifact_category`, `container_native_qc`, `derived_scientific_report`, and `frontend_preview_asset`.
- Updated developer contracts:
  - Documented that frontends, Agent, and RAG should use display metadata to understand workflow capability.
  - Documented that task creation, confirmation fingerprint, database records, and runner dispatch must continue using stable machine ids.
  - Documented that a complete processing workflow must not be reinterpreted as report-only just because the stable id contains `report`.
- Verification:
  - `python -m pytest tests/test_workflow_registry.py -q` -> `10 passed`.
  - `python -m pytest tests/test_skill_and_rag_docs.py::test_developer_contract_documents_workflow_display_metadata_boundary tests/test_workflow_registry.py tests/test_agent_graph.py::test_langgraph_agent_runner_resume_uses_preflight_runtime_workflow_type -q` -> `12 passed`.
  - `python -m pytest tests/test_agent_graph.py::test_langgraph_agent_runner_resume_uses_preflight_runtime_workflow_type tests/test_agent_graph.py::test_langgraph_agent_runner_fixed_workflow_returns_confirmation_without_task_creation tests/test_agent_graph.py::test_langgraph_agent_runner_unknown_workflow_returns_structured_incubation_proposal -q` -> `3 passed`.
  - `npm.cmd test -- workflows.test.ts WorkflowsPage.test.tsx ResultDetailPage.test.tsx` -> `20 passed`.
  - `python apps/api/scripts/audit_skill_maintenance.py --json` -> `status=passed`, no findings.
- Boundary:
  - This remains local mock/control-plane and frontend contract evidence.
  - It does not prove deployed-server local Docker/FSL/MRtrix/FreeSurfer/container execution, native QC artifact quality from real runs, production CORS, or strict deployment acceptance.
