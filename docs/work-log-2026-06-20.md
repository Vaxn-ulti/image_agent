# Work Log 2026-06-20

## Elasticsearch Hybrid RAG Git Bootstrap

- Reframed ES hybrid RAG setup as a Git-managed installation path rather than an LLM/operator prose handoff.
- Added `apps/api/scripts/setup_elasticsearch_hybrid_rag.py`:
  - dry-run JSON plan by default;
  - explicit `--apply` path for Docker probe, pinned Elasticsearch image pull, loopback-bound container start, deployment-local `.env` update, optional RAG rebuild, and prerequisite verification;
  - pinned image `docker.elastic.co/elasticsearch/elasticsearch:9.4.2`;
  - no `latest` image acceptance and no secret values in JSON reports.
- Replaced old ES config plan steps with `setup_elasticsearch_hybrid_rag_from_git_script` in:
  - `apps/api/scripts/build_elasticsearch_hybrid_config_plan.py`;
  - `apps/api/scripts/verify_elasticsearch_hybrid_config_plan.py`;
  - `docs/deployment/remote-elasticsearch-hybrid-config-plan.json`.
- Added repository-level bootstrap entrypoint `scripts/bootstrap_image_agent.py` for GitHub checkout installs:
  - API venv and requirements;
  - frontend dependency install;
  - fixed workflow image preparation through `probe_runtime_environment --prepare-missing-images`;
  - ES hybrid RAG setup through the Git script.
- Updated `README.md` and `docs/deployment.md` to document the Git bootstrap path.

## Verification

- RED tests:
  - `apps/api/tests/test_setup_elasticsearch_hybrid_rag.py` failed because the setup script was missing.
  - `apps/api/tests/test_build_elasticsearch_hybrid_config_plan.py` failed because the plan still contained prose/operator ES configuration steps.
  - `apps/api/tests/test_bootstrap_image_agent.py` failed because the repo-level bootstrap script was missing.
- GREEN verification:
  - `python -m pytest apps/api/tests/test_bootstrap_image_agent.py apps/api/tests/test_setup_elasticsearch_hybrid_rag.py apps/api/tests/test_build_elasticsearch_hybrid_config_plan.py apps/api/tests/test_verify_elasticsearch_hybrid_config_plan.py apps/api/tests/test_runtime_image_preparation.py apps/api/tests/test_runtime_probe.py -q`
  - Result: `41 passed, 3 warnings`.
- Compile verification:
  - `python -m py_compile scripts/bootstrap_image_agent.py apps/api/scripts/setup_elasticsearch_hybrid_rag.py apps/api/scripts/build_elasticsearch_hybrid_config_plan.py apps/api/scripts/verify_elasticsearch_hybrid_config_plan.py`
  - Result: passed.
- `git diff --check`: no whitespace errors; only existing CRLF/LF warnings.
- Secret/proxy scan: no matches for the known proxy markers or API-key-shaped values.

## yyf Dry-Run Evidence

- Copied only the new bootstrap/setup scripts to `/tmp/image_agent_git_bootstrap_verify` on yyf.
- Ran non-mutating dry-runs:
  - `bootstrap_dry_run.json`: `plan_id=image_agent_bootstrap_v1`, `mode=dry_run`, `steps=7`.
  - `es_setup_dry_run.json`: `plan_id=elasticsearch_hybrid_rag_setup_v1`, `mode=dry_run`, `steps=8`.
- Both reports contained:
  - no yyf host hard-code;
  - no `latest`;
  - no `sk-` secret-shaped value.

## Remaining Blockers

- ES hybrid RAG is still not live-configured.
- No remote ES container was started in this slice.
- No live `.env` was modified.
- No production embedding secret was applied.
- No ES RAG rebuild or strict online smoke was run.
- Next ES acceptance step is to run the Git-managed bootstrap/setup with real local secrets on yyf, then verify `/agent/rag/status` and the strict ES prerequisite gate before strict smoke.

## Bootstrap Robustness Follow-Up

- Added early validation for Git bootstrap and ES setup:
  - ES hybrid setup now fails before writing `.env` if embedding model or embedding base URL is missing.
  - Repository bootstrap now applies the same check when Elasticsearch hybrid setup is enabled.
- Added retry/idempotency hardening for local Docker ES setup:
  - the script now inspects the Docker network and volume first;
  - existing resources are reused;
  - missing resources are created only when needed.
- Verification:
  - RED tests failed for missing embedding validation and blind Docker network creation.
  - GREEN command: `python -m pytest apps/api/tests/test_bootstrap_image_agent.py apps/api/tests/test_setup_elasticsearch_hybrid_rag.py apps/api/tests/test_build_elasticsearch_hybrid_config_plan.py apps/api/tests/test_verify_elasticsearch_hybrid_config_plan.py apps/api/tests/test_runtime_image_preparation.py apps/api/tests/test_runtime_probe.py -q`
  - Result: `44 passed, 3 warnings`.
  - `py_compile` passed for the touched bootstrap/setup/config verifier scripts.
  - `git diff --check` passed for touched files with only pre-existing CRLF/LF warnings.
  - Secret/proxy scan returned no matches.

## yyf Read-Only Check

- `/home/yyf/project/image_agent/.env` exists, but all ES/embedding keys remain missing.
- Docker access is currently available to the `yyf` user.
- `127.0.0.1:9200` is not listening.
- Temporary API `127.0.0.1:18082` is reachable, but RAG is still not configured as Elasticsearch hybrid.
- Refreshed non-mutating dry-runs under `/tmp/image_agent_git_bootstrap_verify`:
  - `bootstrap_dry_run_v2.json`;
  - `es_setup_dry_run_v2.json`;
  - `bootstrap_skip_es_dry_run_v2.json`.
- All three dry-run JSON files are valid and contain no `latest`, no API-key-shaped value, and no yyf hard-code.
- The updated objective permits temporary proxy use for remote image pulls or external access, but no proxy URL/token was written to code, docs, scripts, or logs.

## Embedding Env Derivation And Gate Order

- Added `--derive-embedding-from-env` to `apps/api/scripts/setup_elasticsearch_hybrid_rag.py`.
- The setup script can now derive RAG embedding configuration from existing deployment/model env:
  - base URL source can be an existing model env key such as `OPENAI_BASE_URL`;
  - model defaults to `text-embedding-3-small` if no explicit embedding model is configured;
  - API key source is reported only as a safe presence label.
- yyf read-only env presence showed existing model gateway keys are present while RAG-specific ES/embedding keys are still missing. The safe derived dry-run summary from `/tmp/image_agent_git_bootstrap_verify/es_setup_derive_dry_run.json` reported:
  - `plan_id=elasticsearch_hybrid_rag_setup_v1`;
  - `mode=dry_run`;
  - `model_source=default_text_embedding_3_small`;
  - `base_url_source=OPENAI_BASE_URL`;
  - `api_key_source=existing_runtime_fallback_present`.
- Corrected the ES handoff order:
  - setup step now only provisions/configures ES and writes env;
  - API restart happens before RAG rebuild and prerequisite verification;
  - `--rebuild-rag` and `--verify-prerequisites` are explicitly rejected in the setup step by tests/verifier.
- Verification:
  - RED: tests failed while `derive_embedding_from_env` did not exist and while setup still attempted rebuild/verify before restart.
  - GREEN: ES/bootstrap/config/runtime slice returned `45 passed, 3 warnings`.
  - Touched scripts passed `py_compile`.
  - `git diff --check` passed for touched files with only pre-existing CRLF/LF warnings.
  - Secret/proxy scan returned no matches.

## yyf ES Apply And RAG Root Fix

- Ran the Git-managed ES setup apply on yyf with runtime-only proxy environment and no proxy material written to source, docs, scripts, or repo logs.
- Remote ES setup report: `/tmp/image_agent_es_setup_apply_20260620T033302+0800.json`.
  - `status=completed`;
  - image `docker.elastic.co/elasticsearch/elasticsearch:9.4.2` already present;
  - container `image-agent-es` already running;
  - bind endpoint `http://127.0.0.1:9200`;
  - deployment env file `/home/yyf/project/image_agent/.env` updated by script.
- Corrected setup derivation so `--derive-embedding-from-env` skips the local hashing placeholder model `local-token-hash-v1` and derives the production RAG embedding model/base URL from existing deployment env.
- Fixed runtime portability bugs found during remote verification:
  - `app.services.agent_service._repo_root()` and `app.main.REPO_ROOT` now follow `IMAGE_AGENT_ROOT` through `config.ROOT`, so overlay processes read the installed root RAG index instead of the overlay `.rag_index`;
  - `runtime.list_elasticsearch_containers()` now honors `IMAGE_AGENT_DOCKER_COMMAND`, matching the setup script's configurable Docker command behavior.
- Remote runtime probe evidence: `/tmp/image_agent_runtime_probe_es_20260620T033842+0800.json`.
  - ES is configured;
  - local Docker ES discovery is available;
  - one ES container is running;
  - probe remains blocked overall by existing Docker-sudo/runtime reachability codes.
- Remote API on `127.0.0.1:18082` was restarted only for temporary acceptance; production port 8000 was not touched.
- Remote `/agent/rag/status` now reads `/home/yyf/project/image_agent/.rag_index` and reports:
  - `engine=elasticsearch_hybrid`;
  - configured embedding provider/model metadata preserved as `openai` / `text-embedding-3-small`;
  - embedding transport `sdk`;
  - embedding endpoint configured;
  - `mode=embedding_error`, `indexed_chunk_count=0`.
- Root cause for the remaining ES hybrid blocker: the derived embedding endpoint points to a loopback HTTP port on yyf, and no service is listening there. A runtime-only proxy cannot fix a loopback endpoint guarded by `NO_PROXY`.
- Current prereq verifier evidence: `/tmp/image_agent_es_prereq_verify_20260620T034355+0800.txt`.
  - Expected failure remains: hybrid mode is not connected, index is not persisted, indexed chunk count is zero, hybrid/embedding errors are present, and embedding production readiness is false.
- Local verification after these fixes:
  - `python -m pytest apps/api/tests/test_config_paths.py apps/api/tests/test_runtime_probe.py apps/api/tests/test_setup_elasticsearch_hybrid_rag.py apps/api/tests/test_agent_state_and_rag_index.py -q`
  - Result: `56 passed, 3 warnings`.

## yyf Local Embedding And ES Hybrid Connected

- Completed the Git-managed local embedding service path for yyf:
  - script entrypoint: `apps/api/scripts/setup_local_embedding_service.py`;
  - pinned image: `ghcr.io/huggingface/text-embeddings-inference:cpu-1.9`;
  - default model id: `sentence-transformers/all-MiniLM-L6-v2`;
  - served model: `image-agent-minilm-l6-v2`;
  - endpoint: `http://127.0.0.1:18081/v1`;
  - network mode: `host`.
- Fixed the container proxy boundary:
  - host-network TEI keeps loopback runtime proxy values for this run;
  - bridge mode still rewrites loopback proxy hosts to `host.docker.internal`;
  - proxy URL/token remains runtime-only and is not written to source, docs, scripts, or saved logs.
- yyf embedding setup evidence: `/tmp/image_agent_local_embedding_setup_apply_minilm_hostloop_20260620T125325+0800.json`.
  - `status=completed`;
  - endpoint probe passed;
  - direct embedding probe returned dimension `384`.
- Installed missing remote API dependency `elasticsearch==9.4.1` in the yyf API venv so the configured client can talk to the local ES `9.4.2` container.
- yyf RAG rebuild evidence: `/tmp/image_agent_rag_rebuild_es_hybrid_20260620T125635+0800.json`.
  - `mode=connected`;
  - `configured=true`;
  - `persisted=true`;
  - `indexed_chunk_count=239`;
  - `dense_vector_dims=384`;
  - provider/model `openai` / `image-agent-minilm-l6-v2`;
  - transport `sdk`;
  - production ready `true`.
- Restarted only the temporary yyf API on `127.0.0.1:18082`; production port `8000` and the live tree were not modified. Current temporary API log: `/tmp/image_agent_api_18082_es_hybrid_final_20260620T130406+0800.log`.
- yyf `/agent/rag/status` evidence: `/tmp/image_agent_api_rag_status_after_query_fallback_20260620T130239+0800.json`.
  - `engine=elasticsearch_hybrid`;
  - mode `connected`;
  - persisted `true`;
  - chunks `239`;
  - dims `384`;
  - no embedding error.
- yyf `/agent/rag/query` evidence: `/tmp/image_agent_api_rag_query_final_20260620T130411+0800.json`.
  - retrieval mode/source `elasticsearch_hybrid`;
  - citations `5`;
  - official curated QSIPrep source cited;
  - fusion `query_plus_knn`.
- Important ES license boundary:
  - Elasticsearch `9.4.2` rejected the RRF retriever under the current license;
  - code now attempts RRF first and falls back only to ES `query + knn` hybrid when the failure reason is `license_non_compliant`;
  - query/smoke evidence records `rrf_unavailable_reason=license_non_compliant`;
  - local/mock fallback retrieval remains blocked for production acceptance.
- Local verification:
  - `python -m pytest apps/api/tests/test_setup_local_embedding_service.py apps/api/tests/test_bootstrap_image_agent.py apps/api/tests/test_build_elasticsearch_hybrid_config_plan.py apps/api/tests/test_verify_elasticsearch_hybrid_config_plan.py apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_require_elasticsearch_hybrid_rag_records_acceptance_evidence apps/api/tests/test_verify_remote_smoke_acceptance.py -q`
  - Result: `341 passed`.
- Proxy marker scan for the known runtime proxy URL/token markers returned no matches.

## yyf Elasticsearch Trial License And RRF

- Applied the official Elasticsearch trial license endpoint on yyf local ES:
  - request: `POST http://127.0.0.1:9200/_license/start_trial?acknowledge=true`;
  - response evidence: `/tmp/image_agent_es_start_trial_20260620T131432+0800.json`;
  - result: `acknowledged=true`, `trial_was_started=true`.
- License verification evidence: `/tmp/image_agent_es_license_after_trial_20260620T131432+0800.json`.
  - license type `trial`;
  - status `active`.
- Rebuilt ES hybrid RAG and rechecked query behavior after trial activation:
  - rebuild evidence: `/tmp/image_agent_api_rag_rebuild_after_trial_20260620T131432+0800.json`;
  - status evidence: `/tmp/image_agent_api_rag_status_after_trial_20260620T131432+0800.json`;
  - query evidence: `/tmp/image_agent_api_rag_query_after_trial_20260620T131432+0800.json`;
  - rebuild/status/query all report fusion `rrf`;
  - query retrieval mode/source remains `elasticsearch_hybrid`;
  - query citations `5`;
  - `rrf_unavailable_reason` is absent after trial activation.
- Ran the read-only Elasticsearch hybrid prerequisite gate against temporary yyf API `127.0.0.1:18082`:
  - `status=passed`;
  - `rag_status_hybrid_fusion=rrf`;
  - `rag_status_hybrid_official_rrf_source_present=true`;
  - chunks `239`, dims `384`, provider/model `openai` / `image-agent-minilm-l6-v2`;
  - secrets redacted.

## yyf Rawchat Direct Agent And RAG Provenance Gate

- Wrote the deployment model gateway settings into yyf `.env` for the rawchat Responses-compatible target, using the user-supplied key only as a remote secret value. The key was not written to source, docs, scripts, or saved local logs.
- Root-caused the first non-task strict smoke failure to a stale live-root RAG document tree: the temporary API ran from the release overlay but Agent/RAG repo-root compatibility exports still pointed at `/home/yyf/project/image_agent`, whose vendor summaries lacked `raw_source_ids`.
- Fixed release overlay repo-root handling so `IMAGE_AGENT_RELEASE_ROOT` drives `REPO_ROOT` for RAG/docs/skills while `IMAGE_AGENT_ROOT` continues to own persistent data and projects.
- Rebuilt ES hybrid RAG from the release overlay:
  - rebuild evidence: `/tmp/image_agent_api_rag_rebuild_release_root_20260620T132822+0800.json`;
  - status evidence: `/tmp/image_agent_api_rag_status_release_root_20260620T132822+0800.json`;
  - query evidence: `/tmp/image_agent_api_rag_query_release_root_20260620T132822+0800.json`;
  - status reports `engine=elasticsearch_hybrid`, fusion `rrf`, chunks `340`, vendor docs `22`, and `curated_provenance_ok=true`.
- Root-caused the rawchat timeout to inherited system proxy environment variables in the remote shell/process environment. Explicit direct yyf checks with `--noproxy '*'` returned HTTP `200` from the rawchat Responses endpoint in about two seconds.
- Hardened `ModelGateway` so the OpenAI SDK client defaults to `httpx.Client(trust_env=False)`, preventing accidental use of ambient `HTTP_PROXY`/`HTTPS_PROXY`/`ALL_PROXY`. A deployment can explicitly opt in with `IMAGE_AGENT_MODEL_TRUST_ENV_PROXY=1`.
- Added public safe model status evidence for `trust_env_proxy=false`; no proxy values or secrets are exposed.
- Fixed the LangGraph state schema so compiled LangGraph runs preserve intermediate `decision`, `planner_tool_trace`, `skill_context`, and final `result`. This restored public Agent contract fields such as top-level `intent` and `answer`.
- Remote direct Agent smoke now returns `status=answered`, `intent=answer_question`, and a non-empty answer in about 22 seconds.
- Non-task strict smoke passed against temporary yyf API `127.0.0.1:18082`:
  - evidence: `/tmp/image_agent_non_task_strict_rawchat_direct_20260620T135403+0800.json`;
  - model gate `passed`, provider profile `rawchat`, wire API `responses`, model tool loop enabled;
  - ES hybrid RAG gate `passed`, fusion `rrf`, chunks `340`, official RRF source present;
  - raw-source policy and vendor pointer integrity passed with 22 complete vendor docs and 61 raw official snapshots;
  - launchability matrix RAG query passed.
- Production port `8000` and the live code tree were not modified; only the temporary `127.0.0.1:18082` acceptance API and release overlay were updated.

## yyf DWI Full Agent-To-Result Closure

- Continued yyf real-flow acceptance on the temporary API `127.0.0.1:18082`; production port `8000` and the live code tree were not modified.
- Fixed result and observation contract issues found by strict task evidence:
  - nested native task logs are now discovered for task events without leaking host paths;
  - zero-byte files are omitted from downloadable artifact manifests;
  - public result summaries now use the stable task `workflow_type` and redact nested backend paths;
  - Agent confirmation responses preserve safe workflow metadata, preflight, runtime workflow, and data-candidate fields;
  - confirmation fingerprints treat omitted optional fields and explicit `null` optional fields as equivalent.
- Fixed the remote external BOLD fMRIPrep source-script blocker by locking the deployment script reference from `nipreps/fmriprep:latest` to `nipreps/fmriprep:25.2.5`. The managed wrapper path already rejects floating tags; this was an external deployment artifact correction.
- Verified completed T1 DeepPrep result/QC/report observation on existing yyf `task_id=120`:
  - strict evidence: `/tmp/image_agent_existing_t1_task120_strict_observe_20260620T142705+0800.json`;
  - task artifact manifest, result summary, task events, ObserveRepair, container-native QC, scientific report artifacts, rawchat model status, and ES hybrid RAG gates passed.
- Launched DWI through the required Agent fixed-workflow chain on yyf project `15`, upload session `7`, series `27`:
  - Agent run prepared `dwi_fast_gpu_dti` confirmation with `production_task_created=false` and ES hybrid RAG grounding;
  - tampered confirmation resume was blocked with `confirmation_gate=fingerprint_mismatch` and no production task;
  - approved confirmation resume passed `confirmation_gate=fingerprint_verified` and created `task_id=125`;
  - task `125` completed with stable `workflow_type=dwi_fast_gpu_dti` and runtime `dwi_fast_gpu_dti`.
- Task `125` runtime evidence:
  - started `2026-06-20T06:55:16Z`, finished `2026-06-20T07:12:32Z`;
  - runtime manifest records deployment-server-local execution, no external worker requirement, and pinned `pennlinc/qsiprep:26.0.0` with `floating_tags_allowed=false`;
  - result summary exposes DWI tensor maps, MNI152 maps, atlas tables, QC/provenance, and HTML/PNG report artifacts;
  - artifact manifest exposes 29 path-safe served artifacts, including 3 scientific report PNGs;
  - ObserveRepair reports `policy=read_only_observe_repair`, `auto_rerun_allowed=false`, `production_task_created=false`, and human confirmation plus preflight required before retry.
- Added `--skip-agent-run-smoke` to `apps/api/scripts/smoke_remote_agent.py` so task/result acceptance can validate model status, wire API, tool-loop capability, RAG, upload inventory, result summaries, artifact manifests, and ObserveRepair without also forcing a generic LLM answer during that same gate.
  - The generic Agent answer smoke remains available by default and explicit workflow confirmation/resume gates still call `/agent/runs` and `/agent/runs/{thread_id}/resume`.
  - The saved smoke gate now records `skip_agent_run_smoke=true` when this layered validation mode is used.
- Latest DWI task-only acceptance evidence:
  - `/tmp/image_agent_dwi_task125_api_observe_20260620T214102+0800.json`;
  - `task_status_status=passed`;
  - `task_artifact_manifest_status=passed`;
  - `task_result_summary_status=passed`;
  - `observe_repair_status=passed`;
  - `rag_elasticsearch_hybrid_status=passed`;
  - `rag_elasticsearch_hybrid_query_status=passed`;
  - `rag_launchability_matrix_status=passed`;
  - `scientific_report_artifacts_status=passed`;
  - `upload_inventory_contract_status=passed`;
  - `project_workflow_eligibility_metadata_status=passed`.
- Local focused verification:
  - `python -m pytest apps/api/tests/test_task_logs.py apps/api/tests/test_api_flow.py::test_task_logs_redact_secrets_and_backend_paths apps/api/tests/test_agent_tools.py::test_read_task_and_events_include_remote_wrapper_logs -q` returned `4 passed`.
  - `python -m pytest apps/api/tests/test_artifact_manifest_contract.py apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_rejects_bad_artifact_manifest_fields_via_task_gate -q` returned `20 passed`.
  - `python -m pytest apps/api/tests/test_result_service_contract.py apps/api/tests/test_artifact_manifest_contract.py apps/api/tests/test_agent_backend_context.py apps/api/tests/test_agent_tools.py::test_read_result_summary_prefers_registered_summary apps/api/tests/test_agent_tools.py::test_read_result_summary_fallback_enriches_historical_runtime_alias apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_rejects_bad_artifact_manifest_fields_via_task_gate -q` returned `24 passed`.
  - `python -m pytest apps/api/tests/test_agent_api.py::test_langgraph_agent_resume_approved_confirmation_creates_real_task_via_task_service apps/api/tests/test_agent_api.py::test_agent_run_confirmation_does_not_create_task_before_resume apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_requires_agent_workflow_resume apps/api/tests/test_agent_graph.py::test_confirmation_fingerprint_ignores_null_optional_fields apps/api/tests/test_agent_graph.py::test_confirmation_fingerprint_covers_preflight_and_display_metadata -q` returned `5 passed`.
  - `python -m pytest apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_can_skip_generic_agent_run_while_requiring_model_status apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_skips_model_run_when_gateway_unconfigured apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_require_model_fails_when_gateway_unconfigured apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_expected_model_wire_api_rejects_mismatch apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_expected_model_provider_profile_rejects_mismatch apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_requires_agent_workflow_resume -q` returned `6 passed`.
- Remaining risk:
  - a broad strict smoke that combines generic `/agent/runs` freeform answer, RAG rebuild, and task/result validation in one process timed out on the generic Agent answer step; this did not contradict task `125` completion or workflow confirmation/resume evidence, but the generic Agent answer smoke should stay as a separate monitored gate with direct rawchat access.
  - DWI fast full workflow has now closed one real fixed-workflow loop; BOLD full fMRIPrep/XCP-D and any longer production workflows still need separate one-per-workflow acceptance runs.

## yyf BOLD Full Workflow Launch Gate

- Fixed the public Agent confirmation contract exposed by the first BOLD full launch attempt:
  - `fingerprint` is now a public confirmation envelope field and is excluded from its own canonical hash;
  - `confirmation_fingerprint` is treated the same way for compatibility;
  - resume explicitly rejects a wrong public fingerprint before task creation;
  - confirmation payloads now expose top-level `runtime_workflow_type`;
  - API resume schema accepts public `runtime_workflow_type`, `fingerprint`, and `confirmation_fingerprint`.
- Local regression for the confirmation/resume contract passed:
  - `python -m pytest apps/api/tests/test_agent_graph.py::test_confirmation_fingerprint_ignores_public_fingerprint_envelope_field apps/api/tests/test_agent_graph.py::test_agent_runner_resume_rejects_wrong_public_fingerprint_field apps/api/tests/test_agent_graph.py::test_langgraph_agent_runner_resume_blocks_client_confirmation_overrides apps/api/tests/test_agent_api.py::test_agent_resume_approved_confirmation_creates_real_task apps/api/tests/test_agent_api.py::test_agent_resume_preserves_canonical_workflow_type_and_records_runtime_alias apps/api/tests/test_agent_api.py::test_agent_run_confirmation_does_not_create_task_before_resume apps/api/tests/test_agent_api.py::test_langgraph_agent_resume_approved_confirmation_creates_real_task_via_task_service apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_requires_agent_workflow_resume -q`
  - result: `8 passed`.
- Synced the contract fix to the yyf temporary release overlay and restarted only the temporary API on `127.0.0.1:18082`; production port `8000` and the live source tree were not modified. Current restart log: `/tmp/image_agent_api_18082_schema_fingerprint_20260620T221031+0800.log`.
- A broad BOLD smoke that re-ran `/agent/runs` through rawchat direct timed out client-side at 120 seconds during the initial model-planning request. The server later recorded a normal `confirmation_required` run, so this is logged as rawchat latency/timeout pressure, not a task gate failure. Rawchat remains configured for direct access (`trust_env_proxy=false`) rather than ambient proxy.
- Created a fresh deterministic-control-plane BOLD confirmation on yyf to validate the fixed backend chain without waiting on another model call:
  - project `16`, series `31`, workflow `bold_fmriprep_xcpd_report`;
  - preflight passed with runtime `bold_fmriprep_xcpd_report`;
  - public fingerprint was present;
  - fingerprint-negative resume returned `status=blocked`, `confirmation_gate=fingerprint_mismatch`, and `production_task_created=false`;
  - approved resume returned `status=task_created`, `confirmation_gate=fingerprint_verified`, and `production_task_created=true`;
  - evidence: `/tmp/image_agent_bold31_deterministic_confirmation_resume_20260620T221221+0800.json`.
- Created real production task `126` through the required fixed-workflow chain:
  - registry workflow `bold_fmriprep_xcpd_report`;
  - preflight pass;
  - server-side pending confirmation;
  - fingerprint mismatch negative gate before approval;
  - approved `/agent/runs/{thread_id}/resume`;
  - `task_service.create_series_task(..., confirmed_agent_gate=True)`;
  - pipeline runner started task `126`.
- Task `126` runtime evidence:
  - DB status at observation: `running`, progress `20`;
  - task log: `/home/yyf/project/image_agent/data/projects/16/logs/126.log`;
  - runtime manifest records deployment-server-local execution, `nipreps/fmriprep:25.2.5`, `pennlinc/xcp_d:26.0.2`, and `floating_tags_allowed=false`;
  - Docker shows a running `nipreps/fmriprep:25.2.5` container;
  - container log confirms `Running fMRIPrep version 25.2.5` and BIDS dataset `sub-01`;
  - the container began fetching TemplateFlow resources, so the full BOLD run is in progress and may take longer than this acceptance turn.
- Current boundary:
  - The product control-plane launch loop for BOLD is now validated through real runner/container start.
  - Full BOLD completion, result summary/report/QC artifact validation, XCP-D stage, and ObserveRepair/result gates remain to be checked after task `126` finishes.

### Rawchat Direct Transport Check

- Rechecked the yyf temporary API after the user clarified that rawchat endpoint access must be direct, not through the proxy used for Docker/image pulls.
- Safe `/agent/model/status` summary on `127.0.0.1:18082` reports:
  - `provider_profile=rawchat`;
  - `wire_api=responses`;
  - `configured=true`;
  - `trust_env_proxy=false`;
  - `model_gateway_access=direct`;
  - base host `rawchat.cn`.
- The API process environment still contains ambient proxy variables from earlier remote pull/setup work, but `ModelGateway` constructs its OpenAI-compatible client with `httpx.Client(trust_env=False)` unless explicitly opted in with `IMAGE_AGENT_MODEL_TRUST_ENV_PROXY=1`.
- From the same running API process environment, both direct probes reached rawchat without using ambient proxy configuration:
  - `httpx.Client(trust_env=False)` request to the rawchat-compatible `/models` endpoint returned HTTP `200`;
  - `curl --noproxy '*'` to the same endpoint returned HTTP `200`.
- Hardened the Elasticsearch hybrid RAG embedding path as well, because configured embeddings may reuse the same rawchat/OpenAI-compatible endpoint:
  - SDK embeddings now pass `httpx.Client(trust_env=False)`;
  - HTTP fallback embeddings now use an empty `urllib.request.ProxyHandler({})` opener instead of ambient proxy discovery.
- Local verification for this proxy boundary:
  - `python -m pytest apps/api/tests/test_agent_state_and_rag_index.py::test_local_rag_index_openai_embedding_provider_ignores_ambient_proxy_by_default apps/api/tests/test_agent_state_and_rag_index.py::test_local_rag_index_http_embedding_fallback_uses_no_proxy_opener apps/api/tests/test_agent_state_and_rag_index.py::test_local_rag_index_uses_env_configured_openai_embedding_provider apps/api/tests/test_agent_state_and_rag_index.py::test_local_rag_index_uses_openai_compatible_http_embedding_provider_without_sdk -q` returned `4 passed`;
  - `python -m pytest apps/api/tests/test_model_gateway.py::test_request_disables_openai_sdk_environment_proxy_by_default apps/api/tests/test_model_gateway.py::test_request_can_opt_into_openai_sdk_environment_proxy -q` returned `2 passed`;
  - `python -m py_compile apps/api/app/agent/rag_index.py apps/api/app/agent/model_gateway.py` returned exit code `0`.
- Synced the RAG embedding hardening to the yyf temporary release overlay and compiled it there; no API restart or environment mutation was performed because BOLD task `126` is still running. Clearing ambient proxy variables for the temporary API process can be scheduled after the running workflow finishes, but the active model gateway path already ignores those variables for rawchat.

### BOLD Task 126 Follow-up Observation

- Continued read-only monitoring of yyf task `126`; no rerun, task mutation, production port change, or `.env` edit was performed.
- Current DB state remains `running`, progress `20`, with no `error_message`.
- The running Docker container remains `nipreps/fmriprep:25.2.5`; Docker inspect reports it is still running and not OOM-killed.
- fMRIPrep output log confirms:
  - `Running fMRIPrep version 25.2.5`;
  - BIDS dataset `/data`;
  - participant `sub-01`;
  - output spaces `MNI152NLin6Asym:res-2`, `MNI152NLin2009cAsym:res-2`, and `T1w`.
- The task is currently bottlenecked on TemplateFlow downloads from `templateflow.s3.amazonaws.com` inside the running container:
  - `tpl-MNI152NLin6Asym_res-01_T1w.nii.gz` completed after roughly nine minutes;
  - `tpl-MNI152NLin6Asym_res-01_desc-brain_mask.nii.gz` also completed;
  - it then started downloading `tpl-MNI152NLin2009cAsym_res-01_T1w.nii.gz`.
- Recent file evidence under the task workspace shows TemplateFlow files, fMRIPrep work config, layout index, and `output/logs/fmriprep.log` being written. This supports a slow external-resource-download state rather than a dead runner.
- Remaining action for the next acceptance continuation:
  - keep task `126` running undisturbed;
  - after fMRIPrep exits, verify whether the wrapper advances into the XCP-D step;
  - only after task completion run the result-summary/artifact-manifest/report/QC/ObserveRepair gates.

### BOLD Task 126 Failure Diagnosis

- Continued read-only observation showed task `126` failed before fMRIPrep entered processing nodes:
  - API task status: `failed`, progress `20`;
  - `error_message=deployment-local script failed rc=1: run_fmriprep.sh`;
  - finished at `2026-06-20T14:43:49.748024+00:00`.
- Root-cause evidence:
  - fMRIPrep log reached TemplateFlow download progress around `87%`;
  - `Traceback` count `3`;
  - final error: `requests.exceptions.ConnectionError: HTTPSConnectionPool(host='templateflow.s3.amazonaws.com', port=443): Read timed out.`;
  - no XCP-D log was created, so the failure happened in step `1/2` before downstream XCP-D.
- Saved read-only diagnosis evidence:
  - `/tmp/image_agent_bold126_failure_diagnosis_20260620T224551+0800.json`.
- ObserveRepair contract was verified through the remote API for failed task `126`:
  - `status=ok`;
  - `policy=read_only_observe_repair`;
  - `auto_rerun_allowed=false`;
  - `production_task_created=false`;
  - `requires_preflight_before_retry=true`;
  - `requires_human_confirmation_before_retry=true`.
- Interpretation:
  - LangGraph confirmation/resume, stable workflow type, registry/preflight, fingerprint verification, task creation, and runner start were exercised successfully.
  - The real BOLD workflow did not close the result/report/QC loop because runtime data dependency download failed inside the fMRIPrep container.
  - The next attempt should not be an automatic rerun. It should first prepare a shared TemplateFlow cache, pass registry/preflight again, then require human confirmation before creating a new production task.
- Added a portable TemplateFlow prewarm script so this failure mode is handled by Git-installable setup rather than ad hoc remote operations:
  - `apps/api/scripts/prewarm_templateflow_cache.py`;
  - uses pinned `nipreps/fmriprep:25.2.5`;
  - mounts a shared host cache as `/templateflow`;
  - runs the fMRIPrep container's TemplateFlow client for `MNI152NLin2009cAsym` and `MNI152NLin6Asym`;
  - writes `IMAGE_AGENT_TEMPLATEFLOW_HOME` to the chosen env file when applied;
  - does not store proxy URLs, API keys, or host-specific yyf details.
- Connected the prewarm step into `scripts/bootstrap_image_agent.py` behind `--prewarm-templateflow`, keeping it optional and portable.
- Local verification for the new setup path:
  - `python -m pytest apps/api/tests/test_prewarm_templateflow_cache.py apps/api/tests/test_bootstrap_image_agent.py -q` returned `9 passed`;
  - `python -m py_compile apps/api/scripts/prewarm_templateflow_cache.py scripts/bootstrap_image_agent.py` returned exit code `0`.
- Synced the new prewarm script and updated bootstrap script to the yyf temporary release overlay and compiled them there.
- Boundary:
  - no TemplateFlow prewarm was executed yet;
  - no API restart was performed;
  - no new production task was created;
  - no rerun was attempted.

### TemplateFlow Shared Cache Prewarm on yyf

- Continued from the BOLD task `126` failure diagnosis without creating or rerunning any production task.
- Hardened `apps/api/scripts/prewarm_templateflow_cache.py` after yyf evidence showed three separate runtime issues:
  - Docker bridge forwarding of loopback proxy values points at the container, so bridge mode now rewrites loopback proxy hosts to `host.docker.internal` without serializing proxy values; host network mode is also supported.
  - The broad TemplateFlow client call was too coarse and hit S3 read timeouts, so the client path now uses targeted `tflow.get(template, resolution, suffix, desc, extension)` queries with a configurable request timeout and retries.
  - yyf TemplateFlow S3 traffic worked better by direct slow download than through the ambient proxy environment, so the script now supports `--download-method curl --direct-download` for resumable direct downloads of the fixed template files.
- Hardened `scripts/bootstrap_image_agent.py` so Git installs can opt in to TemplateFlow proxy forwarding and network mode through script flags rather than manual operator notes:
  - `--prewarm-templateflow`;
  - `--forward-templateflow-proxy-env`;
  - `--templateflow-network-mode`.
- yyf network probes, with proxy values redacted, showed:
  - direct range download from `templateflow.s3.amazonaws.com` returned HTTP `206` but was slow;
  - the current proxy environment returned HTTP `000` / timeout for the same TemplateFlow object.
- Corrected root-owned cache leftovers from earlier Docker prewarm attempts by changing ownership of the overlay cache directory back to `yyf:yyf` and deleting 0-byte files.
- Executed the Git-managed direct curl prewarm on the yyf temporary overlay:
  - command mode: `--download-method curl --direct-download --attempts 3 --request-timeout 240`;
  - output report: `/tmp/image_agent_templateflow_prewarm_curl_direct_20260620T232253+0800.json`;
  - result: `status=completed`, `completed_commands=8`, `secrets_redacted=true`;
  - `container_proxy_forwarding.enabled=false`, confirming this TemplateFlow fetch did not use the failing proxy path.
- Cache verification after prewarm:
  - zero-size file count: `0`;
  - fixed template files present and non-empty for both `MNI152NLin2009cAsym` and `MNI152NLin6Asym`;
  - largest checked files: `tpl-MNI152NLin2009cAsym_res-01_T1w.nii.gz` (`13685075` bytes) and `tpl-MNI152NLin6Asym_res-01_T1w.nii.gz` (`11027773` bytes).
- Confirmed through the temporary API that no new production task was created after the cache work:
  - latest project `16` task remains task `126` with `status=failed`, `workflow_type=bold_fmriprep_xcpd_report`, `created_at=2026-06-20T14:12:24.124802+00:00`;
  - no automatic rerun was attempted.
- Local verification for the updated setup path:
  - `python -m pytest apps/api/tests/test_prewarm_templateflow_cache.py apps/api/tests/test_bootstrap_image_agent.py -q` returned `17 passed`;
  - the new RED/GREEN coverage includes bridge proxy rewriting, host network mode, targeted TemplateFlow client queries, retry behavior, direct curl mode, and proxy-value redaction.
- Boundary:
  - the shared TemplateFlow cache is now prepared for the next human-confirmed BOLD full workflow attempt;
  - task `126` itself remains failed by design and was not mutated;
  - the next BOLD full run must still go through registry, preflight, human confirmation, fingerprint verification, `task_service.create_series_task()`, and pipeline runner.
- Follow-up Git-install hardening:
  - `scripts/bootstrap_image_agent.py` now forwards the yyf-validated TemplateFlow prewarm settings through the repo bootstrap entrypoint:
    - `--templateflow-download-method curl`;
    - `--direct-templateflow-download`;
    - `--templateflow-attempts`;
    - `--templateflow-request-timeout`.
  - README now documents the direct resumable TemplateFlow prewarm command for BOLD/fMRIPrep deployments.
  - Focused local verification after this bootstrap handoff:
    - `python -m pytest apps/api/tests/test_bootstrap_image_agent.py apps/api/tests/test_prewarm_templateflow_cache.py -q` returned `20 passed`;
    - `python -m py_compile scripts/bootstrap_image_agent.py apps/api/scripts/prewarm_templateflow_cache.py` returned exit code `0`.
  - Synced the updated bootstrap script to the yyf temporary overlay and ran a non-mutating dry-run:
    - evidence: `/tmp/image_agent_bootstrap_templateflow_direct_dryrun_20260620T233704+0800.json`;
    - dry-run prewarm command includes `--download-method curl --attempts 3 --request-timeout 240 --direct-download`;
    - report checks: `contains_latest=False`, `contains_secret_shape=False`, `contains_remote_binding=False`.

### BOLD TemplateFlow Preflight Gate

- Moved the task `126` TemplateFlow failure mode earlier in the fixed-workflow chain by hardening the BOLD fMRIPrep/XCP-D deployment-local preflight.
- `apps/api/app/workflows/remote_scripts.py` now declares `BOLD_REQUIRED_TEMPLATEFLOW_FILES` for the fixed `MNI152NLin2009cAsym` and `MNI152NLin6Asym` T1w / brain-mask files used by the Git-managed prewarm script.
- `preflight_bold_fmriprep_xcpd_remote()` now checks:
  - shared TemplateFlow cache writable;
  - required TemplateFlow files present;
  - required TemplateFlow files non-empty;
  - path-safe public summary labels.
- A missing or zero-byte TemplateFlow cache now blocks preflight before human confirmation and before `task_service.create_series_task()` can be reached.
- Local RED/GREEN coverage added:
  - empty TemplateFlow cache fails with `templateflow_required_files_present`;
  - complete cache passes and exposes `required_count=8`, `missing_count=0`, `zero_size_count=0`;
  - Agent preflight happy path now prepares the same required files.
- yyf remote dry preflight was run against the temporary overlay only; no production task was created and no rerun was attempted:
  - evidence: `/tmp/image_agent_bold_templateflow_preflight_20260620T234210+0800.json`;
  - result `ok=true`;
  - `templateflow_required_files_present.status=pass`;
  - `required_count=8`, `missing_count=0`, `zero_size_count=0`;
  - fixed images remain `nipreps/fmriprep:25.2.5` and `pennlinc/xcp_d:26.0.2`.
- Local verification for this gate:
  - `python -m pytest apps/api/tests/test_remote_scripts.py apps/api/tests/test_agent_tools.py::test_preflight_workflow_checks_remote_runtime_when_project_root_present -q` returned `19 passed`;
  - `python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_xcpd_remote_wrapper_flags_and_artifact_boundaries_are_documented_for_agent_use apps/api/tests/test_skill_and_rag_docs.py::test_registry_preflight_reference_documents_remote_wrapper_hardening -q` returned `2 passed`;
  - `python -m py_compile apps/api/app/workflows/remote_scripts.py apps/api/app/agent/tools.py` returned exit code `0`.

### Rawchat Direct Transport Reasserted

- Tightened `ModelGateway` so rawchat is direct even if a deployment shell accidentally sets `IMAGE_AGENT_MODEL_TRUST_ENV_PROXY=1`:
  - provider profile `rawchat` forces `trust_env_proxy=false`;
  - hosts `rawchat.cn` and `*.rawchat.cn` force `trust_env_proxy=false`;
  - non-rawchat OpenAI-compatible providers can still explicitly opt into environment proxy trust when required.
- Updated the remote deployment documentation to set `IMAGE_AGENT_MODEL_TRUST_ENV_PROXY=0` for the rawchat GPT-5.5 Responses target and to state that Docker/image-pull proxy settings must not be used for rawchat traffic.
- Synced `apps/api/app/agent/model_gateway.py` to the yyf temporary overlay and restarted only the temporary API on `127.0.0.1:18082`; production port `8000` was not touched.
- The yyf temporary API `.env` now explicitly contains the non-secret direct-transport guard `IMAGE_AGENT_MODEL_TRUST_ENV_PROXY=0` and `NO_PROXY` includes `rawchat.cn` / `.rawchat.cn`.
- Remote evidence:
  - `/tmp/image_agent_rawchat_direct_18082_20260620T235031+0800.json`;
  - `/agent/model/status` reports `provider_profile=rawchat`, `wire_api=responses`, `configured=true`, `trust_env_proxy=false`, `model_gateway_access=direct`, and base host `rawchat.cn`;
  - the API process still has ambient proxy env names present from runtime setup, but `IMAGE_AGENT_MODEL_TRUST_ENV_PROXY=0` is active and `NO_PROXY` includes rawchat;
  - direct `httpx.Client(trust_env=False)` probe to the rawchat `/models` endpoint returned HTTP `200`;
  - direct `curl --noproxy '*'` probe to the same endpoint returned HTTP `200`.
- No workflow task was created, no failed BOLD task was rerun, and no proxy URL/token or API key was written to source, docs, scripts, or logs.

### BOLD HTTP Agent Preflight After 18082 Restart

- Exercised the real yyf temporary HTTP Agent entrypoint after restarting `127.0.0.1:18082`; production port `8000` was not touched.
- First `/agent/runs` prepare-only request for project `16`, series `31`, workflow `bold_fmriprep_xcpd_report` returned `status=preflight_failed` without creating any task:
  - evidence: `/tmp/image_agent_bold_http_prepare_prefight_after_restart_20260620T235350+0800.json`;
  - response answer: `TemplateFlow cache is missing required files in templateflow (zero_size=8)`;
  - latest project task after the request remained failed task `126`;
  - `new_task_ids_after_prepare=[]`.
- Root cause: the temporary API process was restarted for rawchat transport but did not inherit `IMAGE_AGENT_TEMPLATEFLOW_HOME`, so `preflight_bold_fmriprep_xcpd_remote()` used the per-project pending work cache instead of the prewarmed shared cache.
- Fixed only yyf local runtime configuration:
  - wrote non-secret `IMAGE_AGENT_TEMPLATEFLOW_HOME` in `/home/yyf/project/image_agent/.env` to the already prewarmed shared cache;
  - preserved `IMAGE_AGENT_MODEL_TRUST_ENV_PROXY=0`;
  - restarted only temporary API `127.0.0.1:18082`;
  - evidence: `/tmp/image_agent_templateflow_env_restart_18082_20260620T235752+0800.json`;
  - required TemplateFlow files: `required_count=8`, `missing_count=0`, `zero_size_count=0`, `min_size_bytes=28557`.
- Re-ran the same prepare-only HTTP Agent request without calling `/resume`:
  - evidence: `/tmp/image_agent_bold_http_prepare_after_templateflow_env_20260620T235829+0800.json`;
  - HTTP status `200`;
  - response `status=confirmation_required`;
  - `thread_id_present=true`, `confirmation_present=true`, and `fingerprint_present=true`;
  - confirmation kept stable `workflow_type=bold_fmriprep_xcpd_report` and `runtime_workflow_type=bold_fmriprep_xcpd_report`;
  - `preflight_ok=true`, `preflight_blocking_errors=[]`;
  - `deployment_local_templateflow_cache_writable=pass`;
  - `deployment_local_templateflow_required_files_present=pass`;
  - `deployment_local_fmriprep_image_locked=pass`;
  - `deployment_local_xcpd_image_locked=pass`;
  - workflow metadata reports `display_name=BOLD fMRIPrep + XCP-D processing, metrics, QC, and report`, `workflow_family=bold`, `workflow_role=complete_processing`, `is_report_only=false`, and non-empty primary/QC/report outputs.
- Safety boundary:
  - `production_task_created=false`;
  - `new_task_ids_after_prepare=[]`;
  - latest project `16` task still task `126` with `status=failed`;
  - `/agent/runs/{thread_id}/resume` was not called;
  - no automatic rerun was attempted.
