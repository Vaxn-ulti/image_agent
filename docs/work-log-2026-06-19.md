# Work Log 2026-06-19

## Workflow Eligibility Metadata Contract

- Propagated registry-backed public `workflow_metadata` into `workflow_eligibility` items while keeping stable `workflow_type` unchanged for machine identity, task creation, confirmation fingerprints, database rows, result summaries, artifacts, logs, and acceptance evidence.
- Tightened strict remote smoke validation so `primary_recommendation`, `runnable_workflows`, and `blocked_workflows` dict entries with `workflow_type` must include matching safe `workflow_metadata`.
- Updated local main-flow smoke evidence sanitization to preserve safe workflow display/capability fields and drop unsafe debug paths or host-path text.
- Updated the image-agent developer contract to state that `workflow_eligibility.workflow_metadata` is display/interpretation evidence only and does not authorize task creation outside registry, preflight, human confirmation, confirmation fingerprint, `task_service.create_series_task()`, and the pipeline runner.
- Hardened weak-metadata negative acceptance: strict remote smoke now rejects eligibility metadata missing `display_name`, `capability_summary`, `workflow_family`, `workflow_role`, `pipeline_stages`, `primary_outputs`, `qc_outputs`, `report_outputs`, `limitations`, `agent_selectable=true`, or `is_report_only=false`.
- Updated the image-agent developer testing matrix so weak `workflow_eligibility.workflow_metadata` is documented as a remote smoke negative case.
- Promoted project-series and upload-inventory eligibility metadata checks into saved strict-acceptance evidence. Saved JSON now carries privacy-safe metadata summary fields for status, required field names, workflow types, and item counts, and the offline verifier requires those summaries.
- Documented the new saved evidence in the image-agent developer testing matrix, including `checked.project_workflow_eligibility_metadata_status=passed` and `checked.upload_inventory_workflow_eligibility_metadata_status=passed`.
- Propagated the saved project-series and upload-inventory workflow eligibility metadata evidence into the release-gate plan, release-gate verifier, stale-task apply builder, and stale-task apply verifier.
- The release/stale gates now require both live smoke evidence and offline `checked.*` evidence for metadata status, workflow-type inclusion, and non-empty item counts, while continuing to use stable `workflow_type` for task creation and fingerprint semantics.
- Added explicit checked booleans `checked.project_workflow_eligibility_metadata_task_workflow_type_included=true` and `checked.upload_inventory_workflow_eligibility_metadata_task_workflow_type_included=true` so offline verifier output directly proves the completed task workflow was present in project-series and upload-inventory eligibility metadata summaries.

## Verification

- RED: `test_workflow_eligibility_items_include_public_metadata_without_replacing_machine_id` first failed with `KeyError: 'workflow_metadata'`.
- RED: `test_smoke_remote_agent_rejects_workflow_eligibility_item_without_matching_metadata` first failed because live remote smoke accepted missing or mismatched eligibility metadata.
- GREEN: focused metadata registry checks returned `2 passed`.
- GREEN: focused remote/local smoke metadata checks returned `4 passed`.
- GREEN: `python -m pytest apps/api/tests/test_smoke_remote_agent.py apps/api/tests/test_smoke_local_main_flow.py -q` returned `158 passed`.
- GREEN: related registry/API checks returned `16 passed, 3 warnings`.
- GREEN: docs contract checks returned `2 passed`.
- GREEN: console workflow helper coverage returned `12 passed`.
- GREEN: touched Python files passed `py_compile`.
- RED: `test_smoke_remote_agent_rejects_weak_workflow_eligibility_metadata` first failed for all nine weak metadata variants because live remote smoke accepted them.
- RED: `test_image_agent_skill_references_document_backend_readiness_contracts` first failed because the testing matrix did not document weak metadata negative cases.
- GREEN: focused weak-metadata smoke checks returned `12 passed`.
- GREEN: `python -m pytest apps/api/tests/test_smoke_remote_agent.py -q` returned `152 passed`.
- RED: `test_smoke_remote_agent_validates_dataset_inventory_workflow_eligibility` first failed because saved smoke JSON lacked `project_workflow_eligibility_metadata_status`.
- RED: six verifier weak-evidence cases first failed because offline strict acceptance accepted skipped or incomplete project/upload eligibility metadata summaries.
- RED: `test_remote_acceptance_docs_require_result_summary_workflow_metadata_evidence` first failed because acceptance docs did not mention the saved eligibility metadata evidence.
- GREEN: focused smoke/verifier/docs checks returned `235 passed`.
- GREEN: `python -m pytest apps/api/tests/test_smoke_remote_agent.py apps/api/tests/test_verify_remote_smoke_acceptance.py -q` returned `447 passed`.
- RED: release-gate tests first failed because static expected-success lists and plan verification omitted project/upload workflow eligibility metadata evidence.
- RED: stale-task builder/verifier coverage first failed because generated follow-up gates and verifier constants did not mirror the release-gate strict evidence.
- GREEN: focused release/stale evidence checks returned `13 passed`, `1 passed`, and `1 passed`.
- GREEN: `python -m pytest apps/api/tests/test_release_gate_command_plan.py apps/api/tests/test_build_stale_task_apply_request.py apps/api/tests/test_verify_stale_task_apply_request.py -q` returned `181 passed`.
- GREEN: touched scripts passed `ast.parse` syntax checks; `py_compile` was blocked by a Windows `__pycache__` permission lock rather than a syntax error.
- RED: saved strict acceptance verifier first failed with `KeyError: 'project_workflow_eligibility_metadata_task_workflow_type_included'`.
- RED: release/stale expected-success checks first failed because static plan, release verifier, and stale-task builder omitted the new checked inclusion booleans.
- RED: docs contract first failed because the image-agent developer testing matrix did not list the new checked inclusion booleans.
- GREEN: focused verifier/release/stale checks returned `1 passed`, `15 passed`, and `1 passed`.
- GREEN: `python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py apps/api/tests/test_release_gate_command_plan.py apps/api/tests/test_build_stale_task_apply_request.py apps/api/tests/test_verify_stale_task_apply_request.py apps/api/tests/test_skill_and_rag_docs.py -q` returned `545 passed`.
- Tightened Elasticsearch hybrid rebuild/status endpoint drift verification: `verify_remote_smoke_acceptance.py` now checks `rag_rebuild_elasticsearch_hybrid.embedding_endpoint_configured` matches `rag_elasticsearch_hybrid.embedding_endpoint_configured` before requiring it to be true.
- RED: the rebuild endpoint weak-evidence cases first failed with the old duplicate truthiness message instead of `rag_rebuild_elasticsearch_hybrid.embedding_endpoint_configured must match status`.
- GREEN: filtered drift checks returned `5 passed, 290 deselected`; full saved strict-acceptance verifier returned `295 passed`; release/stale/docs checks returned `250 passed`; touched script passed `ast.parse`.
- Tightened Elasticsearch hybrid query/status embedding readiness drift verification: the saved strict acceptance verifier now requires query-time `rag_elasticsearch_hybrid_query_embedding_endpoint_configured` and `rag_elasticsearch_hybrid_query_embedding_production_ready` to match connected status evidence before requiring both values to be true.
- RED: query embedding weak-evidence cases first failed with old `must be true` messages rather than `must match status`.
- GREEN: filtered query drift checks returned `5 passed, 290 deselected`; release-gate query evidence checks returned `26 passed, 100 deselected`; stale-task builder focus returned `1 passed`; combined verifier/release/stale/docs checks returned `549 passed`; touched scripts passed `ast.parse`.
- GREEN: `git diff --check` returned no whitespace errors, only existing CRLF/LF warnings.
- Tightened Elasticsearch hybrid rebuild/status component drift verification: live smoke now requires and saves rebuild `lexical_retriever`, `vector_retriever`, `dense_vector_field`, and `fusion` matching connected status, and the saved strict acceptance verifier rejects drift for those fields.
- Release/stale gates and docs now require live and `checked.*` evidence for rebuild/status BM25/kNN/RRF component matching, so status-only or query-only component evidence is not enough for release acceptance.
- RED: saved verifier weak-evidence cases first accepted rebuild component drift, live smoke first omitted the rebuild component fields, and stale-task builder expected-success coverage first omitted the new evidence.
- GREEN: focused smoke/verifier checks returned `238 passed`; release/stale checks returned `187 passed`; combined smoke/verifier/release/stale/docs checks returned `554 passed`.
- Added docs contract coverage requiring product readiness, remote production runbook, remote acceptance template, image-agent testing matrix, and the Elasticsearch hybrid contract to name `rag_rebuild_elasticsearch_hybrid.lexical_retriever`, `rag_rebuild_elasticsearch_hybrid.vector_retriever`, `rag_rebuild_elasticsearch_hybrid.dense_vector_field`, and `rag_rebuild_elasticsearch_hybrid.fusion` as rebuild evidence matching `rag_elasticsearch_hybrid`.
- RED: the new docs contract first failed because product readiness still only described rebuild index/count/dims/embedding matching.
- GREEN: focused rebuild docs test returned `1 passed`; Elasticsearch/product/remote docs slice returned `8 passed, 60 deselected`.
- Tightened `/deployment.fast_launch_readiness` RAG component gating so the live deployment summary now requires `lexical_retriever=standard`, `vector_retriever=knn`, and `dense_vector_field=embedding` in addition to connected Elasticsearch, production embeddings, RRF, official RRF source evidence, and no hybrid/embedding errors.
- Added privacy-safe blocking codes `rag_hybrid_lexical_retriever_not_standard`, `rag_hybrid_vector_retriever_not_knn`, and `rag_hybrid_dense_vector_field_not_embedding`; the readiness payload still omits raw source URLs, endpoint URLs, keys, paths, and raw errors.
- Updated product readiness and remote production docs so frontend/operator tooling sees the same fast-launch component contract as the API.
- RED: the new API regression first showed degraded component values still returned `ready=true`; the new docs contract first failed because fast-launch docs did not name the component fields or blockers.
- GREEN: focused readiness regressions returned `5 passed`; Elasticsearch fast-launch/docs slice returned `11 passed, 111 deselected`.
- Aligned the console Settings fast-launch panel with the backend ES hybrid readiness contract: frontend types now include `rag_elasticsearch_hybrid`, and the UI renders safe BM25/kNN/dense-vector component values, official RRF source presence, and backend-provided blocking codes.
- The Settings panel still omits raw official-source URLs, endpoint URLs, keys, backend paths, and raw errors; it consumes only the backend privacy-safe summary fields.
- RED: the Settings page test first failed because `ES hybrid RAG blocked` and the new component blockers were not rendered from deployment status.
- GREEN: focused Settings fast-launch test returned `1 passed, 5 skipped`; full Settings page test returned `6 passed`; console TypeScript check returned exit 0.
- Added a console `api.deployment()` privacy fallback so deployment readiness JSON is sanitized before UI code consumes it. The client preserves safe summary fields and blocking codes but drops raw official-source lists, raw snapshots/files, endpoint URLs, error fields, keys, tokens, and backend paths.
- RED: the new API client regression first showed `official_sources` from `fast_launch_readiness.checks.rag_elasticsearch_hybrid` reaching UI code.
- GREEN: focused deployment sanitization test returned `1 passed, 17 skipped`; API/Settings/AppShell frontend slice returned `27 passed`; console TypeScript check returned exit 0.
- Aligned the desktop frontend with the same deployment-response privacy fallback: `apps/desktop/src/lib/api.js` now sanitizes `api.deployment()` responses before UI code consumes them, preserving safe readiness summaries and blocking codes while dropping raw official-source lists, raw snapshots/files, endpoint URLs, error fields, keys/tokens/secrets, and backend paths.
- RED: the new desktop API regression first failed because `official_sources` reached UI code.
- GREEN: focused desktop deployment sanitization test returned `1 passed`; desktop API/workflow tests returned `2 passed`; desktop Vite production build completed successfully.
- Extended desktop frontend sanitization to result/QC display APIs. Upload responses, task list/detail responses, legacy outputs, result summaries, and chat responses now use the shared backend-path sanitizer.
- RED: new desktop result-summary and legacy-output tests first failed because `path` fields reached UI code.
- GREEN: focused desktop API tests returned `3 passed`; desktop API/workflow tests returned `4 passed`; desktop Vite production build completed successfully.
- Cleaned desktop local test module-mode warning by declaring `"type": "module"` in `apps/desktop/package.json` and package-lock root metadata. The desktop node:test run no longer emits `MODULE_TYPELESS_PACKAGE_JSON`.
- Verification: before the change, `node --test src/lib/api.test.mjs src/lib/workflows.test.mjs` passed but printed the warning; after the change it returned `4 passed` with no module-type warning, and desktop Vite production build still completed successfully.
- Hardened task observation log privacy in both frontend clients. Console and desktop `api.getLogs()` now route task log payloads through the shared sanitizer before UI code sees them, preserving useful log lines while redacting host paths, `data/projects/...` paths, API-key strings, and token/secret assignments.
- RED: focused console and desktop task-log tests first failed because `/tasks/{id}/logs` returned raw backend paths and secret-looking values.
- GREEN: focused log sanitization tests passed; console API/Settings/AppShell tests returned `28 passed`; console TypeScript check returned exit 0; desktop API/workflow tests returned `5 passed`; desktop Vite production build completed successfully.
- Wired the console task monitor to the structured read-only task-events contract. `api.getTaskEvents()` now calls `/tasks/{task_id}/events` and applies the same sanitizer before UI consumption, while `TasksPage` exposes a dedicated read-only events panel for running/failed/cancelled tasks.
- The task-events panel displays event types, status/progress, remote log source stages, main log tail, and remote log summaries without adding retry, rerun, automatic repair, or task creation actions.
- RED: the API client test first failed with `api.getTaskEvents is not a function`; the page test first failed because the `Inspect task events for RUN-*` action and structured events panel did not exist.
- GREEN: focused task-events API and page tests passed; console API/Tasks/Settings/AppShell tests returned `36 passed`; console TypeScript check returned exit 0; console production build completed successfully.
- Mirrored structured read-only task-events observation in the desktop client. `api.getTaskEvents()` now calls `/tasks/{task_id}/events` and uses the shared desktop sanitizer before task events, main log tails, and remote log summaries reach UI code.
- Added a desktop `Events` task action and `TaskEventsPanel` for event type, status/progress, remote log source stage, main log tail, and remote log summary display. The panel remains observation-only and does not add retry, rerun, automatic repair, or task creation controls.
- RED: the desktop API regression first failed with `api.getTaskEvents is not a function`.
- GREEN: focused desktop API tests returned `5 passed`; desktop API/workflow tests returned `6 passed`; desktop Vite production build completed successfully.
- Aligned desktop DWI upload with the backend JSON sidecar contract. `api.uploadDwi()` now includes the required `json_sidecar` multipart field, and the desktop form requires NIfTI, bval, bvec, and JSON sidecar files before upload.
- Updated the older `test_api_flow.py` DWI sidecar regression to reflect the current fixed-workflow gate order: direct `/series/{id}/run` for fixed workflows returns 403 unless launched through `/agent/runs` confirmation, while the confirmed service path still rejects missing JSON sidecar metadata with 400.
- RED: the desktop API regression first failed because `json_sidecar` was absent from FormData.
- GREEN: desktop API tests returned `6 passed`; desktop API/workflow tests returned `7 passed`; desktop Vite production build completed successfully; focused backend DWI sidecar/gate tests returned `4 passed`; console DWI upload contract checks passed.
- Aligned desktop Agent run/resume response sanitization with the console. `api.runAgent()`, `api.resumeAgent()`, and legacy `api.chat()` now use an Agent-specific sanitizer that preserves safe citation `path` values while redacting host paths, task paths, log paths, and secret/token/API-key style strings before UI code consumes the response.
- This keeps the Agent interaction and human-confirmation resume path privacy-safe without changing the backend API contract or stripping curated RAG citation paths that are useful to operators.
- RED: desktop Agent run/resume regressions first failed because raw `/home/yyf/project/image_agent`, `C:/Users/A/private`, `data/projects/...`, `log_path`, and `sk-*` values reached UI code.
- GREEN: desktop API tests returned `8 passed`; desktop API/workflow tests returned `9 passed`; desktop Vite production build completed successfully; console Agent sanitization contract checks also passed.
- Made result-summary workflow metadata authoritative from task records and the workflow registry. `/tasks/{task_id}/result-summary` now replaces stale or weak runner-written `workflow_metadata` with registry-backed public metadata derived from the task `workflow_type` and `runtime_workflow_type`.
- This prevents a full processing workflow from being displayed as a runtime alias or report-only workflow when old result-summary files contain stale metadata; task creation, confirmation fingerprints, database rows, and artifact routes still use stable `workflow_type`.
- RED: the new API contract regression first failed because a summary file with `workflow_metadata.workflow_type=t1_deepprep`, `display_name=t1_deepprep`, and `is_report_only=true` reached the response unchanged for a `t1_deepprep_anat_report` task.
- GREEN: focused metadata/result-summary/API checks returned `4 passed`; workflow registry checks returned `13 passed`; full fixed-workflow API contract slice returned `23 passed`.
- Extended artifact-manifest report/QC context with the same registry-backed workflow metadata boundary. `/tasks/{task_id}/artifact-manifest` now exposes `runtime_workflow_type` and `workflow_metadata` at the manifest envelope level after normalizing through the public result-summary/task-record path.
- Frontend report/QC panels can now display workflow capability context from the stable preview/download contract without trusting stale runner-written summary metadata or guessing from artifact filenames. The manifest still emits only safe relative `summary_path`/artifact URLs and does not expose backend absolute paths.
- RED: the manifest API regression first failed because `runtime_workflow_type` was missing, then exposed that public summary normalization removed the safe manifest `summary_path`.
- GREEN: manifest/API/contract checks returned `28 passed`; focused docs contract checks returned `2 passed`; touched Python files passed `py_compile`.
- Aligned the console result-detail header with artifact-manifest workflow metadata. `ArtifactManifest` frontend types now include optional `workflow_type`, `runtime_workflow_type`, and `workflow_metadata`, and `ResultDetailPage` falls back to manifest metadata when the result-summary metadata and workflow catalog are unavailable.
- The visible stable workflow-id line still uses `summary.workflow_type`; manifest metadata is display/interpretation context only.
- RED: the new ResultDetail regression first rendered `dwi_fast_gpu_dti workflow execution...` even though the manifest supplied the registry-backed display name.
- GREEN: focused ResultDetail fallback test returned `1 passed`; full ResultDetail test returned `7 passed`; console API + ResultDetail slice returned `27 passed`; `npm.cmd run lint` completed successfully.
- Mirrored artifact-manifest workflow metadata fallback in the desktop result/QC view. `api.getArtifactManifest()` now consumes `/tasks/{task_id}/artifact-manifest` through the desktop sanitizer, and `ResultSummaryPanel` uses result-summary metadata first, then manifest metadata for display/capability context while keeping stable `workflow_type` as the machine label.
- RED: focused desktop regressions first failed because `api.getArtifactManifest` and the result-summary display helper were missing.
- GREEN: desktop API/workflow/result-summary tests returned `13 passed`; desktop Vite production build completed successfully.
- Wired console Agent review to the read-only Agent run history contract. Frontend types/API now cover `GET /projects/{project_id}/agent-runs` and `GET /agent/runs/{agent_run_id}` through the Agent sanitizer, and `AgentPage` displays safe run ids, statuses, request type, selected skill, and event counts without exposing prompts, paths, or secrets.
- The history panel is audit-only; task creation and workflow launch state still come from `/agent/runs/{thread_id}/resume` and backend task APIs.
- RED: focused console tests first failed because `api.listProjectAgentRuns` and Run History UI were missing.
- GREEN: console API + AgentPage slice returned `30 passed`; `npm.cmd run lint` completed successfully.
- Completed console single Agent run lookup from the read-only ledger. Run History items now call `GET /agent/runs/{agent_run_id}` and render Run Detail with safe status, selected skill, gateway access, answer, and sanitized event metadata.
- The Run Detail panel is audit-only and adds no run/resume/retry action before real remote closed-loop testing.
- RED: the AgentPage lookup regression first failed because history rows were not inspectable and no detail panel existed.
- GREEN: console API + AgentPage slice returned `32 passed`; `npm.cmd run lint` completed successfully.
- Ran read-only remote readiness probe on `yyf@10.2.32.14` before any real closed-loop smoke. SSH works and live `/health` returns `app=image_agent`, `version=0.2.0`; `/agent/model/status` reports configured Responses access for `gpt-5.5`.
- Remote blockers found without mutating state: `/home/yyf/project/image_agent` is an older dirty tree at HEAD `0ae5e07` on `feature/claude-frontend-redesign`, the current `verify_elasticsearch_hybrid_prerequisites.py` script is missing from both the live tree and existing gate verifier overlay, live `/agent/rag/status` is still `engine=llama_index` rather than connected Elasticsearch hybrid, and live `/runtime/containers` still exposes old unsafe details plus several `:latest` images.
- No remote restart, rebuild, upload, task launch, or strict smoke was run. Next remote step must create/sync a current release/gate overlay before ES hybrid prerequisite and real upload-Agent-confirm-run-observe-result smoke can begin.
- Hardened the remote release-gate command plan so stale overlays fail before any mutating step. `docs/deployment/remote-release-gate-command-plan.json` now starts with a read-only `verify_release_overlay_contents` step that checks for current gate scripts, `verify_elasticsearch_hybrid_prerequisites.py`, strict smoke/verifier scripts, the Elasticsearch hybrid contract doc, and the restart wrapper using shell `test -f`.
- Updated `apps/api/scripts/verify_release_gate_command_plan.py` to require that overlay-content step, its non-mutating shape, and privacy-safe success markers. This directly addresses the yyf finding that the prepared overlay lacked the ES prerequisite script.
- RED: `python -m pytest apps/api/tests/test_release_gate_command_plan.py::test_remote_release_gate_command_plan_orders_safe_remote_acceptance_steps -q` first failed because the plan still started with `verify_fresh_stale_task_approval`.
- GREEN: the focused order test returned `1 passed`; `python -m pytest apps/api/tests/test_release_gate_command_plan.py apps/api/tests/test_build_release_gate_command_plan.py apps/api/tests/test_verify_remote_smoke_acceptance.py -q` returned `431 passed`; `python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` returned `status=passed` and `step_count=11`; touched release-gate scripts passed `py_compile`.
- Added `apps/api/scripts/build_release_overlay_sync_plan.py` and `apps/api/tests/test_build_release_overlay_sync_plan.py` to make the next yyf overlay sync step reviewable before any remote write. The script defaults to JSON command-plan output; optional `--write-archive` builds a local tarball from git tracked plus unignored untracked files while excluding secret/runtime/dependency/cache/patient-data paths.
- Generated `docs/deployment/remote-release-overlay-sync-plan.json` for `codex-es-hybrid-20260619T213000Z`. The plan uses local preflight, local archive build, scp to `/tmp`, extract to a non-existing `.incoming` overlay, verify required gate files, promote only if the final overlay path is absent, and verify the promoted overlay. It avoids `--delete`, `rm -rf`, existing overlay overwrite, `.env` copying, and any write into the remote live tree.
- RED: overlay-plan tests first failed with `FileNotFoundError` because the builder script did not exist; a later focused test caught nested single-quote remote command quoting around `printf`.
- GREEN: `python -m pytest apps/api/tests/test_build_release_overlay_sync_plan.py -q` returned `3 passed`; `python -m pytest apps/api/tests/test_build_release_overlay_sync_plan.py apps/api/tests/test_release_gate_command_plan.py apps/api/tests/test_build_release_gate_command_plan.py -q` returned `135 passed`; touched scripts passed `py_compile`; direct release-gate verifier returned `status=passed`; `git diff --check` returned no whitespace errors, only existing CRLF/LF warnings.
- No local archive was built and no remote upload, extraction, promotion, restart, RAG rebuild, upload, task launch, or strict smoke was executed.
- Tightened the archive writer evidence before remote upload: release archive reports now include `archive_sha256`, `required_gate_files_present`, and `missing_required_gate_files`.
- Added a tarball regression proving `.env`, `data/projects`, `node_modules`, `.rag_index`, ignored files, and dependency/runtime paths are excluded while safe unignored files and all required gate files are included.
- RED: the archive test first failed because `required_gate_files_present` was missing from the report, then exposed that the fixture did not include all required gate files.
- GREEN: overlay sync plan tests returned `4 passed`; overlay/release-gate tests returned `136 passed`; touched scripts passed `py_compile`; direct release-gate verifier returned `status=passed`.
- Built local archive `/tmp/image_agent_release_codex-es-hybrid-20260619T213000Z.tar.gz`. Report: `member_count=514`, `required_gate_files_present=true`, `missing_required_gate_files=[]`, SHA-256 `33499d4377a7de4e1f4c1d5fb0145e0c7157b958d717a19df2f43624cfed72a5`.
- Inspected the archive: `.env`, `node_modules`, `.venv`, `.rag_index`, and `data/projects` were absent; `apps/api/scripts/verify_elasticsearch_hybrid_prerequisites.py` and `docs/rag/contracts/elasticsearch-hybrid-search.md` were present.
- Ran a read-only yyf path check: target release overlay, `.incoming` overlay, and remote `/tmp` archive path for `codex-es-hybrid-20260619T213000Z` are all absent, so the prepared sync plan's non-overwrite preconditions currently hold.
- Re-bound `docs/deployment/remote-release-gate-command-plan.json` from the stale `codex-gate-verifiers-efca895b-20260613T165132` overlay to `/home/yyf/project/image_agent_releases/codex-es-hybrid-20260619T213000Z`.
- Updated `verify_release_gate_command_plan.py` to validate any release overlay under `/home/yyf/project/image_agent_releases` with a privacy-safe final id and to reject `.incoming` paths, instead of accepting only the old hard-coded overlay.
- RED: the focused release-gate test first failed because the plan still pointed at the stale overlay.
- GREEN: focused release-gate order test returned `1 passed`; overlay/release-gate tests returned `136 passed`; direct release-gate verifier returned `status=passed` with `step_count=11`.
- No scp/upload, remote extraction, remote promotion, restart, RAG rebuild, upload, task launch, or strict smoke was executed.
- Executed the reviewed overlay sync plan through promotion on yyf without touching the live tree:
  - Rechecked local archive `/tmp/image_agent_release_codex-es-hybrid-20260619T213000Z.tar.gz`: size `3028715`, SHA-256 `33499d4377a7de4e1f4c1d5fb0145e0c7157b958d717a19df2f43624cfed72a5`.
  - Rechecked remote preconditions before upload: final overlay, `.incoming`, and remote `/tmp` archive were absent.
  - Uploaded the archive to yyf `/tmp`; remote `sha256sum` and size matched local evidence.
  - Extracted into `/home/yyf/project/image_agent_releases/codex-es-hybrid-20260619T213000Z.incoming`, verified required gate files, promoted to `/home/yyf/project/image_agent_releases/codex-es-hybrid-20260619T213000Z`, and verified promoted contents.
  - Promoted overlay check returned `release_overlay_current=true`, `required_gate_scripts_present=true`, `elasticsearch_hybrid_contract_present=true`.
  - Remote release-gate verifier run from the promoted overlay returned `status=passed`, `step_count=11`.
  - Final path check: promoted overlay exists, `.incoming` absent, remote `/tmp` archive remains present for audit.
- Ran the promoted overlay's read-only ES hybrid prerequisite check. It failed for configuration/runtime reasons, not overlay freshness:
  - `.env` does not define the ES/embedding keys required by the production gate: `IMAGE_AGENT_ELASTICSEARCH_URL`, `IMAGE_AGENT_ELASTICSEARCH_INDEX`, `IMAGE_AGENT_ELASTICSEARCH_API_KEY`, `IMAGE_AGENT_RAG_EMBEDDING_PROVIDER`, `IMAGE_AGENT_RAG_EMBEDDING_MODEL`, `IMAGE_AGENT_RAG_EMBEDDING_BASE_URL`, or `IMAGE_AGENT_RAG_EMBEDDING_API_KEY`.
  - Live `/agent/rag/status` safe summary is still `engine=llama_index`, with no connected hybrid engine, mode, persisted index, indexed chunks, embedding provider, or embedding model.
  - Next required work is ES service/config plus production embedding config, then restart/rebuild/query verification; real upload-Agent-confirm-run-observe-result strict smoke remains blocked until those pass.
- No API restart, stale-task apply, RAG rebuild, upload, production task launch, or strict smoke was executed.

- Continued ES hybrid readiness with read-only yyf probes:
  - `ss -ltn` showed no listener on `127.0.0.1:9200`, so Elasticsearch is not currently available at the default local endpoint.
  - `yyf` is in `sudo` but not `docker`; `/var/run/docker.sock` is owned by `root:docker`, so Docker/Elasticsearch service checks require operator/sudo handling.
  - Presence-only `.env` inspection showed the model gateway key family is present enough for live `/agent/model/status` to report configured Responses access for `gpt-5.5`, while all ES/embedding keys remain missing.
  - No secret values, endpoint URLs, or API keys were printed.
- Added the secret-safe Elasticsearch hybrid configuration handoff:
  - New script: `apps/api/scripts/build_elasticsearch_hybrid_config_plan.py`.
  - New tests: `apps/api/tests/test_build_elasticsearch_hybrid_config_plan.py`.
  - Generated plan: `docs/deployment/remote-elasticsearch-hybrid-config-plan.json`.
  - The plan records observed blockers, missing env keys, placeholder-only env template, read-only presence check, restart/rebuild/prerequisite order, and the handoff back to `remote-release-gate-command-plan.json`.
  - It does not write `.env`, does not include real URL/key material, and keeps strict smoke blocked until the read-only ES prerequisite passes.
- Verification:
  - RED: `python -m pytest apps/api/tests/test_build_elasticsearch_hybrid_config_plan.py -q` first failed because `build_elasticsearch_hybrid_config_plan.py` did not exist.
  - GREEN: focused config-plan tests returned `3 passed`.
  - `python -m pytest apps/api/tests/test_build_elasticsearch_hybrid_config_plan.py apps/api/tests/test_verify_elasticsearch_hybrid_prerequisites.py apps/api/tests/test_release_gate_command_plan.py apps/api/tests/test_build_release_overlay_sync_plan.py -q` returned `158 passed`.
  - `python -m py_compile apps/api/scripts/build_elasticsearch_hybrid_config_plan.py apps/api/scripts/verify_elasticsearch_hybrid_prerequisites.py apps/api/scripts/build_release_overlay_sync_plan.py apps/api/scripts/verify_release_gate_command_plan.py` passed.
  - `python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` returned `status=passed`, `step_count=11`.
  - `git diff --check` passed with only existing CRLF/LF warnings.
- No ES service was started, no remote secret/config value was changed, no API restart, no RAG rebuild, no upload, no production task launch, and no strict smoke was executed in this slice.

- Hardened the ES hybrid configuration handoff with an independent verifier:
  - Added `apps/api/scripts/verify_elasticsearch_hybrid_config_plan.py`.
  - Added `apps/api/tests/test_verify_elasticsearch_hybrid_config_plan.py`.
  - Updated `apps/api/scripts/build_elasticsearch_hybrid_config_plan.py` and regenerated `docs/deployment/remote-elasticsearch-hybrid-config-plan.json` so the prerequisite step explicitly expects the full ES hybrid evidence set: BM25 standard retriever, kNN vector retriever, dense vector field `embedding`, positive dimensions, RRF fusion, official RRF source presence, absent hybrid/embedding errors, and production embedding provider/model/transport/endpoint readiness.
  - The verifier rejects missing ES prerequisite steps, unsafe release overlays, secret-like values, wrong step ordering, and incomplete prerequisite expected-success coverage.
- Verification:
  - RED: `python -m pytest apps/api/tests/test_verify_elasticsearch_hybrid_config_plan.py -q` first failed because the verifier file did not exist.
  - Intermediate failures caught useful issues: the verifier initially treated the safe local `/agent/rag/status` URL as a leak, and the generated plan lacked detailed BM25/kNN/RRF/embedding expected-success items.
  - GREEN: `python -m pytest apps/api/tests/test_verify_elasticsearch_hybrid_config_plan.py -q` returned `14 passed`.
  - `python -m pytest apps/api/tests/test_build_elasticsearch_hybrid_config_plan.py apps/api/tests/test_verify_elasticsearch_hybrid_config_plan.py apps/api/tests/test_verify_elasticsearch_hybrid_prerequisites.py apps/api/tests/test_release_gate_command_plan.py apps/api/tests/test_build_release_overlay_sync_plan.py -q` returned `172 passed`.
  - `python apps/api/scripts/verify_elasticsearch_hybrid_config_plan.py docs/deployment/remote-elasticsearch-hybrid-config-plan.json` returned `status=passed`, `step_count=7`.
  - `python -m py_compile apps/api/scripts/build_elasticsearch_hybrid_config_plan.py apps/api/scripts/verify_elasticsearch_hybrid_config_plan.py apps/api/scripts/verify_elasticsearch_hybrid_prerequisites.py` passed.
  - Secret scan found only deliberate negative-test fixtures and no real URL/key material in the plan JSON.
  - `git diff --check` passed with only existing CRLF/LF warnings.
- No remote mutation was performed in this verifier-hardening slice; ES service/config, restart, rebuild, upload, production task launch, and strict smoke remain pending.

- Hardened the release-overlay evidence chain again so ES hybrid config handoff files cannot be left behind:
  - `apps/api/scripts/build_release_overlay_sync_plan.py` now requires `apps/api/scripts/build_elasticsearch_hybrid_config_plan.py`, `apps/api/scripts/verify_elasticsearch_hybrid_config_plan.py`, and `docs/deployment/remote-elasticsearch-hybrid-config-plan.json` in every incoming/promoted overlay check.
  - `apps/api/scripts/verify_release_gate_command_plan.py` now rejects release-gate plans whose first read-only overlay check omits those ES config handoff files.
  - `docs/deployment/remote-release-gate-command-plan.json`, `docs/deployment/remote-release-overlay-sync-plan.json`, and `docs/deployment/remote-elasticsearch-hybrid-config-plan.json` are now bound to `/home/yyf/project/image_agent_releases/codex-es-hybrid-config-gate2-20260619T231500Z`.
  - Fixed a remote command path issue discovered during yyf verification: commands that verify or materialize repo-root JSON now run from the overlay root with `PYTHONPATH=apps/api`, instead of running from `apps/api` while passing `docs/deployment/...` paths.
  - RED: focused tests first failed because the overlay checks did not include the ES config plan builder/verifier/JSON, and later because the generated commands still used the wrong working directory.
  - GREEN: `python -m pytest apps/api/tests/test_build_release_overlay_sync_plan.py apps/api/tests/test_release_gate_command_plan.py apps/api/tests/test_build_elasticsearch_hybrid_config_plan.py apps/api/tests/test_verify_elasticsearch_hybrid_config_plan.py -q` returned `150 passed`; direct local verifiers returned `status=passed` for both release gate (`step_count=11`) and ES config handoff (`step_count=7`); touched scripts passed `py_compile`; `git diff --check` passed with only existing CRLF/LF warnings.
- Created and verified the new yyf release overlay without touching the live tree:
  - Local archive `/tmp/image_agent_release_codex-es-hybrid-config-gate2-20260619T231500Z.tar.gz` was built with `member_count=519`, `required_gate_files_present=true`, `missing_required_gate_files=[]`, SHA-256 `1aaee37e4c758d2e743c414837bc4bf45045d93e237003b25c63bb832c8b33ed`, size `3039329`.
  - Tar inspection confirmed required gate/config files are present and `.env`, `data/projects`, `node_modules`, and `.rag_index` paths are absent.
  - Remote preconditions showed the final overlay, `.incoming`, and `/tmp` archive paths were absent before upload.
  - Uploaded to yyf `/tmp`; remote `sha256sum` and size matched local evidence; extracted to `.incoming`; verified required files; promoted to `/home/yyf/project/image_agent_releases/codex-es-hybrid-config-gate2-20260619T231500Z`.
  - Promoted overlay content check returned `release_overlay_current=true`, `required_gate_scripts_present=true`, and `elasticsearch_hybrid_contract_present=true`.
  - Remote verifier from the promoted overlay root returned release-gate `status=passed`, `step_count=11`, and ES config handoff `status=passed`, `step_count=7`.
  - Final path check showed the promoted overlay exists, `.incoming` is absent, and the uploaded `/tmp` archive remains present for audit.
- Current remote runtime blocker remains configuration, not overlay freshness:
  - Presence-only `.env` check still reports all ES/embedding keys missing: `IMAGE_AGENT_ELASTICSEARCH_URL`, `IMAGE_AGENT_ELASTICSEARCH_INDEX`, `IMAGE_AGENT_ELASTICSEARCH_API_KEY`, `IMAGE_AGENT_RAG_EMBEDDING_PROVIDER`, `IMAGE_AGENT_RAG_EMBEDDING_MODEL`, `IMAGE_AGENT_RAG_EMBEDDING_BASE_URL`, and `IMAGE_AGENT_RAG_EMBEDDING_API_KEY`.
  - Read-only ES prerequisite from the gate2 overlay exits non-zero because live `/agent/rag/status` is still not `elasticsearch_hybrid`, lacks connected/persisted BM25+kNN/RRF evidence, and has no production embedding provider/model/endpoint readiness.
  - No API restart, `.env` write, Elasticsearch service start, RAG rebuild, upload, production task launch, or strict smoke was run in this slice.

- Rechecked time-to-start for real launch closed-loop testing from yyf with read-only probes:
  - Docker is installed (`Docker version 27.5.1`), but `yyf` is not in the `docker` group and `sudo -n true` fails with `sudo: a password is required`, so Codex cannot start or inspect Docker/Elasticsearch non-interactively.
  - `/var/run/docker.sock` is owned by `root:docker`; plain `docker ps` fails with socket permission denied.
  - `127.0.0.1:9200` is still unreachable, so Elasticsearch is not running at the expected local endpoint.
  - Presence-only `.env` check still shows all ES/embedding keys missing while `OPENAI_BASE_URL` and `OPENAI_API_KEY` are present; no secret values were printed.
  - Live `/agent/rag/status` is still from the old release overlay (`codex-f57a2ea-20260611T023456`) with `engine=llama_index`, so the current API runtime has not yet been switched to ES hybrid.
  - Estimate: if an operator can immediately provide Docker/sudo handling and add the ES/embedding env through the managed secret path, real closed-loop smoke can likely start in about 4-6 hours; if operator access/env decisions wait, the realistic window is 1-2 days. Production launch acceptance is still blocked until ES hybrid prerequisite, restart, RAG rebuild/query, upload-confirm-run-observe-result strict smoke, and evidence freshness all pass.

- Added portable runtime probing before remote real testing:
  - Added `/runtime/probe` in the agent runtime layer as an install-local, machine-discovered contract for Docker access/sudo summary, image-agent-labeled container counts, resource summary, Elasticsearch config/reachability, and per-workflow availability.
  - `/runtime/containers` now embeds `runtime_probe` and strips `fs_license_path` plus Docker `detail_tail` values from the workflow summary before returning API evidence.
  - Added importable CLI `app.scripts.probe_runtime_environment --json` so any installed deployment can emit the same safe probe without yyf-specific paths, accounts, or frontend behavior.
  - Strict smoke now prefers `/runtime/probe` for `--require-runtime-toolchain` and falls back to `/runtime/containers` for older deployments.
  - Verification: `python -m pytest tests/test_runtime_probe.py -q` returned `5 passed`; targeted runtime-toolchain smoke tests returned `3 passed`; broader runtime/release/smoke verifier slice returned `589 passed`.
  - Local CLI check `python -m app.scripts.probe_runtime_environment --json` now returns a blocked but well-formed safe probe when Docker/container inspection cannot run without sudo credentials, instead of crashing or printing the underlying sudo-password error.
  - Per user requirement, real yyf online closed-loop testing waits until mock/control-plane tests are green.

- Closed the remaining backend mock/control-plane failures before any remote real test:
  - Kept the fixed-workflow launch rule strict: direct `/series/{series_id}/run` for fixed validate workflows is rejected unless the call comes through the confirmed Agent gate. The two older tests for `dwi_fast_gpu_dti_validate` and `bold_deepprep_validate` now assert no direct task is created, then verify `task_service.create_series_task(..., confirmed_agent_gate=True)` can create the task and continue into runner dispatch.
  - Renamed the RAG readiness blocking code from `rag_hybrid_mode_not_connected` to `rag_hybrid_mode_not_ready` to satisfy the architecture guard that keeps DB/connection language out of `agent_service.py`; docs and readiness tests were updated with the new safe code.
  - Verification:
    - Focused failure slice returned `5 passed`.
    - Full backend mock/control-plane suite `python -m pytest apps/api/tests -q` returned `1317 passed, 11 warnings`.
    - Touched-file compile check passed for `agent_service.py`, `test_api_flow.py`, `test_agent_api.py`, `runtime.py`, `probe_runtime_environment.py`, and `smoke_remote_agent.py`.
    - `git diff --check` passed with only existing CRLF/LF warnings.
  - Remote real online closed-loop testing is now allowed to proceed to yyf preflight/runtime checks, but strict upload-Agent-confirm-run-observe-result smoke still must wait for live ES hybrid and production embedding prerequisites to pass.

- Advanced from local green tests into yyf remote preflight without launching a production task:
  - Re-bound the current release evidence to `/home/yyf/project/image_agent_releases/codex-es-hybrid-runtime-probe-20260619T215100Z` and regenerated the overlay sync plan plus ES hybrid config handoff plan.
  - Verification after rebind:
    - `python -m pytest apps/api/tests/test_build_release_overlay_sync_plan.py apps/api/tests/test_release_gate_command_plan.py apps/api/tests/test_build_elasticsearch_hybrid_config_plan.py apps/api/tests/test_verify_elasticsearch_hybrid_config_plan.py -q` returned `150 passed`.
    - Local release-gate verifier returned `status=passed`, `step_count=11`.
    - Local ES config handoff verifier returned `status=passed`, `step_count=7`.
    - Full backend mock/control-plane suite remained green: `1317 passed, 11 warnings`.
  - Built current-worktree archive `/tmp/image_agent_release_codex-es-hybrid-runtime-probe-20260619T215100Z.tar.gz`, SHA-256 `e0f5ba19a5802b730d70e6f53e4ef461d896d6b541fa111350ba63f0187ecfb5`, size `3045774`, `member_count=521`, `required_gate_files_present=true`, and no `.env`, `data/projects`, `node_modules`, `.rag_index`, or `.git` members.
  - Uploaded the archive to yyf `/tmp`, verified remote hash/size, extracted to `.incoming`, checked required gate/config files, and promoted it to `/home/yyf/project/image_agent_releases/codex-es-hybrid-runtime-probe-20260619T215100Z`. The live `/home/yyf/project/image_agent` tree was not modified.
  - Remote verifier from the promoted overlay returned release gate `status=passed`, `step_count=11`; remote ES config handoff verifier returned `status=passed`, `step_count=7`.
- Remote runtime and strict-smoke blockers found on yyf:
  - Live API is still the old running service: `/health` returns `app=image_agent`, `version=0.2.0`; live `/runtime/probe` returns 404; live `/runtime/containers` is the older contract and still exposes `fs_license_path`, so the new safe API contract is not serving until a controlled restart.
  - Live `/agent/rag/status` remains `engine=llama_index`; there is no ES hybrid engine, connected mode, persisted index, indexed chunks, embedding provider, or embedding model.
  - The promoted overlay's CLI probe runs on yyf and proves the portable runtime-discovery contract works on the server, but returns `status=blocked`: Docker exists but requires sudo, image-agent-labeled container inspection is unavailable without privileged access, required pinned workflow images are unavailable, `IMAGE_AGENT_ELASTICSEARCH_URL` is not configured, and Elasticsearch is not reachable.
  - Read-only ES prerequisite from the promoted overlay exits non-zero with the expected blockers: missing ES/embedding env, live RAG not `elasticsearch_hybrid`, no connected/persisted BM25+kNN/RRF evidence, and no production embedding readiness.
  - Restart preflight refuses to restart because active tasks 83 and 84 are still marked running; this preserves the no-override drain gate.
  - Sudo Docker read-only inspection found no matching running Image Agent containers and five relevant local images: `pbfslab/deepprep:25.1.0`, `pennlinc/xcp_d:26.0.2`, plus `nipreps/fmriprep:latest`, `pennlinc/qsiprep:latest`, and `pennlinc/qsirecon:latest`. The `latest` tags do not satisfy strict version-lock acceptance.
  - Scoped stale-task dry-run succeeded after setting `IMAGE_AGENT_ROOT=/home/yyf/project/image_agent`: `/tmp/image_agent_stale_tasks_83_84_dry_run_runtime_probe_20260619T215100Z.json` reports `mode=dry_run`, `container_check_status=passed`, `active_task_count=2`, `running_container_task_ids=[]`, and stale candidates `[83, 84]`. No apply was run; human review of the approval fingerprint is still required before task reconciliation and restart.
  - No API restart, live `.env` edit, Elasticsearch service start, RAG rebuild, upload, production task launch, strict smoke, or stale-task apply was executed.

- Fixed the release-gate stale-task command environment after yyf exposed an overlay/live-root mismatch:
  - Root cause: `config.py` derives `DB_PATH` from `IMAGE_AGENT_ROOT`. Restart wrapper commands already set `IMAGE_AGENT_ROOT=/home/yyf/project/image_agent`, but the stale-task refresh/apply/post-apply dry-run commands in `remote-release-gate-command-plan.json` only loaded `.env`. Running them from the release overlay therefore connected to the overlay's empty `data/app.db` and failed with `sqlite3.OperationalError: no such table: tasks`.
  - Added a TDD regression in `test_remote_release_gate_reconcile_commands_load_remote_env_for_docker_checks` requiring all stale-task reconciliation commands to include the live root.
  - Updated `verify_release_gate_command_plan.py` to reject release-gate plans whose stale-task refresh/apply/post-apply dry-run commands omit `IMAGE_AGENT_ROOT=/home/yyf/project/image_agent`.
  - Updated `docs/deployment/remote-release-gate-command-plan.json` so the stale-task refresh command and both reconciliation steps use the live root while still importing scripts from the release overlay.
  - Verification:
    - RED: focused test failed because the refresh command lacked `IMAGE_AGENT_ROOT=/home/yyf/project/image_agent`.
    - GREEN: focused test returned `1 passed`; release-plan/config slice returned `150 passed`; full backend suite returned `1317 passed, 11 warnings`.
    - Local release-gate verifier returned `status=passed`, `step_count=11`; local ES config handoff verifier returned `status=passed`, `step_count=7`.
- Promoted the rootfix candidate overlay to yyf without touching the live tree:
  - New release overlay: `/home/yyf/project/image_agent_releases/codex-es-hybrid-runtime-probe-rootfix-20260619T220500Z`.
  - Archive SHA-256 `bc4d0476916e04bd4365f38f2bd165d5b4f2df7b0e66d08fcc40287383b8fbb2`, size `3047350`, `member_count=521`, required files present, and no `.env`, `.git`, `data/projects`, `node_modules`, or `.rag_index` members.
  - Remote hash/size matched; archive extracted to `.incoming`; required gate/config files checked; promoted overlay verified.
  - Remote release-gate verifier from the rootfix overlay returned `status=passed`, `step_count=11`.
  - Remote ES config handoff verifier from the rootfix overlay returned `status=passed`, `step_count=7`.
  - The rootfix stale-task dry-run command shape was exercised on yyf and now succeeds against the live DB: `/tmp/image_agent_stale_tasks_83_84_dry_run_rootfix_20260619T220500Z.json` reports `mode=dry_run`, `container_check_status=passed`, `active_task_count=2`, `running_container_task_ids=[]`, `blocked_task_ids=[]`, `target_task_ids=[83,84]`, and stale candidates `[83,84]`.
  - No stale-task apply, API restart, live `.env` edit, Elasticsearch service start, RAG rebuild, upload, production task launch, or strict smoke was executed.

- Hardened stale-task apply handoff and promoted a rootfix2 overlay without touching the live tree:
  - Found a second overlay/live-root risk in `build_stale_task_apply_request.py`: its generated apply/post-apply commands still pointed at the previous overlay and did not require `IMAGE_AGENT_ROOT=/home/yyf/project/image_agent`, which could make reviewed stale-task apply requests read an overlay-local empty DB instead of the live DB.
  - Added TDD coverage so stale-task apply-request builder commands must include the live root and the current overlay, and the apply-request verifier rejects requests whose apply or post-apply dry-run commands omit the live root.
  - Added `build_stale_task_apply_request.py` and `verify_stale_task_apply_request.py` to the required release-overlay file checks, so release gates reject overlays missing the human-authorization handoff scripts before any mutation.
  - Re-bound release evidence to `/home/yyf/project/image_agent_releases/codex-es-hybrid-runtime-probe-rootfix2-20260619T221206Z` and regenerated `remote-release-gate-command-plan.json`, `remote-elasticsearch-hybrid-config-plan.json`, and `remote-release-overlay-sync-plan.json`.
  - Verification:
    - RED: focused apply-request tests first failed because commands lacked `IMAGE_AGENT_ROOT` and used the old overlay; overlay-content tests then failed because the release gate did not require the apply-request scripts.
    - GREEN: apply/release/config slice returned `213 passed`; direct local release-gate verifier returned `status=passed`, `step_count=11`; direct local ES config handoff verifier returned `status=passed`, `step_count=7`.
    - Full backend suite returned `1319 passed, 11 warnings`; touched scripts passed `py_compile`; `git diff --check` passed with only existing CRLF/LF warnings.
  - Built local archive `/tmp/image_agent_release_codex-es-hybrid-runtime-probe-rootfix2-20260619T221206Z.tar.gz`, SHA-256 `57857548fa18aa20c67c9a8fe12da0d9810a056ccee1265870ccfc3f517f201e`, size `3049257`, `member_count=521`, required gate files present, and no `.env`, `.git`, `data/projects`, `node_modules`, or `.rag_index` members.
  - Remote path precheck showed the final overlay, incoming overlay, and `/tmp` archive were absent before upload; remote hash matched after upload.
  - Promoted rootfix2 to yyf. Remote release-gate verifier from rootfix2 returned `status=passed`, `step_count=11`; remote ES config handoff verifier returned `status=passed`, `step_count=7`; `.incoming` is absent and the uploaded archive remains in `/tmp` for audit.
  - No stale-task apply, API restart, live `.env` edit, Elasticsearch service start, RAG rebuild, upload, production task launch, or strict smoke was executed.

- Refreshed yyf stale-task approval evidence from rootfix2, still without mutating live state:
  - Ran the rootfix2 stale-task dry-run refresh from `/home/yyf/project/image_agent_releases/codex-es-hybrid-runtime-probe-rootfix2-20260619T221206Z/apps/api` with `IMAGE_AGENT_ROOT=/home/yyf/project/image_agent`.
  - New read-only evidence file: `/tmp/image_agent_stale_tasks_83_84_dry_run_rootfix2_20260619T221206Z.json`.
  - Remote `verify_stale_task_approval.py` returned `status=passed`; checked fields: `target_task_ids=[83,84]`, `stale_candidate_ids=[83,84]`, `container_check_status=passed`, `generated_at_utc=2026-06-19T14:21:27.328242+00:00`, and approval fingerprint `139113571daf0137a3e34be526fd25ccaa8066aed725ab7c0b846cfc7eb3abd0`.
  - Remote rootfix2 ES prerequisite remains blocked for runtime/config reasons: missing `IMAGE_AGENT_ELASTICSEARCH_URL`, production embedding provider/model/base URL configuration, live `/agent/rag/status` not `elasticsearch_hybrid`, no connected/persisted BM25+kNN/RRF evidence, no positive indexed chunk count, and no production embedding readiness.
  - Live API is still the old service: `/health` returns `version=0.2.0`, `/runtime/probe` returns 404, and `/agent/rag/status` still reports the old `llama_index` dependency path.
  - No stale-task apply, API restart, live `.env` edit, Elasticsearch service start, RAG rebuild, upload, production task launch, or strict smoke was executed.

- Hardened the materialized release-gate handoff and promoted rootfix3 without touching the live tree:
  - Root cause: `build_release_gate_command_plan.py` materialized `steps[*].command` placeholders but kept the source `stale_task_approval_refresh.command`, `materialize_plan_command`, and `output_json_pattern` template fields in the operator plan, leaving `<timestamp>` and related placeholders in review handoff metadata after fresh approval.
  - Added TDD coverage requiring operator-authorized materialized plans to replace `stale_task_approval_refresh` with a compact `superseded_by_fresh_reviewed_approval` marker containing only the reviewed approval path, expiry, next step, and non-mutating flags.
  - Updated `verify_release_gate_command_plan.py` so `approval_refresh_required` plans still require the read-only refresh template, while `operator_authorization_required` plans reject any refresh template or placeholder residue and require the superseded marker.
  - Re-bound current remote gate evidence to `/home/yyf/project/image_agent_releases/codex-es-hybrid-runtime-probe-rootfix3-20260619T143210Z` and regenerated `remote-release-gate-command-plan.json`, `remote-elasticsearch-hybrid-config-plan.json`, and `remote-release-overlay-sync-plan.json`.
  - Verification:
    - RED: focused materialization test failed because `stale_task_approval_refresh` still contained template command fields.
    - GREEN: focused materialization/operator tests returned `3 passed`; release-gate tests returned `136 passed`; apply/release/config slice returned `217 passed`; rootfix3 release/config/apply slice returned `220 passed`.
    - Local release-gate verifier returned `status=passed`, `step_count=11`; local ES config handoff verifier returned `status=passed`, `step_count=7`; `git diff --check` passed with only existing CRLF/LF warnings.
  - Built local archive `/tmp/image_agent_release_codex-es-hybrid-runtime-probe-rootfix3-20260619T143210Z.tar.gz`, SHA-256 `47cfdf1ac794e543126a1550cb9f23d5372b70bd83c1cd65dc39526c3ec23f09`, size `3051276`, `member_count=521`, required gate files present, and no `.env`, `.git`, `data/projects`, `node_modules`, or `.rag_index` members.
  - Remote path precheck showed rootfix3 final/incoming overlays were absent; uploaded the archive to yyf `/tmp`, extracted to `.incoming`, verified required gate/config files, and promoted it to the final rootfix3 overlay. The live `/home/yyf/project/image_agent` tree was not modified.
  - Remote rootfix3 release-gate verifier returned `status=passed`, `step_count=11`; remote ES config handoff verifier returned `status=passed`, `step_count=7`; promoted overlay content check returned `release_overlay_current=true`, `required_gate_scripts_present=true`, and `elasticsearch_hybrid_contract_present=true`.
  - Materialized the fresh stale-task approval through rootfix3 into `/tmp/image_agent_remote_release_gate_plan_rootfix3_20260619T143210Z.json` using the existing reviewed approval `/tmp/image_agent_stale_tasks_83_84_dry_run_rootfix2_20260619T221206Z.json`.
  - Remote verification of the materialized operator plan returned `status=passed`, `approval_json_status=fresh_reviewed`, `approval_expires_at_utc=2026-06-20T14:21:27.328242+00:00`, and mutating/operator steps limited to stale-task apply, normal API restart, and strict remote smoke.
  - Safety summary for the materialized plan: `refresh_status=superseded_by_fresh_reviewed_approval`, `refresh_has_placeholders=false`, `steps_have_placeholders=false`, `refresh_has_api_key_shape=false`, and `uses_restart_override=false`.
  - Final local backend mock/control-plane suite returned `1320 passed, 11 warnings`; touched release-gate/config/stale-task scripts passed `py_compile`; `git diff --check` passed with only existing CRLF/LF warnings.
  - Rootfix3 read-only ES prerequisite remains correctly blocked before strict smoke: required ES/embedding env is missing, live `/agent/rag/status` is not `elasticsearch_hybrid`, and there is still no connected/persisted BM25+kNN/RRF or production embedding readiness evidence.
  - No stale-task apply, API restart, live `.env` edit, Elasticsearch service start, RAG rebuild, upload, production task launch, or strict smoke was executed.

- Added portable Elasticsearch runtime discovery and promoted rootfix6 without touching the live tree:
  - `apps/api/app/agent/runtime.py` now includes a Docker-backed `list_elasticsearch_containers()` probe and embeds safe `elasticsearch.runtime_discovery` evidence in the runtime probe. The probe reports counts, running counts, container-running status, and an optional endpoint inferred from container port mappings; it does not print configured URLs or secret values.
  - The ES hybrid config handoff now includes a read-only `inspect_local_elasticsearch_runtime` step before operator secret application. The command uses only `grep` and `echo` success markers, avoiding the Windows/SSH heredoc quoting failure seen in the superseded rootfix4/rootfix5 handoff attempts.
  - Verification:
    - Local ES/runtime slice returned `25 passed, 3 warnings`.
    - Local release-gate verifier returned `status=passed`, `step_count=11`.
    - Local ES config handoff verifier returned `status=passed`, `step_count=8`.
    - Local release/config/runtime/stale-task slice returned `228 passed, 3 warnings`.
    - `git diff --check` returned no whitespace errors, only existing CRLF/LF warnings.
  - Built local archive `/tmp/image_agent_release_codex-es-hybrid-runtime-probe-rootfix6-20260619T145350Z.tar.gz`, SHA-256 `04292c1ea72aacbe04287861b0cceccdb9cd610294c4e76ad6c72b05aad8a170`, size `3055634`, `member_count=521`, required gate files present, and no `.env`, `.git`, dependency/cache, `.rag_index`, or patient-data paths.
  - Remote path precheck showed rootfix6 final and incoming overlays absent; uploaded the archive to yyf `/tmp`, extracted to `.incoming`, verified required gate/config files, and promoted it to `/home/yyf/project/image_agent_releases/codex-es-hybrid-runtime-probe-rootfix6-20260619T145350Z`. The live `/home/yyf/project/image_agent` tree was not modified.
  - Remote rootfix6 release-gate verifier returned `status=passed`, `step_count=11`; remote ES config handoff verifier returned `status=passed`, `step_count=8`; promoted overlay content check returned `release_overlay_current=true`, `required_gate_scripts_present=true`, and `elasticsearch_hybrid_contract_present=true`.
  - Rootfix6 read-only runtime handoff wrote `/tmp/image_agent_runtime_probe_codex-es-hybrid-runtime-probe-rootfix6-20260619T145350Z.json` and printed only safe markers: `runtime_probe.schema_version=1`, `runtime_probe.machine_binding=runtime_discovered`, `elasticsearch.runtime_discovery_present=true`, and `secret_values_not_printed=true`.
  - Remote runtime discovery summary: ES is not configured (`endpoint_source=not_configured`), and no local Docker Elasticsearch container candidates were discovered (`runtime_discovery.status=unavailable`, `count=0`, `running_count=0`, `container_running=false`).
  - Final backend mock/control-plane suite returned `1323 passed, 11 warnings`; touched scripts passed `compileall`; `git diff --check` returned no whitespace errors, only existing CRLF/LF warnings.
  - Real online closed-loop testing remains blocked until an operator provisions ES, applies managed ES/embedding env without committing secrets, rebuilds ES hybrid RAG, passes the read-only ES prerequisite gate, and completes the human-reviewed stale-task reconciliation/restart path. No stale-task apply, API restart, live `.env` edit, Elasticsearch service start, RAG rebuild, upload, production task launch, or strict smoke was executed.

- Added an operator-safe local Docker ES provisioning handoff and promoted rootfix7 without touching the live tree:
  - Updated `build_elasticsearch_hybrid_config_plan.py` and `verify_elasticsearch_hybrid_config_plan.py` so the ES config handoff now has `step_count=9`. The new step `operator_provision_local_elasticsearch_container_if_missing` sits after read-only runtime discovery and before secret/env application.
  - The new handoff is explicitly grounded in Elastic official Docker installation and production Docker guidance, pins `docker.elastic.co/elasticsearch/elasticsearch:9.4.2`, forbids floating image tags, requires generated credentials to stay in the operator-managed secret store, binds the acceptance endpoint to a protected local boundary, and records `production_ha_requires_operator_managed_elasticsearch=true`.
  - Updated `docs/rag/contracts/elasticsearch-hybrid-search.md` and `docs/rag/vendor/elastic_official_hybrid_search.md` to document the Docker runtime source boundary: single-node Docker is an acceptance/dev-test runtime path, while production HA remains operator-managed Elasticsearch.
  - TDD/verification:
    - RED: ES config plan tests failed because the provisioning step was missing.
    - GREEN: ES config plan builder/verifier tests returned `19 passed`.
    - Regression slice returned `216 passed`; local release-gate verifier returned `status=passed`, `step_count=11`; local ES config handoff verifier returned `status=passed`, `step_count=9`; touched scripts passed `compileall`; `git diff --check` returned no whitespace errors, only existing CRLF/LF warnings.
  - Built local archive `/tmp/image_agent_release_codex-es-hybrid-runtime-probe-rootfix7-20260619T150604Z.tar.gz`, SHA-256 `c8a55a92b4b68d0ea85d4df4e2cc81218e0a81f4c708141f78ea9a71604a8ea5`, size `3059860`, `member_count=521`, required gate files present, and no secret/runtime/patient-data paths.
  - Remote path precheck showed rootfix7 final/incoming overlays absent; uploaded the archive to yyf `/tmp`, extracted to `.incoming`, verified required gate/config files, and promoted it to `/home/yyf/project/image_agent_releases/codex-es-hybrid-runtime-probe-rootfix7-20260619T150604Z`. The live `/home/yyf/project/image_agent` tree was not modified.
  - Remote rootfix7 release-gate verifier returned `status=passed`, `step_count=11`; remote ES config handoff verifier returned `status=passed`, `step_count=9`; grep checks confirmed the provisioning step, pinned image, and production HA boundary are present.
  - Rootfix7 read-only runtime handoff wrote `/tmp/image_agent_runtime_probe_codex-es-hybrid-runtime-probe-rootfix7-20260619T150604Z.json`; current yyf evidence remains blocked with no configured ES URL and no discovered local ES container candidates.
  - No stale-task apply, API restart, live `.env` edit, Elasticsearch service/container start, RAG rebuild, upload, production task launch, or strict smoke was executed.

- Promoted rootfix8 with complete Elastic Docker raw-source provenance:
  - Root cause found during final full-suite verification: adding Elastic Docker official URLs to the curated ES vendor doc made the RAG metadata audit fail because the raw-source manifest still only contained the three hybrid retrieval snapshots.
  - Fixed by downloading and recording official raw snapshots for `elastic_docker_install`, `elastic_docker_basic`, and `elastic_docker_prod`, then updating the curated vendor doc and tests so the RAG vendor pointer audit covers all six Elastic source ids.
  - Final local verification after the provenance fix: `python -m pytest apps/api/tests -q` returned `1324 passed, 11 warnings`; `git diff --check` returned no whitespace errors, only existing CRLF/LF warnings; release-gate verifier returned `status=passed`, `step_count=11`; ES config handoff verifier returned `status=passed`, `step_count=9`.
  - Built local archive `/tmp/image_agent_release_codex-es-hybrid-runtime-probe-rootfix8-20260619T151146Z.tar.gz`, SHA-256 `14f42aff16f2123698926b7db28543c3d7b1709ad5457d1f3522f23999f4535c`, size `3143464`, `member_count=524`, required gate files present, and no secret/runtime/patient-data paths.
  - Remote path precheck showed rootfix8 final/incoming overlays absent; uploaded archive to yyf `/tmp`, extracted to `.incoming`, verified required gate/config files plus the three Elastic Docker raw snapshots, and promoted it to `/home/yyf/project/image_agent_releases/codex-es-hybrid-runtime-probe-rootfix8-20260619T151146Z`. The live `/home/yyf/project/image_agent` tree was not modified.
  - Remote rootfix8 release-gate verifier returned `status=passed`, `step_count=11`; remote ES config handoff verifier returned `status=passed`, `step_count=9`; raw-source/pinned-image checks returned `elastic_docker_manifest_sources_present=true` and `elasticsearch_image_pinned=true`.
  - Rootfix8 read-only runtime handoff wrote `/tmp/image_agent_runtime_probe_codex-es-hybrid-runtime-probe-rootfix8-20260619T151146Z.json`; current yyf evidence remains blocked with `elasticsearch_configured=false`, `endpoint_source=not_configured`, and no discovered local ES container candidates.
  - No stale-task apply, API restart, live `.env` edit, Elasticsearch service/container start, RAG rebuild, upload, production task launch, or strict smoke was executed.

- Promoted rootfix10 with explicit fixed-workflow image preparation control:
  - Added the setting `IMAGE_AGENT_AUTO_PULL_MISSING_WORKFLOW_IMAGES` and CLI flag `probe_runtime_environment --prepare-missing-images`. Default probes remain read-only; explicit preparation pulls missing fixed-workflow Docker images, re-inspects them, and records `runtime_preparation` counters and per-workflow `pull_status`.
  - Updated `/runtime/probe` to expose safe `runtime_preparation` evidence, and updated the ES config handoff to include `operator_prepare_fixed_workflow_images_if_missing` as a mutating/operator-authorized step before RAG rebuild and strict smoke.
  - TDD evidence: the new runtime image-preparation tests first failed because the CLI lacked `--prepare-missing-images`, and ES config plan tests failed because the handoff still had no workflow-image preparation step.
  - Local verification: `python -m pytest apps/api/tests/test_runtime_image_preparation.py apps/api/tests/test_runtime_probe.py apps/api/tests/test_build_elasticsearch_hybrid_config_plan.py apps/api/tests/test_verify_elasticsearch_hybrid_config_plan.py -q` returned `36 passed, 3 warnings`; release/config/stale-task slice returned `239 passed, 3 warnings`; full backend suite returned `1342 passed, 11 warnings`; direct release-gate verifier returned `status=passed`, `step_count=11`; direct ES config handoff verifier returned `status=passed`, `step_count=10`; `git diff --check` returned no whitespace errors, only existing CRLF/LF warnings.
  - Built local archive `/tmp/image_agent_release_codex-es-hybrid-runtime-probe-rootfix10-20260619T154306Z.tar.gz`, SHA-256 `461e1159b244ddf8e29906d1323991ec410f298a4d880068e1405b844fdb227c`, size `3151315`, `member_count=525`, required files present, and no `.env`, `.git`, dependency/cache, `.rag_index`, or patient-data paths.
  - Remote path precheck showed rootfix10 final/incoming/archive absent; uploaded archive to yyf `/tmp`, verified SHA-256, extracted to `.incoming`, confirmed the handoff contains `--prepare-missing-images` and `IMAGE_AGENT_AUTO_PULL_MISSING_WORKFLOW_IMAGES`, and promoted it to `/home/yyf/project/image_agent_releases/codex-es-hybrid-runtime-probe-rootfix10-20260619T154306Z`. The live tree was not modified.
  - Remote rootfix10 release-gate verifier returned `status=passed`, `step_count=11`; remote ES config handoff verifier returned `status=passed`, `step_count=10`.
  - Rootfix10 read-only runtime probe wrote `/tmp/image_agent_runtime_probe_codex-es-hybrid-runtime-probe-rootfix10-20260619T154306Z.json` and confirmed default non-mutation: `runtime_preparation.auto_pull_missing_images=false`, `pull_attempted_count=0`, `elasticsearch.configured=false`, and `elasticsearch.runtime_discovery.status=unavailable`.
  - No `--prepare-missing-images` image pull, stale-task apply, API restart, live `.env` edit, Elasticsearch service/container start, RAG rebuild, upload ingestion, production task launch, or strict smoke was executed.

- Began remote real fixed-workflow acceptance on yyf from rootfix10:
  - The old live API on port 8000 is still not rootfix10 (`/runtime/probe` is 404). A temporary rootfix10 API was started on `127.0.0.1:18082` using `IMAGE_AGENT_ROOT=/home/yyf/project/image_agent`, after backing up the live SQLite DB under `/tmp`.
  - Startup migration added `tasks.runtime_workflow_type` to the live DB through the backend's own `init_db()` path.
  - `task_id=119` proved the full production gate path reaches the real DeepPrep container: registry workflow `t1_deepprep_anat_report`, preflight pass, pending confirmation fingerprint, `/agent/runs/{thread_id}/resume`, `task_service.create_series_task(..., confirmed_agent_gate=True)`, and pipeline runner. It failed inside FreeSurfer because the selected synthetic T1 input was tiny; read-only ObserveRepair reported repair suggestions and did not auto-rerun.
  - `task_id=120` was launched through the same fingerprint-verified path on a real-size T1 candidate (`project_id=13`, `series_id=22`). Current evidence shows `production_task_created=true`, `confirmation_gate=fingerprint_verified`, runtime `t1_deepprep`, real `pbfslab/deepprep:25.1.0` container running, and DeepPrep passing the previous failure point into later anatomical stages.
  - `/home/yyf/mdd_upload` was scanned as the next richer data source. It contains real-size T1 candidates at `MDD_nii/sub-*/ses-*/anat/*t1_mprage*.nii.gz`, BOLD candidates under `func`, and DWI candidates under `dwi`; logs intentionally avoid recording raw file names with subject/person-like labels.
- Added parallel yyf real-flow validation evidence while the T1 production task continued:
  - `task_id=121` completed as a safe validate run for canonical workflow `t1_deepprep_anat_report` with runtime `t1_deepprep_validate`, created only after registry/preflight/human-confirmation fingerprint and `task_service.create_series_task(..., confirmed_agent_gate=True)`. Its runtime manifest records pinned `pbfslab/deepprep:25.1.0` and rejects floating tags.
  - BOLD fMRIPrep+XCP-D validation did not create a task because preflight correctly rejected the deployed script evidence for a `:latest` container image lock.
  - `task_id=122` was created through the same compliant gate chain for `dwi_fast_gpu_dti_validate`, then failed during the pinned QSIPrep tool-availability check because `pennlinc/qsiprep:1.0.2` was still being pulled and did not become ready within the runner timeout. This is a pinned-image preparation blocker; ObserveRepair remains read-only and no rerun was triggered.
  - Saved read-only parallel evidence path: `/tmp/image_agent_parallel_real_validate_20260620T014625+0800`. The matrix confirms T1 real-size and DWI fast candidates pass preflight, modality mismatch and unknown workflows do not create production tasks, and full runtime readiness still waits on pinned images plus ES hybrid config.
  - After the user clarified that each fixed workflow only needs one validation, an attempted extra T1 GPU-capacity fill was treated as a no-op: no task beyond `id=122` was created and only the existing task 120 DeepPrep container is running. Further real validation should cover distinct fixed workflows rather than duplicating T1.
- Corrected the QSIPrep image-lock error found by the DWI validate blocker:
  - yyf inspection showed local `pennlinc/qsiprep:latest` is actually QSIPrep `26.0.0` with digest `pennlinc/qsiprep@sha256:b578687c4667ae43f4229212b3ae91c2ff842d49a29d42e64c013aaae9600373`, and tool probes confirmed MRtrix commands plus `eddy_cuda11.0`.
  - The obsolete `pennlinc/qsiprep:1.0.2` pull watcher was stopped without restarting Docker.
  - Current code/docs/skills now lock QSIPrep/MRtrix toolbox usage to `pennlinc/qsiprep:26.0.0`; yyf tagged the existing image with that fixed tag so runtime scripts no longer depend on `latest`.
  - Verification: runtime-image/docs slice returned `71 passed, 3 warnings`; focused smoke runtime-toolchain slice returned `3 passed, 150 deselected`; touched workflow/config modules passed `py_compile`.
  - `task_id=120` completed successfully as the real-size T1 DeepPrep full workflow.
  - Saved task 120 completion evidence under `/tmp/image_agent_task120_completed_evidence_20260620T020957+0800`: 133 registered outputs, result-summary present, artifact manifest present with 143 artifacts including 9 container-native QC artifacts and 4 derived scientific report artifacts. ObserveRepair remained read-only with `auto_rerun_allowed=false`.
- Completed the QSIPrep 26.0.0 correction follow-up:
  - Current execution docs and the legacy acceptance matrix script now lock QSIPrep to `pennlinc/qsiprep:26.0.0`; current QSIRecon docs/scripts lock to `pennlinc/qsirecon:26.0.0`.
  - The acceptance matrix script no longer contains a hard-coded sudo password and instead requires `IMAGE_AGENT_SUDO_PASSWORD` at runtime.
  - Strengthened QSI container-contract tests so current RAG/skill workflow docs reject `pennlinc/qsirecon:latest`.
  - Local regression returned `190 passed, 3 warnings`; `py_compile` passed for touched runtime/config modules; `git diff --check` had no whitespace errors beyond existing line-ending warnings.
  - Promoted `/home/yyf/project/image_agent_releases/codex-qsiprep260-runtime-probe-20260619T181921Z` with archive SHA-256 `8fef54e8690564ef94d91b96daa42f67539a8781fcfdef5696359fac8fe9e556`; the live tree was not modified.
  - Restarted only yyf temporary port `18082` into that overlay. Remote contract probe shows all DWI/QSI QSIPrep references at `pennlinc/qsiprep:26.0.0`; `/runtime/containers` reports DeepPrep, QSIPrep, QSIRecon, fMRIPrep, and XCP-D fixed images available. ES hybrid remains unconfigured.
- Revalidated DWI under the corrected QSIPrep fixed image:
  - The temporary yyf API on `127.0.0.1:18082` was restarted with `IMAGE_AGENT_ROOT=/home/yyf/project/image_agent` so it uses the live remote data/DB while loading the `codex-qsiprep260-runtime-probe-20260619T181921Z` overlay. The old live 8000 service was not touched.
  - Created `task_id=123` through the fixed workflow gate using canonical workflow `dwi_fast_gpu_dti` and validate runtime `dwi_fast_gpu_dti_validate`: registry entry, preflight pass, server-side pending confirmation, fingerprint `417923093fdae885b5c2514f05900ae26f0fe7d719f62508a864424af01ac719`, `/agent/runs/{thread_id}/resume`, `confirmation_gate=fingerprint_verified`, `task_service.create_series_task(..., confirmed_agent_gate=True)`, and pipeline runner.
  - The task completed successfully (`status=completed`, `progress=100`). Its runtime manifest records `pennlinc/qsiprep:26.0.0`, `floating_tags_allowed=false`, and the DWI fast DTI validate command using `--mrtrix-image pennlinc/qsiprep:26.0.0`.
  - Evidence saved under `/tmp/image_agent_dwi_validate_qsiprep260_20260620T022946+0800`: launch, final task, outputs, events, result summary, artifact manifest, ObserveRepair, redacted log tail, and safe summary JSON. Outputs count is `2`, artifact manifest count is `16`, result summary is validation-only, and ObserveRepair remains read-only with `auto_rerun_allowed=false`.
  - Remote evidence secret scan was clean for proxy/API-key/sudo-password markers. This closes the specific task 122 regression caused by the mistaken `pennlinc/qsiprep:1.0.2` lock; it does not yet satisfy full strict online smoke because ES hybrid is still unconfigured and BOLD fMRIPrep+XCP-D preflight still needs its deployment-local script image-lock blocker addressed.
- Fixed and revalidated the BOLD fMRIPrep/XCP-D deployment-local script image lock:
  - Added managed locked wrapper materialization for BOLD remote scripts. The wrapper generation preserves deployment-local script behavior but replaces container image references with task environment variables and fixed defaults: `nipreps/fmriprep:25.2.5` and `pennlinc/xcp_d:26.0.2`.
  - Built and promoted temporary overlay `/home/yyf/project/image_agent_releases/codex-bold-wrapper-runtime-probe-20260619T183828Z`, archive SHA-256 `46018ba63977bd5efa1a0f462fbb4d575fdb3720d3bbe428c7ec4155c31d0efa`, without touching live 8000 or the live code tree.
  - Generated yyf managed wrappers under `/home/yyf/project/image_agent/runtime_wrappers/bold_fmriprep_xcpd`; the source fMRIPrep script still contains `nipreps/fmriprep:latest`, but the generated wrapper uses `IMAGE_AGENT_TASK_FMRIPREP_IMAGE` with fixed default `nipreps/fmriprep:25.2.5`.
  - Created `task_id=124` through canonical `bold_fmriprep_xcpd_report` registry/preflight/human-confirmation fingerprint/resume/task_service/pipeline runner with validate runtime `bold_fmriprep_xcpd_report_validate`. It completed with `status=completed`, `progress=100`, `confirmation_gate=fingerprint_verified`, and runtime manifest fixed images plus `floating_tags_allowed=false`.
  - Evidence saved under `/tmp/image_agent_bold_validate_locked_wrapper_20260620T0247+0800`. Validate-only BOLD registers command/preflight evidence and intentionally has no full result-summary; `/outputs`, `/events`, `/artifact-manifest`, and `/observe-repair` returned 200, while `/result-summary` returned 404 by design. ObserveRepair remained read-only with `auto_rerun_allowed=false`.
  - Local verification for this slice returned `23 passed, 3 warnings`; touched Python modules passed `py_compile`; `git diff --check` reported no whitespace errors, only existing line-ending warnings.
