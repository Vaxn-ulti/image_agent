# Work Log 2026-06-10

## Agent API contract hardening

Time: 2026-06-10T22:08:31+08:00

Scope:

- Followed the updated operating constraint: no multi-subagent strategy. Previously started subagent tasks were stopped; current implementation and verification were completed serially by the main agent.
- Hardened the `/agent/runs` contract family with a stable `agent_api_error.v1` error envelope for blank messages, missing run lookup, model-call failure, and resume failure.
- Extended `agent_run_lookup.v1` to expose safe ledger audit fields: `message_sha256`, `error_message`, `created_at`, `updated_at`, and `finished_at`.
- Added tests for OpenAPI error response schemas, stable error payloads, resume unknown-status normalization, and resume failure redaction.
- Updated `docs/api.md` and the image-agent developer contract reference so `/agent/runs` is the primary product agent API and `/chat` is compatibility only.

Verification:

- Local syntax check passed:
  - `python -m compileall -q apps/api/app/agent/contracts.py apps/api/app/main.py apps/api/tests/test_agent_api.py`
- Local repo-root pytest was blocked by the host Python missing `python-multipart`; no local dependency install was performed.
- Remote isolated verification passed without touching the dirty remote project worktree:
  - Uploaded a tracked-tree archive plus this local patch to `/tmp`.
  - Applied the patch in `/tmp/image_agent_contract_test_20260610_2108`.
  - Used the remote API venv at `/home/yyf/project/image_agent/apps/api/.venv/bin/python`.
  - `python -m pytest apps/api/tests/test_agent_api.py -q` -> `26 passed, 3 warnings`.

Release gate:

- Frontend design remains blocked. The current work improves the agent API contract, but it is not strict remote product acceptance evidence with real project/upload/task/run ids, model smoke pass, artifact manifest, container-native QC, scientific report provenance, and path-leak verification.

## Durable confirmation state

Time: 2026-06-10T22:20:00+08:00

Scope:

- Continued with the updated constraint: no multi-subagent strategy.
- Added SQLite `agent_confirmations` and `agent_confirmation_events` tables.
- Updated `AgentThreadStore` so pending confirmations are persisted to SQLite when the DB is initialized, while the legacy JSON file remains a compatibility mirror/fallback.
- Added SQLite reload support so pending confirmations can survive missing JSON mirror files.
- Added transition auditing for `confirmation_created` and `confirmation_marked`, including `from_status`, `to_status`, and redacted metadata.
- Documented confirmation states and source-of-truth rules in `docs/workflow-state.md` and `docs/rag/contracts/agent-run-ledger.md`.

Verification:

- RED test run on remote isolated tree:
  - `test_agent_thread_store_persists_pending_confirmation_in_sqlite`
  - `test_agent_thread_store_loads_pending_confirmation_from_sqlite_after_json_missing`
  - `test_agent_thread_store_marks_confirmation_transition_in_sqlite`
  - Expected failures confirmed: missing `agent_confirmations` table and no SQLite reload path.
- Local syntax/diff checks passed:
  - `python -m compileall -q apps/api/app/agent/thread_store.py apps/api/tests/test_agent_graph.py`
  - `git diff --check`
- Remote isolated verification passed without touching the dirty remote project worktree:
  - Applied the patch in `/tmp/image_agent_thread_store_green_20260610_2220`.
  - Used the remote API venv at `/home/yyf/project/image_agent/apps/api/.venv/bin/python`.
  - `python -m pytest apps/api/tests/test_agent_graph.py apps/api/tests/test_agent_api.py -q` -> `42 passed, 3 warnings`.

Release gate:

- Frontend design remains blocked. This improves durable agent state, but strict remote product acceptance evidence is still not complete.

## RAG metadata audit

Time: 2026-06-10T22:36:00+08:00

Scope:

- Continued with the updated constraint: no multi-subagent strategy.
- Added `audit_rag_metadata` for machine-readable RAG metadata checks across workflow, vendor, contract, and safety documents.
- Required workflow docs to expose launch/answering metadata: `source_type`, `workflow_type`, `status`, `official_grounding`, `expected_artifacts`, and `unsupported_boundaries`.
- Required vendor docs to expose `source_type: rag_vendor`, `source_url`, `raw_source_ids`, and `status`, with raw source ids tied back to the downloaded official-source manifest.
- Added missing contract/safety frontmatter and made vendor document source types explicit.
- Added regression tests for the curated repo docs and for a minimal broken vendor doc.

Verification:

- RED remote isolated verification confirmed the new test failed before implementation:
  - `python -m pytest apps/api/tests/test_rag_metadata_audit.py -q` -> failed with `ModuleNotFoundError: No module named 'app.scripts.audit_rag_metadata'`.
- Local checks passed:
  - `python -m compileall -q apps/api/app/scripts/audit_rag_metadata.py apps/api/tests/test_rag_metadata_audit.py`
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_rag_metadata_audit.py -q` -> `2 passed`.
- Remote isolated verification passed without touching the dirty remote project worktree:
  - Applied tracked changes and copied new files in `/tmp/image_agent_rag_metadata_green_20260610`.
  - Used the remote API venv at `/home/yyf/project/image_agent/apps/api/.venv/bin/python`.
  - `python -m pytest apps/api/tests/test_rag_metadata_audit.py apps/api/tests/test_skill_and_rag_docs.py::test_workflow_rag_docs_have_machine_readable_frontmatter -q` -> `3 passed`.

Release gate:

- Frontend design remains blocked. RAG metadata is now auditable, but release readiness still requires strict remote smoke acceptance with real project/upload/task/run ids, model smoke pass, artifact manifest, container-native QC, scientific report provenance, and path-leak verification.

## Artifact manifest native QC hardening

Time: 2026-06-10T22:27:38+08:00

Scope:

- Continued with the updated constraint: no multi-subagent strategy.
- Hardened `/tasks/{task_id}/artifact-manifest` classification so `container_native_qc=true` requires strict container provenance, not just `native_artifact=true`.
- Required container-native QC classification to include `artifact_origin=container_output`, `provenance.generated_from=container_native_qc`, `provenance.replaces_native_qc=false`, a valid native QC role, and matching non-empty top-level/provenance `official_source_ids`.
- Demoted native-looking artifacts with incomplete provenance to regular frontend preview assets so they remain displayable but cannot satisfy strict native QC evidence.
- Updated artifact manifest tests and API fixtures to reflect the strict provenance contract expected by remote release gates.

Verification:

- RED local test confirmed the old implementation incorrectly promoted a native-looking HTML artifact without strict provenance:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_artifact_manifest_contract.py -q` -> failed on `test_artifact_manifest_demotes_native_qc_without_strict_container_provenance`.
- Local checks passed:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_artifact_manifest_contract.py -q` -> `2 passed`.
  - Local API pytest remained blocked by missing host `python-multipart`; no local dependency install was performed.
  - `python -m compileall -q apps/api/app/workflows/artifact_manifest.py apps/api/tests/test_artifact_manifest_contract.py apps/api/tests/test_artifact_manifest_api.py`
  - `git diff --check`
- Remote isolated verification passed without touching the dirty remote project worktree:
  - Applied the patch in `/tmp/image_agent_artifact_manifest_green_20260610`.
  - Used the remote API venv at `/home/yyf/project/image_agent/apps/api/.venv/bin/python`.
  - `python -m pytest apps/api/tests/test_artifact_manifest_contract.py apps/api/tests/test_artifact_manifest_api.py apps/api/tests/test_smoke_remote_agent.py -k "container_native_qc" -q` -> `14 passed, 58 deselected, 2 warnings`.

Release gate:

- Frontend design remains blocked. This makes artifact-manifest native QC evidence stricter, but the product still needs a full strict remote smoke acceptance JSON from real project/upload/task/run ids before frontend design can begin.

## Skill maintenance audit hardening

Time: 2026-06-10T22:32:48+08:00

Scope:

- Continued with the updated constraint: no multi-subagent strategy.
- Extended `audit_skill_maintenance.py` so skill references are audited as first-class maintenance artifacts.
- Added reference inventory counts to the audit output: checked reference count, long reference count, long references with Contents/Table of Contents, and the long-reference line threshold.
- Required long skill reference files to include a `## Contents`, `## Table of Contents`, or `## Quick Navigation` section so skill-creator-style progressive disclosure stays navigable.
- Kept sensitive-token scanning for reference files while ignoring explicit `...` placeholders such as `api_key=...`.
- Added Contents sections to the seven long skill reference files currently above the audit threshold.

Verification:

- RED local test confirmed the previous audit had no `references` output:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_skill_maintenance_audit.py -q` -> failed with missing `result["references"]`.
- Local checks passed:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_skill_maintenance_audit.py -q` -> `2 passed`.
  - `python apps/api/scripts/audit_skill_maintenance.py --json` -> `status=passed`, `checked_reference_count=31`, `long_reference_count=7`, `long_references_with_toc=7`.
  - `python -m compileall -q apps/api/scripts/audit_skill_maintenance.py apps/api/tests/test_skill_maintenance_audit.py`
  - `git diff --check` produced only CRLF normalization warnings for touched Markdown files.
- Remote isolated verification passed without touching the dirty remote project worktree:
  - Applied the patch in `/tmp/image_agent_skill_audit_green_20260610`.
  - Used the remote API venv at `/home/yyf/project/image_agent/apps/api/.venv/bin/python`.
  - `python -m pytest apps/api/tests/test_skill_maintenance_audit.py apps/api/tests/test_skill_and_rag_docs.py::test_skill_creator_style_skills_have_references_and_parseable_evals -q` -> `3 passed`.

Release gate:

- Frontend design remains blocked. Skill maintenance is more auditable, but release readiness still requires strict remote smoke acceptance with real project/upload/task/run ids, model smoke pass, artifact manifest, container-native QC, scientific report provenance, and path-leak verification.

## Remote smoke gate compatibility hardening

Time: 2026-06-10T23:24:02+08:00

Scope:

- Continued with the updated constraint: no multi-subagent strategy.
- Preserved official raw-source snapshots as exact provenance evidence in Git archives by marking `docs/rag/vendor/raw-sources/**` as non-text in `.gitattributes`.
- Added a regression test proving `git archive --worktree-attributes` preserves the raw-source manifest hash for the DPABI Dockerfile snapshot.
- Added SOCKS proxy support to API requirements because the remote model gateway environment exposes SOCKS proxy variables.
- Hardened the OpenAI-compatible model gateway:
  - Retries Responses API requests without `metadata` when an OpenAI-compatible provider rejects that parameter.
  - Preserves `function_call` items when sending `function_call_output` items back to Responses-compatible models.
- Enriched legacy container-native QC artifacts in the artifact manifest when their stage and role clearly map to known container-native QC sources.
- Enriched legacy dataset inventory responses with `workflow_eligibility_v1` so older upload inventory JSON can still drive stable workflow routing.

Verification:

- Local static checks passed without installing local dependencies:
  - `python -m compileall -q apps/api/app/agent/model_gateway.py apps/api/app/workflows/artifact_manifest.py apps/api/app/main.py apps/api/tests/test_model_gateway.py apps/api/tests/test_artifact_manifest_contract.py apps/api/tests/test_requirements_contract.py apps/api/tests/test_skill_and_rag_docs.py apps/api/tests/test_api_flow.py`
  - `git diff --check`
- Local pytest remained blocked by the host environment missing `python-multipart`; no local dependency install was performed.
- Remote isolated verification passed without touching the dirty remote project worktree:
  - Applied the current patch in `/tmp/image_agent_inventory_green_20260610`.
  - Used the remote API venv at `/home/yyf/project/image_agent/apps/api/.venv/bin/python`.
  - `python -m pytest apps/api/tests/test_api_flow.py::test_dataset_inventory_endpoint_enriches_legacy_series_workflow_eligibility apps/api/tests/test_model_gateway.py apps/api/tests/test_artifact_manifest_contract.py apps/api/tests/test_requirements_contract.py apps/api/tests/test_skill_and_rag_docs.py::test_vendor_raw_sources_are_not_text_normalized_in_git_archives -q` -> `25 passed, 2 warnings`.
- Remote latest-code smoke passed on an isolated API at `http://127.0.0.1:18180` against real data:
  - `model_smoke_status=passed`
  - RAG indexed `73` documents and `290` chunks.
  - Raw-source pointer integrity: `0` missing files, `0` hash mismatches.
  - Task `118` exposed `41` container-native QC artifacts.
  - Native QC provenance was grounded to `docs/rag/vendor/fmriprep_official_outputs.md` and `docs/rag/vendor/xcp_d_official_outputs.md`.

Deployment notes:

- The current live API on port `8000` still needs deployment/update; during this work it was observed to be older than the isolated latest-code API and did not expose the latest artifact-manifest behavior for real tasks.
- The remote venv now has `socksio==1.0.0` installed so SOCKS proxy environments can initialize the OpenAI/httpx client.

Release gate:

- Frontend design remains blocked. This smoke intentionally did not require scientific report artifacts, and the task `118` evidence set has container-native QC but no derived scientific report artifact count. The strict release gate still needs a single remote acceptance bundle with real project/upload/task/run ids, model smoke pass, artifact manifest, container-native QC, scientific report provenance, and path-leak verification.

## Scientific report and native QC full-gate evidence

Time: 2026-06-10T23:34:31+08:00

Scope:

- Continued with the updated constraint: no multi-subagent strategy.
- Fixed scientific report backfill so generated report assets are merged into `outputs.reports` instead of replacing existing container-native report entries.
- Added a generic remote-safe CLI, `apps/api/scripts/regenerate_scientific_reports.py`, for regenerating scientific report bundles by output directory or real task id.
- Added regression coverage proving the CLI/backfill preserves existing native reports while adding `reports/index.html`, `reports/report_manifest.json`, and PNG report assets.
- Created a scoped remote backup before modifying task `118` outputs:
  - `/tmp/image_agent_task118_scireport_backup_20260610.tar.gz`
- Used the isolated patched code in `/tmp/image_agent_scireport_green_20260610` to regenerate scientific report assets for real task `118` without touching the dirty remote project worktree code.

Verification:

- Local checks passed:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_regenerate_scientific_reports_script.py apps/api/tests/test_result_contract.py::test_scientific_report_backfill_preserves_existing_native_reports apps/api/tests/test_result_contract.py::test_build_scientific_report_summary_indexes_report_artifacts apps/api/tests/test_artifact_manifest_contract.py -q` -> `6 passed`.
- Remote isolated verification passed:
  - Applied the patch in `/tmp/image_agent_scireport_green_20260610`.
  - Used the remote API venv at `/home/yyf/project/image_agent/apps/api/.venv/bin/python`.
  - `python -m pytest apps/api/tests/test_regenerate_scientific_reports_script.py apps/api/tests/test_result_contract.py::test_scientific_report_backfill_preserves_existing_native_reports apps/api/tests/test_artifact_manifest_contract.py apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_require_scientific_report_artifacts_reports_derived_evidence -q` -> `6 passed`.
- Real task `118` report regeneration passed:
  - `derived_report_count=6`
  - generated `reports/index.html`, `reports/report_manifest.json`, and four BOLD PNG report assets.
- Latest-code isolated API manifest evidence for task `118` passed the combined artifact shape:
  - total artifacts: `320`
  - container-native QC artifacts: `41`
  - container-native QC images: `28`
  - container-native QC HTML reports: `13`
  - derived scientific report artifacts: `6`
  - derived scientific report images: `4`
  - includes `reports/index.html` and `reports/report_manifest.json`
- Full remote smoke passed on isolated latest-code API `http://127.0.0.1:18181`:
  - output JSON: `/tmp/image_agent_task118_full_smoke_20260610.json`
  - `model_smoke_status=passed`
  - `remote_evidence_ids_status=passed`
  - `rag_vendor_pointer_integrity_status=passed`
  - `rag_launchability_query_status=passed`
  - `project_contract_status=passed`
  - `upload_inventory_contract_status=passed`
  - `task_artifact_manifest_status=passed`
  - `container_native_qc_status=passed`
  - `scientific_report_artifacts_status=passed`
- Offline strict acceptance verifier passed:
  - `python apps/api/scripts/verify_remote_smoke_acceptance.py /tmp/image_agent_task118_full_smoke_20260610.json` -> `status=passed`

Release gate:

- This is the first strict full-gate evidence bundle with real project/upload/task ids, live model pass, RAG provenance, launchability citation, artifact manifest, container-native QC, scientific report provenance, and route-served artifacts. Frontend design should still wait until the latest committed code is deployed to the intended remote live API surface rather than only the isolated `18181` smoke server.

## Live remote API strict gate deployment

Time: 2026-06-10T23:40:07+08:00

Scope:

- Continued with the updated constraint: no multi-subagent strategy.
- Deployed committed local HEAD `7e7ff94` to the formal remote API port `8000` without overwriting the dirty remote main worktree.
- Used a release overlay outside the dirty repo:
  - release path: `/home/yyf/project/image_agent_releases/codex-7e7ff94-20260610`
  - shared real data symlink: `/home/yyf/project/image_agent/data`
  - remote API venv: `/home/yyf/project/image_agent/apps/api/.venv`
- Recorded pre-deploy process information for rollback:
  - `/tmp/image_agent_api_8000_predeploy_20260610.txt`
  - old API cwd: `/home/yyf/project/image_agent/apps/api`
  - old API pid: `685739`
- Started the live API on `0.0.0.0:8000` from the release overlay; the running process cwd is `/home/yyf/project/image_agent_releases/codex-7e7ff94-20260610/apps/api`.

Verification:

- Live health passed:
  - `GET http://127.0.0.1:8000/health` -> `{"status":"ok","app":"image_agent","version":"0.2.0"}`
- Live model status passed:
  - provider configured as OpenAI-compatible Responses API.
  - model and review model set to `gpt-5.5`.
  - reasoning effort set to `high`.
- Full strict smoke passed on the formal live API port `8000`:
  - output JSON: `/tmp/image_agent_task118_live8000_full_smoke_20260610.json`
  - `model_smoke_status=passed`
  - `remote_evidence_ids_status=passed`
  - `rag_vendor_pointer_integrity_status=passed`
  - `rag_vendor_coverage_catalog_status=complete`
  - `rag_launchability_query_status=passed`
  - `project_contract_status=passed`
  - `upload_inventory_contract_status=passed`
  - `task_artifact_manifest_status=passed`
  - `container_native_qc_status=passed`
  - `scientific_report_artifacts_status=passed`
- Offline strict acceptance verifier passed against the saved live-port JSON:
  - `python apps/api/scripts/verify_remote_smoke_acceptance.py /tmp/image_agent_task118_live8000_full_smoke_20260610.json` -> `status=passed`

Release gate:

- The formal remote API surface now has strict remote acceptance evidence with real project/upload/task ids, model pass, RAG provenance, launchability citation, safe artifact manifest, container-native QC, derived scientific report provenance, and route-served artifacts.
- Remaining maturity work before declaring the full product finished: make the release overlay operationally durable as the normal restart path or service definition, and keep the dirty remote main worktree from becoming the accidental source of production truth.
