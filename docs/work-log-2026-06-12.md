# Work Log 2026-06-12

## Strict acceptance deployment identity gate

Time: 2026-06-12T12:34:25+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Hardened strict remote acceptance evidence so a fresh JSON cannot be accepted unless it also names the accepted deployment identity.
- Added `--require-deployment-identity` and `--deployment-id` to `apps/api/scripts/smoke_remote_agent.py`.
- Kept the deployment id privacy-safe: it must be a short symbol such as an accepted release overlay name or commit hash, not a full remote path.
- Added `deployment_identity_status` and `deployment_identity` to saved smoke JSON when the gate is required.
- Hardened `apps/api/scripts/verify_remote_smoke_acceptance.py` so strict saved evidence must include `deployment_identity_status=passed`, a privacy-safe `deployment_identity.deployment_id`, and a match between `deployment_identity.deployment_id` and `smoke_gate.deployment_id`.
- Updated remote production and acceptance docs to require the deployment identity gate alongside the existing model, RAG, real-id, artifact, and freshness gates.
- Updated `docs/product-readiness.md` so frontend design remains blocked until the fresh strict remote evidence includes the verified deployment identity.

Verification:

- Focused local verification:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py apps/api/tests/test_smoke_remote_agent.py -q` -> `163 passed`.
  - `python -m py_compile apps/api/scripts/verify_remote_smoke_acceptance.py apps/api/scripts/smoke_remote_agent.py` -> passed.

Release gate:

- Frontend design remains blocked.
- No local install, local service run, local container run, remote deployment, remote restart, or production stale-task apply was performed in this slice.
- Next maturity step remains the operator-reviewed stale-task resolution flow for tasks `83` and `84`, followed by a normal remote restart and fresh strict smoke acceptance JSON that passes `--max-age-hours 24` and the new deployment identity gate.

## Deployment identity health-version hardening

Time: 2026-06-12T13:05:00+08:00

Scope:

- Tightened the strict deployment identity gate so `deployment_identity.health_version` must be present and privacy-safe.
- `smoke_remote_agent.py` now fails immediately after `/health` when `--require-deployment-identity` is active and `/health.version` is missing or path-like, instead of continuing to build a bad acceptance JSON.
- `verify_remote_smoke_acceptance.py` now applies the same privacy-safe symbol rule to saved `deployment_identity.health_version`.
- Updated remote production, acceptance template, readiness, and task-plan notes to state that health version cannot be a full remote backend path.

Verification:

- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_rejects_bad_health_version_for_deployment_identity -q` failed because the script continued to `/agent/model/status` after a bad health version.
- GREEN:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_rejects_bad_health_version_for_deployment_identity -q` -> `2 passed`.
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_rejects_bad_health_version_for_deployment_identity apps/api/tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_rejects_bad_deployment_identity -q` -> `7 passed`.
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py apps/api/tests/test_smoke_remote_agent.py -q` -> `166 passed`.
  - `python -m py_compile apps/api/scripts/smoke_remote_agent.py apps/api/scripts/verify_remote_smoke_acceptance.py` -> passed.

Release gate:

- Frontend design remains blocked.
- This remains local code/docs hardening only. Real acceptance still requires remote operator stale-task resolution, normal restart, and fresh strict smoke evidence from the remote server.
