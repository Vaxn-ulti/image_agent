# Work Log 2026-06-12

## Strict acceptance deployment identity gate

Time: 2026-06-12T12:34:25+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Hardened strict remote acceptance evidence so a fresh JSON cannot be accepted unless it also names the accepted deployment identity.
- Added `--require-deployment-identity` and `--deployment-id` to `apps/api/scripts/smoke_remote_agent.py`.
- Kept the deployment id privacy-safe: it must be a short symbol such as an accepted release overlay name or commit hash, not a full remote path.
- Added `deployment_identity_status` and `deployment_identity` to saved smoke JSON when the gate is required.
- Hardened `apps/api/scripts/verify_remote_smoke_acceptance.py` so strict saved evidence must include `deployment_identity_status=passed`, a privacy-safe `deployment_identity.deployment_id`, and a match between `deployment_identity.deployment_id` and `smoke_gate.deployment_id`.
- Updated remote production and acceptance docs to require the deployment identity gate alongside the existing model, RAG, real-id, artifact, and freshness gates.
- Updated `docs/product-readiness.md` so frontend design remains blocked until the fresh strict remote evidence includes the verified deployment identity.

Verification:

- Focused local verification:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py apps/api/tests/test_smoke_remote_agent.py -q` -> `163 passed`.
  - `python -m py_compile apps/api/scripts/verify_remote_smoke_acceptance.py apps/api/scripts/smoke_remote_agent.py` -> passed.

Release gate:

- Frontend design remains blocked.
- No local install, local service run, local container run, remote deployment, remote restart, or production stale-task apply was performed in this slice.
- Next maturity step remains the operator-reviewed stale-task resolution flow for tasks `83` and `84`, followed by a normal remote restart and fresh strict smoke acceptance JSON that passes `--max-age-hours 24` and the new deployment identity gate.

## Deployment identity health-version hardening

Time: 2026-06-12T13:05:00+08:00

Scope:

- Tightened the strict deployment identity gate so `deployment_identity.health_version` must be present and privacy-safe.
- `smoke_remote_agent.py` now fails immediately after `/health` when `--require-deployment-identity` is active and `/health.version` is missing or path-like, instead of continuing to build a bad acceptance JSON.
- `verify_remote_smoke_acceptance.py` now applies the same privacy-safe symbol rule to saved `deployment_identity.health_version`.
- Updated remote production, acceptance template, readiness, and task-plan notes to state that health version cannot be a full remote backend path.

Verification:

- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_rejects_bad_health_version_for_deployment_identity -q` failed because the script continued to `/agent/model/status` after a bad health version.
- GREEN:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_rejects_bad_health_version_for_deployment_identity -q` -> `2 passed`.
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_rejects_bad_health_version_for_deployment_identity apps/api/tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_rejects_bad_deployment_identity -q` -> `7 passed`.
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py apps/api/tests/test_smoke_remote_agent.py -q` -> `166 passed`.
  - `python -m py_compile apps/api/scripts/smoke_remote_agent.py apps/api/scripts/verify_remote_smoke_acceptance.py` -> passed.

Release gate:

- Frontend design remains blocked.
- This remains local code/docs hardening only. Real acceptance still requires remote operator stale-task resolution, normal restart, and fresh strict smoke evidence from the remote server.

## Developer testing-matrix strict smoke parity

Time: 2026-06-12T13:24:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept implementation local to tests/docs only; no local install, local service run, local container run, remote install, remote restart, or remote smoke was performed.
- Added a documentation contract test requiring the Image Agent developer testing matrix to include the strict smoke deployment identity gate.
- Updated `docs/skills/image-agent-developer/references/testing-matrix.md` so the remote strict smoke command includes `--require-deployment-identity` and `--deployment-id`.
- Documented that saved strict smoke JSON must include `deployment_identity_status=passed`, matching `deployment_identity.deployment_id` and `smoke_gate.deployment_id`, and a privacy-safe `deployment_identity.health_version`.
- Documented the offline verifier command with the freshness gate: `python scripts/verify_remote_smoke_acceptance.py --max-age-hours 24 <remote-smoke-acceptance.json>`.

Verification:

- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_developer_testing_matrix_requires_deployment_identity_for_strict_smoke -q` failed because the developer testing matrix did not mention `--require-deployment-identity`.
- GREEN:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_developer_testing_matrix_requires_deployment_identity_for_strict_smoke -q` -> `1 passed`.
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_remote_smoke_acceptance_json_verifier_is_documented apps/api/tests/test_skill_and_rag_docs.py::test_developer_testing_matrix_requires_deployment_identity_for_strict_smoke apps/api/tests/test_skill_and_rag_docs.py::test_product_readiness_gate_blocks_frontend_until_agent_contracts_are_verified apps/api/tests/test_verify_remote_smoke_acceptance.py apps/api/tests/test_smoke_remote_agent.py -q` -> `169 passed`.
  - `python apps/api/scripts/audit_skill_maintenance.py --json` -> `status=passed`.

Release gate:

- Frontend design remains blocked.
- The next remote acceptance target is still operator-reviewed stale-task resolution, normal remote restart, and fresh strict smoke evidence from the remote server that passes the 24-hour verifier plus deployment identity gate.

## Developer testing-matrix stale-task gate parity

Time: 2026-06-12T13:47:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to tests/docs only; no local install, local service run, local container run, remote install, remote stale-task apply, remote restart, or remote smoke was performed.
- Added a documentation contract test requiring the Image Agent developer testing matrix to include the stale-task approval/apply/resolution gate before strict remote acceptance.
- Updated `docs/skills/image-agent-developer/references/testing-matrix.md` so operational release acceptance requires:
  - dry-run approval evidence verified by `verify_stale_task_approval.py`;
  - apply mode using `--approval-json` and the reviewed `approval_fingerprint`;
  - post-apply dry-run evidence verified by `verify_stale_task_resolution.py --require-empty-active`;
  - `out_of_scope_stale_task_ids=[]`, `running_container_task_ids=[]`, and `blocked_task_ids=[]`;
  - a normal restart without `IMAGE_AGENT_ALLOW_RESTART_WITH_ACTIVE_TASKS=1` before strict smoke can count as release evidence.

Verification:

- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_developer_testing_matrix_requires_stale_task_gate_before_strict_acceptance -q` failed because the developer testing matrix did not mention approved stale-task reconciliation.
- GREEN: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_developer_testing_matrix_requires_stale_task_gate_before_strict_acceptance -q` -> `1 passed`.

Release gate:

- Frontend design remains blocked.
- The actual production task rows `83` and `84` still require approved remote apply, clean post-apply resolution verification, normal remote restart, and fresh strict remote acceptance evidence.

## RAG raw-source manifest audit hardening

Time: 2026-06-12T14:16:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to tests/docs/audit code only; no local install, local service run, local container run, remote install, remote restart, remote RAG rebuild, or remote smoke was performed.
- Hardened `apps/api/app/scripts/audit_rag_metadata.py` so the reusable RAG metadata audit now validates raw official-source manifest entries directly.
- The audit now checks each manifest source for required fields, HTTPS URL, `official_` source type, `downloaded` status, safe file path inside `docs/rag/vendor/raw-sources`, existing raw snapshot file, positive bytes, matching file size, and matching SHA-256.
- Added a regression test proving the audit rejects invalid raw-source manifest evidence instead of only checking curated Markdown frontmatter.

Verification:

- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_rag_metadata_audit.py::test_rag_metadata_audit_reports_invalid_raw_source_manifest_entries -q` failed because the audit only reported curated `source_url` drift and did not validate the manifest entry itself.
- GREEN:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_rag_metadata_audit.py::test_rag_metadata_audit_reports_invalid_raw_source_manifest_entries -q` -> `1 passed`.
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_rag_metadata_audit.py -q` -> `4 passed`.
  - `PYTHONPATH=apps/api python -c "from pathlib import Path; from app.scripts.audit_rag_metadata import audit_rag_metadata; import json; print(json.dumps(audit_rag_metadata(root=Path('.'), strict=True), indent=2, sort_keys=True))"` -> `ok=true`, `vendor_provenance_issues=[]`, `raw_sources_indexed=false`.

Release gate:

- Frontend design remains blocked.
- This strengthens official-source RAG evidence validation, but final readiness still requires remote stale-task cleanup, normal restart, and fresh strict remote acceptance.
