# Work Log 2026-06-15

## API route/service split

- Split the FastAPI entrypoint into a thin `app.main` module that registers route modules and keeps startup/validation handling.
- Added route modules for system, agent, auth, projects, uploads, series, tasks, results, reports, and chat endpoints.
- Added service modules for project, upload, task, result, and agent boundaries. The first pass keeps a compatibility bridge to the former main implementation so existing endpoint behavior and tests remain stable while later work moves legacy logic into the focused services.
- Added `apps/api/app/schemas.py` for request models and `apps/api/app/dependencies.py` as the shared helper surface for route/service code.
- Added `tests/test_main_architecture.py` to keep `main.py` thin and require the route/service module layout.

## Test evidence

- `cd apps/api; python -m pytest tests/test_main_architecture.py -q` -> `1 passed`.
- `cd apps/api; python -m pytest tests/test_agent_api.py -q` -> `32 passed`.
- `cd apps/api; python -m pytest tests/test_artifact_manifest_api.py -q` -> `1 passed`.
- `cd apps/api; python -m pytest tests/test_api_flow.py -q` -> `59 passed`.
- `cd apps/api; python -m pytest tests/test_fixed_workflow_api_contract.py -q` -> `20 passed`.
- From repo root: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests -q` -> `641 passed`.

## Notes

- Installed the project-declared local test dependency `nibabel==5.3.2` to run fixed workflow contract tests.
- Fixed `pipeline._write_bold_fmriprep_xcpd_summary` so file-summary tests without an initialized SQLite schema skip output registration instead of failing on a missing `outputs` table. Real workflow paths still register outputs when the DB schema exists.
- This was a local code/test step only. Product release readiness still requires the existing remote-server install, run, restart, and strict smoke acceptance gates before telling the user the product is ready for frontend page design or real use.

## Service-boundary follow-up

- Moved low-risk project/auth service logic out of the transitional compatibility bridge: login, project listing, and project creation now live directly in `project_service.py`.
- Moved simple system/model endpoints out of the bridge in `agent_service.py`: health, workflow list, result contract, deployment status, model status, runtime containers, and admin container listing.
- Extended `tests/test_main_architecture.py` so these low-risk service functions cannot regress back to `legacy_service` delegation.
- Verification from repo root: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests -q` -> `642 passed`.

## Task/result read-path service split

- Moved read-only task and series service paths out of the compatibility bridge: project task listing, series lookup, and task lookup now live directly in `task_service.py`.
- Moved result read paths out of the bridge: task logs, outputs, result summaries, artifact manifests, and artifact file serving now live directly in `result_service.py`.
- Kept task creation/workflow launch and BOLD report mutation paths behind the compatibility bridge for a later, narrower migration because those paths start background work or mutate scientific outputs.
- Extended `tests/test_main_architecture.py` to prevent these read-path services from regressing to `legacy_service` delegation.
- Verification from repo root: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests -q` -> `642 passed`.

## Upload service split

- Moved upload and ingest service paths out of the compatibility bridge: single-file upload, DWI sidecar upload, DICOM archive upload, upload session creation, dataset ingest, inventory reads, and series listing now live directly in `upload_service.py`.
- Preserved the product boundary that workflow execution and scientific QC remain deterministic backend/container responsibilities; this step only relocates upload/orchestration logic into the service layer.
- Extended `tests/test_main_architecture.py` so upload service entrypoints cannot regress back to `legacy_service` delegation.
- Focused verification: `cd apps/api; python -m pytest tests/test_main_architecture.py -q` -> `2 passed`.
- Focused verification: `cd apps/api; python -m pytest tests/test_api_flow.py -q` -> `59 passed`.
- Full verification from repo root: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests -q` -> `642 passed`.

## Task creation service split

- Moved workflow run validation and task creation out of the compatibility bridge into `task_service.py`, including DWI sidecar validation, QSIRecon prerequisite checks, BOLD prerequisite checks, task row creation, log path assignment, and deterministic runner dispatch.
- Kept the current MVP `Thread(...)` execution model intentionally unchanged; worker queue migration remains a later product hardening step after API/service boundaries settle.
- Preserved explicit monkeypatch compatibility for `app.main.PROJECTS_ROOT`, `app.main.run_mock_deepprep`, and `app.main.run_pipeline_task` so existing tests and scripts can still isolate local runs.
- Extended `tests/test_main_architecture.py` so `validate_run_request`, `create_series_task`, and `run_series` cannot regress back to `legacy_service` delegation.
- TDD red evidence: `cd apps/api; python -m pytest tests/test_main_architecture.py -q` failed on `legacy().validate_run_request`.
- Focused verification: `cd apps/api; python -m pytest tests/test_main_architecture.py -q` -> `2 passed`.
- Focused verification: `cd apps/api; python -m pytest tests/test_api_flow.py tests/test_fixed_workflow_api_contract.py -q` -> `79 passed`.
- Full verification from repo root: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests -q` -> `642 passed`.

## Result mutation service split

- Moved remaining non-Agent result service paths out of the compatibility bridge: direct DB row reads, BOLD group analysis dispatch, and BOLD descriptive review dispatch now live in `result_service.py`.
- Preserved explicit monkeypatch compatibility for BOLD report runners while avoiding default fallback through `app.main` compatibility exports.
- Added API coverage proving `/projects/{project_id}/bold/group-analysis` and `/projects/{project_id}/bold/descriptive-review` validate project existence and dispatch through `result_service`.
- Extended `tests/test_main_architecture.py` so `result_service.py` cannot import `app.services.compat` or call `legacy()`.
- TDD red evidence: `cd apps/api; python -m pytest tests/test_main_architecture.py -q` failed on `from app.services.compat import legacy` in `result_service.py`.
- Focused verification: `cd apps/api; python -m pytest tests/test_main_architecture.py tests/test_api_flow.py::test_bold_report_routes_dispatch_through_result_service tests/test_bold_group_analysis.py tests/test_bold_descriptive_review.py -q` -> `5 passed`.
- Focused verification: `cd apps/api; python -m pytest tests/test_api_flow.py tests/test_fixed_workflow_api_contract.py -q` -> `80 passed`.
- Full verification from repo root: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests -q` -> `643 passed`.

## Agent RAG/service tool split

- Moved low-risk Agent support endpoints out of the compatibility bridge: RAG status, RAG rebuild, RAG query, and scientific report verification now live directly in `agent_service.py`.
- Preserved Agent/RAG product boundaries: RAG responses still include backend task/output context and grounding policy; scientific report verification remains read-only and checks container-native QC evidence when requested.
- Kept complex Agent execution paths (`agent_run`, `agent_resume`, run lookup/history, and legacy chat compatibility) behind the bridge for a later, narrower migration because they involve ledgers, LLM gateway errors, and task-creation handoff.
- Extended `tests/test_main_architecture.py` so the migrated RAG/verify endpoints cannot regress to `legacy_service` delegation.
- TDD red evidence: `cd apps/api; python -m pytest tests/test_main_architecture.py -q` failed on `legacy().agent_rag_status`.
- Focused verification: `cd apps/api; python -m pytest tests/test_agent_api.py::test_agent_rag_rebuild_endpoint_builds_persistent_index tests/test_agent_api.py::test_agent_rag_status_reports_persistent_index tests/test_agent_api.py::test_agent_rag_status_reports_vendor_raw_source_traceability tests/test_agent_api.py::test_agent_rag_query_launchability_uses_matrix_citations tests/test_agent_api.py::test_agent_rag_query_returns_raw_source_evidence_for_vendor_citations tests/test_verify_scientific_reports.py::test_agent_verify_scientific_reports_endpoint_accepts_task_ids tests/test_verify_scientific_reports.py::test_agent_verify_scientific_reports_endpoint_reports_missing_required_modality tests/test_verify_scientific_reports.py::test_agent_verify_scientific_reports_endpoint_enforces_native_qc_requirement tests/test_verify_scientific_reports.py::test_agent_verify_scientific_reports_endpoint_accepts_native_qc_requirement -q` -> `9 passed`.
- Focused verification: `cd apps/api; python -m pytest tests/test_agent_api.py tests/test_rag_query.py tests/test_verify_scientific_reports.py -q` -> `64 passed`.
- Full verification from repo root: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests -q` -> `643 passed`.

## Agent execution service split

- Moved the remaining Agent execution and compatibility paths out of the compatibility bridge: Agent run, run lookup, resume, project run history, and `/chat` now live directly in `agent_service.py`.
- Preserved the product safety boundary: Agent resume still creates workflow tasks only through deterministic `task_service.create_series_task` after explicit confirmation, and Agent/RAG responses continue to prioritize backend records over retrieved text.
- Preserved privacy-safe Agent ledger behavior by keeping run lifecycle writes in `agent.run_ledger` and response sanitization in `agent.contracts`.
- Preserved `/chat` as a compatibility endpoint whose response points users toward `/agent/runs` while continuing to use backend task/output context and RAG grounding.
- Extended `tests/test_main_architecture.py` so `agent_service.py` cannot import `app.services.compat` or call `legacy()`.
- TDD red evidence: `cd apps/api; python -m pytest tests/test_main_architecture.py -q` failed on `from app.services.compat import legacy` in `agent_service.py`.
- Focused verification: `cd apps/api; python -m pytest tests/test_main_architecture.py -q` -> `2 passed`.
- Focused verification: `cd apps/api; python -m pytest tests/test_agent_api.py::test_agent_run_returns_answer_and_persists_privacy_safe_ledger tests/test_agent_api.py::test_agent_run_response_redacts_nested_backend_paths tests/test_agent_api.py::test_agent_run_requires_message tests/test_agent_api.py::test_agent_run_failure_ledger_redacts_sensitive_error_text tests/test_agent_api.py::test_agent_run_lookup_returns_safe_ledger_trace tests/test_agent_api.py::test_agent_run_lookup_returns_404_for_unknown_run tests/test_agent_api.py::test_project_agent_runs_lists_only_safe_project_history tests/test_agent_api.py::test_project_agent_runs_empty_history_returns_empty_list tests/test_agent_api.py::test_agent_resume_returns_ready_to_launch tests/test_agent_api.py::test_agent_resume_contract_normalizes_unknown_runner_status tests/test_agent_api.py::test_agent_resume_failure_returns_stable_error_contract tests/test_agent_api.py::test_agent_resume_approved_confirmation_creates_real_task tests/test_agent_api.py::test_agent_resume_rejects_confirmation_mismatch -q` -> `13 passed`.
- Focused verification: `cd apps/api; python -m pytest tests/test_agent_api.py -q` -> `32 passed`.
- Full verification initially caught a chat regression in `test_chat_status_uses_requested_task_ids_beyond_recent_limit`: the migrated task-id parser only returned the first Chinese task anchor from `任务41、111、114`.
- Fixed `_requested_task_ids` to keep scanning the anchored message tail for additional numeric task IDs.
- Regression verification: `cd apps/api; python -m pytest tests/test_api_flow.py::test_chat_status_uses_requested_task_ids_beyond_recent_limit tests/test_agent_api.py -q` -> `33 passed`.
- Full verification from repo root: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests -q` -> `643 passed`.

## Main compatibility export cleanup

- Removed `app.main`'s direct import of `legacy_service`; compatibility exports now point at focused services or real domain modules instead of the former monolithic implementation.
- Kept existing monkeypatch-compatible names (`PROJECTS_ROOT`, `REPO_ROOT`, `AgentRunner`, `ModelGateway`, `complete_chat`, `run_pipeline_task`, result/task helpers, etc.) so existing tests and scripts can continue to isolate behavior while the implementation lives behind routes/services.
- Added architecture coverage so `main.py` cannot regress to importing `legacy_service` or using `_legacy`.
- TDD red evidence: `cd apps/api; python -m pytest tests/test_main_architecture.py -q` failed on `legacy_service` in `main.py`.
- Focused verification: `cd apps/api; python -m pytest tests/test_main_architecture.py -q` -> `2 passed`.
- Focused verification: `cd apps/api; python -m pytest tests/test_agent_api.py tests/test_api_flow.py tests/test_fixed_workflow_api_contract.py tests/test_artifact_manifest_api.py -q` -> `113 passed`.
- Full verification from repo root: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests -q` -> `643 passed`.

## Legacy bridge removal

- Removed the transitional `services/compat.py` and `services/legacy_service.py` bridge files after all route/service entrypoints had been moved into focused modules.
- Simplified `dependencies.py` back to a shared API dependency surface instead of a dynamic fallback into the former monolithic service.
- Added architecture coverage so the old bridge files cannot be reintroduced and `dependencies.py` cannot proxy through `services.compat`, `legacy_service`, or `legacy()`.
- Preserved the product boundary: Agent/RAG remains an explanatory and confirmation-driven layer, while deterministic routes/services and container workflow outputs remain the source of truth.
- TDD red evidence: `cd apps/api; python -m pytest tests/test_main_architecture.py -q` failed while `services/compat.py` still existed.
- Focused verification: `cd apps/api; python -m pytest tests/test_main_architecture.py -q` -> `3 passed`.
- Focused verification: `cd apps/api; python -m pytest tests/test_api_flow.py tests/test_agent_api.py tests/test_fixed_workflow_api_contract.py tests/test_artifact_manifest_api.py -q` -> `113 passed`.

## Background execution boundary

- Added `services/background.py` as the single local background execution boundary; the default implementation still uses a daemon `Thread` so behavior stays compatible with the current MVP runtime.
- Moved direct background launch calls in `task_service.py` and `upload_service.py` behind `submit_background(...)`, preparing the codebase for a later remote worker or database queue without changing API contracts now.
- Added architecture coverage so focused services do not directly import or start `Thread(...)`.
- Kept the worker migration intentionally scoped: no Celery/RQ/Dramatiq or local installation was introduced; remote server execution remains the acceptance target for real workflow runs.
- TDD red evidence: `cd apps/api; python -m pytest tests/test_main_architecture.py -q` failed while `services/background.py` was missing.
- Focused verification: `cd apps/api; python -m pytest tests/test_main_architecture.py -q` -> `4 passed`.
- Focused verification: `cd apps/api; python -m pytest tests/test_api_flow.py tests/test_fixed_workflow_api_contract.py -q` -> `80 passed`.

## Runtime override helper boundary

- Added `services/runtime_overrides.py` so focused service modules no longer reach directly into `sys.modules["app.main"]` for monkeypatch-compatible runtime overrides.
- Migrated project, upload, task, result, and Agent services to use `main_patch_attr(...)`, `main_patch_attr_if_changed(...)`, and `main_projects_root(...)` instead of local `_main_attr` helpers.
- Preserved existing test and script compatibility for patched `PROJECTS_ROOT`, workflow runners, Agent runner/gateway, RAG builders, report verifiers, and container inspectors while keeping `main.py` out of service internals.
- Added architecture coverage so service modules cannot regress to direct `app.main` module lookups.
- TDD red evidence: `cd apps/api; python -m pytest tests/test_main_architecture.py -q` failed while `services/runtime_overrides.py` was missing.
- Focused verification: `cd apps/api; python -m pytest tests/test_main_architecture.py -q` -> `5 passed`.
- Focused verification: `cd apps/api; python -m pytest tests/test_api_flow.py tests/test_agent_api.py tests/test_artifact_manifest_api.py tests/test_fixed_workflow_api_contract.py -q` -> `113 passed`.

## DB query helper boundary

- Added `app/db/queries.py` with `fetch_rows(...)` so repeated SQLite row-to-dict read helpers live in the database layer instead of focused service modules.
- Migrated Agent, upload, task, and result services away from private `_rows(...)` helpers while preserving existing SQL and API behavior.
- Updated the `app.main.rows` compatibility export to point at `fetch_rows(...)`, keeping existing tests/scripts stable without reintroducing service-local DB helpers.
- Added architecture coverage so focused services do not define their own `_rows(...)` helpers.
- TDD red evidence: `cd apps/api; python -m pytest tests/test_main_architecture.py -q` failed while `app/db/queries.py` was missing.
- Regression evidence: the first focused API run caught `app.main.rows = task_service._rows`; fixed by exporting `fetch_rows`.
- Focused verification: `cd apps/api; python -m pytest tests/test_main_architecture.py -q` -> `6 passed`.
- Focused verification: `cd apps/api; python -m pytest tests/test_api_flow.py tests/test_agent_api.py tests/test_artifact_manifest_api.py tests/test_fixed_workflow_api_contract.py -q` -> `113 passed`.

## Main compatibility export boundary

- Added `app/main_compat.py` to own the compatibility exports that existing tests and scripts monkeypatch through `app.main`.
- Reduced `main.py` to FastAPI app assembly, route registration, startup, validation error handling, and one compatibility installer call.
- Preserved `app.main` names such as `AgentRunner`, `ModelGateway`, `complete_chat`, `PROJECTS_ROOT`, `REPO_ROOT`, `run_pipeline_task`, `rows`, and request models while moving their import details out of the entrypoint.
- Added architecture coverage so direct Agent/workflow/schema compatibility imports do not drift back into `main.py`.
- TDD red evidence: `cd apps/api; python -m pytest tests/test_main_architecture.py -q` failed while `app/main_compat.py` was missing.
- Compatibility probe: importing `app.main` confirmed `AgentRunner`, `PROJECTS_ROOT`, `REPO_ROOT`, `run_pipeline_task`, `complete_chat`, `rows`, `RunRequest`, and `WORKFLOWS` remain available.
- Focused verification: `cd apps/api; python -m pytest tests/test_main_architecture.py tests/test_agent_api.py tests/test_api_flow.py tests/test_fixed_workflow_api_contract.py tests/test_artifact_manifest_api.py tests/test_verify_scientific_reports.py -q` -> `138 passed`.

## App hooks boundary

- Added `app/app_hooks.py` to own application startup and request-validation error handler registration.
- Reduced `main.py` to app construction, middleware/router registration, hook registration, and compatibility export installation; it no longer imports database initialization or Agent API error-contract helpers directly.
- Preserved the Agent API validation boundary: `/agent/runs` request-contract violations still return the stable `agent_api_error.v1` payload, while other routes keep FastAPI's default 422 behavior.
- Added architecture coverage so startup/error handler details do not drift back into `main.py`.
- TDD red evidence: `cd apps/api; python -m pytest tests/test_main_architecture.py -q` failed while `app/app_hooks.py` was missing.
- Focused verification: `cd apps/api; python -m pytest tests/test_main_architecture.py -q` -> `8 passed`.
- Focused verification: `cd apps/api; python -m pytest tests/test_agent_api.py::test_agent_run_rejects_unknown_request_fields_with_stable_error tests/test_agent_api.py::test_agent_resume_rejects_unknown_request_fields_with_stable_error tests/test_agent_api.py::test_agent_request_validation_handler_preserves_default_errors_for_other_routes tests/test_agent_api.py::test_agent_run_requires_message tests/test_api_flow.py::test_health_returns_app_identity tests/test_api_flow.py::test_workflow_catalog_exposes_implemented_workflows -q` -> `6 passed`.

## ASGI app factory boundary

- Added `app/app_factory.py` to own FastAPI app creation, CORS middleware setup, route registration, and hook registration.
- Reduced `main.py` to the ASGI exposure point: import `create_app`, construct `app`, and install `app.main` compatibility exports.
- Preserved existing route availability, OpenAPI contract generation, Agent validation error contract, and `app.main` monkeypatch-compatible names.
- Added architecture coverage so FastAPI construction, CORS, route registration, and hook registration do not drift back into `main.py`.
- TDD red evidence: `cd apps/api; python -m pytest tests/test_main_architecture.py -q` failed while `app/app_factory.py` was missing.
- Compatibility probe: `TestClient(main.app)` confirmed `/health`, `/workflows`, OpenAPI path generation, and key `app.main` compatibility names still work.
- Focused verification: `cd apps/api; python -m pytest tests/test_main_architecture.py -q` -> `9 passed`.
- Focused verification: `cd apps/api; python -m pytest tests/test_api_flow.py::test_health_returns_app_identity tests/test_api_flow.py::test_workflow_catalog_exposes_implemented_workflows tests/test_agent_api.py::test_agent_api_openapi_declares_stable_response_contracts tests/test_agent_api.py::test_agent_run_rejects_unknown_request_fields_with_stable_error tests/test_agent_api.py::test_agent_request_validation_handler_preserves_default_errors_for_other_routes -q` -> `5 passed`.

## Imaging series parser boundary

- Added `app/imaging/series_records.py` to own imaging-series row normalization, metadata JSON decoding, file-context enrichment, and workflow eligibility attachment.
- Removed duplicated `_parse_series_row(...)` implementations from `task_service.py` and `upload_service.py`; both services now reuse `parse_series_row(...)`.
- Preserved the compatibility export `app.main.parse_series_row` by pointing `main_compat.py` at the shared imaging helper instead of a task-service private function.
- Kept the Agent/product safety boundary unchanged: this refactor only centralizes deterministic backend record formatting and does not let Agent/RAG become a workflow fact source or direct task executor.
- TDD red evidence: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_main_architecture.py -q` failed while `app/imaging/series_records.py` was missing.
- Focused verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_main_architecture.py -q` -> `10 passed`.
- Focused verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_api_flow.py apps/api/tests/test_fixed_workflow_api_contract.py -q` -> `80 passed, 3 warnings`.

## Project existence boundary

- Added `project_service.require_project(...)` as the shared project-existence guard for service-layer workflows that operate under a project.
- Removed duplicated project lookup and `Project not found` handling from `upload_service.py` and `result_service.py`; upload/session creation and BOLD review/group analysis now call the project service boundary.
- Added architecture coverage so upload/result services cannot drift back to local `SELECT * FROM projects WHERE id=?` checks.
- Preserved the existing API behavior and 404 error message while making the service ownership clearer for future development.
- TDD red evidence: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_main_architecture.py -q` failed while `project_service.require_project(...)` was missing.
- Focused verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_main_architecture.py -q` -> `11 passed`.
- Focused verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_api_flow.py apps/api/tests/test_artifact_manifest_api.py apps/api/tests/test_fixed_workflow_api_contract.py -q` -> `81 passed, 3 warnings`.

## Agent confirmation store precedence

- Full API verification caught `test_agent_runner_resume_blocks_expired_pending_confirmation`: an expired JSON confirmation could be ignored because `AgentThreadStore.load(...)` preferred the SQLite persistence copy over the local JSON record.
- Root cause: SQLite was introduced as a persistence/fallback store, but the loader read it before the JSON thread file; tests and behavior expect SQLite to be used when JSON is missing.
- Updated `AgentThreadStore.load(...)` to prefer the JSON record when present and fall back to SQLite only when the JSON record is absent.
- This preserves the Agent safety boundary: expired server-side confirmations remain blocked instead of becoming launch-ready through a stale persistence copy.
- Regression verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_agent_graph.py::test_agent_runner_resume_blocks_expired_pending_confirmation apps/api/tests/test_agent_graph.py::test_agent_thread_store_loads_pending_confirmation_from_sqlite_after_json_missing apps/api/tests/test_agent_graph.py::test_agent_thread_store_marks_confirmation_transition_in_sqlite -q` -> `3 passed`.
- Focused Agent API verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_agent_api.py::test_agent_resume_approved_confirmation_creates_real_task apps/api/tests/test_agent_api.py::test_agent_resume_rejects_confirmation_mismatch apps/api/tests/test_agent_api.py::test_agent_resume_failure_returns_stable_error_contract -q` -> `3 passed, 3 warnings`.

## DWI sidecar imaging boundary

- Added `app/imaging/dwi_sidecars.py` to own DWI sidecar discovery, same-stem BIDS fallback, and eddy JSON metadata checks.
- Removed DWI sidecar helper implementations from `task_service.py`; task validation now calls `dwi_has_required_sidecars(...)` from the imaging layer.
- Kept workflow execution behavior unchanged: `dwi_fast_gpu_dti` still requires DWI modality plus bval, bvec, and JSON sidecar metadata with `PhaseEncodingDirection` and `TotalReadoutTime`.
- Added architecture coverage so task service does not drift back to maintaining private DWI file-detection helpers.
- TDD red evidence: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_main_architecture.py -q` failed while `app/imaging/dwi_sidecars.py` was missing.
- Focused verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_main_architecture.py -q` -> `12 passed`.
- Focused verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_dwi_fast_dti.py apps/api/tests/test_fixed_workflow_api_contract.py apps/api/tests/test_api_flow.py -q` -> `100 passed, 3 warnings`.

## Agent backend context boundary

- Added `app/agent/backend_context.py` to own backend task, output, series, and result-summary context queries used by Agent chat and RAG responses.
- Moved chat/RAG backend fact collection out of `agent_service.py`; the service now focuses on endpoint orchestration, ledger handling, model/RAG invocation, and response shaping.
- Preserved the product boundary: Agent/RAG still answers from deterministic backend records first and uses retrieved references only as supporting context; task creation remains behind explicit resume confirmation and deterministic task services.
- Added architecture coverage so `_task_context(...)`, `_output_context(...)`, and `_result_summary_context(...)` do not drift back into `agent_service.py`.
- TDD red evidence: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_main_architecture.py -q` failed while `app/agent/backend_context.py` was missing.
- Focused verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_main_architecture.py -q` -> `13 passed`.
- Focused verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_agent_api.py apps/api/tests/test_rag_query.py apps/api/tests/test_api_flow.py::test_chat_status_uses_requested_task_ids_beyond_recent_limit -q` -> `47 passed, 3 warnings`.

## QSIPrep output workflow boundary

- Added `app/workflows/qsiprep_outputs.py` to own QSIPrep derivative-output discovery and anat-derivative checks used before QSIRecon launch.
- Removed `_qsiprep_output_dir(...)` and `_qsiprep_output_has_anat(...)` from `task_service.py`; task validation now calls `qsiprep_output_has_anat(...)` from the workflows layer.
- Kept the Agent/task safety boundary unchanged: QSIRecon still requires an explicit `qsiprep_task_id`, completed QSIPrep task status, and QSIPrep output containing subject anat derivatives.
- Added architecture coverage so workflow output filesystem checks do not drift back into the task service.
- TDD red evidence: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_main_architecture.py -q` failed while `app/workflows/qsiprep_outputs.py` was missing.
- Focused verification initially exposed QSIRECON profile test contamination: `test_qsirecon_profile_contract.py` left `pipeline.QSIRECON_PROFILE='typo_profile'` after the invalid-profile test.
- Fixed the test isolation by reloading pipeline defaults after the invalid-profile assertion.
- Focused verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_fixed_workflow_api_contract.py apps/api/tests/test_qsirecon_profile_contract.py apps/api/tests/test_api_flow.py -q` -> `84 passed, 3 warnings`.
- Focused verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_main_architecture.py -q` -> `14 passed`.

## Artifact response route boundary

- Moved FastAPI `FileResponse` ownership from `result_service.py` into `routes/results.py`.
- Added `result_service.resolve_task_artifact(...)` so the service layer validates artifact path safety and returns only a resolved file path plus media type.
- Preserved the existing artifact endpoint behavior, including slash-relative path traversal protection, Windows-style backslash rejection, 404 handling, and `.nii.gz` media type handling.
- Kept `result_service.get_task_artifact(...)` as a thin compatibility wrapper returning the resolved artifact descriptor while routes use the explicit resolver.
- Added architecture coverage so HTTP file responses do not drift back into the result service.
- TDD red evidence: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_main_architecture.py -q` failed while `result_service.py` still imported `FileResponse`.
- Focused verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_main_architecture.py -q` -> `15 passed`.
- Focused verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_artifact_manifest_api.py apps/api/tests/test_fixed_workflow_api_contract.py::test_task_artifact_endpoint_serves_files_inside_output_dir -q` -> `2 passed, 3 warnings`.
- Full API verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests -q` -> `656 passed, 3 warnings`.

## Upload file storage boundary

- Added `app/storage/upload_files.py` as the storage boundary for streamed upload persistence, safe filename normalization, sha256 calculation, file-type inference, and `files` table registration.
- Reduced `upload_service.py` so upload workflows call `save_project_upload(...)` or `save_stream_to_path(...)` instead of directly looping over `UploadFile.file.read(...)`.
- Preserved upload behavior for regular NIfTI uploads, DWI sidecar uploads, DICOM zip uploads, and dataset ingest archives.
- Added a focused storage test proving sanitized filename persistence, stored bytes, size, sha256, inferred `NIFTI` type, and DB row fields.
- Added architecture coverage so raw upload file persistence does not drift back into `upload_service.py`.
- TDD red evidence: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_main_architecture.py::test_upload_file_persistence_lives_in_storage_layer -q` failed while `app/storage/upload_files.py` was missing.
- TDD red evidence: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_upload_file_storage.py -q` failed with `ModuleNotFoundError: No module named 'app.storage'`.
- Focused verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_main_architecture.py apps/api/tests/test_upload_file_storage.py -q` -> `17 passed`.
- Focused upload verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_api_flow.py::test_pipeline_bids_preserves_uncompressed_nifti_and_sidecars apps/api/tests/test_api_flow.py::test_dwi_stage_copies_uploaded_json_sidecar_by_file_id apps/api/tests/test_api_flow.py::test_mixed_dataset_ingest_inventory_and_bids apps/api/tests/test_fixed_workflow_api_contract.py::test_dwi_fast_gpu_dti_requires_json_sidecar_metadata apps/api/tests/test_fixed_workflow_api_contract.py::test_dwi_stage_copies_uploaded_json_sidecar_by_file_id -q` -> `5 passed, 3 warnings`.
- Full API verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests -q` -> `658 passed, 3 warnings`.

## Agent status boundary

- Added `app/agent/status.py` to own safe model-provider status, RAG index health, vendor raw-source traceability status, vendor pointer integrity, vendor coverage catalog, and RAG rebuild orchestration.
- Reduced `agent_service.py` so `/agent/model/status`, `/agent/rag/status`, `/agent/rag/rebuild`, and `/deployment` delegate to the Agent layer status boundary.
- Preserved the Agent/RAG product boundary: status endpoints remain read-only, safe, and do not expose API keys, reverse-tunnel commands, raw source HTML, absolute local paths, or raw snapshots in public vendor coverage catalog output.
- Added architecture coverage so model provider status and RAG health assembly do not drift back into `agent_service.py`.
- TDD red evidence: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_main_architecture.py::test_agent_status_and_rag_health_live_in_agent_layer -q` failed while `app/agent/status.py` was missing.
- Focused status verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_agent_api.py::test_agent_model_status_uses_model_gateway apps/api/tests/test_agent_api.py::test_deployment_status_uses_safe_agent_model_summary apps/api/tests/test_agent_api.py::test_agent_rag_rebuild_endpoint_builds_persistent_index apps/api/tests/test_agent_api.py::test_agent_rag_status_reports_persistent_index apps/api/tests/test_agent_api.py::test_agent_rag_status_reports_vendor_raw_source_traceability -q` -> `5 passed, 3 warnings`.
- Focused Agent/RAG verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_agent_api.py apps/api/tests/test_rag_query.py apps/api/tests/test_api_flow.py::test_chat_status_uses_requested_task_ids_beyond_recent_limit apps/api/tests/test_api_flow.py::test_deepseek_chat_provider_can_be_used -q` -> `48 passed, 3 warnings`.
- Architecture verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_main_architecture.py -q` -> `17 passed`.
- Full API verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests -q` -> `659 passed, 3 warnings`.

## Legacy chat runtime boundary

- Added `app/agent/chat.py` to own the legacy `/chat` compatibility runtime: backend context assembly, RAG response use, OpenAI SDK model gateway call, DeepSeek fallback, rules-based replies, chat message persistence, and `chat_compat.v1` response shaping.
- Reduced `agent_service.chat(...)` to a thin delegation boundary that passes repo root, projects root, and workflow registry context into the Agent-layer chat runtime.
- Preserved the Agent/RAG product boundary: legacy chat still answers from backend records first, uses RAG as supporting context, keeps launchability/status answers rules-based, and does not bypass deterministic task APIs.
- Added architecture coverage so legacy chat runtime details do not drift back into `agent_service.py`.
- TDD red evidence: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_main_architecture.py::test_legacy_chat_runtime_lives_in_agent_layer -q` failed while `app/agent/chat.py` was missing.
- Debugging note: the first wide architecture run exposed a stale assertion that still expected `build_chat_backend_context` in `agent_service.py`; the assertion now tracks the new boundary by requiring chat context assembly in `app/agent/chat.py` while `/agent/rag/query` keeps `build_rag_backend_context` in service orchestration.
- Focused chat verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_agent_api.py::test_legacy_chat_prefers_openai_model_gateway_for_freeform_answers apps/api/tests/test_agent_api.py::test_chat_launchability_uses_rules_and_does_not_call_model_gateway apps/api/tests/test_api_flow.py::test_deepseek_chat_provider_can_be_used apps/api/tests/test_api_flow.py::test_chat_status_uses_requested_task_ids_beyond_recent_limit apps/api/tests/test_api_flow.py::test_chat_mentions_real_bold_metric_outputs_after_implementation apps/api/tests/test_api_flow.py::test_chat_exposes_intent_and_next_step_hints -q` -> `6 passed, 3 warnings`.
- Focused Agent/RAG verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests/test_main_architecture.py apps/api/tests/test_agent_api.py apps/api/tests/test_rag_query.py -q` -> `64 passed, 3 warnings`.
- Full API verification: `$env:PYTHONPATH='.;apps/api;apps/api/tests'; python -m pytest apps/api/tests -q` -> `660 passed, 3 warnings`.
