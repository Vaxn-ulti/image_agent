- Hardened Agent read-only chat routing for frontend project conversations:
  - reproduced a real regression where the Chinese phrase "do not start t1_deepprep_anat_report" was incorrectly treated as a fixed-workflow launch intent and returned `confirmation_required`;
  - added regression coverage for negated Chinese launch requests so they stay `answered`, keep `production_task_created=false`, and do not expose a confirmation payload;
  - expanded the fixed-workflow confirmation gate so negated launch phrases in English and Chinese take priority over launch verbs;
  - tightened `/agent/runs` serialization with `response_model_exclude_none=True`, preventing `answered` responses from carrying empty `confirmation` fields into the console;
  - annotated read-only API responses as `inventory_capability` or `result_analysis` when the user asks about uploads, runnable workflows, or completed results, while preserving the internal LangGraph read-only lane;
  - verified backend Agent graph/API/tool tests and focused console Agent/Dashboard tests passed locally;
  - no production task creation, workflow rerun, ObserveRepair rerun, Docker pull, remote server mutation, API key handling, or private deployment configuration change was performed.
