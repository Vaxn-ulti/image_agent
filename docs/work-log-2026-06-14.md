# Image Agent Work Log - 2026-06-14

## Stale-task verifier report freshness evidence

Time: 2026-06-14T00:15:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to stale-task verifier scripts, tests, and work log only; no local install, local service run, local container run, remote install, remote restart, remote model smoke, remote stale-task apply, or remote workflow execution was performed.
- Hardened `apps/api/scripts/verify_stale_task_approval.py` and `apps/api/scripts/verify_stale_task_resolution.py` so the JSON verifier reports include the effective freshness limit used during validation.
- Approval reports now include `checked.max_age_hours`; resolution reports include `checked.max_age_hours` and `checked.resolution_max_age_hours`.
- This makes saved stale-task verifier output auditable when the operator runs `--max-age-hours 24` before normal restart.

Verification:

- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_verify_stale_task_approval.py::test_verify_stale_task_approval_cli_prints_passed_report apps/api/tests/test_verify_stale_task_resolution.py::test_verify_stale_task_resolution_cli_prints_passed_report -q` failed with missing `checked.max_age_hours` in both verifier reports.
- GREEN:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_verify_stale_task_approval.py::test_verify_stale_task_approval_cli_prints_passed_report apps/api/tests/test_verify_stale_task_resolution.py::test_verify_stale_task_resolution_cli_prints_passed_report -q` -> `2 passed`.
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_verify_stale_task_approval.py apps/api/tests/test_verify_stale_task_resolution.py -q` -> `30 passed`.
  - `python -m py_compile apps/api/scripts/verify_stale_task_approval.py apps/api/scripts/verify_stale_task_resolution.py apps/api/tests/test_verify_stale_task_approval.py apps/api/tests/test_verify_stale_task_resolution.py` -> passed.
  - `git diff --check` -> passed.

Release gate:

- Frontend design remains blocked.
- This improves stale-task cleanup evidence auditability, but final readiness still requires approved remote apply for tasks `83` and `84`, a clean path-safe post-apply dry-run report, normal remote restart without active-task override, and fresh strict remote acceptance evidence within 24 hours.

## Strict smoke verifier report freshness evidence

Time: 2026-06-14T00:30:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to strict smoke verifier scripts, tests, and work log only; no local install, local service run, local container run, remote install, remote restart, remote model smoke, remote stale-task apply, or remote workflow execution was performed.
- Hardened `apps/api/scripts/verify_remote_smoke_acceptance.py` so passing verifier reports include `checked.max_age_hours` when `--max-age-hours` is supplied.
- This makes saved strict remote acceptance verifier output prove that the 24-hour freshness gate was applied, instead of only implying it from the command line.

Verification:

- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_cli_prints_passed_report -q` failed with missing `checked.max_age_hours`.
- GREEN:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_cli_prints_passed_report -q` -> `1 passed`.
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py -q` -> `103 passed`.
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_strict_output_passes_offline_acceptance_verifier -q` -> `104 passed`.
  - `python -m py_compile apps/api/scripts/verify_remote_smoke_acceptance.py apps/api/tests/test_verify_remote_smoke_acceptance.py` -> passed.
  - `git diff --check` -> passed.

Release gate:

- Frontend design remains blocked.
- This improves strict remote acceptance report auditability, but final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean path-safe post-apply dry-run report, normal remote restart without active-task override, and fresh strict remote acceptance evidence within 24 hours.

## Machine-checkable remote release gate command plan

Time: 2026-06-14T15:20:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Added `docs/deployment/remote-release-gate-command-plan.json` as the ordered post-authorization command plan for tasks `83` and `84`.
- Added `apps/api/scripts/verify_release_gate_command_plan.py` so the command plan can be machine-checked before an operator uses it.
- The plan encodes fresh stale-task approval verification, operator-approved apply, post-apply clean dry-run, resolution verification, restart preflight-only, normal restart without active-task override, strict remote smoke, and offline strict smoke JSON verification.
- Updated `docs/product-readiness.md` and `docs/deployment/remote-agent-production.md` so the plan is treated as a guardrail, not as acceptance evidence.
- No production task rows were mutated, no `--apply` command was run, no live restart was attempted, no live smoke run was started, and no push was performed.

Verification:

- RED:
  - `$env:PYTHONPATH='apps/api'; python -m pytest apps/api/tests/test_release_gate_command_plan.py -q` first failed because `apps/api/scripts/verify_release_gate_command_plan.py` and `docs/deployment/remote-release-gate-command-plan.json` did not exist.
  - `$env:PYTHONPATH='apps/api'; python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_product_readiness_gate_blocks_frontend_until_agent_contracts_are_verified -q` failed because product readiness did not mention the new command plan.
- GREEN:
  - `$env:PYTHONPATH='apps/api'; python -m pytest apps/api/tests/test_release_gate_command_plan.py -q` -> `3 passed`.
  - `$env:PYTHONPATH='apps/api'; python -m pytest apps/api/tests/test_release_gate_command_plan.py apps/api/tests/test_skill_and_rag_docs.py::test_product_readiness_gate_blocks_frontend_until_agent_contracts_are_verified -q` -> `4 passed`.
  - `python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` -> `status=passed`.
  - `python -m py_compile apps/api/scripts/verify_release_gate_command_plan.py apps/api/tests/test_release_gate_command_plan.py apps/api/tests/test_skill_and_rag_docs.py` -> passed.

Release gate:

- Frontend design remains blocked.
- This reduces operator error risk for the next approved remote step, but final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean post-apply dry-run verified with `--require-empty-active --max-age-hours 24`, a preflight-only restart that reaches `restart_preflight:ok`, a normal restart without `IMAGE_AGENT_ALLOW_RESTART_WITH_ACTIVE_TASKS=1`, and fresh strict remote acceptance evidence within 24 hours.

## Remote command-plan overlay verification

Time: 2026-06-14T15:35:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Packaged local commit `dc1bdaf9` command-plan files with `git archive`.
- Uploaded the package to `/tmp/image_agent_release_gate_plan_dc1bdaf9.tar` on the remote server.
- Overlaid the package onto the non-live remote verifier directory `/home/yyf/project/image_agent_releases/codex-gate-verifiers-efca895b-20260613T165132`.
- No production task rows were mutated, no `--apply` command was run, no live restart was attempted, no live smoke run was started, and no push was performed.

Verification:

- Remote overlay sync check:
  - `test -f apps/api/scripts/verify_release_gate_command_plan.py && test -f docs/deployment/remote-release-gate-command-plan.json` -> `overlay_plan_synced`.
- Remote command-plan verifier:
  - `/home/yyf/project/image_agent/apps/api/.venv/bin/python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` -> `status=passed`, `checked.plan_id=remote_release_gate_after_stale_task_approval_v1`, `checked.step_count=8`, `checked.frontend_gate_status=blocked`.
- Remote focused test slice:
  - `PYTHONPATH=. /home/yyf/project/image_agent/apps/api/.venv/bin/python -m pytest tests/test_release_gate_command_plan.py tests/test_skill_and_rag_docs.py::test_product_readiness_gate_blocks_frontend_until_agent_contracts_are_verified -q` -> `4 passed`.

Release gate:

- Frontend design remains blocked.
- The remote verifier overlay now has the command plan and verifier needed for the next approved operator step, but final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean post-apply dry-run verified with `--require-empty-active --max-age-hours 24`, a preflight-only restart that reaches `restart_preflight:ok`, a normal restart without `IMAGE_AGENT_ALLOW_RESTART_WITH_ACTIVE_TASKS=1`, and fresh strict remote acceptance evidence within 24 hours.

## Stale-task approval refresh command plan

Time: 2026-06-14T15:50:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Added a `stale_task_approval_refresh` block to `docs/deployment/remote-release-gate-command-plan.json`.
- Hardened `apps/api/scripts/verify_release_gate_command_plan.py` so the plan must include a read-only approval refresh command for `approval_json_missing_or_older_than_24h`.
- The refresh command runs `reconcile_stale_tasks.py --max-age-hours 24 --check-containers --task-id 83 --task-id 84` and writes a new `/tmp/image_agent_stale_tasks_83_84_dry_run_<timestamp>.json`.
- The plan requires operator review of the refreshed dry-run JSON and `approval_fingerprint` before that JSON can replace the apply approval input.
- No production task rows were mutated, no `--apply` command was run, no live restart was attempted, no live smoke run was started, and no push was performed.

Verification:

- RED:
  - `$env:PYTHONPATH='apps/api'; python -m pytest apps/api/tests/test_release_gate_command_plan.py -q` failed with `KeyError: 'stale_task_approval_refresh'`.
- GREEN:
  - `$env:PYTHONPATH='apps/api'; python -m pytest apps/api/tests/test_release_gate_command_plan.py -q` -> `4 passed`.
  - `$env:PYTHONPATH='apps/api'; python -m pytest apps/api/tests/test_release_gate_command_plan.py apps/api/tests/test_skill_and_rag_docs.py::test_product_readiness_gate_blocks_frontend_until_agent_contracts_are_verified -q` -> `5 passed`.
  - `python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` -> `status=passed`, `checked.approval_refresh_required_when=approval_json_missing_or_older_than_24h`.
  - `python -m py_compile apps/api/scripts/verify_release_gate_command_plan.py apps/api/tests/test_release_gate_command_plan.py apps/api/tests/test_skill_and_rag_docs.py` -> passed.

Release gate:

- Frontend design remains blocked.
- This prevents the operator from relying on stale approval evidence, but final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean post-apply dry-run verified with `--require-empty-active --max-age-hours 24`, a preflight-only restart that reaches `restart_preflight:ok`, a normal restart without `IMAGE_AGENT_ALLOW_RESTART_WITH_ACTIVE_TASKS=1`, and fresh strict remote acceptance evidence within 24 hours.

## Remote approval-refresh plan overlay verification

Time: 2026-06-14T16:05:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Packaged local commit `cd9f4a3a` command-plan refresh files with `git archive`.
- Uploaded the package to `/tmp/image_agent_release_gate_refresh_cd9f4a3a.tar` on the remote server.
- Overlaid the package onto the non-live remote verifier directory `/home/yyf/project/image_agent_releases/codex-gate-verifiers-efca895b-20260613T165132`.
- No production task rows were mutated, no `--apply` command was run, no live restart was attempted, no live smoke run was started, and no push was performed.

Verification:

- Remote overlay sync check:
  - overlay command returned `overlay_refresh_plan_synced`.
- Remote command-plan verifier:
  - `/home/yyf/project/image_agent/apps/api/.venv/bin/python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` -> `status=passed`, `checked.approval_refresh_required_when=approval_json_missing_or_older_than_24h`, `checked.frontend_gate_status=blocked`.
- Remote focused test slice:
  - `PYTHONPATH=. /home/yyf/project/image_agent/apps/api/.venv/bin/python -m pytest tests/test_release_gate_command_plan.py tests/test_skill_and_rag_docs.py::test_product_readiness_gate_blocks_frontend_until_agent_contracts_are_verified -q` -> `5 passed`.

Release gate:

- Frontend design remains blocked.
- The remote verifier overlay now includes a read-only approval refresh path for stale or missing approval JSON, but final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean post-apply dry-run verified with `--require-empty-active --max-age-hours 24`, a preflight-only restart that reaches `restart_preflight:ok`, a normal restart without `IMAGE_AGENT_ALLOW_RESTART_WITH_ACTIVE_TASKS=1`, and fresh strict remote acceptance evidence within 24 hours.

## Fresh stale-task approval refresh evidence

Time: 2026-06-14T16:10:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Exercised the read-only `stale_task_approval_refresh` path on the remote verifier overlay.
- First attempt exposed a plan bug: Docker label checks failed with `RuntimeError: IMAGE_AGENT_SUDO_PASSWORD is required for docker commands` because the machine-readable command plan did not load `/home/yyf/project/image_agent/.env`.
- Added tests and verifier checks requiring `set -a; . /home/yyf/project/image_agent/.env; set +a` on the refresh, apply, and post-apply dry-run commands.
- Updated `docs/deployment/remote-release-gate-command-plan.json` so the current `approval_json` points to the refreshed evidence `/tmp/image_agent_stale_tasks_83_84_dry_run_20260614T080202Z.json`.
- No production task rows were mutated, no `--apply` command was run, no live restart was attempted, no live smoke run was started, and no push was performed.

Verification:

- RED:
  - The remote refresh command without env loading failed with `IMAGE_AGENT_SUDO_PASSWORD is required for docker commands`.
  - `$env:PYTHONPATH='apps/api'; python -m pytest apps/api/tests/test_release_gate_command_plan.py -q` failed because reconcile commands did not include `set -a; . /home/yyf/project/image_agent/.env; set +a`.
  - `$env:PYTHONPATH='apps/api'; python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_product_readiness_gate_blocks_frontend_until_agent_contracts_are_verified -q` failed because readiness did not mention the refreshed approval JSON.
- GREEN:
  - Remote read-only refresh command with env loading produced `REFRESH_JSON=/tmp/image_agent_stale_tasks_83_84_dry_run_20260614T080202Z.json`.
  - Remote `verify_stale_task_approval.py /tmp/image_agent_stale_tasks_83_84_dry_run_20260614T080202Z.json --task-id 83 --task-id 84 --max-age-hours 24` -> `status=passed`, `checked.generated_at_utc=2026-06-14T08:02:02.843606+00:00`, `checked.container_check_status=passed`, `checked.approval_fingerprint=139113571daf0137a3e34be526fd25ccaa8066aed725ab7c0b846cfc7eb3abd0`.

Release gate:

- Frontend design remains blocked.
- Fresh operator-review evidence exists again, but approved remote stale-task apply has not been run; final readiness still requires approved apply for tasks `83` and `84`, a clean post-apply dry-run verified with `--require-empty-active --max-age-hours 24`, a preflight-only restart that reaches `restart_preflight:ok`, a normal restart without `IMAGE_AGENT_ALLOW_RESTART_WITH_ACTIVE_TASKS=1`, and fresh strict remote acceptance evidence within 24 hours.

## Remote fresh-approval plan overlay verification

Time: 2026-06-14T16:20:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Packaged local commit `dc5e9471` fresh-approval plan files with `git archive`.
- Uploaded the package to `/tmp/image_agent_fresh_approval_dc5e9471.tar` on the remote server.
- Overlaid the package onto the non-live remote verifier directory `/home/yyf/project/image_agent_releases/codex-gate-verifiers-efca895b-20260613T165132`.
- No production task rows were mutated, no `--apply` command was run, no live restart was attempted, no live smoke run was started, and no push was performed.

Verification:

- Remote overlay sync check:
  - overlay command returned `overlay_fresh_approval_plan_synced`.
- Remote command-plan verifier:
  - `/home/yyf/project/image_agent/apps/api/.venv/bin/python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` -> `status=passed`, `checked.approval_refresh_required_when=approval_json_missing_or_older_than_24h`, `checked.frontend_gate_status=blocked`.
- Remote focused test slice:
  - `PYTHONPATH=. /home/yyf/project/image_agent/apps/api/.venv/bin/python -m pytest tests/test_release_gate_command_plan.py tests/test_skill_and_rag_docs.py::test_product_readiness_gate_blocks_frontend_until_agent_contracts_are_verified -q` -> `6 passed`.

Release gate:

- Frontend design remains blocked.
- The remote verifier overlay now points at the refreshed approval evidence and loads remote `.env` for Docker label checks, but final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean post-apply dry-run verified with `--require-empty-active --max-age-hours 24`, a preflight-only restart that reaches `restart_preflight:ok`, a normal restart without `IMAGE_AGENT_ALLOW_RESTART_WITH_ACTIVE_TASKS=1`, and fresh strict remote acceptance evidence within 24 hours.

## Stale-task apply approval request generator

Time: 2026-06-14T16:35:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Added `apps/api/scripts/build_stale_task_apply_request.py` to build a machine-readable `stale_task_apply_approval` handoff from a verified stale-task approval dry-run JSON.
- Added focused tests in `apps/api/tests/test_build_stale_task_apply_request.py`.
- The generated request includes `authorization_required=true`, the reviewed `approval_fingerprint`, target task ids, exact env-loading apply command, post-apply clean verification, preflight-only restart, normal restart, strict remote smoke, and offline strict smoke JSON verification.
- Updated `docs/deployment/remote-agent-production.md` and `docs/product-readiness.md` so operators know the request generator prepares approval context but does not run `--apply`.
- No production task rows were mutated, no `--apply` command was run, no live restart was attempted, no live smoke run was started, and no push was performed.

Verification:

- RED:
  - `$env:PYTHONPATH='apps/api/tests'; python -m pytest apps/api/tests/test_build_stale_task_apply_request.py -q` failed because `apps/api/scripts/build_stale_task_apply_request.py` did not exist.
  - `$env:PYTHONPATH='apps/api'; python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_product_readiness_gate_blocks_frontend_until_agent_contracts_are_verified -q` failed because readiness did not mention `build_stale_task_apply_request.py`.
- GREEN:
  - `$env:PYTHONPATH='apps/api/tests'; python -m pytest apps/api/tests/test_build_stale_task_apply_request.py -q` -> `3 passed`.

Release gate:

- Frontend design remains blocked.
- This prepares a safer human authorization packet, but final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean post-apply dry-run verified with `--require-empty-active --max-age-hours 24`, a preflight-only restart that reaches `restart_preflight:ok`, a normal restart without `IMAGE_AGENT_ALLOW_RESTART_WITH_ACTIVE_TASKS=1`, and fresh strict remote acceptance evidence within 24 hours.

## Remote stale-task apply approval request evidence

Time: 2026-06-14T16:50:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Packaged local commit `8d800449` approval-request files with `git archive`.
- Uploaded the package to `/tmp/image_agent_apply_request_8d800449.tar` on the remote server.
- Overlaid the package onto the non-live remote verifier directory `/home/yyf/project/image_agent_releases/codex-gate-verifiers-efca895b-20260613T165132`.
- Generated `/tmp/image_agent_stale_tasks_83_84_apply_request_20260614T080202Z.json` from the fresh approval dry-run JSON.
- No production task rows were mutated, no `--apply` command was run, no live restart was attempted, no live smoke run was started, and no push was performed.

Verification:

- Remote overlay sync check:
  - overlay command returned `overlay_apply_request_complete_synced`.
- Remote focused test slice:
  - `PYTHONPATH=tests /home/yyf/project/image_agent/apps/api/.venv/bin/python -m pytest tests/test_build_stale_task_apply_request.py -q` -> `3 passed`.
- Remote approval request generation:
  - `scripts/build_stale_task_apply_request.py /tmp/image_agent_stale_tasks_83_84_dry_run_20260614T080202Z.json --task-id 83 --task-id 84 --max-age-hours 24 --output-timestamp 20260614T080202Z --output-json /tmp/image_agent_stale_tasks_83_84_apply_request_20260614T080202Z.json` -> wrote request JSON.
  - Saved request summary: `status=operator_authorization_required`, `request_type=stale_task_apply_approval`, `authorization_required=true`, `approval_fingerprint=139113571daf0137a3e34be526fd25ccaa8066aed725ab7c0b846cfc7eb3abd0`.
  - Saved request follow-up ids: `collect_post_apply_clean_dry_run`, `verify_post_apply_clean_resolution`, `restart_api_preflight_only`, `restart_api_normally`, `run_strict_remote_smoke_acceptance`, `verify_strict_remote_smoke_acceptance_json_after_normal_restart`.

Release gate:

- Frontend design remains blocked.
- The remote approval request is ready for operator review, but final readiness still requires explicit approval before running its apply command, a clean post-apply dry-run verified with `--require-empty-active --max-age-hours 24`, a preflight-only restart that reaches `restart_preflight:ok`, a normal restart without `IMAGE_AGENT_ALLOW_RESTART_WITH_ACTIVE_TASKS=1`, and fresh strict remote acceptance evidence within 24 hours.

## Stale-task apply request verifier

Time: 2026-06-14T17:05:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Added `apps/api/scripts/verify_stale_task_apply_request.py` to statically verify the generated `stale_task_apply_approval` JSON before operator authorization.
- Added focused tests in `apps/api/tests/test_verify_stale_task_apply_request.py`.
- The verifier checks `authorization_required=true`, target task ids, reviewed approval fingerprint, approval evidence freshness, env-loading apply command, follow-up step order, no active-task restart override, normal restart, strict smoke, and offline strict smoke verification.
- Updated `docs/deployment/remote-agent-production.md` and `docs/product-readiness.md` so operators run this verifier before asking for approval.
- No production task rows were mutated, no `--apply` command was run, no live restart was attempted, no live smoke run was started, and no push was performed.

Verification:

- RED:
  - `$env:PYTHONPATH='apps/api/tests'; python -m pytest apps/api/tests/test_verify_stale_task_apply_request.py -q` failed because `apps/api/scripts/verify_stale_task_apply_request.py` did not exist.
  - `$env:PYTHONPATH='apps/api'; python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_product_readiness_gate_blocks_frontend_until_agent_contracts_are_verified -q` failed because readiness did not mention `verify_stale_task_apply_request.py`.
- GREEN:
  - `$env:PYTHONPATH='apps/api/tests'; python -m pytest apps/api/tests/test_verify_stale_task_apply_request.py -q` -> `5 passed`.

Release gate:

- Frontend design remains blocked.
- This adds a pre-authorization verifier for the approval request, but final readiness still requires explicit approval before running its apply command, a clean post-apply dry-run verified with `--require-empty-active --max-age-hours 24`, a preflight-only restart that reaches `restart_preflight:ok`, a normal restart without `IMAGE_AGENT_ALLOW_RESTART_WITH_ACTIVE_TASKS=1`, and fresh strict remote acceptance evidence within 24 hours.

## Remote stale-task apply request verifier evidence

Time: 2026-06-14T17:15:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Packaged local commit `5b78d91f` apply-request verifier files with `git archive`.
- Uploaded the package to `/tmp/image_agent_apply_request_verifier_5b78d91f.tar` on the remote server.
- Overlaid the package onto the non-live remote verifier directory `/home/yyf/project/image_agent_releases/codex-gate-verifiers-efca895b-20260613T165132`.
- Verified the saved apply approval request `/tmp/image_agent_stale_tasks_83_84_apply_request_20260614T080202Z.json`.
- No production task rows were mutated, no `--apply` command was run, no live restart was attempted, no live smoke run was started, and no push was performed.

Verification:

- Remote overlay sync check:
  - overlay command returned `overlay_apply_request_verifier_synced`.
- Remote focused test slice:
  - `PYTHONPATH=tests /home/yyf/project/image_agent/apps/api/.venv/bin/python -m pytest tests/test_verify_stale_task_apply_request.py -q` -> `5 passed`.
- Remote apply request verifier:
  - `PYTHONPATH=. /home/yyf/project/image_agent/apps/api/.venv/bin/python scripts/verify_stale_task_apply_request.py /tmp/image_agent_stale_tasks_83_84_apply_request_20260614T080202Z.json --task-id 83 --task-id 84 --max-age-hours 24` -> `status=passed`, `checked.authorization_required=true`, `checked.verified_approval_generated_at_utc=2026-06-14T08:02:02.843606+00:00`.

Release gate:

- Frontend design remains blocked.
- The apply approval request is now generated and verifier-passed on the remote overlay, but final readiness still requires explicit approval before running its apply command, a clean post-apply dry-run verified with `--require-empty-active --max-age-hours 24`, a preflight-only restart that reaches `restart_preflight:ok`, a normal restart without `IMAGE_AGENT_ALLOW_RESTART_WITH_ACTIVE_TASKS=1`, and fresh strict remote acceptance evidence within 24 hours.

## Remote apply-request freshness recheck

Time: 2026-06-15T00:04:03+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Performed a read-only remote verifier recheck of the saved stale-task apply approval request.
- No production task rows were mutated, no `--apply` command was run, no live restart was attempted, no live smoke run was started, and no push was performed.
- The check confirms the current approval request is still usable as operator-review input at the checked time, but it does not replace explicit operator approval or any post-apply gate.

Verification:

- Remote apply request verifier:
  - `PYTHONPATH=. /home/yyf/project/image_agent/apps/api/.venv/bin/python scripts/verify_stale_task_apply_request.py /tmp/image_agent_stale_tasks_83_84_apply_request_20260614T080202Z.json --task-id 83 --task-id 84 --max-age-hours 24 --now-utc 2026-06-14T16:04:03Z` -> `status=passed`, `checked.authorization_required=true`, `checked.verified_approval_generated_at_utc=2026-06-14T08:02:02.843606+00:00`, `checked.max_age_hours=24.0`.

Release gate:

- Frontend design remains blocked.
- The approval request remains within the 24-hour freshness window at this check time. If operator approval is not granted and applied before `2026-06-15T08:02:02Z`, the approval evidence must be refreshed through the read-only `stale_task_approval_refresh` path and re-reviewed before apply.
- Final readiness still requires explicit approval before running the apply command, a clean post-apply dry-run verified with `--require-empty-active --max-age-hours 24`, a preflight-only restart that reaches `restart_preflight:ok`, a normal restart without `IMAGE_AGENT_ALLOW_RESTART_WITH_ACTIVE_TASKS=1`, and fresh strict remote acceptance evidence within 24 hours.

## Apply-request verifier expiry timestamp

Time: 2026-06-15T00:20:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Hardened `apps/api/scripts/verify_stale_task_apply_request.py` so passing reports include `checked.expires_at_utc`.
- Added focused tests for the exact freshness boundary: the request is accepted at exactly `generated_at_utc + max_age_hours` and rejected after that boundary.
- No production task rows were mutated, no `--apply` command was run, no live restart was attempted, no live smoke run was started, and no push was performed.

Verification:

- RED:
  - `$env:PYTHONPATH='apps/api/tests'; python -m pytest apps/api/tests/test_verify_stale_task_apply_request.py -q` failed because the verifier report did not include `checked.expires_at_utc`.
- GREEN:
  - `$env:PYTHONPATH='apps/api/tests'; python -m pytest apps/api/tests/test_verify_stale_task_apply_request.py -q` -> `7 passed`.
  - `python -m py_compile apps/api/scripts/verify_stale_task_apply_request.py apps/api/tests/test_verify_stale_task_apply_request.py` -> passed.
  - `$env:PYTHONPATH='apps/api'; python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_product_readiness_gate_blocks_frontend_until_agent_contracts_are_verified -q` -> `1 passed`.
  - `git diff --check` -> passed.

Release gate:

- Frontend design remains blocked.
- The apply-request verifier can now report the exact UTC expiry timestamp for operator approval evidence. This reduces ambiguity at the 24-hour boundary, but final readiness still requires explicit approved remote apply for tasks `83` and `84`, clean post-apply dry-run verification, restart preflight, normal restart, and fresh strict remote acceptance evidence within 24 hours.

## Remote apply-request expiry verifier overlay

Time: 2026-06-15T00:38:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Packaged local commit `cb665c79` apply-request expiry files with `git archive`.
- Uploaded the package to `/tmp/image_agent_apply_request_expiry_cb665c79.tar` on the remote server.
- Overlaid the package onto the non-live remote verifier directory `/home/yyf/project/image_agent_releases/codex-gate-verifiers-efca895b-20260613T165132`.
- No production task rows were mutated, no `--apply` command was run, no live restart was attempted, no live smoke run was started, and no push was performed.

Verification:

- Remote overlay sync check:
  - overlay command returned `overlay_apply_request_expiry_synced`.
- Remote apply request verifier:
  - `PYTHONPATH=. /home/yyf/project/image_agent/apps/api/.venv/bin/python scripts/verify_stale_task_apply_request.py /tmp/image_agent_stale_tasks_83_84_apply_request_20260614T080202Z.json --task-id 83 --task-id 84 --max-age-hours 24 --now-utc 2026-06-14T16:04:03Z` -> `status=passed`, `checked.expires_at_utc=2026-06-15T08:02:02.843606+00:00`.
- Remote focused test slice:
  - `PYTHONPATH=tests /home/yyf/project/image_agent/apps/api/.venv/bin/python -m pytest tests/test_verify_stale_task_apply_request.py -q` -> `7 passed`.
  - `PYTHONPATH=tests /home/yyf/project/image_agent/apps/api/.venv/bin/python -m pytest tests/test_verify_stale_task_apply_request.py::test_verify_stale_task_apply_request_accepts_exact_freshness_boundary -q` -> `1 passed`.

Release gate:

- Frontend design remains blocked.
- The non-live remote verifier overlay now reports the exact approval-request expiry timestamp, but this remains pre-authorization evidence only. Final readiness still requires explicit approved remote apply for tasks `83` and `84`, clean post-apply dry-run verification, restart preflight, normal restart, and fresh strict remote acceptance evidence within 24 hours.

## Apply-request expiry field generation

Time: 2026-06-15T00:55:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Hardened `apps/api/scripts/build_stale_task_apply_request.py` so generated stale-task apply approval requests include top-level `approval_expires_at_utc`.
- Hardened `apps/api/scripts/verify_stale_task_apply_request.py` so it rejects requests whose top-level `approval_expires_at_utc` does not match `verified_approval.checked.generated_at_utc + max_age_hours`.
- No production task rows were mutated, no `--apply` command was run, no live restart was attempted, no live smoke run was started, and no push was performed.

Verification:

- RED:
  - `$env:PYTHONPATH='apps/api/tests'; python -m pytest apps/api/tests/test_build_stale_task_apply_request.py -q` failed because generated request JSON did not include `approval_expires_at_utc`.
  - `$env:PYTHONPATH='apps/api/tests'; python -m pytest apps/api/tests/test_verify_stale_task_apply_request.py -q` failed because the verifier did not reject a mismatched `approval_expires_at_utc`.
- GREEN:
  - `$env:PYTHONPATH='apps/api/tests'; python -m pytest apps/api/tests/test_build_stale_task_apply_request.py apps/api/tests/test_verify_stale_task_apply_request.py -q` -> `11 passed`.
  - `python -m py_compile apps/api/scripts/build_stale_task_apply_request.py apps/api/scripts/verify_stale_task_apply_request.py apps/api/tests/test_build_stale_task_apply_request.py apps/api/tests/test_verify_stale_task_apply_request.py` -> passed.
  - `$env:PYTHONPATH='apps/api'; python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_product_readiness_gate_blocks_frontend_until_agent_contracts_are_verified -q` -> `1 passed`.
  - `python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` -> `status=passed`.

Release gate:

- Frontend design remains blocked.
- The approval request JSON itself now carries the exact expiry time the verifier enforces, reducing operator ambiguity before approval. Final readiness still requires explicit approved remote apply for tasks `83` and `84`, clean post-apply dry-run verification, restart preflight, normal restart, and fresh strict remote acceptance evidence within 24 hours.

## Remote apply-request expiry consistency overlay

Time: 2026-06-15T01:05:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Packaged local commit `2b6b89cf` apply-request expiry consistency files with `git archive`.
- Uploaded the package to `/tmp/image_agent_apply_request_expiry_consistency_2b6b89cf.tar` on the remote server.
- Overlaid the package onto the non-live remote verifier directory `/home/yyf/project/image_agent_releases/codex-gate-verifiers-efca895b-20260613T165132`.
- Regenerated `/tmp/image_agent_stale_tasks_83_84_apply_request_20260614T080202Z.json` from the refreshed approval dry-run JSON so it now includes top-level `approval_expires_at_utc`.
- No production task rows were mutated, no `--apply` command was run, no live restart was attempted, no live smoke run was started, and no push was performed.

Verification:

- Remote overlay sync:
  - Initial parallel extract attempted before upload completion and failed with `tar: This does not look like a tar archive`.
  - Sequential retry returned `overlay_apply_request_expiry_consistency_synced`.
- Remote apply request generation:
  - `scripts/build_stale_task_apply_request.py /tmp/image_agent_stale_tasks_83_84_dry_run_20260614T080202Z.json --task-id 83 --task-id 84 --max-age-hours 24 --now-utc 2026-06-14T16:04:03Z --output-timestamp 20260614T080202Z --output-json /tmp/image_agent_stale_tasks_83_84_apply_request_20260614T080202Z.json` -> wrote a request with `approval_expires_at_utc=2026-06-15T08:02:02.843606+00:00`.
- Remote focused test slice:
  - `PYTHONPATH=tests /home/yyf/project/image_agent/apps/api/.venv/bin/python -m pytest tests/test_build_stale_task_apply_request.py tests/test_verify_stale_task_apply_request.py -q` -> `11 passed`.
- Remote apply request verifier:
  - `PYTHONPATH=. /home/yyf/project/image_agent/apps/api/.venv/bin/python scripts/verify_stale_task_apply_request.py /tmp/image_agent_stale_tasks_83_84_apply_request_20260614T080202Z.json --task-id 83 --task-id 84 --max-age-hours 24 --now-utc 2026-06-14T16:04:03Z` -> `status=passed`, `checked.expires_at_utc=2026-06-15T08:02:02.843606+00:00`.

Release gate:

- Frontend design remains blocked.
- The non-live remote verifier overlay and saved apply request now agree on the exact approval expiry timestamp. This is still pre-authorization evidence only; final readiness still requires explicit approved remote apply for tasks `83` and `84`, clean post-apply dry-run verification, restart preflight, normal restart, and fresh strict remote acceptance evidence within 24 hours.

## Apply-request expiry documentation gate

Time: 2026-06-15T01:15:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Updated `docs/product-readiness.md` so the frontend freeze gate records `approval_expires_at_utc=2026-06-15T08:02:02.843606+00:00` and the matching verifier field `checked.expires_at_utc`.
- Updated `docs/deployment/remote-agent-production.md` so operator-facing handoff instructions explain that generated stale-task apply approval requests include `approval_expires_at_utc`.
- No production task rows were mutated, no `--apply` command was run, no live restart was attempted, no live smoke run was started, and no push was performed.

Verification:

- RED:
  - `$env:PYTHONPATH='apps/api'; python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_product_readiness_gate_blocks_frontend_until_agent_contracts_are_verified -q` failed because `docs/product-readiness.md` did not mention `approval_expires_at_utc`.
- GREEN:
  - `$env:PYTHONPATH='apps/api'; python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_product_readiness_gate_blocks_frontend_until_agent_contracts_are_verified -q` -> `1 passed`.
  - `git diff --check` -> passed.

Release gate:

- Frontend design remains blocked.
- The operator-facing docs and readiness gate now treat approval expiry as an explicit field rather than an implicit calculation. Final readiness still requires explicit approved remote apply for tasks `83` and `84`, clean post-apply dry-run verification, restart preflight, normal restart, and fresh strict remote acceptance evidence within 24 hours.

## Remote apply-request expiry documentation overlay

Time: 2026-06-15T01:25:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Packaged local commit `7e8b411a` documentation-gate files with `git archive`.
- Uploaded the package to `/tmp/image_agent_apply_request_expiry_docs_7e8b411a.tar` on the remote server.
- Overlaid the package onto the non-live remote verifier directory `/home/yyf/project/image_agent_releases/codex-gate-verifiers-efca895b-20260613T165132`.
- No production task rows were mutated, no `--apply` command was run, no live restart was attempted, no live smoke run was started, and no push was performed.

Verification:

- Remote overlay sync:
  - overlay command returned `overlay_apply_request_expiry_docs_synced`.
- Remote readiness/doc test:
  - `PYTHONPATH=. /home/yyf/project/image_agent/apps/api/.venv/bin/python -m pytest tests/test_skill_and_rag_docs.py::test_product_readiness_gate_blocks_frontend_until_agent_contracts_are_verified -q` -> `1 passed`.
- Remote apply-request focused tests:
  - `PYTHONPATH=tests /home/yyf/project/image_agent/apps/api/.venv/bin/python -m pytest tests/test_build_stale_task_apply_request.py tests/test_verify_stale_task_apply_request.py -q` -> `11 passed`.
- Remote command-plan verifier:
  - `/home/yyf/project/image_agent/apps/api/.venv/bin/python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` -> `status=passed`, `checked.frontend_gate_status=blocked`.

Release gate:

- Frontend design remains blocked.
- The non-live remote verifier overlay now has the operator-facing docs, readiness test, apply-request tests, and command-plan verifier aligned on explicit approval expiry. Final readiness still requires explicit approved remote apply for tasks `83` and `84`, clean post-apply dry-run verification, restart preflight, normal restart, and fresh strict remote acceptance evidence within 24 hours.

## Release gate approval-expiry requirement

Time: 2026-06-15T01:35:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Added `approval_request_requirements` to `docs/deployment/remote-release-gate-command-plan.json` so the machine-checkable release gate explicitly requires `approval_fingerprint` and `approval_expires_at_utc`.
- Hardened `apps/api/scripts/verify_release_gate_command_plan.py` so the verifier checks the new approval-request requirement and preserves `approval_expires_at_utc` in the serialized plan.
- Extended `apps/api/tests/test_release_gate_command_plan.py` to lock this requirement into the plan contract.
- No production task rows were mutated, no `--apply` command was run, no live restart was attempted, no live smoke run was started, and no push was performed.

Verification:

- RED:
  - `$env:PYTHONPATH='apps/api'; python -m pytest apps/api/tests/test_release_gate_command_plan.py -q` failed because the plan did not yet include `approval_request_requirements` or `approval_expires_at_utc`.
- GREEN:
  - `$env:PYTHONPATH='apps/api'; python -m pytest apps/api/tests/test_release_gate_command_plan.py -q` -> `5 passed`.
  - `python -m py_compile apps/api/scripts/verify_release_gate_command_plan.py apps/api/tests/test_release_gate_command_plan.py` -> passed.
  - `python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` -> `status=passed`.
  - `$env:PYTHONPATH='apps/api'; python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_product_readiness_gate_blocks_frontend_until_agent_contracts_are_verified -q` -> `1 passed`.
  - `git diff --check` -> passed.

Release gate:

- Frontend design remains blocked.
- The release plan now treats approval expiry as a first-class machine-checkable requirement. Final readiness still requires explicit approved remote apply for tasks `83` and `84`, clean post-apply dry-run verification, restart preflight, normal restart, and fresh strict remote acceptance evidence within 24 hours.

## Remote release-plan approval-expiry overlay

Time: 2026-06-15T01:45:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Packaged local commit `87e5d83c` release-plan approval-expiry files with `git archive`.
- Uploaded the package to `/tmp/image_agent_release_plan_approval_expiry_87e5d83c.tar` on the remote server.
- Overlaid the package onto the non-live remote verifier directory `/home/yyf/project/image_agent_releases/codex-gate-verifiers-efca895b-20260613T165132`.
- No production task rows were mutated, no `--apply` command was run, no live restart was attempted, no live smoke run was started, and no push was performed.

Verification:

- Remote overlay sync:
  - overlay command returned `overlay_release_plan_approval_expiry_synced`.
- Remote command-plan verifier:
  - An initial verifier command from `apps/api` used a duplicated `apps/api/apps/api/...` path and failed before running the verifier.
  - Corrected command from the overlay root: `/home/yyf/project/image_agent/apps/api/.venv/bin/python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` -> `status=passed`, `checked.frontend_gate_status=blocked`.
- Remote focused tests:
  - `PYTHONPATH=. /home/yyf/project/image_agent/apps/api/.venv/bin/python -m pytest tests/test_release_gate_command_plan.py tests/test_skill_and_rag_docs.py::test_product_readiness_gate_blocks_frontend_until_agent_contracts_are_verified -q` -> `6 passed`.
  - `PYTHONPATH=tests /home/yyf/project/image_agent/apps/api/.venv/bin/python -m pytest tests/test_build_stale_task_apply_request.py tests/test_verify_stale_task_apply_request.py -q` -> `11 passed`.

Release gate:

- Frontend design remains blocked.
- The non-live remote verifier overlay now enforces approval expiry as a machine-checkable release-plan requirement. Final readiness still requires explicit approved remote apply for tasks `83` and `84`, clean post-apply dry-run verification, restart preflight, normal restart, and fresh strict remote acceptance evidence within 24 hours.

## Release-plan verifier approval-request fields

Time: 2026-06-15T01:55:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Hardened `apps/api/scripts/verify_release_gate_command_plan.py` so its checked report now exposes `approval_request_required_fields`.
- Extended `apps/api/tests/test_release_gate_command_plan.py` to require the plan verifier report to surface the approval-request fields that the plan mandates.
- No production task rows were mutated, no `--apply` command was run, no live restart was attempted, no live smoke run was started, and no push was performed.

Verification:

- RED:
  - `$env:PYTHONPATH='apps/api'; python -m pytest apps/api/tests/test_release_gate_command_plan.py -q` failed because the verifier report did not yet include `approval_request_required_fields`.
- GREEN:
  - `$env:PYTHONPATH='apps/api'; python -m pytest apps/api/tests/test_release_gate_command_plan.py -q` -> `5 passed`.
  - `python -m py_compile apps/api/scripts/verify_release_gate_command_plan.py apps/api/tests/test_release_gate_command_plan.py` -> passed.
  - `python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` -> `status=passed`, `checked.approval_request_required_fields=["approval_fingerprint","approval_expires_at_utc"]`.
  - `$env:PYTHONPATH='apps/api'; python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_product_readiness_gate_blocks_frontend_until_agent_contracts_are_verified -q` -> `1 passed`.
  - `git diff --check` -> passed.

Release gate:

- Frontend design remains blocked.
- The release-plan verifier now reports the approval-request fields it requires, making the gate report itself more auditable. Final readiness still requires explicit approved remote apply for tasks `83` and `84`, clean post-apply dry-run verification, restart preflight, normal restart, and fresh strict remote acceptance evidence within 24 hours.

## Remote release-plan checked-fields overlay

Time: 2026-06-15T02:05:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Packaged local commit `c79466a0` release-plan checked-fields files with `git archive`.
- Uploaded the package to `/tmp/image_agent_release_plan_checked_fields_c79466a0.tar` on the remote server.
- Overlaid the package onto the non-live remote verifier directory `/home/yyf/project/image_agent_releases/codex-gate-verifiers-efca895b-20260613T165132`.
- No production task rows were mutated, no `--apply` command was run, no live restart was attempted, no live smoke run was started, and no push was performed.

Verification:

- Remote overlay sync:
  - overlay command returned `overlay_release_plan_checked_fields_synced`.
- Remote command-plan verifier:
  - `/home/yyf/project/image_agent/apps/api/.venv/bin/python apps/api/scripts/verify_release_gate_command_plan.py docs/deployment/remote-release-gate-command-plan.json` -> `status=passed`, `checked.approval_request_required_fields=["approval_fingerprint","approval_expires_at_utc"]`.
- Remote focused tests:
  - `PYTHONPATH=. /home/yyf/project/image_agent/apps/api/.venv/bin/python -m pytest tests/test_release_gate_command_plan.py tests/test_skill_and_rag_docs.py::test_product_readiness_gate_blocks_frontend_until_agent_contracts_are_verified -q` -> `6 passed`.
  - `PYTHONPATH=tests /home/yyf/project/image_agent/apps/api/.venv/bin/python -m pytest tests/test_build_stale_task_apply_request.py tests/test_verify_stale_task_apply_request.py -q` -> `11 passed`.

Release gate:

- Frontend design remains blocked.
- The non-live remote verifier overlay now reports the approval-request required fields directly in the release-plan verifier output. Final readiness still requires explicit approved remote apply for tasks `83` and `84`, clean post-apply dry-run verification, restart preflight, normal restart, and fresh strict remote acceptance evidence within 24 hours.

## Remote stale-task approval evidence refresh

Time: 2026-06-14T00:45:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Performed read-only remote inspection only; no remote task rows were mutated, no `--apply` command was run, no restart was attempted, no workflow execution was started, and no push was performed.
- Confirmed the remote API still reports `/health` as `{"status":"ok","app":"image_agent","version":"0.2.0"}` and `/agent/model/status` as configured for OpenAI-compatible Responses access with `model=gpt-5.5`, `wire_api=responses`, `reasoning_effort=high`, `store=false`, and `metadata_enabled=false`.
- Used the non-live remote release overlay `/home/yyf/project/image_agent_releases/codex-stale-resolution-nopath-20260611T1715/apps/api` to run a scoped stale-task dry-run for task ids `83` and `84`.
- A dry-run without Docker label checking confirmed both rows still exist as stale `running` `dwi_qsirecon` tasks at about 601 hours old.
- A dry-run with the shared remote `.env` and `--check-containers` produced `container_check_status=passed`, `running_container_task_ids=[]`, `stale_candidates=[83,84]`, and `approval_fingerprint=139113571daf0137a3e34be526fd25ccaa8066aed725ab7c0b846cfc7eb3abd0`.
- Saved the fresh approval dry-run JSON on the remote host at `/tmp/image_agent_stale_tasks_83_84_dry_run_20260613T1640Z.json`.
- The remote overlay verifier is older than the current local verifier and rejects `--max-age-hours`; the saved JSON was copied to a local temp path and verified with the current repository verifier instead.

Verification:

- Remote read-only dry-run:
  - `PYTHONPATH=. /home/yyf/project/image_agent/apps/api/.venv/bin/python scripts/reconcile_stale_tasks.py --max-age-hours 24 --check-containers --task-id 83 --task-id 84` -> produced `mode=dry_run`, `container_check_status=passed`, `running_container_task_ids=[]`, `updated_task_ids=[]`, and `stale_candidates` for `83` and `84`.
- Local verifier against the saved remote JSON:
  - `PYTHONPATH=apps/api python apps/api/scripts/verify_stale_task_approval.py <local-copy-of-/tmp/image_agent_stale_tasks_83_84_dry_run_20260613T1640Z.json> --task-id 83 --task-id 84 --max-age-hours 24` -> `status=passed`.
  - The verifier report included `checked.generated_at_utc=2026-06-13T16:40:24.758668+00:00`, `checked.max_age_hours=24.0`, `checked.container_check_status=passed`, and `checked.approval_fingerprint=139113571daf0137a3e34be526fd25ccaa8066aed725ab7c0b846cfc7eb3abd0`.

Release gate:

- Frontend design remains blocked.
- Fresh approval evidence now exists for operator review, but this does not mutate production state. Final readiness still requires approved remote apply for tasks `83` and `84`, a path-safe clean post-apply dry-run verified by `verify_stale_task_resolution.py --require-empty-active --max-age-hours 24`, a normal remote restart without `IMAGE_AGENT_ALLOW_RESTART_WITH_ACTIVE_TASKS=1`, and fresh strict remote acceptance evidence within 24 hours.

## Remote gate verifier overlay refresh

Time: 2026-06-14T00:52:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Created a non-live remote gate verifier overlay for future operator-approved stale-task resolution; no production task rows were mutated, no `--apply` command was run, no service restart was attempted, no workflow execution was started, and no push was performed.
- Root cause for the prior remote verifier mismatch: existing remote stale-task overlays did not expose the current `--max-age-hours` stale-task verifier options, while local `HEAD=efca895b` did.
- Built a clean `git archive HEAD` package containing the stale-task reconciliation/verifier scripts, stale-task workflow helper, related tests, and gate documentation.
- Uploaded the package to the remote server and created `/home/yyf/project/image_agent_releases/codex-gate-verifiers-efca895b-20260613T165132` by copying `/home/yyf/project/image_agent_releases/codex-stale-resolution-nopath-20260611T1715` and overlaying the current package.

Verification:

- Remote CLI checks:
  - `scripts/verify_stale_task_approval.py -h` in the new overlay exposes `--max-age-hours`.
  - `scripts/verify_stale_task_resolution.py -h` in the new overlay exposes `--max-age-hours`.
- Remote approval verifier:
  - `PYTHONPATH=. /home/yyf/project/image_agent/apps/api/.venv/bin/python scripts/verify_stale_task_approval.py /tmp/image_agent_stale_tasks_83_84_dry_run_20260613T1640Z.json --task-id 83 --task-id 84 --max-age-hours 24` -> `status=passed`.
  - The report included `checked.generated_at_utc=2026-06-13T16:40:24.758668+00:00`, `checked.max_age_hours=24.0`, `checked.container_check_status=passed`, and `checked.approval_fingerprint=139113571daf0137a3e34be526fd25ccaa8066aed725ab7c0b846cfc7eb3abd0`.
- Remote test slice:
  - `PYTHONPATH=. /home/yyf/project/image_agent/apps/api/.venv/bin/python -m pytest tests/test_verify_stale_task_approval.py tests/test_verify_stale_task_resolution.py tests/test_stale_task_reconciliation.py -q` -> `40 passed`.

Release gate:

- Frontend design remains blocked.
- The new remote gate verifier overlay removes the stale remote-script blocker for the next operator-approved step, but final readiness still requires approved remote apply for tasks `83` and `84`, a path-safe clean post-apply dry-run verified by `verify_stale_task_resolution.py --require-empty-active --max-age-hours 24`, a normal remote restart without `IMAGE_AGENT_ALLOW_RESTART_WITH_ACTIVE_TASKS=1`, and fresh strict remote acceptance evidence within 24 hours.

## Remote strict-smoke verifier overlay refresh

Time: 2026-06-14T00:58:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Updated the existing non-live remote gate verifier overlay with current strict smoke gate scripts and tests; no production task rows were mutated, no `--apply` command was run, no service restart was attempted, no smoke run against the live API was started, and no push was performed.
- Root cause for this preparatory step: `/home/yyf/project/image_agent_releases/codex-gate-verifiers-efca895b-20260613T165132` had current stale-task verifier scripts but still carried 2026-06-11 strict-smoke scripts.
- Built a clean `git archive HEAD` package containing `smoke_remote_agent.py`, `verify_remote_smoke_acceptance.py`, their focused tests, and related gate documentation.
- Uploaded the package to the remote server and overlaid it onto `/home/yyf/project/image_agent_releases/codex-gate-verifiers-efca895b-20260613T165132`.

Verification:

- Remote CLI check:
  - `verify_remote_smoke_acceptance.py -h` in the non-live overlay exposes `--max-age-hours` and `--now-utc`.
- Remote strict-smoke test slice:
  - `PYTHONPATH=. /home/yyf/project/image_agent/apps/api/.venv/bin/python -m pytest tests/test_verify_remote_smoke_acceptance.py tests/test_smoke_remote_agent.py -q` -> `179 passed`.

Release gate:

- Frontend design remains blocked.
- The overlay now has current stale-task gate tooling and current strict smoke gate scripts, but final readiness still requires approved remote apply for tasks `83` and `84`, a path-safe clean post-apply dry-run verified by `verify_stale_task_resolution.py --require-empty-active --max-age-hours 24`, a normal remote restart without `IMAGE_AGENT_ALLOW_RESTART_WITH_ACTIVE_TASKS=1`, and fresh strict remote acceptance evidence within 24 hours.

## Restart preflight-only gate

Time: 2026-06-14T01:08:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Added `IMAGE_AGENT_RESTART_PREFLIGHT_ONLY=1` to `tools/restart_remote_image_agent_api.sh` so the wrapper can run `load_env`, active-task drain, and port-owner checks without stopping or starting the API.
- A clean preflight prints `restart_preflight:ok` and exits before `stop_api`, `start_api`, or health wait.
- Updated `docs/deployment/remote-agent-production.md` and `docs/product-readiness.md` so operators use preflight-only after stale-task resolution and before normal restart.
- No production task rows were mutated, no `--apply` command was run, no live restart was attempted, no live smoke run was started, and no push was performed.

Verification:

- RED:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_remote_restart_script.py -q` failed because the restart wrapper and deployment docs did not expose `IMAGE_AGENT_RESTART_PREFLIGHT_ONLY=1`.
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_product_readiness_gate_blocks_frontend_until_agent_contracts_are_verified -q` failed because the product readiness gate did not mention the restart preflight-only evidence.
- GREEN:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_remote_restart_script.py -q` -> `6 passed`.
- Remote non-live overlay verification:
  - Repackaged committed `HEAD=97cf6eb9` and overlaid it onto `/home/yyf/project/image_agent_releases/codex-gate-verifiers-efca895b-20260613T165132`.
  - `bash -n tools/restart_remote_image_agent_api.sh` in that overlay -> passed.
  - `PYTHONPATH=. /home/yyf/project/image_agent/apps/api/.venv/bin/python -m pytest tests/test_remote_restart_script.py -q` in that overlay -> `6 passed`.
  - `IMAGE_AGENT_RESTART_PREFLIGHT_ONLY=1 bash tools/restart_remote_image_agent_api.sh` in that overlay exited `1` at the active-task drain gate, reporting tasks `83` and `84` as active `running` `dwi_qsirecon` rows; this confirms the current blocker without stopping or starting the API.

Release gate:

- Frontend design remains blocked.
- The restart wrapper can now preflight restart blockers without mutating the service, but final readiness still requires approved remote apply for tasks `83` and `84`, a path-safe clean post-apply dry-run verified by `verify_stale_task_resolution.py --require-empty-active --max-age-hours 24`, a preflight-only restart check that reaches `restart_preflight:ok`, a normal remote restart without `IMAGE_AGENT_ALLOW_RESTART_WITH_ACTIVE_TASKS=1`, and fresh strict remote acceptance evidence within 24 hours.

## Stale-task verifier report timestamp evidence

Time: 2026-06-14T00:58:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to stale-task verifier scripts, tests, and work log only; no local install, local service run, local container run, remote install, remote restart, remote model smoke, remote stale-task apply, or remote workflow execution was performed.
- Hardened `apps/api/scripts/verify_stale_task_approval.py` so passing approval verifier reports include the parsed `checked.generated_at_utc` timestamp from the reviewed dry-run JSON.
- Hardened `apps/api/scripts/verify_stale_task_resolution.py` so passing resolution verifier reports include both `checked.apply_generated_at_utc` and `checked.resolution_generated_at_utc`.
- This lets saved stale-task approval/apply/resolution verifier reports prove both the checked evidence timestamps and the freshness limits used before normal restart.

Verification:

- RED:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_verify_stale_task_approval.py::test_verify_stale_task_approval_cli_prints_passed_report -q` failed with missing `checked.generated_at_utc`.
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_verify_stale_task_resolution.py::test_verify_stale_task_resolution_cli_prints_passed_report -q` failed with missing `checked.apply_generated_at_utc`.
- GREEN:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_verify_stale_task_approval.py::test_verify_stale_task_approval_cli_prints_passed_report -q` -> `1 passed`.
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_verify_stale_task_resolution.py::test_verify_stale_task_resolution_cli_prints_passed_report -q` -> `1 passed`.
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_verify_stale_task_approval.py apps/api/tests/test_verify_stale_task_resolution.py -q` -> `30 passed`.
  - `python -m py_compile apps/api/scripts/verify_stale_task_approval.py apps/api/scripts/verify_stale_task_resolution.py apps/api/tests/test_verify_stale_task_approval.py apps/api/tests/test_verify_stale_task_resolution.py` -> passed.
  - `git diff --check` -> passed.

Release gate:

- Frontend design remains blocked.
- This improves stale-task cleanup report auditability, but final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean path-safe post-apply dry-run report, normal remote restart without active-task override, and fresh strict remote acceptance evidence within 24 hours.

## Strict smoke verifier report timestamp evidence

Time: 2026-06-14T00:42:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to strict smoke verifier scripts, tests, and work log only; no local install, local service run, local container run, remote install, remote restart, remote model smoke, remote stale-task apply, or remote workflow execution was performed.
- Hardened `apps/api/scripts/verify_remote_smoke_acceptance.py` so passing verifier reports include the parsed `checked.generated_at_utc` timestamp from the saved strict smoke JSON.
- This lets a saved verifier report prove both the checked evidence timestamp and the freshness limit used during validation.

Verification:

- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_cli_prints_passed_report -q` failed with missing `checked.generated_at_utc`.
- GREEN:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_cli_prints_passed_report -q` -> `1 passed`.
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py -q` -> `103 passed`.
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_strict_output_passes_offline_acceptance_verifier -q` -> `104 passed`.
  - `python -m py_compile apps/api/scripts/verify_remote_smoke_acceptance.py apps/api/tests/test_verify_remote_smoke_acceptance.py` -> passed.
  - `git diff --check` -> passed.

Release gate:

- Frontend design remains blocked.
- This improves strict remote acceptance report auditability, but final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean path-safe post-apply dry-run report, normal remote restart without active-task override, and fresh strict remote acceptance evidence within 24 hours.
