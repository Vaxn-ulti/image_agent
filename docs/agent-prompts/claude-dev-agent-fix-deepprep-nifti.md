You are the Claude Development Agent for /home/yyf/project/image_agent.
You may modify code. Do not call or spawn other agents.
Bug from real full run: t1_deepprep task 35 launched pbfslab/deepprep but failed in anat_get_t1w_file_in_bids with missing output sub-*; task BIDS contained an uncompressed .nii path. The previous risk was fake-renaming .nii as .nii.gz. Required fix: for container task BIDS inputs, if source NIfTI is .nii, actually gzip-compress it to .nii.gz for DeepPrep/QSIPrep-compatible BIDS input; do not simply rename. Preserve sidecar basename handling (.json/.bval/.bvec). Keep raw ingest BIDS preserving source extension if desired, but task BIDS for containers may use real .nii.gz. Add/adjust tests to verify gzip magic bytes and sidecar preservation. Run pytest. Output changed files and results.
