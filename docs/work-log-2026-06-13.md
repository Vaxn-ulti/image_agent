# Image Agent Work Log - 2026-06-13

## Agent resume confirmation contract hardening

Time: 2026-06-13T02:23:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to API/tests/docs only; no local install, local service run, local container run, remote install, remote restart, remote model smoke, remote stale-task apply, or remote workflow execution was performed.
- Hardened `POST /agent/runs/{thread_id}/resume` so the nested `confirmation` payload is a strict typed contract instead of an unconstrained dict.
- Unknown nested confirmation fields now return `request_contract_violation` through the stable `agent_api_error.v1` envelope before ledger, runner, or task-creation state is touched.
- Updated API docs, the agent-run ledger RAG contract, and the Image Agent developer skill contract so future RAG/skill answers describe the same nested strictness as the backend.

Verification:

- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_agent_api.py::test_agent_resume_rejects_unknown_confirmation_fields_with_stable_error -q` failed with `200 == 422` because `absolute_path_hint` inside `confirmation` was accepted.
- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_agent_run_ledger_contract_is_documented_for_agent_use -q` failed because the docs did not say nested confirmation fields are also strict.
- GREEN:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_agent_api.py::test_agent_api_openapi_declares_stable_response_contracts apps/api/tests/test_agent_api.py::test_agent_resume_rejects_unknown_request_fields_with_stable_error apps/api/tests/test_agent_api.py::test_agent_resume_rejects_unknown_confirmation_fields_with_stable_error apps/api/tests/test_agent_api.py::test_agent_resume_returns_ready_to_launch apps/api/tests/test_agent_api.py::test_agent_resume_approved_confirmation_creates_real_task apps/api/tests/test_skill_and_rag_docs.py::test_agent_run_ledger_contract_is_documented_for_agent_use -q` -> `6 passed`.

Release gate:

- Frontend design remains blocked.
- This strengthens the frozen Agent API contract, but final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean path-safe post-apply dry-run report, normal remote restart without active-task override, and fresh strict remote acceptance evidence within 24 hours.

## OpenAI Responses strict function tool specs

Time: 2026-06-13T02:41:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to API/tests/docs only; no local install, local service run, local container run, remote install, remote restart, remote model smoke, remote stale-task apply, or remote workflow execution was performed.
- Hardened `app.agent.tool_registry.openai_tool_specs()` so Responses function tools are emitted with `strict=true`.
- The OpenAI boundary now recursively adds `additionalProperties=false` to every object schema in tool `parameters`, including nested objects such as `confirmation`, `proposal`, `filters`, and declared primitive objects.
- The internal registry remains unmutated; strictness is applied at the OpenAI Responses export layer while the backend dispatcher still enforces allowlisted tools, argument handling, production gating, and redaction.
- Updated the OpenAI Responses RAG summary and Image Agent developer contract so future RAG/skill answers preserve this strict function-tool boundary.

Verification:

- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_agent_prompt_and_tool_registry.py::test_openai_function_tool_specs_are_strict_responses_schemas -q` failed because `read_project_context` exposed `strict=False`.
- RED: the same test then failed on nested object schemas because they lacked `additionalProperties=false`.
- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_openai_responses_tool_contract_is_documented_for_agent_use -q` failed because the docs did not say function tool specs are strict.
- GREEN:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_openai_responses_tool_contract_is_documented_for_agent_use apps/api/tests/test_agent_prompt_and_tool_registry.py apps/api/tests/test_agent_tool_dispatcher.py -q` -> `12 passed`.
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_model_gateway.py::test_responses_payload_prefers_json_schema_when_structured_schema_is_available apps/api/tests/test_model_gateway.py::test_request_forwards_json_schema_to_openai_sdk_responses_client apps/api/tests/test_model_gateway.py::test_request_retries_without_metadata_for_openai_compatible_responses -q` -> `3 passed`.

Release gate:

- Frontend design remains blocked.
- This strengthens OpenAI SDK-style tool calling, but final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean path-safe post-apply dry-run report, normal remote restart without active-task override, and fresh strict remote acceptance evidence within 24 hours.
