# Image Agent Work Log - 2026-06-13

## Agent resume confirmation contract hardening

Time: 2026-06-13T02:23:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to API/tests/docs only; no local install, local service run, local container run, remote install, remote restart, remote model smoke, remote stale-task apply, or remote workflow execution was performed.
- Hardened `POST /agent/runs/{thread_id}/resume` so the nested `confirmation` payload is a strict typed contract instead of an unconstrained dict.
- Unknown nested confirmation fields now return `request_contract_violation` through the stable `agent_api_error.v1` envelope before ledger, runner, or task-creation state is touched.
- Updated API docs, the agent-run ledger RAG contract, and the Image Agent developer skill contract so future RAG/skill answers describe the same nested strictness as the backend.

Verification:

- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_agent_api.py::test_agent_resume_rejects_unknown_confirmation_fields_with_stable_error -q` failed with `200 == 422` because `absolute_path_hint` inside `confirmation` was accepted.
- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_agent_run_ledger_contract_is_documented_for_agent_use -q` failed because the docs did not say nested confirmation fields are also strict.
- GREEN:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_agent_api.py::test_agent_api_openapi_declares_stable_response_contracts apps/api/tests/test_agent_api.py::test_agent_resume_rejects_unknown_request_fields_with_stable_error apps/api/tests/test_agent_api.py::test_agent_resume_rejects_unknown_confirmation_fields_with_stable_error apps/api/tests/test_agent_api.py::test_agent_resume_returns_ready_to_launch apps/api/tests/test_agent_api.py::test_agent_resume_approved_confirmation_creates_real_task apps/api/tests/test_skill_and_rag_docs.py::test_agent_run_ledger_contract_is_documented_for_agent_use -q` -> `6 passed`.

Release gate:

- Frontend design remains blocked.
- This strengthens the frozen Agent API contract, but final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean path-safe post-apply dry-run report, normal remote restart without active-task override, and fresh strict remote acceptance evidence within 24 hours.

## OpenAI Responses strict function tool specs

Time: 2026-06-13T02:41:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to API/tests/docs only; no local install, local service run, local container run, remote install, remote restart, remote model smoke, remote stale-task apply, or remote workflow execution was performed.
- Hardened `app.agent.tool_registry.openai_tool_specs()` so Responses function tools are emitted with `strict=true`.
- The OpenAI boundary now recursively adds `additionalProperties=false` to every object schema in tool `parameters`, including nested objects such as `confirmation`, `proposal`, `filters`, and declared primitive objects.
- The internal registry remains unmutated; strictness is applied at the OpenAI Responses export layer while the backend dispatcher still enforces allowlisted tools, argument handling, production gating, and redaction.
- Updated the OpenAI Responses RAG summary and Image Agent developer contract so future RAG/skill answers preserve this strict function-tool boundary.

Verification:

- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_agent_prompt_and_tool_registry.py::test_openai_function_tool_specs_are_strict_responses_schemas -q` failed because `read_project_context` exposed `strict=False`.
- RED: the same test then failed on nested object schemas because they lacked `additionalProperties=false`.
- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_openai_responses_tool_contract_is_documented_for_agent_use -q` failed because the docs did not say function tool specs are strict.
- GREEN:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_openai_responses_tool_contract_is_documented_for_agent_use apps/api/tests/test_agent_prompt_and_tool_registry.py apps/api/tests/test_agent_tool_dispatcher.py -q` -> `12 passed`.
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_model_gateway.py::test_responses_payload_prefers_json_schema_when_structured_schema_is_available apps/api/tests/test_model_gateway.py::test_request_forwards_json_schema_to_openai_sdk_responses_client apps/api/tests/test_model_gateway.py::test_request_retries_without_metadata_for_openai_compatible_responses -q` -> `3 passed`.

Release gate:

- Frontend design remains blocked.
- This strengthens OpenAI SDK-style tool calling, but final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean path-safe post-apply dry-run report, normal remote restart without active-task override, and fresh strict remote acceptance evidence within 24 hours.

## Dispatcher unknown tool argument guard

Time: 2026-06-13T02:52:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to API/tests/docs only; no local install, local service run, local container run, remote install, remote restart, remote model smoke, remote stale-task apply, or remote workflow execution was performed.
- Hardened `app.agent.tool_dispatcher.dispatch_tool_call()` so registered tools reject unknown top-level arguments before execution.
- This is a backend safety net for compatible gateways, tests, or manual calls that bypass OpenAI strict function-tool schema enforcement.
- Updated the OpenAI Responses RAG summary and Image Agent developer contract so RAG/skill guidance says both the OpenAI tool spec and backend dispatcher reject ad hoc tool parameters.

Verification:

- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_agent_tool_dispatcher.py::test_dispatcher_rejects_unknown_tool_arguments_before_execution -q` failed because `list_workflows` ignored `adhoc_frontend_field` and returned `status=ok`.
- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_openai_responses_tool_contract_is_documented_for_agent_use -q` failed because the docs did not say the dispatcher rejects unknown tool arguments.
- GREEN:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_agent_tool_dispatcher.py::test_dispatcher_rejects_unknown_tool_arguments_before_execution apps/api/tests/test_agent_tool_dispatcher.py -q` -> `8 passed`.

Release gate:

- Frontend design remains blocked.
- This strengthens backend guardrails around model-requested tools, but final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean path-safe post-apply dry-run report, normal remote restart without active-task override, and fresh strict remote acceptance evidence within 24 hours.

## Dispatcher missing required tool argument guard

Time: 2026-06-13T03:02:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to API/tests/docs only; no local install, local service run, local container run, remote install, remote restart, remote model smoke, remote stale-task apply, or remote workflow execution was performed.
- Hardened `app.agent.tool_dispatcher.dispatch_tool_call()` so registered tools reject missing required arguments before handler execution.
- Missing required inputs now produce a stable blocked tool result instead of handler-level `KeyError`/exception-shaped traces.
- Updated the OpenAI Responses RAG summary and Image Agent developer contract so RAG/skill guidance covers unknown and missing tool argument rejection.

Verification:

- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_agent_tool_dispatcher.py::test_dispatcher_rejects_missing_required_tool_arguments_before_execution -q` failed because missing `workflow_type` returned `status=error`.
- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_openai_responses_tool_contract_is_documented_for_agent_use -q` failed because the docs did not say the dispatcher rejects missing required tool arguments.
- GREEN:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_agent_tool_dispatcher.py::test_dispatcher_rejects_missing_required_tool_arguments_before_execution apps/api/tests/test_agent_tool_dispatcher.py -q` -> `9 passed`.

Release gate:

- Frontend design remains blocked.
- This strengthens backend guardrails around malformed model-requested tools, but final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean path-safe post-apply dry-run report, normal remote restart without active-task override, and fresh strict remote acceptance evidence within 24 hours.

## Dispatcher invalid tool argument type guard

Time: 2026-06-13T03:13:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to API/tests/docs only; no local install, local service run, local container run, remote install, remote restart, remote model smoke, remote stale-task apply, or remote workflow execution was performed.
- Hardened `app.agent.tool_dispatcher.dispatch_tool_call()` so registered tools reject invalid top-level argument types before handler execution.
- The guard covers current JSON-schema primitives used by the tool registry: `integer`, `number`, `string`, `boolean`, `object`, `array`, `null`, unions, and `oneOf` branches.
- Invalid values now produce a stable blocked tool result instead of handler casts, `ValueError`, or database-backed reads.
- Updated the OpenAI Responses RAG summary and Image Agent developer contract so RAG/skill guidance covers unknown, missing, and invalid-typed tool argument rejection.

Verification:

- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_agent_tool_dispatcher.py::test_dispatcher_rejects_invalid_tool_argument_types_before_execution -q` failed because `task_id="not-an-int"` returned `status=error`.
- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_openai_responses_tool_contract_is_documented_for_agent_use -q` failed because the docs did not say the dispatcher rejects invalid tool argument types.
- GREEN:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_agent_tool_dispatcher.py::test_dispatcher_rejects_invalid_tool_argument_types_before_execution apps/api/tests/test_agent_tool_dispatcher.py -q` -> `10 passed`.

Release gate:

- Frontend design remains blocked.
- This strengthens backend guardrails around malformed model-requested tools, but final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean path-safe post-apply dry-run report, normal remote restart without active-task override, and fresh strict remote acceptance evidence within 24 hours.

## Preflight workflow enum contract

Time: 2026-06-13T14:28:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to API/tests/docs only; no local install, local service run, local container run, remote install, remote restart, remote model smoke, remote stale-task apply, or remote workflow execution was performed.
- Hardened `app.agent.tool_registry.list_function_tools()` so `preflight_workflow.workflow_type` exposes an enum derived from `app.workflows.registry.allowed_runtime_workflows()`.
- Extended `app.agent.tool_dispatcher.dispatch_tool_call()` so registered tool arguments with schema enums are rejected before handler execution when values fall outside the model-facing contract.
- Kept nullable enum arguments compatible by ensuring `list_workflows.lane` includes `None` in its enum when its schema type includes `null`.
- This keeps OpenAI-style function tool specs and backend dispatch validation tied to the same workflow registry source instead of a hand-written list.
- Updated the OpenAI Responses RAG summary and Image Agent developer contract so RAG/skill guidance records the workflow enum source.

Verification:

- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_agent_prompt_and_tool_registry.py::test_preflight_workflow_tool_schema_exposes_registered_workflow_enum -q` failed because `preflight_workflow.workflow_type` had no `enum`.
- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_agent_tool_dispatcher.py::test_dispatcher_rejects_invalid_tool_argument_enum_before_execution -q` failed because `workflow_type="made_up_workflow"` reached the handler-level `project_context` check.
- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_openai_responses_tool_contract_is_documented_for_agent_use -q` failed because the docs did not say the preflight workflow_type enum is derived from the workflow registry.
- GREEN:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_agent_prompt_and_tool_registry.py::test_preflight_workflow_tool_schema_exposes_registered_workflow_enum apps/api/tests/test_agent_tool_dispatcher.py::test_dispatcher_rejects_invalid_tool_argument_enum_before_execution -q` -> `2 passed`.
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_agent_tool_dispatcher.py::test_dispatcher_allows_nullable_enum_arguments apps/api/tests/test_agent_tool_dispatcher.py -q` -> `12 passed`.

Release gate:

- Frontend design remains blocked.
- This strengthens the model-facing workflow launch boundary, but final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean path-safe post-apply dry-run report, normal remote restart without active-task override, and fresh strict remote acceptance evidence within 24 hours.

## Frontend-safe nested Agent response fields

Time: 2026-06-13T14:46:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to API/tests/docs only; no local install, local service run, local container run, remote install, remote restart, remote model smoke, remote stale-task apply, or remote workflow execution was performed.
- Hardened `app.agent.contracts.build_agent_run_response_payload()` so immediate `/agent/runs` and resume responses sanitize nested `task`, `tool_input`, and `confirmation` dictionaries through allowlisted frontend-safe scalar fields.
- Removed `log_path`, `output_dir`, free-text nested `error_message`, backend absolute paths, raw nested provenance, and arbitrary runner dictionaries from the Agent API response contract.
- Updated `docs/api.md`, the agent-run ledger RAG contract, and the Image Agent developer contract so frontend-facing Agent API docs describe the nested-field safety boundary.

Verification:

- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_agent_api.py::test_agent_run_response_redacts_nested_backend_paths -q` failed first because `tool_input.output_dir` leaked `D:/project/private-output`, then failed again after the test was extended because nested `task.error_message` leaked patient/path text through the immediate response.
- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_agent_run_ledger_contract_is_documented_for_agent_use -q` failed because the docs did not say task, tool_input, and confirmation use allowlisted frontend-safe scalar fields.
- GREEN:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_agent_api.py::test_agent_run_response_redacts_nested_backend_paths apps/api/tests/test_agent_api.py::test_agent_resume_returns_ready_to_launch apps/api/tests/test_agent_api.py::test_agent_resume_approved_confirmation_creates_real_task -q` -> `3 passed`.

Release gate:

- Frontend design remains blocked.
- This strengthens frontend-safe Agent API payloads, but final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean path-safe post-apply dry-run report, normal remote restart without active-task override, and fresh strict remote acceptance evidence within 24 hours.

## Artifact manifest reports native QC demotion

Time: 2026-06-13T15:08:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to API/tests/docs only; no local install, local service run, local container run, remote install, remote restart, remote model smoke, remote stale-task apply, or remote workflow execution was performed.
- Hardened `app.workflows.artifact_manifest.build_artifact_manifest()` so report-layer artifacts under `reports/*` are not auto-promoted to container-native QC by legacy native-looking `source_stage` or `artifact_role` metadata.
- `reports/*` artifacts now stay in `derived_scientific_report` with `source_stage=scientific_report`, `artifact_role=derived_presentation_asset`, `artifact_origin=generated_from_result_summary`, `native_artifact=false`, and `provenance.replaces_native_qc=false` unless strict non-report native provenance exists.
- Updated the container QC RAG contract and Image Agent developer contract so RAG/skill guidance records that generated scientific report images cannot satisfy container-native QC evidence.

Verification:

- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_artifact_manifest_contract.py::test_artifact_manifest_does_not_promote_reports_assets_to_legacy_native_qc -q` failed because `reports/fmriprep-panel.png` was classified as `container_native_qc`.
- RED: after skipping legacy enrichment for `reports/*`, the same test failed because the artifact still kept `source_stage=fmriprep` instead of the report-layer `source_stage=scientific_report`.
- GREEN:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_artifact_manifest_contract.py::test_artifact_manifest_does_not_promote_reports_assets_to_legacy_native_qc apps/api/tests/test_artifact_manifest_contract.py -q` -> `4 passed`.
  - `python -m py_compile apps/api/app/workflows/artifact_manifest.py apps/api/tests/test_artifact_manifest_contract.py` -> passed.

Release gate:

- Frontend design remains blocked.
- This prevents derived report images from satisfying native QC gates, but final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean path-safe post-apply dry-run report, normal remote restart without active-task override, and fresh strict remote acceptance evidence within 24 hours.

## Scientific report verifier reports-path native QC guard

Time: 2026-06-13T20:10:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to API/tests/docs only; no local install, local service run, local container run, remote install, remote restart, remote model smoke, remote stale-task apply, or remote workflow execution was performed.
- Hardened `app.scripts.verify_scientific_reports.check_output()` so `reports/*` artifacts cannot satisfy `--require-container-native-qc`, even when they carry legacy native-looking `native_artifact`, `artifact_origin`, `artifact_role`, `provenance.generated_from`, and accepted `official_source_ids` values.
- Preserved the packaged CLI entrypoint `apps/api/scripts/verify_scientific_reports.py`, which imports the app module used by the endpoint and tests.

Verification:

- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_verify_scientific_reports.py::test_check_output_rejects_reports_path_native_qc_impersonation -q` failed because `reports/fake_native.png` made `check_output(..., require_container_native_qc=True, min_native_qc_images=1)` return `ok=True`.
- GREEN:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_verify_scientific_reports.py::test_check_output_rejects_reports_path_native_qc_impersonation apps/api/tests/test_verify_scientific_reports.py -q` -> `18 passed`.
  - `python -m py_compile apps/api/app/scripts/verify_scientific_reports.py apps/api/tests/test_verify_scientific_reports.py apps/api/scripts/verify_scientific_reports.py` -> passed.
  - `git diff --check` -> passed.

Release gate:

- Frontend design remains blocked.
- This tightens the report-layer verifier used by remote acceptance, but final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean path-safe post-apply dry-run report, normal remote restart without active-task override, and fresh strict remote acceptance evidence within 24 hours.

## Strict smoke JSON reports-path native QC guard

Time: 2026-06-13T20:20:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to API/tests/docs only; no local install, local service run, local container run, remote install, remote restart, remote model smoke, remote stale-task apply, or remote workflow execution was performed.
- Hardened `apps/api/scripts/verify_remote_smoke_acceptance.py` so saved strict smoke JSON cannot list `reports/*` paths under `container_native_qc_artifacts`, even if the saved route, relative-path summary, content type, preview kind, and official source ids are internally consistent.
- Updated the remote acceptance template so operator-facing JSON handoff guidance keeps `reports/*` artifacts under `scientific_report_artifacts` instead of native QC evidence.

Verification:

- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_rejects_reports_path_container_native_qc_artifact -q` failed with `DID NOT RAISE` because a self-consistent `reports/fake_native.png` container-native QC artifact passed the offline verifier.
- GREEN:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_rejects_reports_path_container_native_qc_artifact apps/api/tests/test_verify_remote_smoke_acceptance.py -q` -> `94 passed`.
  - `python -m py_compile apps/api/scripts/verify_remote_smoke_acceptance.py apps/api/tests/test_verify_remote_smoke_acceptance.py` -> passed.
  - `git diff --check` -> passed.

Release gate:

- Frontend design remains blocked.
- This tightens saved strict smoke JSON verification, but final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean path-safe post-apply dry-run report, normal remote restart without active-task override, and fresh strict remote acceptance evidence within 24 hours.

## Strict smoke generator reports-path native QC guard

Time: 2026-06-13T20:32:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to API/tests/docs only; no local install, local service run, local container run, remote install, remote restart, remote model smoke, remote stale-task apply, or remote workflow execution was performed.
- Hardened `apps/api/scripts/smoke_remote_agent.py` so the generated strict smoke JSON cannot collect `reports/*` manifest items as `container_native_qc_artifacts`, even when those items carry native-looking `native_artifact`, `artifact_origin`, `artifact_role`, `provenance.generated_from`, and accepted `official_source_ids` values.
- This aligns the strict smoke generator with the artifact manifest classifier, scientific report verifier, and offline strict smoke JSON verifier.

Verification:

- RED: `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_require_container_native_qc_rejects_reports_path_native_impersonation -q` failed with `DID NOT RAISE` because a `reports/fake_native.png` manifest item was accepted as native QC evidence.
- GREEN:
  - `PYTHONPATH=apps/api python -m pytest apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_require_container_native_qc_rejects_reports_path_native_impersonation apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_require_container_native_qc_reports_served_evidence apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_require_container_native_qc_rejects_derived_only_manifest apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_require_container_native_qc_rejects_missing_official_source_ids apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_require_container_native_qc_rejects_untrusted_official_source_ids apps/api/tests/test_smoke_remote_agent.py::test_smoke_remote_agent_require_container_native_qc_rejects_incomplete_native_provenance -q` -> `12 passed`.
  - `python -m py_compile apps/api/scripts/smoke_remote_agent.py apps/api/tests/test_smoke_remote_agent.py` -> passed.
  - `git diff --check` -> passed.

Release gate:

- Frontend design remains blocked.
- This tightens strict smoke JSON generation, but final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean path-safe post-apply dry-run report, normal remote restart without active-task override, and fresh strict remote acceptance evidence within 24 hours.

## Stale-task resolution approval-payload summary guard

Time: 2026-06-13T21:05:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to API/tests/docs only; no local install, local service run, local container run, remote install, remote restart, remote model smoke, remote stale-task apply, or remote workflow execution was performed.
- Hardened `apps/api/scripts/verify_stale_task_resolution.py` so reviewed apply/follow-up evidence must have `approval_payload` summary ids consistent with the semantic evidence:
  - apply `approval_payload.target_task_ids` and `stale_candidate_ids` must match the expected task ids;
  - resolution `approval_payload.target_task_ids` must match the expected task ids and `stale_candidate_ids` must be empty;
  - both summaries must keep empty running-container, blocked, and out-of-scope task ids with `container_check_status=passed`.
- Stabilized stale-task approval/resolution tests by injecting a fixed `now` for historical fixture evidence, while CLI tests refresh temporary `generated_at` timestamps so the real 24-hour freshness gate remains exercised.

Verification:

- RED:
  - `pytest tests/test_verify_stale_task_resolution.py::test_verify_stale_task_resolution_rejects_weak_evidence -q` first failed with `DID NOT RAISE` for mismatched apply `approval_payload.stale_candidate_ids`.
  - After adding the resolution-side case, the same test failed with `DID NOT RAISE` for non-empty resolution `approval_payload.stale_candidate_ids`.
- GREEN:
  - `pytest tests/test_verify_stale_task_resolution.py::test_verify_stale_task_resolution_rejects_weak_evidence -q` -> `12 passed`.
  - `pytest tests/test_verify_stale_task_resolution.py tests/test_verify_stale_task_approval.py tests/test_stale_task_reconciliation.py -q` -> `38 passed`.
  - `python -m py_compile scripts/verify_stale_task_resolution.py scripts/verify_stale_task_approval.py app/workflows/stale_tasks.py` -> passed.
  - `git diff --check` -> passed.

Release gate:

- Frontend design remains blocked.
- This makes the stale task 83/84 resolution handoff harder to spoof with internally re-fingerprinted but semantically inconsistent JSON. Final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean path-safe post-apply dry-run report, normal remote restart without active-task override, and fresh strict remote acceptance evidence within 24 hours.

## Strict smoke model-status evidence safety guard

Time: 2026-06-13T21:38:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to API/tests/docs only; no local install, local service run, local container run, remote install, remote restart, remote model smoke, remote stale-task apply, or remote workflow execution was performed.
- Hardened `apps/api/scripts/smoke_remote_agent.py` so generated strict smoke JSON stores a safe `model_status` summary instead of blindly preserving every field returned by `/agent/model/status`.
- The safe summary keeps `configured`, privacy-safe provider/model fields, optional sanitized `base_url` without URL credentials, safe numeric/boolean settings, and safe deployment mode/access hints. It omits secret-bearing fields such as API keys, tokens, passwords, authorization headers, and reverse tunnel command details.
- Hardened `apps/api/scripts/verify_remote_smoke_acceptance.py` so saved strict smoke JSON is rejected if `model_status` exposes secret-like keys or a `base_url` with embedded credentials.
- Updated the remote acceptance template to require `model_status.configured=true` with no secret-bearing keys or credentialed URLs.

Verification:

- RED:
  - `pytest tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_rejects_weak_evidence -q` failed with `DID NOT RAISE` for `model_status.base_url=https://sk-test-secret@example.invalid/v1` and `model_status.api_key`.
  - `pytest tests/test_smoke_remote_agent.py::test_smoke_remote_agent_strict_gate_reports_successful_run -q` failed because generated strict smoke JSON preserved `api_key` and the credentialed `base_url`.
- GREEN:
  - `pytest tests/test_smoke_remote_agent.py::test_smoke_remote_agent_strict_gate_reports_successful_run -q` -> `1 passed`.
  - `pytest tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_rejects_weak_evidence -q` -> `44 passed`.
  - `pytest tests/test_smoke_remote_agent.py tests/test_verify_remote_smoke_acceptance.py -q` -> `170 passed`.
  - `python -m py_compile scripts/smoke_remote_agent.py scripts/verify_remote_smoke_acceptance.py` -> passed.
  - `git diff --check` -> passed.

Release gate:

- Frontend design remains blocked.
- This reduces the chance that saved remote strict smoke evidence leaks model gateway secrets or credentialed URLs. Final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean path-safe post-apply dry-run report, normal remote restart without active-task override, and fresh strict remote acceptance evidence within 24 hours.

## Strict smoke nested model deployment evidence guard

Time: 2026-06-13T21:58:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to API/tests/docs only; no local install, local service run, local container run, remote install, remote restart, remote model smoke, remote stale-task apply, or remote workflow execution was performed.
- Hardened `apps/api/scripts/verify_remote_smoke_acceptance.py` so saved strict smoke JSON cannot hide secret-like keys or command details inside `model_status.deployment`.
- The offline verifier now allows only `backend_runtime_mode` and `model_gateway_access` inside `model_status.deployment`, and both values must be privacy-safe symbols.
- Extended `apps/api/tests/test_smoke_remote_agent.py` to assert the generated strict smoke JSON keeps only the safe model deployment summary and omits `reverse_tunnel_command`.
- Updated the remote acceptance template so operators know nested model deployment command details are not accepted evidence.

Verification:

- RED: `pytest tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_rejects_weak_evidence -q` failed with `DID NOT RAISE` for `model_status.deployment.reverse_tunnel_command` and `model_status.deployment.access_token`.
- GREEN:
  - `pytest tests/test_smoke_remote_agent.py::test_smoke_remote_agent_strict_gate_reports_successful_run tests/test_verify_remote_smoke_acceptance.py::test_verify_remote_smoke_acceptance_rejects_weak_evidence -q` -> `47 passed`.
  - `pytest tests/test_smoke_remote_agent.py tests/test_verify_remote_smoke_acceptance.py -q` -> `172 passed`.
  - `python -m py_compile scripts/smoke_remote_agent.py scripts/verify_remote_smoke_acceptance.py` -> passed.
  - `git diff --check` -> passed.

Release gate:

- Frontend design remains blocked.
- This further hardens saved strict remote acceptance evidence against leaked model tunnel commands or nested credentials. Final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean path-safe post-apply dry-run report, normal remote restart without active-task override, and fresh strict remote acceptance evidence within 24 hours.

## Agent project-history status normalization audit

Time: 2026-06-13T22:18:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to API/tests/docs only; no local install, local service run, local container run, remote install, remote restart, remote model smoke, remote stale-task apply, or remote workflow execution was performed.
- Hardened `build_project_agent_run_history_response()` so project history matches single-run lookup behavior when persisted ledger status values drift outside the stable Agent API enum.
- Unknown persisted history statuses now return `status=failed` and include the original privacy-safe symbol in `safe_metadata.contract_status_normalized_from`.
- Updated `docs/api.md` to document the project-history normalization/audit behavior for frontend timeline consumers.

Verification:

- RED: `pytest tests/test_agent_api.py::test_project_agent_runs_list_resanitizes_persisted_json_fields -q` failed because history returned `status=failed` but did not include `safe_metadata.contract_status_normalized_from`.
- GREEN:
  - `pytest tests/test_agent_api.py::test_project_agent_runs_list_resanitizes_persisted_json_fields -q` -> `1 passed`.
  - `pytest tests/test_agent_api.py tests/test_skill_and_rag_docs.py::test_agent_run_ledger_contract_is_documented_for_agent_use tests/test_skill_and_rag_docs.py::test_agent_run_ledger_trace_fields_do_not_allow_model_free_text -q` -> `33 passed`.
  - `python -m py_compile app/agent/contracts.py tests/test_agent_api.py` -> passed.
  - `git diff --check` -> passed.

Release gate:

- Frontend design remains blocked.
- This improves Agent API stability for future frontend timeline use, but final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean path-safe post-apply dry-run report, normal remote restart without active-task override, and fresh strict remote acceptance evidence within 24 hours.

## Public model-status safe deployment summary

Time: 2026-06-13T22:36:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to API/tests/docs only; no local install, local service run, local container run, remote install, remote restart, remote model smoke, remote stale-task apply, or remote workflow execution was performed.
- Hardened the public `/agent/model/status` endpoint so it returns the same safe deployment summary used by strict smoke evidence: `backend_runtime_mode` and `model_gateway_access`, without `reverse_tunnel_command`.
- Hardened `/deployment` so its nested `agent` status uses the same public-safe summary.
- Left the lower-level `model_gateway.provider_status()` behavior intact for internal diagnostics/tests, so operational code can still classify reverse-tunnel access without exposing command text through public API responses.
- Updated remote deployment docs to state that `/agent/model/status` never returns API keys or reverse-tunnel command text.

Verification:

- RED: `pytest tests/test_agent_api.py::test_agent_model_status_uses_model_gateway -q` failed because `/agent/model/status` included `deployment.reverse_tunnel_command`.
- GREEN:
  - `pytest tests/test_agent_api.py::test_agent_model_status_uses_model_gateway -q` -> `1 passed`.
  - `pytest tests/test_agent_api.py::test_agent_model_status_uses_model_gateway tests/test_agent_api.py::test_deployment_status_uses_safe_agent_model_summary tests/test_model_gateway.py::test_provider_status_reports_remote_reverse_tunnel_hint -q` -> `3 passed`.
  - `python -m py_compile app/main.py tests/test_agent_api.py app/agent/model_gateway.py` -> passed.

Release gate:

- Frontend design remains blocked.
- This reduces public API operational-detail leakage before frontend integration, but final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean path-safe post-apply dry-run report, normal remote restart without active-task override, and fresh strict remote acceptance evidence within 24 hours.

## Container-native QC smoke evidence provenance guard

Time: 2026-06-13T23:05:00+08:00

Scope:

- Continued the Image Agent product-maturity goal without using subagents.
- Kept this slice local to verifier/tests/docs only; no local install, local service run, local container run, remote install, remote restart, remote model smoke, remote stale-task apply, or remote workflow execution was performed.
- Hardened `apps/api/scripts/verify_remote_smoke_acceptance.py` so saved strict smoke JSON must prove each `container_native_qc_artifacts` entry is container-native evidence, not merely an artifact with a trusted source id.
- The offline verifier now requires native QC artifacts to include `artifact_origin=container_output`, `native_artifact=true`, `provenance.generated_from=container_native_qc`, `provenance.replaces_native_qc=false`, and provenance `official_source_ids` matching the top-level artifact `official_source_ids`.
- Extended `apps/api/tests/test_verify_remote_smoke_acceptance.py` fixture and rejection cases to lock the stricter native QC provenance contract.

Verification:

- RED: `pytest tests/test_verify_remote_smoke_acceptance.py -q` failed with five `DID NOT RAISE` cases for weak native QC artifact provenance.
- GREEN:
  - `pytest tests/test_verify_remote_smoke_acceptance.py tests/test_smoke_remote_agent.py tests/test_artifact_manifest_contract.py tests/test_artifact_manifest_api.py -q` -> `182 passed`.
  - `python -m py_compile scripts/verify_remote_smoke_acceptance.py scripts/smoke_remote_agent.py tests/test_verify_remote_smoke_acceptance.py app/workflows/artifact_manifest.py` -> passed.
  - `git diff --check` -> passed.

Release gate:

- Frontend design remains blocked.
- This makes the offline strict acceptance verifier better aligned with the product requirement that UI-visible workflow results depend on Docker/container-native QC first. Final readiness still requires approved remote stale-task apply for tasks `83` and `84`, a clean path-safe post-apply dry-run report, normal remote restart without active-task override, and fresh strict remote acceptance evidence within 24 hours.
