# Image Agent Work Log - 2026-06-07

## 23:58 CST - XCP-D Remote Wrapper RAG Boundary

Context:

- Continued the narrow TDD slice for the XCP-D remote wrapper official-doc/RAG contract.
- The existing worktree is intentionally dirty; no reset, checkout, or cleanup was performed.
- This slice supports the broader goal of detailed RAG, explicit workflow boundaries, and container-native QC display.

Changes:

- Added RAG/skill documentation coverage for `run_xcpd_deepprep_115.sh`.
- Documented the XCP-D handoff as DeepPrep-derived fMRIPrep-compatible input, not raw BIDS.
- Documented wrapper flags: `--mode linc`, `--input-type fmriprep`, `--file-format nifti`, `--linc-qc y`, and `--abcc-qc y`.
- Documented `IMAGE_AGENT_TASK_XCPD_DIR` and `IMAGE_AGENT_TASK_TEMPLATEFLOW_DIR`.
- Clarified that TemplateFlow cache is a support mount.
- Clarified that XCP-D native reports/figures are container-native XCP-D QC and should be labeled with `source_stage: xcpd`.

Verification:

- Red check before docs patch:
  - `python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_xcpd_remote_wrapper_flags_and_artifact_boundaries_are_documented_for_agent_use -q`
  - Result: failed on missing `run_xcpd_deepprep_115.sh`, as expected.
- Focused green check after docs patch:
  - `python -m pytest apps/api/tests/test_skill_and_rag_docs.py::test_xcpd_remote_wrapper_flags_and_artifact_boundaries_are_documented_for_agent_use -q`
  - Result: 1 passed.
- Small regression:
  - `python -m pytest apps/api/tests/test_skill_and_rag_docs.py apps/api/tests/test_remote_scripts.py -q`
  - Result: 29 passed.

Touched files:

- `apps/api/tests/test_skill_and_rag_docs.py`
- `docs/rag/workflows/bold_fmriprep_xcpd_report.md`
- `docs/rag/vendor/xcp_d_official_container_usage.md`
- `docs/rag/vendor/xcp_d_official_outputs.md`
- `docs/rag/troubleshooting/common-errors.md`
- `docs/skills/image-agent-workflow-runner/references/container-qc-artifacts.md`
- `docs/skills/image-agent-workflow-runner/references/registry-and-preflight.md`

Next recommended slice:

- Choose one small OpenAI SDK architecture/RAG/skill contract update, write the contract test first, then patch only the relevant docs or skill references.
- Keep real installation and container execution on the remote server; local development should remain limited to code, docs, and existing tests.

## 00:20 CST - Git Noise Cleanup

Context:

- User asked to fix the git issue without deleting files.
- The worktree contains many meaningful untracked source/docs/tests plus generated cache, backup archives, and dev-server process files.
- No `git reset`, `git checkout --`, cleanup, removal, or deletion was performed.

Changes:

- Updated `.gitignore` so generated local artifacts stay on disk but stop cluttering `git status`:
  - `.rag_index/`
  - `*.tgz`
  - `git_backup_*/`
  - Vite `.out`, `.err`, and `.pid` files
  - `*.tsbuildinfo`
- Added `.gitattributes` to document stable line-ending policy for source/docs/config files.
- Ran `git add --ignore-removal .` so additions/modifications are staged while deletions are explicitly not staged.

Verification:

- `git check-ignore -v` confirms `.rag_index`, backup archives/directories, Vite logs/PIDs, and TypeScript build info are ignored.
- `git diff --cached --name-status | Select-String -Pattern '^D\s'` returned no staged deletions.
- `git diff --cached --name-status` shows source/docs/tests are staged, while generated backup/cache artifacts are not staged.

Notes:

- Git still reports line-ending normalization warnings for some existing dirty files. Those warnings are now governed by `.gitattributes`, but files were not mass-rewritten because that would create unnecessary churn in the already dirty tree.
- The remaining untracked files are mostly product source, tests, docs, skills, RAG, console app files, and tools that appear to be part of the current task context.

Next candidate slices from parallel scouts:

- OpenAI SDK: add optional Responses `json_schema` structured output support while preserving `json_object` fallback.
- RAG: add `official_grounding` source IDs to BOLD fMRIPrep/XCP-D workflow RAG frontmatter.
- Skills: add a TDD doc gate requiring `## Contents` near the top of long skill reference files, then add concise TOCs.
