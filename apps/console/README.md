# Brain Image Agent Console

New React/Vite frontend for project ingest, workflow execution, task monitoring, structured result review, artifact downloads, and Agent/RAG explanation.

## Run

```bash
npm install
npm run dev
```

The API base defaults to the current host on port `8000`. Set `localStorage.apiBase` in the browser to override it.
